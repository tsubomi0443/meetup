diff --git a/.air.toml b/.air.toml
index 26f31a0..188df86 100644
--- a/.air.toml
+++ b/.air.toml
@@ -10 +10 @@
-  include_file = ["frontend/package.json", "./.env"]
+  include_file = ["frontend/package.json", "./.env", "./.air.toml"]
diff --git a/_mac_infrastructure/converter.go b/_mac_infrastructure/converter.go
index 2ce9092..32eba5f 100644
--- a/_mac_infrastructure/converter.go
+++ b/_mac_infrastructure/converter.go
@@ -138,0 +139 @@ func UserFromEntity(e User) UserForm {
+		Memo:   e.Memo,
@@ -153,0 +155 @@ func UserFromEntityNoRole(e User) UserForm {
+		Memo:   e.Memo,
@@ -162,0 +165 @@ func UserToEntityNoRole(f UserForm) User {
+		Memo:   f.Memo,
@@ -179,0 +183 @@ func UserToEntity(f UserForm) User {
+		Memo:   f.Memo,
@@ -239,0 +244,8 @@ func tagFromEntityShallow(e Tag) TagForm {
+func TagFromEntities(e []Tag) []TagForm {
+	forms := []TagForm{}
+	for _, tag := range e {
+		forms = append(forms, TagFromEntity(tag))
+	}
+	return forms
+}
+
@@ -250,8 +261,0 @@ func TagFromEntity(e Tag) TagForm {
-func TagFromEntities(e []Tag) []TagForm {
-	forms := []TagForm{}
-	for _, tag := range e {
-		forms = append(forms, TagFromEntity(tag))
-	}
-	return forms
-}
-
@@ -501,0 +506,7 @@ func NoticeToEntity(f NoticeForm) Notice {
+func QuestionFromEntities(e []Question) []QuestionForm {
+	forms := []QuestionForm{}
+	for _, q := range e {
+		forms = append(forms, QuestionFromEntity(q))
+	}
+	return forms
+}
@@ -536,7 +547,11 @@ func QuestionFromEntity(e Question) QuestionForm {
-	return f
-}
-
-func QuestionFromEntities(e []Question) []QuestionForm {
-	forms := []QuestionForm{}
-	for _, q := range e {
-		forms = append(forms, QuestionFromEntity(q))
+	seenRelated := make(map[int64]struct{})
+	for _, rq := range e.RelatedQuestions {
+		rid := rq.RelatedQuestionID
+		if rid == 0 || rid == e.ID {
+			continue
+		}
+		if _, ok := seenRelated[rid]; ok {
+			continue
+		}
+		seenRelated[rid] = struct{}{}
+		f.RelatedQuestions = append(f.RelatedQuestions, RelatedQuestionFromEntity(rq))
@@ -544 +559 @@ func QuestionFromEntities(e []Question) []QuestionForm {
-	return forms
+	return f
@@ -597,0 +613,67 @@ func QuestionToEntity(f QuestionForm) Question {
+	seenRelated := make(map[int64]struct{})
+	for _, rf := range f.RelatedQuestions {
+		rq := RelatedQuestionToEntity(rf, qid)
+		if rq.RelatedQuestionID == 0 || rq.RelatedQuestionID == qid {
+			continue
+		}
+		if _, ok := seenRelated[rq.RelatedQuestionID]; ok {
+			continue
+		}
+		seenRelated[rq.RelatedQuestionID] = struct{}{}
+		e.RelatedQuestions = append(e.RelatedQuestions, rq)
+	}
+	return e
+}
+
+func questionFormShallowFromEntity(e Question) QuestionForm {
+	var originQuestionID *string
+	if e.OriginQuestionID != nil {
+		s := strconv.FormatInt(*e.OriginQuestionID, 10)
+		originQuestionID = &s
+	}
+	return QuestionForm{
+		ID:               e.ID,
+		OriginQuestionID: originQuestionID,
+		AnswerID:         e.AnswerID,
+		SupportID:        e.SupportID,
+		Title:            e.Title,
+		Content:          e.Content,
+		Deleted:          e.Deleted,
+		Due:              timePtrToISO(e.Due),
+		CreatedAt:        timeToISO(e.CreatedAt),
+	}
+}
+
+func RelatedQuestionFromEntity(r RelatedQuestion) RelatedQuestionForm {
+	f := RelatedQuestionForm{
+		ID:                r.ID,
+		QuestionID:        strconv.FormatInt(r.QuestionID, 10),
+		RelatedQuestionID: strconv.FormatInt(r.RelatedQuestionID, 10),
+	}
+	if r.RelatedQuestion.ID != 0 {
+		q := questionFormShallowFromEntity(r.RelatedQuestion)
+		f.RelatedQuestion = &q
+	}
+	return f
+}
+
+func RelatedQuestionToEntity(f RelatedQuestionForm, parentQuestionID int64) RelatedQuestion {
+	qid := f.QuestionIDInt64()
+	if qid < 0 || qid == 0 {
+		qid = parentQuestionID
+	}
+	rid := f.RelatedQuestionIDInt64()
+	if rid < 0 {
+		rid = 0
+	}
+	if rid == 0 && f.RelatedQuestion != nil && f.RelatedQuestion.ID != 0 {
+		rid = f.RelatedQuestion.ID
+	}
+	e := RelatedQuestion{
+		ID:                f.ID,
+		QuestionID:        qid,
+		RelatedQuestionID: rid,
+	}
+	if f.RelatedQuestion != nil && f.RelatedQuestion.ID != 0 {
+		e.RelatedQuestion = QuestionToEntity(*f.RelatedQuestion)
+	}
diff --git a/_mac_infrastructure/db.go b/_mac_infrastructure/db.go
index 05b0024..2598843 100644
--- a/_mac_infrastructure/db.go
+++ b/_mac_infrastructure/db.go
@@ -10,0 +11,12 @@ import (
+func GetUserByID(ctx context.Context, db *gorm.DB, id int64) (model User, err error) {
+	model, err = gorm.G[User](db).Where("id = ?", id).Select("id, name, email, memo, role_id").
+		Preload("Role", nil).
+		First(ctx)
+	return
+}
+
+func GetUserInfo(ctx context.Context, db *gorm.DB, email, pass string) (user User, err error) {
+	user, err = gorm.G[User](db).Where("email = ? AND password = ?", email, pass).First(ctx)
+	return
+}
+
@@ -16 +28 @@ func GetUsers(ctx context.Context, db *gorm.DB) (models []User, err error) {
-		Select("id, name, email, role_id").Find(ctx)
+		Select("id, name, email, memo, role_id").Find(ctx)
@@ -60 +72 @@ func UpdateInTransaction[T any](ctx context.Context, db *gorm.DB, model T, omit
-// 1対多の関連（Answer, Support, TagManagers, Memos, Notices）の同期を1トランザクションで行う。
+// 1対多の関連（Answer, Support, TagManagers, Memos, RelatedQuestions）の同期を1トランザクションで行う。
@@ -110 +122 @@ func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) e
-			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "CreatedAt").
+			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "RelatedQuestions", "CreatedAt").
@@ -159 +171,21 @@ func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) e
-		// // 6) 通知
+		// 6) 関連質問（related_questions）— tag_managers / memos と同様に DELETE + INSERT
+		var relatedRows []RelatedQuestion
+		for _, rq := range q.RelatedQuestions {
+			if rq.RelatedQuestionID == 0 || rq.RelatedQuestionID == q.ID {
+				continue
+			}
+			relatedRows = append(relatedRows, RelatedQuestion{
+				ID:                0,
+				QuestionID:        q.ID,
+				RelatedQuestionID: rq.RelatedQuestionID,
+			})
+		}
+		if err := tx.Where("question_id = ?", q.ID).Delete(&RelatedQuestion{}).Error; err != nil {
+			return err
+		}
+		if len(relatedRows) > 0 {
+			if err := tx.Create(&relatedRows).Error; err != nil {
+				return err
+			}
+		}
+		// // 7) 通知
@@ -179,0 +212,5 @@ func DeleteQuestionByID(ctx context.Context, db *gorm.DB, id int64) error {
+	if err := db.WithContext(ctx).
+		Where("question_id = ? OR related_question_id = ?", id, id).
+		Delete(&RelatedQuestion{}).Error; err != nil {
+		return err
+	}
@@ -240,0 +278,2 @@ func GetQuestion(ctx context.Context, db *gorm.DB, id int64) (model Question, er
+		Preload("RelatedQuestions", nil).
+		Preload("RelatedQuestions.RelatedQuestion", nil).
@@ -271,0 +311,2 @@ func GetQuestions(ctx context.Context, db *gorm.DB) (models []Question, err erro
+		Preload("RelatedQuestions", nil).
+		Preload("RelatedQuestions.RelatedQuestion", nil).
diff --git a/_mac_infrastructure/entity.go b/_mac_infrastructure/entity.go
index e8b4dd4..b4c9b23 100644
--- a/_mac_infrastructure/entity.go
+++ b/_mac_infrastructure/entity.go
@@ -27,0 +28 @@ type User struct {
+	Memo     string `gorm:"column:memo"`
@@ -69,13 +70,14 @@ type Question struct {
-	ID               int64        `gorm:"column:id;primaryKey"`
-	OriginQuestionID *int64       `gorm:"column:origin_question_id"`
-	AnswerID         *int64       `gorm:"column:answer_id"`
-	SupportID        *int64       `gorm:"column:support_id"`
-	Title            string       `gorm:"column:title"`
-	Content          string       `gorm:"column:content"`
-	Deleted          bool         `gorm:"column:deleted"`
-	Due              *time.Time   `gorm:"column:due"`
-	CreatedAt        time.Time    `gorm:"column:created_at"`
-	Answer           *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
-	Memos            []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
-	TagManagers      []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
-	Support          *Support     `gorm:"foreignKey:SupportID;references:ID"`
+	ID               int64             `gorm:"column:id;primaryKey"`
+	OriginQuestionID *int64            `gorm:"column:origin_question_id"`
+	AnswerID         *int64            `gorm:"column:answer_id"`
+	SupportID        *int64            `gorm:"column:support_id"`
+	Title            string            `gorm:"column:title"`
+	Content          string            `gorm:"column:content"`
+	Deleted          bool              `gorm:"column:deleted"`
+	Due              *time.Time        `gorm:"column:due"`
+	CreatedAt        time.Time         `gorm:"column:created_at"`
+	Answer           *Answer           `gorm:"foreignKey:AnswerID;references:ID"`
+	Memos            []Memo            `gorm:"foreignKey:QuestionID;references:ID"`
+	TagManagers      []TagManager      `gorm:"foreignKey:QuestionID;references:ID"`
+	RelatedQuestions []RelatedQuestion `gorm:"foreignKey:QuestionID;references:ID"`
+	Support          *Support          `gorm:"foreignKey:SupportID;references:ID"`
@@ -237,0 +240,14 @@ func (Notice) TableName() string {
+
+type RelatedQuestion struct {
+	ID                int64 `gorm:"column:id;primaryKey"`
+	QuestionID        int64 `gorm:"column:question_id"`
+	RelatedQuestionID int64 `gorm:"column:related_question_id"`
+
+	// related_questions: question_id → 親質問、related_question_id → 関連先質問
+	Question        Question `gorm:"foreignKey:QuestionID;references:ID"`
+	RelatedQuestion Question `gorm:"foreignKey:RelatedQuestionID;references:ID"`
+}
+
+func (RelatedQuestion) TableName() string {
+	return "related_questions"
+}
diff --git a/_mac_infrastructure/form.go b/_mac_infrastructure/form.go
index efcd10e..5d698ec 100644
--- a/_mac_infrastructure/form.go
+++ b/_mac_infrastructure/form.go
@@ -49,0 +50 @@ type UserForm struct {
+	Memo     string        `json:"memo"`
@@ -55 +56 @@ type UserForm struct {
-	Password string        `json:"password,omitempty"`
+	Password string        `json:"pass,omitempty"`
@@ -193,17 +194,18 @@ type QuestionForm struct {
-	ID               int64            `json:"id"`
-	OriginQuestionID *string          `json:"originQuestionId,omitempty"`
-	AnswerID         *int64           `json:"answerId,omitempty"`
-	SupportID        *int64           `json:"supportId,omitempty"`
-	Title            string           `json:"title"`
-	Content          string           `json:"content"`
-	Deleted          bool             `json:"deleted"`
-	Due              *string          `json:"due,omitempty"`
-	CreatedAt        *string          `json:"createdAt,omitempty"`
-	OriginQuestion   *QuestionForm    `json:"originQuestion,omitempty"`
-	SubQuestions     []QuestionForm   `json:"subQuestions,omitempty"`
-	Support          *SupportForm     `json:"support,omitempty"`
-	Answer           *AnswerForm      `json:"answer,omitempty"`
-	Memos            []MemoForm       `json:"memos,omitempty"`
-	Tags             []TagForm        `json:"tags,omitempty"`
-	EscalationsFrom  []EscalationForm `json:"escalationsFrom,omitempty"`
-	EscalationsTo    []EscalationForm `json:"escalationsTo,omitempty"`
+	ID               int64                 `json:"id"`
+	OriginQuestionID *string               `json:"originQuestionId,omitempty"`
+	AnswerID         *int64                `json:"answerId,omitempty"`
+	SupportID        *int64                `json:"supportId,omitempty"`
+	Title            string                `json:"title"`
+	Content          string                `json:"content"`
+	Deleted          bool                  `json:"deleted"`
+	Due              *string               `json:"due,omitempty"`
+	CreatedAt        *string               `json:"createdAt,omitempty"`
+	OriginQuestion   *QuestionForm         `json:"originQuestion,omitempty"`
+	SubQuestions     []QuestionForm        `json:"subQuestions,omitempty"`
+	Support          *SupportForm          `json:"support,omitempty"`
+	Answer           *AnswerForm           `json:"answer,omitempty"`
+	Memos            []MemoForm            `json:"memos,omitempty"`
+	Tags             []TagForm             `json:"tags,omitempty"`
+	EscalationsFrom  []EscalationForm      `json:"escalationsFrom,omitempty"`
+	EscalationsTo    []EscalationForm      `json:"escalationsTo,omitempty"`
+	RelatedQuestions []RelatedQuestionForm `json:"relatedQuestions,omitempty"`
@@ -266,0 +269,23 @@ func (f TagManagerForm) QuestionIDInt64() int64 {
+
+// RelatedQuestionForm corresponds to RelatedQuestion (related_questions).
+type RelatedQuestionForm struct {
+	ID                int64         `json:"id"`
+	QuestionID        string        `json:"questionId"`
+	RelatedQuestionID string        `json:"relatedQuestionId"`
+	Question          *QuestionForm `json:"question,omitempty"`
+	RelatedQuestion   *QuestionForm `json:"relatedQuestion,omitempty"`
+}
+
+func (f RelatedQuestionForm) QuestionIDInt64() int64 {
+	if val, err := strconv.ParseInt(f.QuestionID, 10, 64); err == nil {
+		return val
+	}
+	return -1
+}
+
+func (f RelatedQuestionForm) RelatedQuestionIDInt64() int64 {
+	if val, err := strconv.ParseInt(f.RelatedQuestionID, 10, 64); err == nil {
+		return val
+	}
+	return -1
+}
diff --git a/bash.exe.stackdump b/bash.exe.stackdump
new file mode 100644
index 0000000..6036ff8
--- /dev/null
+++ b/bash.exe.stackdump
@@ -0,0 +1,29 @@
+Stack trace:
+Frame         Function      Args
+0007FFFF9EF0  00021006118E (00021028DEE8, 000210272B3E, 000000000000, 0007FFFF8DF0) msys-2.0.dll+0x2118E
+0007FFFF9EF0  0002100469BA (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x69BA
+0007FFFF9EF0  0002100469F2 (00021028DF99, 0007FFFF9DA8, 000000000000, 000000000000) msys-2.0.dll+0x69F2
+0007FFFF9EF0  00021006A41E (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A41E
+0007FFFF9EF0  00021006A545 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A545
+0001004F94B7  00021006B9A5 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2B9A5
+End of stack trace
+Loaded modules:
+000100400000 bash.exe
+7FFCFB4B0000 ntdll.dll
+7FFCFA0D0000 KERNEL32.DLL
+7FFCF8900000 KERNELBASE.dll
+7FFCFA6B0000 USER32.dll
+7FFCF8CD0000 win32u.dll
+7FFCF9260000 GDI32.dll
+7FFCF87E0000 gdi32full.dll
+7FFCF8650000 msvcp_win.dll
+7FFCF8E40000 ucrtbase.dll
+000210040000 msys-2.0.dll
+7FFCFA010000 advapi32.dll
+7FFCFA920000 msvcrt.dll
+7FFCFA870000 sechost.dll
+7FFCF86F0000 bcrypt.dll
+7FFCF9C50000 RPCRT4.dll
+7FFCF7DD0000 CRYPTBASE.DLL
+7FFCF8F60000 bcryptPrimitives.dll
+7FFCF9FD0000 IMM32.DLL
diff --git a/bun.lock b/bun.lock
deleted file mode 100644
index 0074c27..0000000
--- a/bun.lock
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-  "lockfileVersion": 1,
-  "configVersion": 1,
-  "workspaces": {
-    "": {
-      "dependencies": {
-        "htmx-ext-sse": "^2.2.4",
-      },
-    },
-  },
-  "packages": {
-    "htmx-ext-sse": ["htmx-ext-sse@2.2.4", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-LJmxVhykyflBWgh5PvbRidcyuqMHlgfajmmzumvKctv9puvsufeH6OaejSMZTNTnEI8O2wXXn4ZZtBdpEMqmEQ=="],
-
-    "htmx.org": ["htmx.org@2.0.8", "", {}, "sha512-fm297iru0iWsNJlBrjvtN7V9zjaxd+69Oqjh4F/Vq9Wwi2kFisLcrLCiv5oBX0KLfOX/zG8AUo9ROMU5XUB44Q=="],
-  }
-}
diff --git a/diff_20260423.md b/diff_20260423.md
new file mode 100644
index 0000000..92bde9f
--- /dev/null
+++ b/diff_20260423.md
@@ -0,0 +1,3102 @@
+diff --git a/.air.toml b/.air.toml
+index 26f31a0..188df86 100644
+--- a/.air.toml
++++ b/.air.toml
+@@ -10 +10 @@
+-  include_file = ["frontend/package.json", "./.env"]
++  include_file = ["frontend/package.json", "./.env", "./.air.toml"]
+diff --git a/_mac_infrastructure/converter.go b/_mac_infrastructure/converter.go
+index ba85bda..033bcb9 100644
+--- a/_mac_infrastructure/converter.go
++++ b/_mac_infrastructure/converter.go
+@@ -462 +462,5 @@ func NoticeFromEntity(e Notice) NoticeForm {
+-		qf := QuestionForm{ID: e.Question.ID, Title: e.Question.Title, Content: e.Question.Content}
++		// due := e.Question.Due.Format(time.DateTime)
++
++		// const tag = TagFromEntity(e.Question.TagManagers)
++		// qf := QuestionForm{ID: e.Question.ID, Title: e.Question.Title, Content: e.Question.Content, Due: &due, Tags: tag}
++		qf := QuestionFromEntity(*e.Question)
+@@ -523,0 +528,3 @@ func QuestionFromEntity(e Question) QuestionForm {
++	for _, n := range e.Notices {
++		f.Notices = append(f.Notices, NoticeFromEntity(n))
++	}
+@@ -584,0 +592,8 @@ func QuestionToEntity(f QuestionForm) Question {
++	for _, nf := range f.Notices {
++		n := NoticeToEntity(nf)
++		if n.QuestionID == nil && qid != 0 {
++			qidCopy := qid
++			n.QuestionID = &qidCopy
++		}
++		e.Notices = append(e.Notices, n)
++	}
+diff --git a/_mac_infrastructure/db.go b/_mac_infrastructure/db.go
+new file mode 100644
+index 0000000..d825d58
+--- /dev/null
++++ b/_mac_infrastructure/db.go
+@@ -0,0 +1,312 @@
++package infrastructure
++
++import (
++	"context"
++
++	"gorm.io/gorm"
++	"gorm.io/gorm/logger"
++)
++
++func GetUsers(ctx context.Context, db *gorm.DB) (models []User, err error) {
++	models, err = gorm.G[User](db).
++		Where("role_id <> ?", 1).
++		Preload("Role", nil).
++		Not("role_id = 1").
++		Select("id, name, email, role_id").Find(ctx)
++	return
++}
++
++func Register[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) error {
++	var v = gorm.G[T](db)
++	for _, preload := range preloads {
++		v.Preload(preload, nil)
++	}
++	return v.Create(ctx, &model)
++}
++
++func Updates[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) (int, error) {
++	var v = gorm.G[T](db)
++	for _, preload := range preloads {
++		v.Preload(preload, nil)
++	}
++	return v.Updates(ctx, model)
++}
++
++// updateInTransaction は単一の DB トランザクション内で gorm.Updates を実行する。
++// omit に関連名を渡し、Role/Category など中間テーブル向けの関連を更新対象から外す。
++// 既存の updates と違い、こちらは更新用。事前読み込みは行わない。
++func UpdateInTransaction[T any](ctx context.Context, db *gorm.DB, model T, omit ...string) (rowsAffected int, err error) {
++	err = db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
++		m := model
++		res := tx.Omit(omit...).Model(&m).Updates(&m)
++		rowsAffected = int(res.RowsAffected)
++		return res.Error
++	})
++	return
++}
++
++// updateQuestionInTransaction は Question の1行更新と、QuestionToEntity で組み立てた
++// 1対多の関連（Answer, Support, TagManagers, Memos, Notices）の同期を1トランザクションで行う。
++// フォーム上の下位の質問（SubQuestions）やエスカレーション等は本関数では永続化しない。
++func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) error {
++	return db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
++		// 1) Answer: 新規作成 or 既存更新し、親の answer_id を整合させる
++		if q.Answer != nil {
++			a := *q.Answer
++			if a.ID == 0 {
++				if err := tx.Create(&a).Error; err != nil {
++					return err
++				}
++				aid := a.ID
++				q.AnswerID = &aid
++				q.Answer = &a
++			} else {
++				if _, err := gorm.G[Answer](tx).
++					Omit("User", "ReferManagers").
++					Where("id = ?", a.ID).
++					Updates(ctx, a); err != nil {
++					return err
++				}
++				if q.AnswerID == nil {
++					aid := a.ID
++					q.AnswerID = &aid
++				}
++			}
++		}
++		// 2) Support: 新規作成 or 既存更新
++		if q.Support != nil {
++			s := *q.Support
++			if s.ID == 0 {
++				if err := tx.Create(&s).Error; err != nil {
++					return err
++				}
++				sid := s.ID
++				q.SupportID = &sid
++				q.Support = &s
++			} else {
++				if err := tx.Model(&s).Omit("User", "SupportStatus").Updates(&s).Error; err != nil {
++					return err
++				}
++				if q.SupportID == nil {
++					sid := s.ID
++					q.SupportID = &sid
++					q.Support = &s
++				}
++			}
++		}
++		// 3) 親の questions: スカラ列と FK のみ（CreatedAt は更新で変えない）
++		if _, err := gorm.G[Question](tx).
++			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "CreatedAt").
++			Where("id = ?", q.ID).
++			Updates(ctx, q); err != nil {
++			return err
++		}
++		ref := Question{ID: q.ID}
++		// 4) タグ紐づけ（tag_managers）
++		for i := range q.TagManagers {
++			if q.TagManagers[i].QuestionID == 0 {
++				q.TagManagers[i].QuestionID = q.ID
++			}
++		}
++		if len(q.TagManagers) == 0 {
++			if err := tx.Model(&ref).Association("TagManagers").Clear(); err != nil {
++				return err
++			}
++		} else {
++			if err := tx.Model(&ref).Association("TagManagers").Replace(q.TagManagers); err != nil {
++				return err
++			}
++		}
++		// 5) メモ
++		for i := range q.Memos {
++			if q.Memos[i].QuestionID == 0 {
++				q.Memos[i].QuestionID = q.ID
++			}
++		}
++		if len(q.Memos) == 0 {
++			if err := tx.Model(&ref).Association("Memos").Clear(); err != nil {
++				return err
++			}
++		} else {
++			if err := tx.Model(&ref).Association("Memos").Replace(q.Memos); err != nil {
++				return err
++			}
++		}
++		// 6) 通知
++		for i := range q.Notices {
++			if q.Notices[i].QuestionID == nil {
++				qid := q.ID
++				q.Notices[i].QuestionID = &qid
++			}
++		}
++		if len(q.Notices) == 0 {
++			if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
++				return err
++			}
++		} else {
++			if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
++				return err
++			}
++		}
++		return nil
++	})
++}
++
++func DeleteQuestionByID(ctx context.Context, db *gorm.DB, id int64) error {
++	if _, err := gorm.G[Question](db).
++		Preload("Answer", nil).
++		Preload("Answer.User", nil).
++		Preload("Answer.User.Role", nil).
++		Preload("Answer.ReferManagers", nil).
++		Preload("Answer.ReferManagers.Refer", nil).
++		Preload("Memos", nil).
++		Preload("Memos.User", nil).
++		Preload("Memos.User.Role", nil).
++		Preload("TagManagers", nil).
++		Preload("TagManagers.Tag", nil).
++		Preload("TagManagers.Tag.Category", nil).
++		Preload("Support", nil).
++		Preload("Support.User", nil).
++		Preload("Support.User.Role", nil).
++		Preload("Support.SupportStatus", nil).
++		Where("id = ?", id).
++		Limit(1).
++		Delete(ctx); err != nil {
++		return err
++	}
++	return nil
++}
++
++func DeleteUserByID(ctx context.Context, db *gorm.DB, id int64) error {
++	if _, err := gorm.G[User](db).
++		Preload("Role", nil).
++		Where("id = ?", id).
++		Limit(1).
++		Delete(ctx); err != nil {
++		return err
++	}
++	return nil
++}
++
++func GetQuestion(ctx context.Context, db *gorm.DB, id int64) (model Question, err error) {
++	model, err = gorm.G[Question](db).
++		Preload("Answer", nil).
++		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Answer.User.Role", nil).
++		Preload("Answer.ReferManagers", nil).
++		Preload("Answer.ReferManagers.Refer", nil).
++		Preload("Memos", nil).
++		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Memos.User.Role", nil).
++		Preload("TagManagers", nil).
++		Preload("TagManagers.Tag", nil).
++		Preload("TagManagers.Tag.Category", nil).
++		Preload("Support", nil).
++		Preload("Support.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Support.User.Role", nil).
++		Preload("Support.SupportStatus", nil).
++		Where("id = ?", id).
++		First(ctx)
++	return
++}
++
++func GetQuestions(ctx context.Context, db *gorm.DB) (models []Question, err error) {
++	models, err = gorm.G[Question](db).
++		Preload("Answer", nil).
++		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Answer.User.Role", nil).
++		Preload("Answer.ReferManagers", nil).
++		Preload("Answer.ReferManagers.Refer", nil).
++		Preload("Memos", nil).
++		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Memos.User.Role", nil).
++		Preload("TagManagers", nil).
++		Preload("TagManagers.Tag", nil).
++		Preload("TagManagers.Tag.Category", nil).
++		Preload("Support", nil).
++		Preload("Support.User", func(db gorm.PreloadBuilder) error {
++			db.Select("id", "name", "email", "role_id")
++			return nil
++		}).
++		Preload("Support.User.Role", nil).
++		Preload("Support.SupportStatus", nil).
++		Find(ctx)
++	return
++}
++
++func GetTags(ctx context.Context, db *gorm.DB) (models []Tag, err error) {
++	models, err = gorm.G[Tag](db).
++		Preload("Category", nil).
++		Find(ctx)
++	return
++}
++
++func DeleteTagByID(ctx context.Context, db *gorm.DB, id int64) error {
++	if _, err := gorm.G[Tag](db).Where("id = ?", id).Limit(1).Delete(ctx); err != nil {
++		return err
++	}
++	return nil
++}
++
++func GetNoticeByQuestionIDs(ctx context.Context, db *gorm.DB, questionIDs []int64) (models []Notice, err error) {
++	if len(questionIDs) > 0 {
++		models, err = gorm.G[Notice](db).Where("question_id IN ?", questionIDs).Find(ctx)
++	}
++	return
++}
++
++func GetNoticeByQuestion(ctx context.Context, db *gorm.DB, question Question) (models Notice, err error) {
++	models, err = gorm.G[Notice](db).Where("question_id = ?", question.ID).First(ctx)
++	return
++}
++
++func GetNotice(ctx context.Context, db *gorm.DB) (models []Notice, err error) {
++	models, err = gorm.G[Notice](db).
++		Preload("NoticeType", nil).
++		Preload("Question", nil).
++		Preload("Question.Support", nil).
++		Preload("Question.Support.SupportStatus", nil).
++		Preload("Question.TagManagers", nil).
++		Preload("Question.TagManagers.Tag", nil).
++		Find(ctx)
++	return
++}
++
++func GetNoticeByQuestionSilent(ctx context.Context, db *gorm.DB, question Question) (models Notice, err error) {
++	models, err = gorm.G[Notice](db.Session(&gorm.Session{
++		Logger: db.Logger.LogMode(logger.Silent),
++	})).Where("question_id = ?", question.ID).First(ctx)
++	return
++}
++
++func RegisterNoticeByQuestionID(ctx context.Context, db *gorm.DB, questionID int64) error {
++	var content = "質問の回答期日が近づいています。"
++	notice := Notice{
++		TypeID:     3,
++		QuestionID: &questionID,
++		Content:    &content,
++	}
++	return gorm.G[Notice](db).Create(ctx, &notice)
++}
++
++func DeleteNoticeByID(ctx context.Context, db *gorm.DB, id int64) error {
++	if _, err := gorm.G[Notice](db).Where("id = ?", id).Delete(ctx); err != nil {
++		return err
++	}
++	return nil
++}
+diff --git a/_mac_infrastructure/entity.go b/_mac_infrastructure/entity.go
+index 86b015f..49ae6ac 100644
+--- a/_mac_infrastructure/entity.go
++++ b/_mac_infrastructure/entity.go
+@@ -13,2 +13 @@ type Role struct {
+-
+-	Users []User `gorm:"foreignKey:RoleID;references:ID"`
++	Users    []User `gorm:"foreignKey:RoleID;references:ID"`
+@@ -43,2 +42 @@ type Category struct {
+-
+-	Tags []Tag `gorm:"foreignKey:CategoryID;references:ID"`
++	Tags         []Tag  `gorm:"foreignKey:CategoryID;references:ID"`
+@@ -55,5 +53,4 @@ type Tag struct {
+-	ID         int64  `gorm:"column:id;primaryKey"`
+-	Title      string `gorm:"column:title"`
+-	Usage      int    `gorm:"column:usage"`
+-	CategoryID int64  `gorm:"column:category_id"`
+-
++	ID          int64        `gorm:"column:id;primaryKey"`
++	Title       string       `gorm:"column:title"`
++	Usage       int          `gorm:"column:usage"`
++	CategoryID  int64        `gorm:"column:category_id"`
+@@ -72,15 +69,14 @@ type Question struct {
+-	ID               int64      `gorm:"column:id;primaryKey"`
+-	OriginQuestionID *int64     `gorm:"column:origin_question_id"`
+-	AnswerID         *int64     `gorm:"column:answer_id"`
+-	SupportID        *int64     `gorm:"column:support_id"`
+-	Title            string     `gorm:"column:title"`
+-	Content          string     `gorm:"column:content"`
+-	Deleted          bool       `gorm:"column:deleted"`
+-	Due              *time.Time `gorm:"column:due"`
+-	CreatedAt        time.Time  `gorm:"column:created_at"`
+-
+-	Answer      *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
+-	Memos       []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
+-	TagManagers []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
+-	Notices     []Notice     `gorm:"foreignKey:QuestionID;references:ID"`
+-	Support     *Support     `gorm:"foreignKey:SupportID;references:ID"`
++	ID               int64        `gorm:"column:id;primaryKey"`
++	OriginQuestionID *int64       `gorm:"column:origin_question_id"`
++	AnswerID         *int64       `gorm:"column:answer_id"`
++	SupportID        *int64       `gorm:"column:support_id"`
++	Title            string       `gorm:"column:title"`
++	Content          string       `gorm:"column:content"`
++	Deleted          bool         `gorm:"column:deleted"`
++	Due              *time.Time   `gorm:"column:due"`
++	CreatedAt        time.Time    `gorm:"column:created_at"`
++	Answer           *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
++	Memos            []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
++	TagManagers      []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
++	Notices          []Notice     `gorm:"foreignKey:QuestionID;references:ID"`
++	Support          *Support     `gorm:"foreignKey:SupportID;references:ID"`
+@@ -97,6 +93,5 @@ type Answer struct {
+-	ID         int64      `gorm:"column:id;primaryKey"`
+-	UserID     int64      `gorm:"column:user_id"`
+-	Content    string     `gorm:"column:content"`
+-	AnsweredAt *time.Time `gorm:"column:answered_at"`
+-	CreatedAt  time.Time  `gorm:"column:created_at"`
+-
++	ID            int64          `gorm:"column:id;primaryKey"`
++	UserID        int64          `gorm:"column:user_id"`
++	Content       string         `gorm:"column:content"`
++	AnsweredAt    *time.Time     `gorm:"column:answered_at"`
++	CreatedAt     time.Time      `gorm:"column:created_at"`
+@@ -131,4 +126,3 @@ type Refer struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-	URL   string `gorm:"column:url"`
+-
++	ID            int64          `gorm:"column:id;primaryKey"`
++	Title         string         `gorm:"column:title"`
++	URL           string         `gorm:"column:url"`
+@@ -146,6 +140,5 @@ type ReferManager struct {
+-	ID       int64 `gorm:"column:id;primaryKey"`
+-	AnswerID int64 `gorm:"column:answer_id"`
+-	ReferID  int64 `gorm:"column:refer_id"`
+-
+-	Answer Answer `gorm:"foreignKey:AnswerID;references:ID"`
+-	Refer  Refer  `gorm:"foreignKey:ReferID;references:ID"`
++	ID       int64  `gorm:"column:id;primaryKey"`
++	AnswerID int64  `gorm:"column:answer_id"`
++	ReferID  int64  `gorm:"column:refer_id"`
++	Answer   Answer `gorm:"foreignKey:AnswerID;references:ID"`
++	Refer    Refer  `gorm:"foreignKey:ReferID;references:ID"`
+@@ -162,6 +155,5 @@ type TagManager struct {
+-	ID         int64 `gorm:"column:id;primaryKey"`
+-	TagID      int64 `gorm:"column:tag_id"`
+-	QuestionID int64 `gorm:"column:question_id"`
+-
+-	Tag      Tag      `gorm:"foreignKey:TagID;references:ID"`
+-	Question Question `gorm:"foreignKey:QuestionID;references:ID"`
++	ID         int64    `gorm:"column:id;primaryKey"`
++	TagID      int64    `gorm:"column:tag_id"`
++	QuestionID int64    `gorm:"column:question_id"`
++	Tag        Tag      `gorm:"foreignKey:TagID;references:ID"`
++	Question   Question `gorm:"foreignKey:QuestionID;references:ID"`
+@@ -182,3 +174,2 @@ type Escalation struct {
+-
+-	FromQuestion Question `gorm:"foreignKey:FromQuestionID;references:ID"`
+-	ToQuestion   Question `gorm:"foreignKey:ToQuestionID;references:ID"`
++	FromQuestion   Question  `gorm:"foreignKey:FromQuestionID;references:ID"`
++	ToQuestion     Question  `gorm:"foreignKey:ToQuestionID;references:ID"`
+@@ -195,3 +186,2 @@ type SupportStatus struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-
++	ID       int64     `gorm:"column:id;primaryKey"`
++	Title    string    `gorm:"column:title"`
+@@ -209,6 +199,5 @@ type Support struct {
+-	ID              int64 `gorm:"column:id;primaryKey"`
+-	UserID          int64 `gorm:"column:user_id"`
+-	SupportStatusID int64 `gorm:"column:support_status_id"`
+-
+-	User          User          `gorm:"foreignKey:UserID;references:ID"`
+-	SupportStatus SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
++	ID              int64         `gorm:"column:id;primaryKey"`
++	UserID          int64         `gorm:"column:user_id"`
++	SupportStatusID int64         `gorm:"column:support_status_id"`
++	User            User          `gorm:"foreignKey:UserID;references:ID"`
++	SupportStatus   SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
+@@ -243 +231,0 @@ type Notice struct {
+-
+diff --git a/_mac_infrastructure/entity.txt b/_mac_infrastructure/entity.txt
+deleted file mode 100644
+index 1f086b7..0000000
+--- a/_mac_infrastructure/entity.txt
++++ /dev/null
+@@ -1,208 +0,0 @@
+-package infrastructure
+-
+-import (
+-	"time"
+-)
+-
+-// =====================
+-// ROLE
+-// =====================
+-type Role struct {
+-	ID       int64  `gorm:"column:id;primaryKey"`
+-	RoleName string `gorm:"column:role_name"`
+-
+-	Users []User `gorm:"foreignKey:RoleID;references:ID"`
+-}
+-
+-func (Role) TableName() string {
+-	return "roles"
+-}
+-
+-// =====================
+-// USER
+-// =====================
+-type User struct {
+-	ID        int64  `gorm:"column:id;primaryKey"`
+-	Name      string `gorm:"column:name"`
+-	Passwordd string `gorm:"column:passwordd"`
+-	Email     string `gorm:"column:email"`
+-	RoleID    int64  `gorm:"column:role_id"`
+-
+-	Role Role `gorm:"foreignKey:RoleID;references:ID"`
+-}
+-
+-func (User) TableName() string {
+-	return "users"
+-}
+-
+-// =====================
+-// CATEGORY
+-// =====================
+-type Category struct {
+-	ID           int64  `gorm:"column:id;primaryKey"`
+-	CategoryName string `gorm:"column:category_name"`
+-
+-	Tags []Tag `gorm:"foreignKey:CategoryID;references:ID"`
+-}
+-
+-func (Category) TableName() string {
+-	return "categories"
+-}
+-
+-// =====================
+-// TAG
+-// =====================
+-type Tag struct {
+-	ID         int64  `gorm:"column:id;primaryKey"`
+-	Title      string `gorm:"column:title"`
+-	Usage      int    `gorm:"column:usage"`
+-	CategoryID int64  `gorm:"column:category_id"`
+-
+-	Category    Category     `gorm:"foreignKey:CategoryID;references:ID"`
+-	TagManagers []TagManager `gorm:"foreignKey:TagID;references:ID"`
+-}
+-
+-func (Tag) TableName() string {
+-	return "tags"
+-}
+-
+-// =====================
+-// QUESTION
+-// =====================
+-type Question struct {
+-	ID               int64      `gorm:"column:id;primaryKey"`
+-	OriginQuestionID *int64     `gorm:"column:origin_question_id"`
+-	SupportID        *int64     `gorm:"foreignKey:SupportID;references:ID"`
+-	Title            string     `gorm:"column:title"`
+-	Content          string     `gorm:"column:content"`
+-	Due              *time.Time `gorm:"column:due"`
+-	CreatedAt        time.Time  `gorm:"column:created_at"`
+-
+-	Answers     []Answer     `gorm:"foreignKey:QuestionID;references:ID"`
+-	Memos       []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
+-	TagManagers []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
+-	Support     Support      `gorm:"foreignKey:SupportID;references:ID"`
+-}
+-
+-func (Question) TableName() string {
+-	return "questions"
+-}
+-
+-// =====================
+-// ANSWER
+-// =====================
+-type Answer struct {
+-	ID         int64      `gorm:"column:id;primaryKey"`
+-	UserID     int64      `gorm:"column:user_id"`
+-	QuestionID int64      `gorm:"column:question_id"`
+-	Content    string     `gorm:"column:content"`
+-	AnsweredAt *time.Time `gorm:"column:answered_at"`
+-	CreatedAt  time.Time  `gorm:"column:created_at"`
+-
+-	User          User           `gorm:"foreignKey:UserID;references:ID"`
+-	ReferManagers []ReferManager `gorm:"foreignKey:AnswerID;references:ID"`
+-}
+-
+-func (Answer) TableName() string {
+-	return "answers"
+-}
+-
+-// =====================
+-// MEMO
+-// =====================
+-type Memo struct {
+-	ID         int64  `gorm:"column:id;primaryKey"`
+-	UserID     int64  `gorm:"foreignKey:UserID;references:ID"`
+-	Content    string `gorm:"column:content"`
+-	QuestionID int64  `gorm:"foreignKey:QuestionID;references:ID"`
+-	User       User   `gorm:"foreignKey:UserID;references:ID"`
+-}
+-
+-func (Memo) TableName() string {
+-	return "memos"
+-}
+-
+-// =====================
+-// REFER
+-// =====================
+-type Refer struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-	URL   string `gorm:"column:url"`
+-
+-	ReferManagers []ReferManager `gorm:"foreignKey:ReferID;references:ID"`
+-}
+-
+-func (Refer) TableName() string {
+-	return "refers"
+-}
+-
+-// =====================
+-// REFER_MANAGER
+-// =====================
+-type ReferManager struct {
+-	ID       int64 `gorm:"column:id;primaryKey"`
+-	AnswerID int64 `gorm:"column:answer_id"`
+-	ReferID  int64 `gorm:"column:refer_id"`
+-
+-	Answer Answer `gorm:"foreignKey:AnswerID;references:ID"`
+-	Refer  Refer  `gorm:"foreignKey:ReferID;references:ID"`
+-}
+-
+-func (ReferManager) TableName() string {
+-	return "refer_managers"
+-}
+-
+-// =====================
+-// TAG_MANAGER
+-// =====================
+-type TagManager struct {
+-	ID         int64 `gorm:"column:id;primaryKey"`
+-	TagID      int64 `gorm:"column:tag_id"`
+-	QuestionID int64 `gorm:"column:question_id"`
+-
+-	Tag      Tag      `gorm:"foreignKey:TagID;references:ID"`
+-	Question Question `gorm:"foreignKey:QuestionID;references:ID"`
+-}
+-
+-func (TagManager) TableName() string {
+-	return "tag_managers"
+-}
+-
+-// =====================
+-// ESCALATION
+-// =====================
+-type Escalation struct {
+-	ID             int64     `gorm:"column:id;primaryKey"`
+-	FromQuestionID int64     `gorm:"column:from_question_id"`
+-	ToQuestionID   int64     `gorm:"column:to_question_id"`
+-	EscalatedAt    time.Time `gorm:"column:escalated_at"`
+-}
+-
+-func (Escalation) TableName() string {
+-	return "escalations"
+-}
+-
+-type SupportStatus struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-
+-	Supports []Support `gorm:"foreignKey:SupportStatusID;references:ID"`
+-}
+-
+-func (SupportStatus) TableName() string {
+-	return "support_statuses"
+-}
+-
+-type Support struct {
+-	ID              int64 `gorm:"column:id;primaryKey"`
+-	UserID          int64 `gorm:"column:user_id"`
+-	SupportStatusID int64 `gorm:"column:support_status_id"`
+-
+-	User          User          `gorm:"foreignKey:UserID;references:ID"`
+-	SupportStatus SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
+-}
+-
+-func (Support) TableName() string {
+-	return "supports"
+-}
+diff --git a/bash.exe.stackdump b/bash.exe.stackdump
+new file mode 100644
+index 0000000..6036ff8
+--- /dev/null
++++ b/bash.exe.stackdump
+@@ -0,0 +1,29 @@
++Stack trace:
++Frame         Function      Args
++0007FFFF9EF0  00021006118E (00021028DEE8, 000210272B3E, 000000000000, 0007FFFF8DF0) msys-2.0.dll+0x2118E
++0007FFFF9EF0  0002100469BA (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x69BA
++0007FFFF9EF0  0002100469F2 (00021028DF99, 0007FFFF9DA8, 000000000000, 000000000000) msys-2.0.dll+0x69F2
++0007FFFF9EF0  00021006A41E (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A41E
++0007FFFF9EF0  00021006A545 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A545
++0001004F94B7  00021006B9A5 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2B9A5
++End of stack trace
++Loaded modules:
++000100400000 bash.exe
++7FFCFB4B0000 ntdll.dll
++7FFCFA0D0000 KERNEL32.DLL
++7FFCF8900000 KERNELBASE.dll
++7FFCFA6B0000 USER32.dll
++7FFCF8CD0000 win32u.dll
++7FFCF9260000 GDI32.dll
++7FFCF87E0000 gdi32full.dll
++7FFCF8650000 msvcp_win.dll
++7FFCF8E40000 ucrtbase.dll
++000210040000 msys-2.0.dll
++7FFCFA010000 advapi32.dll
++7FFCFA920000 msvcrt.dll
++7FFCFA870000 sechost.dll
++7FFCF86F0000 bcrypt.dll
++7FFCF9C50000 RPCRT4.dll
++7FFCF7DD0000 CRYPTBASE.DLL
++7FFCF8F60000 bcryptPrimitives.dll
++7FFCF9FD0000 IMM32.DLL
+diff --git a/bun.lock b/bun.lock
+deleted file mode 100644
+index 0074c27..0000000
+--- a/bun.lock
++++ /dev/null
+@@ -1,16 +0,0 @@
+-{
+-  "lockfileVersion": 1,
+-  "configVersion": 1,
+-  "workspaces": {
+-    "": {
+-      "dependencies": {
+-        "htmx-ext-sse": "^2.2.4",
+-      },
+-    },
+-  },
+-  "packages": {
+-    "htmx-ext-sse": ["htmx-ext-sse@2.2.4", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-LJmxVhykyflBWgh5PvbRidcyuqMHlgfajmmzumvKctv9puvsufeH6OaejSMZTNTnEI8O2wXXn4ZZtBdpEMqmEQ=="],
+-
+-    "htmx.org": ["htmx.org@2.0.8", "", {}, "sha512-fm297iru0iWsNJlBrjvtN7V9zjaxd+69Oqjh4F/Vq9Wwi2kFisLcrLCiv5oBX0KLfOX/zG8AUo9ROMU5XUB44Q=="],
+-  }
+-}
+diff --git a/doc/db/INIT.sql b/doc/db/INIT.sql
+index e6389bb..2c23f3e 100644
+--- a/doc/db/INIT.sql
++++ b/doc/db/INIT.sql
+@@ -0,0 +1,5 @@
++DROP SCHEMA public CASCADE;
++CREATE SCHEMA public;
++GRANT ALL ON SCHEMA public TO postgres;
++GRANT ALL ON SCHEMA public TO public;
++
+@@ -245,0 +251,5 @@ INSERT INTO users (name, password, email, role_id) VALUES
++<<<<<<< HEAD
++('Admin', 'Admin', 'Admin', 1),
++('Taro Yamada', 'hashed_password_1', 'taro@example.com', 2),
++('Hanako Suzuki', 'hashed_password_2', 'hanako@example.com', 3),
++=======
+@@ -247,0 +258 @@ INSERT INTO users (name, password, email, role_id) VALUES
++>>>>>>> add-mock
+@@ -314 +325,2 @@ INSERT INTO notice_types (name) VALUES
+-('DUE');
++('ALERT'),
++('QUESTION');
+@@ -319 +331 @@ INSERT INTO notices (type_id, question_id, content, display_due) VALUES
+-(1, NULL, 'システムメンテナンスのお知らせ', NULL);
+\ No newline at end of file
++(1, NULL, 'システムメンテナンスのお知らせ', NULL);
+diff --git a/doc/db/er.md b/doc/db/er.md
+index d6c8234..31e7de4 100644
+--- a/doc/db/er.md
++++ b/doc/db/er.md
+@@ -21 +20,0 @@ erDiagram
+-        number support_id FK
+@@ -23,0 +23 @@ erDiagram
++        number support_id FK
+@@ -88 +88 @@ erDiagram
+-        string title
++        string name
+@@ -120 +120,16 @@ erDiagram
+-```
++
++    NOTICE {
++        number id PK
++        number type_id FK
++        number question_id FK
++        string content
++        datetime displayDue
++    }
++    NOTICE ||--|| NOTICE_TYPE: "通知は必ず１つの通知タイプを持つ"
++    NOTICE ||--o| QUESTION: "質問期限通知の場合は質問を持っている場合もある"
++
++    NOTICE_TYPE {
++        number id PK
++        string name "SYSTEM|DUE"
++    }
++```
+\ No newline at end of file
+diff --git a/env/env.go b/env/env.go
+index a706925..80bf249 100644
+--- a/env/env.go
++++ b/env/env.go
+@@ -9,0 +10,9 @@ import (
++const (
++	host     = "PSQL_HOST"
++	port     = "PSQL_PORT"
++	user     = "PSQL_USER"
++	password = "PSQL_PASSWORD"
++	dbname   = "PSQL_DBNAME"
++	sslmode  = "PSQL_SSLMODE"
++)
++
+diff --git a/frontend/bun.lock b/frontend/bun.lock
+index fc464ba..2d8d80a 100644
+--- a/frontend/bun.lock
++++ b/frontend/bun.lock
+@@ -43 +43 @@
+-    "@types/bun": ["@types/bun@1.3.11", "", { "dependencies": { "bun-types": "1.3.11" } }, "sha512-5vPne5QvtpjGpsGYXiFyycfpDF2ECyPcTSsFBMa0fraoxiQyMJ3SmuQIGhzPg2WJuWxVBoxWJ2kClYTcw/4fAg=="],
++    "@types/bun": ["@types/bun@1.3.12", "", { "dependencies": { "bun-types": "1.3.12" } }, "sha512-DBv81elK+/VSwXHDlnH3Qduw+KxkTIWi7TXkAeh24zpi5l0B2kUg9Ga3tb4nJaPcOFswflgi/yAvMVBPrxMB+A=="],
+@@ -45 +45 @@
+-    "@types/node": ["@types/node@25.5.0", "", { "dependencies": { "undici-types": "~7.18.0" } }, "sha512-jp2P3tQMSxWugkCUKLRPVUpGaL5MVFwF8RDuSRztfwgN1wmqJeMSbKlnEtQqU8UrhTmzEmZdu2I6v2dpp7XIxw=="],
++    "@types/node": ["@types/node@25.6.0", "", { "dependencies": { "undici-types": "~7.19.0" } }, "sha512-+qIYRKdNYJwY3vRCZMdJbPLJAtGjQBudzZzdzwQYkEPQd+PJGixUL5QfvCLDaULoLv+RhT3LDkwEfKaAkgSmNQ=="],
+@@ -61 +61 @@
+-    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.13", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-BL2sTuHOdy0YT1lYieUxTw/QMtPBC3pmlJC6xk8BBYVv6vcw3SGdKemQ+Xsx9ik2F/lYDO9tqsFQH1r9PFuHKw=="],
++    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.20", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-1AaXxEPfXT+GvTBJFuy4yXVHWJBXa4OdbIebGN/wX5DlsIkU0+wzGnd2lOzokSk51d5LUmqjgBLRLlypLUqInQ=="],
+@@ -69 +69 @@
+-    "bun-types": ["bun-types@1.3.11", "", { "dependencies": { "@types/node": "*" } }, "sha512-1KGPpoxQWl9f6wcZh57LvrPIInQMn2TQ7jsgxqpRzg+l0QPOFvJVH7HmvHo/AiPgwXy+/Thf6Ov3EdVn1vOabg=="],
++    "bun-types": ["bun-types@1.3.12", "", { "dependencies": { "@types/node": "*" } }, "sha512-HqOLj5PoFajAQciOMRiIZGNoKxDJSr6qigAttOX40vJuSp6DN/CxWp9s3C1Xwm4oH7ybueITwiaOcWXoYVoRkA=="],
+@@ -73 +73 @@
+-    "caniuse-lite": ["caniuse-lite@1.0.30001788", "", {}, "sha512-6q8HFp+lOQtcf7wBK+uEenxymVWkGKkjFpCvw5W25cmMwEDU45p1xQFBQv8JDlMMry7eNxyBaR+qxgmTUZkIRQ=="],
++    "caniuse-lite": ["caniuse-lite@1.0.30001790", "", {}, "sha512-bOoxfJPyYo+ds6W0YfptaCWbFnJYjh2Y1Eow5lRv+vI2u8ganPZqNm1JwNh0t2ELQCqIWg4B3dWEusgAmsoyOw=="],
+@@ -91 +91,3 @@
+-    "electron-to-chromium": ["electron-to-chromium@1.5.331", "", {}, "sha512-IbxXrsTlD3hRodkLnbxAPP4OuJYdWCeM3IOdT+CpcMoIwIoDfCmRpEtSPfwBXxVkg9xmBeY7Lz2Eo2TDn/HC3Q=="],
++    "electron-to-chromium": ["electron-to-chromium@1.5.343", "", {}, "sha512-YHnQ3MXI08icvL9ZKnEBy05F2EQ8ob01UaMOuMbM8l+4UcAq6MPPbBTJBbsBUg3H8JeZNt+O4fjsoWth3p6IFg=="],
++
++    "es-errors": ["es-errors@1.3.0", "", {}, "sha512-Zf5H2Kxt2xjTvbJvP2ZWLEICxA6j+hAmMzIlypy4xcBg1vKVnx89Wy0GbS+kf5cwCVFFzdCFh2XSCFNULS6csw=="],
+@@ -115,3 +117 @@
+-    "hasown": ["hasown@2.0.2", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-0hJU9SCPvmMzIBdZFqNPXWa6dqh7WdH0cII9y+CyS8rG3nL48Bclra9HmKhVVUHyPWNH5Y7xDwAB7bfgSjkUMQ=="],
+-
+-    "htmx-ext-client-side-templates": ["htmx-ext-client-side-templates@2.0.2", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-d2dA5HOJuMPN+QmAa4MtriTc1OZaaidG/UK/uwDsKJzstMFae2M7tFgykFXOjuFoV2XNCcexr3p+a9p5Jb54Dg=="],
++    "hasown": ["hasown@2.0.3", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-ej4AhfhfL2Q2zpMmLo7U1Uv9+PyhIZpgQLGT1F9miIGmiCJIoCgSmczFdrc97mWT4kVY72KA+WnnhJ5pghSvSg=="],
+@@ -149 +149 @@
+-    "node-releases": ["node-releases@2.0.37", "", {}, "sha512-1h5gKZCF+pO/o3Iqt5Jp7wc9rH3eJJ0+nh/CIoiRwjRxde/hAHyLPXYN4V3CqKAbiZPSeJFSWHmJsbkicta0Eg=="],
++    "node-releases": ["node-releases@2.0.38", "", {}, "sha512-3qT/88Y3FbH/Kx4szpQQ4HzUbVrHPKTLVpVocKiLfoYvw9XSGOX2FmD2d6DrXbVYyAQTF2HeF6My8jmzx7/CRw=="],
+@@ -187 +187 @@
+-    "resolve": ["resolve@1.22.11", "", { "dependencies": { "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-RfqAvLnMl313r7c9oclB1HhUEAezcpLjz95wFH4LVuhk9JF/r22qmVP9AMmOU4vMX7Q8pN8jwNg/CSpdFnMjTQ=="],
++    "resolve": ["resolve@1.22.12", "", { "dependencies": { "es-errors": "^1.3.0", "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-TyeJ1zif53BPfHootBGwPRYT1RUt6oGWsaQr8UyZW/eAm9bKoijtvruSDEmZHm92CwS9nj7/fWttqPCgzep8CA=="],
+@@ -205 +205 @@
+-    "tinyglobby": ["tinyglobby@0.2.15", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.3" } }, "sha512-j2Zq4NyQYG5XMST4cbs02Ak8iJUdxRM0XI5QyxXuZOzKOINmWurp3smXu3y5wDcJrptwpSjgXHzIQxR0omXljQ=="],
++    "tinyglobby": ["tinyglobby@0.2.16", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.4" } }, "sha512-pn99VhoACYR8nFHhxqix+uvsbXineAasWm5ojXoN8xEwK5Kd3/TrhNn1wByuD52UxWRLy8pu+kRMniEi6Eq9Zg=="],
+@@ -213 +213 @@
+-    "undici-types": ["undici-types@7.18.2", "", {}, "sha512-AsuCzffGHJybSaRrmr5eHr81mwJU3kjw6M+uprWvCXiNeN9SOGwQ3Jn8jb8m3Z6izVgknn1R0FTCEAP2QrLY/w=="],
++    "undici-types": ["undici-types@7.19.2", "", {}, "sha512-qYVnV5OEm2AW8cJMCpdV20CDyaN3g0AjDlOGf1OW4iaDEx8MwdtChUp4zu4H0VP3nDRF/8RKWH+IPp9uW0YGZg=="],
+@@ -221,2 +220,0 @@
+-    "browserslist/caniuse-lite": ["caniuse-lite@1.0.30001784", "", {}, "sha512-WU346nBTklUV9YfUl60fqRbU5ZqyXlqvo1SgigE1OAXK5bFL8LL9q1K7aap3N739l4BvNqnkm3YrGHiY9sfUQw=="],
+-
+@@ -227,2 +224,0 @@
+-    "tailwindcss/postcss": ["postcss@8.5.8", "", { "dependencies": { "nanoid": "^3.3.11", "picocolors": "^1.1.1", "source-map-js": "^1.2.1" } }, "sha512-OW/rX8O/jXnm82Ey1k44pObPtdblfiuWnrd8X7GJ7emImCOstunGbXUpp7HdBrFQX6rJzn3sPT397Wp5aCwCHg=="],
+-
+diff --git a/handler.go b/handler.go
+deleted file mode 100644
+index 06ab7d0..0000000
+--- a/handler.go
++++ /dev/null
+@@ -1 +0,0 @@
+-package main
+diff --git a/handler/apiHandler.go b/handler/apiHandler.go
+index f7d4288..d09faae 100644
+--- a/handler/apiHandler.go
++++ b/handler/apiHandler.go
+@@ -4 +3,0 @@ import (
+-	"context"
+@@ -11 +9,0 @@ import (
+-	"time"
+@@ -14 +11,0 @@ import (
+-	"gorm.io/gorm"
+@@ -77,2 +74 @@ func (hm *HandlerManager) registerUser() echo.HandlerFunc {
+-		fmt.Println(data)
+-		return register(c.Request().Context(), hm.db, &data)
++		return infrastructure.Register(c.Request().Context(), hm.db, &data)
+@@ -96 +92 @@ func (hm *HandlerManager) registerQuestion() echo.HandlerFunc {
+-		return register(c.Request().Context(), hm.db, &data)
++		return infrastructure.Register(c.Request().Context(), hm.db, &data)
+@@ -113 +109 @@ func (hm *HandlerManager) registerTag() echo.HandlerFunc {
+-		return register(c.Request().Context(), hm.db, &model)
++		return infrastructure.Register(c.Request().Context(), hm.db, &model)
+@@ -130,2 +126 @@ func (hm *HandlerManager) updateTag() echo.HandlerFunc {
+-		fmt.Println(model)
+-		if _, err := updateInTransaction(c.Request().Context(), hm.db, model, "Category", "TagManagers"); err != nil {
++		if _, err := infrastructure.UpdateInTransaction(c.Request().Context(), hm.db, model, "Category", "TagManagers"); err != nil {
+@@ -147 +142 @@ func (hm *HandlerManager) deleteTagByID() echo.HandlerFunc {
+-		if err := deleteTag(c.Request().Context(), hm.db, id); err != nil {
++		if err := infrastructure.DeleteTagByID(c.Request().Context(), hm.db, id); err != nil {
+@@ -156 +151 @@ func (hm *HandlerManager) getUsers() echo.HandlerFunc {
+-		users, err := getUsers(c.Request().Context(), hm.db)
++		users, err := infrastructure.GetUsers(c.Request().Context(), hm.db)
+@@ -173 +168 @@ func (hm *HandlerManager) getQuestionByID() echo.HandlerFunc {
+-		model, err := getQuestion(c.Request().Context(), hm.db, id)
++		model, err := infrastructure.GetQuestion(c.Request().Context(), hm.db, id)
+@@ -193 +188 @@ func (hm *HandlerManager) updateQuestionByID() echo.HandlerFunc {
+-		if err := updateQuestionInTransaction(c.Request().Context(), hm.db, updatedModel); err != nil {
++		if err := infrastructure.UpdateQuestionInTransaction(c.Request().Context(), hm.db, updatedModel); err != nil {
+@@ -208 +203 @@ func (hm *HandlerManager) deleteQuestionByID() echo.HandlerFunc {
+-		if err := deleteQuestion(c.Request().Context(), hm.db, id); err != nil {
++		if err := infrastructure.DeleteQuestionByID(c.Request().Context(), hm.db, id); err != nil {
+@@ -222 +217 @@ func (hm *HandlerManager) deleteUserByID() echo.HandlerFunc {
+-		if err := deleteUser(c.Request().Context(), hm.db, id); err != nil {
++		if err := infrastructure.DeleteUserByID(c.Request().Context(), hm.db, id); err != nil {
+@@ -243,2 +238 @@ func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
+-		fmt.Println(updatedModel)
+-		if _, err := updateInTransaction(c.Request().Context(), hm.db, updatedModel, "Role"); err != nil {
++		if _, err := infrastructure.UpdateInTransaction(c.Request().Context(), hm.db, updatedModel, "Role"); err != nil {
+@@ -250,273 +243,0 @@ func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
+-
+-func getUsers(ctx context.Context, db *gorm.DB) (models []infrastructure.User, err error) {
+-	models, err = gorm.G[infrastructure.User](db).
+-		Where("role_id <> ?", 1).
+-		Preload("Role", nil).
+-		Select("id, name, email, role_id").Find(ctx)
+-	return
+-}
+-
+-func register[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) error {
+-	var v = gorm.G[T](db)
+-	for _, preload := range preloads {
+-		v.Preload(preload, nil)
+-	}
+-	return v.Create(ctx, &model)
+-}
+-
+-func updates[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) (int, error) {
+-	var v = gorm.G[T](db)
+-	for _, preload := range preloads {
+-		v.Preload(preload, nil)
+-	}
+-	return v.Updates(ctx, model)
+-}
+-
+-// updateInTransaction は単一の DB トランザクション内で gorm.Updates を実行する。
+-// omit に関連名を渡し、Role/Category など中間テーブル向けの関連を更新対象から外す。
+-// 既存の updates と違い、こちらは更新用。事前読み込みは行わない。
+-func updateInTransaction[T any](ctx context.Context, db *gorm.DB, model T, omit ...string) (rowsAffected int, err error) {
+-	err = db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
+-		m := model
+-		res := tx.Omit(omit...).Model(&m).Updates(&m)
+-		rowsAffected = int(res.RowsAffected)
+-		return res.Error
+-	})
+-	return
+-}
+-
+-// updateQuestionInTransaction は Question の1行更新と、QuestionToEntity で組み立てた
+-// 1対多の関連（Answer, Support, TagManagers, Memos, Notices）の同期を1トランザクションで行う。
+-// フォーム上の下位の質問（SubQuestions）やエスカレーション等は本関数では永続化しない。
+-func updateQuestionInTransaction(ctx context.Context, db *gorm.DB, q infrastructure.Question) error {
+-	return db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
+-		// 1) Answer: 新規作成 or 既存更新し、親の answer_id を整合させる
+-		if q.Answer != nil {
+-			a := *q.Answer
+-			if a.ID == 0 {
+-				if err := tx.Create(&a).Error; err != nil {
+-					return err
+-				}
+-				aid := a.ID
+-				q.AnswerID = &aid
+-				q.Answer = &a
+-			} else {
+-				if _, err := gorm.G[infrastructure.Answer](tx).
+-					Omit("User", "ReferManagers").
+-					Where("id = ?", a.ID).
+-					Updates(ctx, a); err != nil {
+-					return err
+-				}
+-				if q.AnswerID == nil {
+-					aid := a.ID
+-					q.AnswerID = &aid
+-				}
+-			}
+-		}
+-		// 2) Support: 新規作成 or 既存更新
+-		if q.Support != nil {
+-			s := *q.Support
+-			if s.ID == 0 {
+-				if err := tx.Create(&s).Error; err != nil {
+-					return err
+-				}
+-				sid := s.ID
+-				q.SupportID = &sid
+-				q.Support = &s
+-			} else {
+-				if err := tx.Model(&s).Omit("User", "SupportStatus").Updates(&s).Error; err != nil {
+-					return err
+-				}
+-				if q.SupportID == nil {
+-					sid := s.ID
+-					q.SupportID = &sid
+-				}
+-			}
+-		}
+-		// 3) 親の questions: スカラ列と FK のみ（CreatedAt は更新で変えない）
+-		if _, err := gorm.G[infrastructure.Question](tx).
+-			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "CreatedAt").
+-			Where("id = ?", q.ID).
+-			Updates(ctx, q); err != nil {
+-			return err
+-		}
+-		ref := infrastructure.Question{ID: q.ID}
+-		// 4) タグ紐づけ（tag_managers）
+-		for i := range q.TagManagers {
+-			if q.TagManagers[i].QuestionID == 0 {
+-				q.TagManagers[i].QuestionID = q.ID
+-			}
+-		}
+-		if len(q.TagManagers) == 0 {
+-			if err := tx.Model(&ref).Association("TagManagers").Clear(); err != nil {
+-				return err
+-			}
+-		} else {
+-			if err := tx.Model(&ref).Association("TagManagers").Replace(q.TagManagers); err != nil {
+-				return err
+-			}
+-		}
+-		// 5) メモ
+-		for i := range q.Memos {
+-			if q.Memos[i].QuestionID == 0 {
+-				q.Memos[i].QuestionID = q.ID
+-			}
+-		}
+-		if len(q.Memos) == 0 {
+-			if err := tx.Model(&ref).Association("Memos").Clear(); err != nil {
+-				return err
+-			}
+-		} else {
+-			if err := tx.Model(&ref).Association("Memos").Replace(q.Memos); err != nil {
+-				return err
+-			}
+-		}
+-		// 6) 通知
+-		for i := range q.Notices {
+-			if q.Notices[i].QuestionID == nil {
+-				qid := q.ID
+-				q.Notices[i].QuestionID = &qid
+-			}
+-		}
+-		if len(q.Notices) == 0 {
+-			if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
+-				return err
+-			}
+-		} else {
+-			if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
+-				return err
+-			}
+-		}
+-		return nil
+-	})
+-}
+-
+-func deleteQuestion(ctx context.Context, db *gorm.DB, id int64) error {
+-	if _, err := gorm.G[infrastructure.Question](db).
+-		Preload("Answer", nil).
+-		Preload("Answer.User", nil).
+-		Preload("Answer.User.Role", nil).
+-		Preload("Answer.ReferManagers", nil).
+-		Preload("Answer.ReferManagers.Refer", nil).
+-		Preload("Memos", nil).
+-		Preload("Memos.User", nil).
+-		Preload("Memos.User.Role", nil).
+-		Preload("TagManagers", nil).
+-		Preload("TagManagers.Tag", nil).
+-		Preload("TagManagers.Tag.Category", nil).
+-		Preload("Support", nil).
+-		Preload("Support.User", nil).
+-		Preload("Support.User.Role", nil).
+-		Preload("Support.SupportStatus", nil).
+-		Where("id = ?", id).
+-		Delete(ctx); err != nil {
+-		return err
+-	}
+-	return nil
+-}
+-
+-func deleteUser(ctx context.Context, db *gorm.DB, id int64) error {
+-	if _, err := gorm.G[infrastructure.User](db).
+-		Preload("Role", nil).
+-		Where("id = ?", id).
+-		Delete(ctx); err != nil {
+-		return err
+-	}
+-	return nil
+-}
+-
+-func getQuestion(ctx context.Context, db *gorm.DB, id int64) (model infrastructure.Question, err error) {
+-	model, err = gorm.G[infrastructure.Question](db).
+-		Preload("Answer", nil).
+-		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Answer.User.Role", nil).
+-		Preload("Answer.ReferManagers", nil).
+-		Preload("Answer.ReferManagers.Refer", nil).
+-		Preload("Memos", nil).
+-		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Memos.User.Role", nil).
+-		Preload("TagManagers", nil).
+-		Preload("TagManagers.Tag", nil).
+-		Preload("TagManagers.Tag.Category", nil).
+-		Preload("Support", nil).
+-		Preload("Support.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Support.User.Role", nil).
+-		Preload("Support.SupportStatus", nil).
+-		Where("id = ?", id).
+-		First(ctx)
+-	return
+-}
+-
+-func getQuestions(ctx context.Context, db *gorm.DB) (models []infrastructure.Question, err error) {
+-	models, err = gorm.G[infrastructure.Question](db).
+-		Preload("Answer", nil).
+-		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Answer.User.Role", nil).
+-		Preload("Answer.ReferManagers", nil).
+-		Preload("Answer.ReferManagers.Refer", nil).
+-		Preload("Memos", nil).
+-		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Memos.User.Role", nil).
+-		Preload("TagManagers", nil).
+-		Preload("TagManagers.Tag", nil).
+-		Preload("TagManagers.Tag.Category", nil).
+-		Preload("Support", nil).
+-		Preload("Support.User", func(db gorm.PreloadBuilder) error {
+-			db.Select("id", "name", "email", "role_id")
+-			return nil
+-		}).
+-		Preload("Support.User.Role", nil).
+-		Preload("Support.SupportStatus", nil).
+-		Find(ctx)
+-	return
+-}
+-
+-func getTags(ctx context.Context, db *gorm.DB) (models []infrastructure.Tag, err error) {
+-	models, err = gorm.G[infrastructure.Tag](db).
+-		Preload("Category", nil).
+-		Find(ctx)
+-	return
+-}
+-
+-func deleteTag(ctx context.Context, db *gorm.DB, id int64) error {
+-	if _, err := gorm.G[infrastructure.Tag](db).Where("id = ?", id).Delete(ctx); err != nil {
+-		return err
+-	}
+-	return nil
+-}
+-
+-func getNotice(ctx context.Context, db *gorm.DB) ([]infrastructure.Question, error) {
+-	questions, err := gorm.G[infrastructure.Question](db).Find(ctx)
+-	if err != nil {
+-		return nil, err
+-	}
+-
+-	now := time.Now()
+-	limit := now.Add(3 * 24 * time.Hour)
+-	var recent []infrastructure.Question
+-	for _, q := range questions {
+-		if q.Due == nil {
+-			continue
+-		}
+-		d := *q.Due
+-		if !d.Before(now) && d.Before(limit) {
+-			recent = append(recent, q)
+-		}
+-	}
+-	return recent, nil
+-}
+diff --git a/handler/authHandler.go b/handler/authHandler.go
+index 53bcd92..6111eec 100644
+--- a/handler/authHandler.go
++++ b/handler/authHandler.go
+@@ -6 +5,0 @@ import (
+-	"fmt"
+@@ -58 +56,0 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
+-			fmt.Println("User not found")
+@@ -70 +67,0 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
+-			fmt.Println("Signed Error")
+diff --git a/handler/sseHub.go b/handler/eventHub.go
+similarity index 80%
+rename from handler/sseHub.go
+rename to handler/eventHub.go
+index bd6838e..a63cf74 100644
+--- a/handler/sseHub.go
++++ b/handler/eventHub.go
+@@ -50 +49,0 @@ func (h *Hub) Run() {
+-			fmt.Println(*client)
+@@ -82 +81 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-			hub.Broadcast <- Event{Event: "time-tick", Data: fmt.Sprintf(`<div>%s</div>`, t.In(local).Format("15:04:05"))}
++			hub.Broadcast <- Event{Event: "time-tick", Data: t.In(local).Format(time.DateTime)}
+@@ -88 +87 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-			models, err := getQuestions(ctx, db)
++			models, err := infrastructure.GetQuestions(ctx, db)
+@@ -107 +106 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-			models, err := getUsers(ctx, db)
++			models, err := infrastructure.GetUsers(ctx, db)
+@@ -126 +125 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-			models, err := getTags(ctx, db)
++			models, err := infrastructure.GetTags(ctx, db)
+@@ -142,0 +142,18 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++	go func() {
++		for range tickHalfSecond.C {
++			models, err := infrastructure.GetNotice(ctx, db)
++			if err != nil {
++				hub.Broadcast <- Event{Event: "error", Data: fmt.Sprintf(`Error: %v\n`, err)}
++				continue
++			}
++			for _, model := range models {
++				nf := infrastructure.NoticeFromEntity(model)
++				if data, err := json.Marshal(nf); err != nil {
++					hub.Broadcast <- Event{Event: "error", Data: fmt.Sprintf(`Error: %v\n`, err)}
++					continue
++				} else {
++					hub.Broadcast <- Event{Event: "notice", Data: string(data)}
++				}
++			}
++		}
++	}()
+diff --git a/handler/notice.go b/handler/notice.go
+new file mode 100644
+index 0000000..a52bb45
+--- /dev/null
++++ b/handler/notice.go
+@@ -0,0 +1,51 @@
++package handler
++
++import (
++	"context"
++	"errors"
++	"fmt"
++	infrastructure "meetup/_mac_infrastructure"
++	"time"
++
++	"gorm.io/gorm"
++)
++
++// Questionを定期監視し、期限が近づいたものをNoticeへと登録する
++func (hm *HandlerManager) PollingStart(ctx context.Context) error {
++	return hm.checkQuesiton(ctx)
++}
++
++func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
++	var ticker = time.NewTicker(30 * time.Minute)
++	var checkedQuestions map[int64]any = make(map[int64]any)
++
++	for {
++		select {
++		case <-ticker.C:
++			questions, err := infrastructure.GetQuestions(context.Background(), hm.db)
++			if err != nil {
++				return err
++			}
++
++			for _, question := range questions {
++				if _, ok := checkedQuestions[question.ID]; ok {
++					continue
++				}
++				if question.Due.Add(-24 * time.Hour).Before(time.Now()) {
++					// question_idでの検索でRecordNotFoundが出る想定のため、ログ出力を抑えた関数を呼びだしています
++					if _, err := infrastructure.GetNoticeByQuestionSilent(context.Background(), hm.db, question); err != nil {
++						if errors.Is(err, gorm.ErrRecordNotFound) {
++							infrastructure.RegisterNoticeByQuestionID(ctx, hm.db, question.ID)
++							checkedQuestions[question.ID] = struct{}{}
++							continue
++						}
++						fmt.Println(err.Error())
++					}
++				}
++				checkedQuestions[question.ID] = struct{}{}
++			}
++		case <-ctx.Done():
++			return nil
++		}
++	}
++}
+diff --git a/main.go b/main.go
+index 162346f..4ce15f8 100644
+--- a/main.go
++++ b/main.go
+@@ -3,0 +4 @@ import (
++	"context"
+@@ -31,0 +33 @@ func main() {
++	go handlerManager.PollingStart(context.Background())
+diff --git a/package.json b/package.json
+deleted file mode 100644
+index 8d7055f..0000000
+--- a/package.json
++++ /dev/null
+@@ -1,5 +0,0 @@
+-{
+-  "dependencies": {
+-    "htmx-ext-sse": "^2.2.4"
+-  }
+-}
+\ No newline at end of file
+diff --git a/static/js/hrAppData.js b/static/js/hrAppData.js
+index e6718ef..124bbbc 100644
+--- a/static/js/hrAppData.js
++++ b/static/js/hrAppData.js
+@@ -1,0 +2 @@ import { Question, User, Tag } from '/static/js/model.js';
++import { SSE_ADD_NOTICE, SSE_ADD_QUESTION, SSE_ADD_TAG, SSE_ADD_USER, SSE_TIME_TICKER } from './sse.js';
+@@ -4,0 +6 @@ document.addEventListener('alpine:init', () => {
++        timeNow: new Date(),
+@@ -50,4 +52 @@ document.addEventListener('alpine:init', () => {
+-        notices: [
+-            { id: 1, type: 'alert', title: '期日接近: 質問 #1002', time: '10分前', content: '「通勤手当の経路変更」の期日が明日です。早急な対応をお願いします。' },
+-            { id: 2, type: 'info', title: 'システムアップデート完了', time: '1時間前', content: 'FAQ検索エンジンの精度を向上させるアップデートを適用しました。' }
+-        ],
++        notices: [],
+@@ -104,0 +104 @@ document.addEventListener('alpine:init', () => {
++            this.activeQuestion = { support: { supportStatusID: "", user: { name: "" } } };
+@@ -113 +113,5 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener('sse-notice', (event) => {
++            document.addEventListener(SSE_TIME_TICKER, (event) => {
++                this.timeNow = event.detail
++            });
++
++            document.addEventListener(SSE_ADD_NOTICE, (event) => {
+@@ -115,3 +119,14 @@ document.addEventListener('alpine:init', () => {
+-                this.notices.unshift(notice);
+-                if (notice.type === 'alert') this.unreadNotices += 1;
+-                this.refreshIcons();
++                const index = this.notices.findIndex(n => n.id === notice.id)
++                if (index === -1) {
++                    this.notices.unshift(notice);
++                    this.refreshIcons();
++                } else {
++                    if (!_.isEqual(this.notices[index], notice)) {
++                        this.notices.splice(index, 1, {
++                            ...this.notices[index],
++                            ...notice,
++                        });
++                        this.refreshIcons();
++                    }
++                }
++                if (notice.noticeType.name === 'ALERT') this.unreadNotices += 1;
+@@ -120 +135 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener('sse-question', (event) => {
++            document.addEventListener(SSE_ADD_QUESTION, (event) => {
+@@ -122 +136,0 @@ document.addEventListener('alpine:init', () => {
+-                console.log(this.questions)
+@@ -141 +155,2 @@ document.addEventListener('alpine:init', () => {
+-                                const originalVal = _.get(this.originalQuestion, key);
++                                const originalVal = _.get(this.originalQuestion, key)
++                                // ローカル変更されてたら優先
+@@ -143 +158 @@ document.addEventListener('alpine:init', () => {
+-                                    return localVal;
++                                    return localVal
+@@ -145 +160,2 @@ document.addEventListener('alpine:init', () => {
+-                                return serverVal;
++                                // server採用
++                                return serverVal
+@@ -147,2 +163,2 @@ document.addEventListener('alpine:init', () => {
+-                        );
+-                        this.originalQuestion = _.cloneDeep(question);
++                        )
++                        this.originalQuestion = _.cloneDeep(question)
+@@ -152,2 +167,0 @@ document.addEventListener('alpine:init', () => {
+-
+-                this.refreshIcons();
+@@ -156 +170 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener('sse-user', (event) => {
++            document.addEventListener(SSE_ADD_USER, (event) => {
+@@ -173 +187 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener('sse-tag', (event) => {
++            document.addEventListener(SSE_ADD_TAG, (event) => {
+@@ -192,0 +207,8 @@ document.addEventListener('alpine:init', () => {
++        currentTimeFormatted(dt = new Date()) {
++            return new Intl.DateTimeFormat('ja-JP', {
++                hour: '2-digit',
++                minute: '2-digit',
++                second: '2-digit'
++            }).format(dt);
++        },
++
+@@ -206 +227,0 @@ document.addEventListener('alpine:init', () => {
+-
+@@ -272,2 +293 @@ document.addEventListener('alpine:init', () => {
+-            const idx = this.activeQuestion.tags.findIndex(t => t.id === tag.id);
+-            if (idx !== -1) {
++            if (this.activeQuestion.tags.filter(t => t.id === tag.id).length === 1) {
+@@ -296 +315,0 @@ document.addEventListener('alpine:init', () => {
+-                console.log(Question.toModel(this.activeQuestion));
+@@ -350,2 +368,0 @@ document.addEventListener('alpine:init', () => {
+-            const json = await res.json();
+-            console.log(json);
+@@ -360,2 +377,2 @@ document.addEventListener('alpine:init', () => {
+-                    const newTags = [...this.tags.filter(t => t.id !== id)];
+-                    this.tags = newTags;
++                    const newTags = [...this.tags.filter(t => t.id !== id)]
++                    this.tags = newTags
+@@ -395,0 +413,14 @@ document.addEventListener('alpine:init', () => {
++        calcRemainingTimeAndString(question) {
++            const [hours, minutes] = this.calcRemainingTime(new Date(question.due), this.timeNow)
++            return `${hours}時間${minutes}分`;
++        },
++
++
++        calcRemainingTime(baseDate, d = new Date()) {
++            const diff = baseDate - d;
++            const hours = Math.floor(diff / (1000 * 60 * 60));
++            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
++
++            return [hours, minutes];
++        },
++
+diff --git a/static/js/model.js b/static/js/model.js
+index eb4f303..1fef66b 100644
+--- a/static/js/model.js
++++ b/static/js/model.js
+@@ -41 +41 @@ export class Role {
+-    this.id       = id;
++    this.id = id;
+@@ -43 +43 @@ export class Role {
+-    this.users    = users;
++    this.users = users;
+@@ -71,2 +71,2 @@ export class SupportStatus {
+-    this.id       = id;
+-    this.title    = title;
++    this.id = id;
++    this.title = title;
+@@ -97,2 +97,2 @@ export class Support {
+-   * @param {number|string} userId
+-   * @param {number|string} supportStatusId
++   * @param {number}        userId
++   * @param {string}        supportStatusId
+@@ -104,2 +104,2 @@ export class Support {
+-    this.id              = id;
+-    this.userId          = userId;
++    this.id = id;
++    this.userId = userId;
+@@ -107,3 +107,3 @@ export class Support {
+-    this.user            = user;
+-    this.supportStatus   = supportStatus;
+-    this.question        = question;
++    this.user = user;
++    this.supportStatus = supportStatus;
++    this.question = question;
+@@ -145,5 +145,5 @@ export class User {
+-    this.id       = id;
+-    this.name     = name;
+-    this.email    = email;
+-    this.roleId   = roleId;
+-    this.role     = role;
++    this.id = id;
++    this.name = name;
++    this.email = email;
++    this.roleId = roleId;
++    this.role = role;
+@@ -151,2 +151,2 @@ export class User {
+-    this.answers  = answers;
+-    this.memos    = memos;
++    this.answers = answers;
++    this.memos = memos;
+@@ -158 +158 @@ export class User {
+-    // const roleIdNum =
++    // const roleIdNum = 
+@@ -192 +192 @@ export class Category {
+-    this.id           = id;
++    this.id = id;
+@@ -194 +194 @@ export class Category {
+-    this.tags         = tags;
++    this.tags = tags;
+@@ -225,3 +225,3 @@ export class Tag {
+-    this.id         = id;
+-    this.title      = title;
+-    this.usage      = usage;
++    this.id = id;
++    this.title = title;
++    this.usage = usage;
+@@ -229,2 +229,2 @@ export class Tag {
+-    this.category   = category;
+-    this.questions  = questions;
++    this.category = category;
++    this.questions = questions;
+@@ -269,3 +269,3 @@ export class Refer {
+-    this.id      = id;
+-    this.title   = title;
+-    this.url     = url;
++    this.id = id;
++    this.title = title;
++    this.url = url;
+@@ -304 +304 @@ export class Memo {
+-    this.id         = id;
++    this.id = id;
+@@ -306,4 +306,4 @@ export class Memo {
+-    this.userId     = userId;
+-    this.content    = content;
+-    this.question   = question;
+-    this.user       = user;
++    this.userId = userId;
++    this.content = content;
++    this.question = question;
++    this.user = user;
+@@ -344,3 +344,3 @@ export class Answer {
+-    this.id         = id;
+-    this.userId     = userId;
+-    this.content    = content;
++    this.id = id;
++    this.userId = userId;
++    this.content = content;
+@@ -348,3 +348,3 @@ export class Answer {
+-    this.createdAt  = new Date(createdAt);
+-    this.user       = user;
+-    this.refers     = refers;
++    this.createdAt = new Date(createdAt);
++    this.user = user;
++    this.refers = refers;
+@@ -385 +385 @@ export class Escalation {
+-    this.id             = id;
++    this.id = id;
+@@ -387,4 +387,4 @@ export class Escalation {
+-    this.toQuestionId   = toQuestionId;
+-    this.escalatedAt    = new Date(escalatedAt);
+-    this.fromQuestion   = fromQuestion;
+-    this.toQuestion     = toQuestion;
++    this.toQuestionId = toQuestionId;
++    this.escalatedAt = new Date(escalatedAt);
++    this.fromQuestion = fromQuestion;
++    this.toQuestion = toQuestion;
+@@ -421,2 +421,2 @@ export class NoticeType {
+-    this.id      = id;
+-    this.name    = name;
++    this.id = id;
++    this.name = name;
+@@ -455,2 +455,2 @@ export class Notice {
+-    this.id         = id;
+-    this.typeId     = typeId;
++    this.id = id;
++    this.typeId = typeId;
+@@ -458 +458 @@ export class Notice {
+-    this.content    = content;
++    this.content = content;
+@@ -461 +461 @@ export class Notice {
+-    this.question   = question;
++    this.question = question;
+@@ -488,13 +487,0 @@ export class Question {
+-<<<<<<< HEAD
+-<<<<<<< Updated upstream
+- * @param {number}         id
+- * @param {number|null}    originQuestionId
+- * @param {number|null}    supportId
+- * @param {string}         title
+- * @param {string}         content
+- * @param {string|null}    due              ISO8601 文字列 or null
+- * @param {string}         createdAt        ISO8601 文字列
+-=======
+-   * @param {number}         id
+-   * @param {number|null}    messageId
+-=======
+@@ -502 +488,0 @@ export class Question {
+->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
+@@ -511,4 +496,0 @@ export class Question {
+-<<<<<<< HEAD
+->>>>>>> Stashed changes
+-=======
+->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
+@@ -531 +513 @@ export class Question {
+-    this.id               = id;
++    this.id = id;
+@@ -533,16 +515,16 @@ export class Question {
+-    this.answerId         = answerId;
+-    this.supportId        = supportId;
+-    this.title            = title;
+-    this.content          = content;
+-    this.deleted          = deleted;
+-    this.due              = due ? new Date(due) : null;
+-    this.createdAt        = new Date(createdAt);
+-    this.originQuestion   = originQuestion;
+-    this.subQuestions     = subQuestions;
+-    this.support          = support;
+-    this.answer           = answer;
+-    this.memos            = memos;
+-    this.tags             = tags;
+-    this.notices          = notices;
+-    this.escalationsFrom  = escalationsFrom;
+-    this.escalationsTo    = escalationsTo;
++    this.answerId = answerId;
++    this.supportId = supportId;
++    this.title = title;
++    this.content = content;
++    this.deleted = deleted;
++    this.due = due ? new Date(due) : null;
++    this.createdAt = new Date(createdAt);
++    this.originQuestion = originQuestion;
++    this.subQuestions = subQuestions;
++    this.support = support;
++    this.answer = answer;
++    this.memos = memos;
++    this.tags = tags;
++    this.notices = notices;
++    this.escalationsFrom = escalationsFrom;
++    this.escalationsTo = escalationsTo;
+@@ -597 +579 @@ export class ReferManager {
+-    this.id       = id;
++    this.id = id;
+@@ -599,3 +581,3 @@ export class ReferManager {
+-    this.referId  = referId;
+-    this.answer   = answer;
+-    this.refer    = refer;
++    this.referId = referId;
++    this.answer = answer;
++    this.refer = refer;
+@@ -633,2 +615,2 @@ export class TagManager {
+-    this.id         = id;
+-    this.tagId      = tagId;
++    this.id = id;
++    this.tagId = tagId;
+@@ -636,2 +618,2 @@ export class TagManager {
+-    this.tag        = tag;
+-    this.question   = question;
++    this.tag = tag;
++    this.question = question;
+diff --git a/static/js/sse.js b/static/js/sse.js
+index 4ae6276..08f84e7 100644
+--- a/static/js/sse.js
++++ b/static/js/sse.js
+@@ -1 +1,7 @@
+-import { Question, User, Tag } from '/static/js/model.js';
++import { Question, User, Tag, Notice } from '/static/js/model.js';
++
++export const SSE_TIME_TICKER = "time-ticker";
++export const SSE_ADD_NOTICE = "sse-notice";
++export const SSE_ADD_USER = "sse-user";
++export const SSE_ADD_QUESTION = "sse-question";
++export const SSE_ADD_TAG = "sse-tag";
+@@ -17,0 +24,6 @@ document.addEventListener('DOMContentLoaded', () => {
++    es.addEventListener('time-tick', (event) => {
++        document.dispatchEvent(new CustomEvent(SSE_TIME_TICKER, {
++            detail: new Date(event.data),
++        }))
++    })
++
+@@ -20,2 +32,4 @@ document.addEventListener('DOMContentLoaded', () => {
+-            document.dispatchEvent(new CustomEvent('sse-notice', {
+-                detail: JSON.parse(event.data),
++            const json = JSON.parse(event.data);
++            const notice = Notice.fromJSON(json);
++            document.dispatchEvent(new CustomEvent(SSE_ADD_NOTICE, {
++                detail: notice,
+@@ -29,2 +43 @@ document.addEventListener('DOMContentLoaded', () => {
+-            console.log(question);
+-            document.dispatchEvent(new CustomEvent('sse-question', {
++            document.dispatchEvent(new CustomEvent(SSE_ADD_QUESTION, {
+@@ -39 +52 @@ document.addEventListener('DOMContentLoaded', () => {
+-            document.dispatchEvent(new CustomEvent('sse-user', {
++            document.dispatchEvent(new CustomEvent(SSE_ADD_USER, {
+@@ -48 +61 @@ document.addEventListener('DOMContentLoaded', () => {
+-            document.dispatchEvent(new CustomEvent('sse-tag', {
++            document.dispatchEvent(new CustomEvent(SSE_ADD_TAG, {
+diff --git a/templates/components/login_head.html b/templates/components/login_head.html
+index 352793f..b2fad6e 100644
+--- a/templates/components/login_head.html
++++ b/templates/components/login_head.html
+@@ -23,0 +24,6 @@
++            init() {
++                if (typeof lucide !== 'undefined') {
++                    lucide.createIcons();
++                }
++            },
++
+@@ -51 +57 @@
+-{{end}}
++{{end}}
+\ No newline at end of file
+diff --git a/templates/components/mock5_header.html b/templates/components/mock5_header.html
+index 84a5236..1b7a60f 100644
+--- a/templates/components/mock5_header.html
++++ b/templates/components/mock5_header.html
+@@ -20,2 +20,2 @@
+-        <div hx-ext="sse" sse-connect="/sse">
+-            <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
++        <div>
++            <span class="font-mono text-sm text-base-content/60" x-text="currentTimeFormatted(timeNow)">
+diff --git a/templates/components/mock5_view_detail.html b/templates/components/mock5_view_detail.html
+index f7c342d..5e244f4 100644
+--- a/templates/components/mock5_view_detail.html
++++ b/templates/components/mock5_view_detail.html
+@@ -62,0 +63 @@
++                <div class="divider my-0"></div>
+@@ -68 +69 @@
+-                            x-text="activeQuestion.support?.user?.name?.charAt(0)"></div>
++                            x-text="activeQuestion.support.user.name?.charAt(0)"></div>
+@@ -70 +71 @@
+-                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.support?.user?.name"></div>
++                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.support.user.name"></div>
+@@ -75,2 +75,0 @@
+-                <div class="divider my-0"></div>
+-
+diff --git a/templates/components/mock5_view_home.html b/templates/components/mock5_view_home.html
+index 72467df..d0815ae 100644
+--- a/templates/components/mock5_view_home.html
++++ b/templates/components/mock5_view_home.html
+@@ -113 +113 @@
+-                            :class="statusColorMap[q.support?.supportStatus?.title]" x-text="q.support?.supportStatus?.title"></span>
++                            :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
+@@ -125 +125 @@
+-                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+@@ -127 +127 @@
+-                            <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                            <span x-text="q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+@@ -143 +143 @@
+-                        :class="statusColorMap[q.support?.supportStatus?.title]" x-text="q.support?.supportStatus?.title"></span>
++                        :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
+@@ -155 +155 @@
+-                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+@@ -157 +157 @@
+-                        <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                        <span x-text="q.support ?? q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+diff --git a/templates/components/mock5_view_notice.html b/templates/components/mock5_view_notice.html
+index 484f0e6..c95b602 100644
+--- a/templates/components/mock5_view_notice.html
++++ b/templates/components/mock5_view_notice.html
+@@ -7,2 +7,2 @@
+-                :class="n.type === 'alert' ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
+-                <i :data-lucide="n.type === 'alert' ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++                :class="[2, 3].includes(n.typeId) ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
++                <i :data-lucide="[2, 3].includes(n.typeId) ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
+@@ -12,2 +12,11 @@
+-                    <h3 class="font-bold text-sm text-gray-800" x-text="n.title"></h3>
+-                    <span class="text-[10px] font-bold text-gray-400" x-text="n.time"></span>
++                    <h3 class="font-bold text-sm text-gray-800" x-show="[1, 2].includes(n.typeId)" x-text="n.content"></h3>
++                    <h3 class="font-bold text-sm text-gray-800" x-show="n.typeId === 3" x-text="n.question && (n.question.title ? `回答期限接近中: ${n.question.title}` : `回答期限接近中: 質問ID#${n.question.id}`)"></h3>
++                    <div class="justify-between">
++                        <span class="text-[10px] font-bold text-gray-400" x-text="n.question && n.question.due && `残り時間: ${calcRemainingTimeAndString(n.question)}`"></span>
++                        <div class="flex gap-1 shrink-0">
++                            <template x-for="tag in (n.question.tags || [])" :key="tag.id">
++                                <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase"
++                                    x-text="tag.title"></span>
++                            </template>
++                        </div>
++                    </div>
+@@ -15 +24 @@
+-                <p class="text-xs text-gray-500 line-clamp-2" x-text="n.content"></p>
++                <p class="text-xs text-gray-500 line-clamp-2" x-show="n.question" x-text="n.question && n.question.content"></p>
+diff --git a/templates/sample/index.html b/templates/sample/index.html
+index dea253f..0f7e8d2 100644
+--- a/templates/sample/index.html
++++ b/templates/sample/index.html
+@@ -1,455 +0,0 @@
+-<<<<<<< Updated upstream
+-<!DOCTYPE html>
+-<html lang="ja" data-theme="cupcake">
+-
+-<head>
+-    <meta charset="UTF-8">
+-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+-    <title>{{.Title}}</title>
+-    <link href="/static/css/output.css" rel="stylesheet">
+-
+-    <!--
+-        module スクリプトは defer 扱いのため、後続の defer Alpine より先に実行される。
+-        alpine:init リスナーを登録 → Alpine が初期化時に発火 → Alpine.data() が登録される。
+-    -->
+-    <script type="module">
+-        import { Question } from '/static/js/model.js';
+-
+-        document.addEventListener('alpine:init', () => {
+-            Alpine.data('questionBoard', () => ({
+-                questions: [],
+-                questionIDMap: new Map([]),
+-                filter: '',
+-                connected: false,
+-
+-                form: {content: '', due: '' },
+-                submitting: false,
+-                formError: '',
+-
+-                currentView: 'home',
+-
+-                questionDetail: {},
+-
+-                async getQuestionDetail(id) {
+-                    const res = await fetch(`/api/v1/question/${id}`);
+-                    const body = await res.json();
+-                    this.questionDetail = Question.fromJSON(body);
+-                },
+-
+-                async deleteQuestion(id) {
+-                    const res = await fetch(`/api/v1/question/${id}`, {
+-                        method: "DELETE"
+-                    }).then(v => {
+-                        if (v.status == 200) {
+-                            this.questions = this.questions.filter(q => q.id != id);
+-                            this.closeDetail()
+-                        } else {
+-                            this.formError = '質問データの削除に失敗しました。'
+-                        }
+-                    });
+-                },
+-
+-                openDetail(id) {
+-                    this.getQuestionDetail(id)
+-                    document.getElementById('question-detail-modal').showModal();
+-                },
+-                closeDetail() {
+-                    document.getElementById('question-detail-modal').close();
+-                },
+-
+-                openForm() {
+-                    this.form = { title: '', content: '', due: '' };
+-                    this.formError = '';
+-                    document.getElementById('question-form-modal').showModal();
+-                },
+-                closeForm() {
+-                    document.getElementById('question-form-modal').close();
+-                },
+-                async submitForm() {
+-                    if (!this.form.content.trim()) {
+-                        this.formError = '内容を入力してください';
+-                        return;
+-                    }
+-                    this.submitting = true;
+-                    this.formError = '';
+-                    try {
+-                        const body = { content: this.form.content.trim(), title: this.form.title.trim() };
+-                        if (this.form.due) body.due = `${this.form.due}:00+09:00`;
+-                        const res = await fetch('/api/v1/question', {
+-                            method: 'POST',
+-                            headers: { 'Content-Type': 'application/json' },
+-                            body: JSON.stringify(body),
+-                        });
+-                        if (!res.ok) throw new Error(`HTTP ${res.status}`);
+-                        this.closeForm();
+-                    } catch (err) {
+-                        this.formError = `送信に失敗しました (${err.message})`;
+-                    } finally {
+-                        this.submitting = false;
+-                    }
+-                },
+-
+-                get filteredQuestions() {
+-                    const f = this.filter.toLowerCase().trim();
+-                    if (!f) return this.questions;
+-                    return this.questions.filter(q =>
+-                        q.title.toLowerCase().includes(f) ||
+-                        (q.tags ?? []).some(t => t.title.toLowerCase().includes(f))
+-                    );
+-                },
+-
+-                isIncludeMap(id) {
+-                    return this.questionIDMap[id] == undefined ? false : true;
+-                },
+-
+-                init() {
+-                    const es = new EventSource('/sse');
+-                    es.onopen = () => { this.connected = true; };
+-                    es.onerror = () => { this.connected = false; };
+-                    es.addEventListener('question', (e) => {
+-                        try {
+-                            const q = Question.fromJSON(JSON.parse(e.data));
+-                            if (!this.isIncludeMap(q.id)) {
+-                                this.questionIDMap[q.id] = true;
+-                                this.questions.unshift(q);
+-                            }
+-                        } catch (err) {
+-                            console.error('Failed to parse question:', err);
+-                        }
+-                    });
+-                },
+-
+-                formatDate(d) {
+-                    if (!d) return null;
+-                    return new Intl.DateTimeFormat('ja-JP', {
+-                        year: 'numeric', month: '2-digit', day: '2-digit',
+-                        hour: '2-digit', minute: '2-digit',
+-                    }).format(new Date(d));
+-                },
+-            }));
+-        });
+-    </script>
+-
+-    <script defer src="/static/vendor/htmx.min.js"></script>
+-    <script defer src="/static/vendor/sse.min.js"></script>
+-    <script defer src="/static/vendor/alpine.min.js"></script>
+-</head>
+-
+-<body class="min-h-screen bg-base-200">
+-
+-    <!-- ========== ヘッダー ========== -->
+-    <header class="sticky top-0 z-10 bg-base-100 shadow-sm">
+-        <div class="max-w-6xl mx-auto px-6 py-3 flex items-center justify-between gap-4">
+-
+-            <h1 class="text-xl font-bold text-primary tracking-tight">Question Board</h1>
+-
+-            <div class="flex items-center gap-3">
+-                <!-- 接続状態インジケーター -->
+-                <div x-data x-cloak class="hidden"><!-- Alpine 読み込み前の FOUC 防止 --></div>
+-
+-                <!-- サーバー時刻 (htmx SSE) -->
+-                <div hx-ext="sse" sse-connect="/sse">
+-                    <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
+-                        --:--:--
+-                    </span>
+-                </div>
+-            </div>
+-
+-        </div>
+-    </header>
+-
+-    <!-- ========== メインコンテンツ ========== -->
+-    <main class="max-w-6xl mx-auto px-6 py-8" x-data="questionBoard()" x-init="init()">
+-        <!-- ---- フィルターバー ---- -->
+-        <div class="mb-6 flex flex-col sm:flex-row items-stretch sm:items-center gap-3 flex-wrap">
+-
+-            <!-- 検索ボックス -->
+-            <label class="flex-1 input input-bordered flex items-center gap-2">
+-                <!-- search icon -->
+-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 opacity-50" fill="none"
+-                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                    <path stroke-linecap="round" stroke-linejoin="round"
+-                        d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
+-                </svg>
+-                <input type="text" x-model="filter" placeholder="タイトル・タグで絞り込み..."
+-                    class="grow outline-none bg-transparent text-sm" />
+-                <!-- クリアボタン -->
+-                <button x-show="filter !== ''" @click="filter = ''" class="btn btn-ghost btn-xs btn-circle"
+-                    aria-label="クリア">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="2.5">
+-                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
+-                    </svg>
+-                </button>
+-            </label>
+-
+-            <!-- 質問追加ボタン -->
+-            <button @click="openForm()" class="btn btn-primary btn-sm gap-1.5 shrink-0">
+-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
+-                    stroke="currentColor" stroke-width="2.5">
+-                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
+-                </svg>
+-                質問を追加
+-            </button>
+-
+-            <!-- 件数バッジ + 接続状態 -->
+-            <div class="flex items-center gap-2 shrink-0">
+-                <span class="badge badge-primary badge-outline font-mono"
+-                    x-text="`${filteredQuestions.length} 件`"></span>
+-
+-                <!-- 接続インジケーター -->
+-                <div class="flex items-center gap-1 text-xs" :class="connected ? 'text-success' : 'text-error'">
+-                    <span class="inline-block w-2 h-2 rounded-full animate-pulse"
+-                        :class="connected ? 'bg-success' : 'bg-error'"></span>
+-                    <span x-text="connected ? 'LIVE' : 'disconnected'"></span>
+-                </div>
+-            </div>
+-
+-        </div>
+-
+-        <!-- ---- カードグリッド ---- -->
+-        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
+-
+-            <template x-for="q in filteredQuestions" :key="q.id">
+-                <div x-transition:enter="transition ease-out duration-300"
+-                    x-transition:enter-start="opacity-0 scale-95 translate-y-2"
+-                    x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                    x-transition:leave="transition ease-in duration-200"
+-                    x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                    x-transition:leave-end="opacity-0 scale-95 translate-y-2"
+-                    class="card bg-base-100 shadow hover:shadow-lg transition-shadow duration-200 cursor-pointer"
+-                    @click="openDetail(q.id)">
+-                    <div class="card-body gap-3 p-5">
+-
+-                        <!-- タイトル + ID -->
+-                        <div class="flex items-start justify-between gap-2">
+-                            <h2 class="card-title text-sm leading-snug" x-text="q.title"></h2>
+-                            <span class="badge badge-ghost badge-sm shrink-0 font-mono" x-text="`#${q.id}`"></span>
+-                        </div>
+-
+-                        <div class="flex items-start gap-2">
+-                            <span x-text="q.content"></span>
+-                        </div>
+-
+-                        <!-- 期限 -->
+-                        <template x-if="q.due">
+-                            <div class="flex items-center gap-1 text-xs text-warning font-medium">
+-                                <!-- clock icon -->
+-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0" fill="none"
+-                                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                                    <path stroke-linecap="round" stroke-linejoin="round"
+-                                        d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />
+-                                </svg>
+-                                <span x-text="`期限: ${formatDate(q.due)}`"></span>
+-                            </div>
+-                        </template>
+-
+-                        <!-- サポートステータス -->
+-                        <template x-if="q.support && q.support.supportStatus">
+-                            <div class="flex items-center gap-1 text-xs">
+-                                <!-- tag icon -->
+-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0 opacity-60"
+-                                    fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                                    <path stroke-linecap="round" stroke-linejoin="round"
+-                                        d="M7 7h.01M7 3h14a1 1 0 0 1 1 1v9.586a1 1 0 0 1-.293.707l-7 7a1 1 0 0 1-1.414 0l-9.586-9.586A1 1 0 0 1 3 11V4a1 1 0 0 1 1-1h3z" />
+-                                </svg>
+-                                <span class="badge badge-secondary badge-sm"
+-                                    x-text="q.support.supportStatus.title"></span>
+-                            </div>
+-                        </template>
+-
+-                        <!-- タグ一覧 -->
+-                        <template x-if="q.tags && q.tags.length > 0">
+-                            <div class="flex flex-wrap gap-1">
+-                                <template x-for="tag in q.tags" :key="tag.id">
+-                                    <span class="badge badge-outline badge-xs" x-text="tag.title"></span>
+-                                </template>
+-                            </div>
+-                        </template>
+-
+-                        <!-- フッター: 作成日時 -->
+-                        <div class="mt-auto pt-2 border-t border-base-200 flex justify-end">
+-                            <span class="text-xs text-base-content/40 font-mono"
+-                                x-text="formatDate(q.createdAt)"></span>
+-                        </div>
+-
+-                    </div>
+-                </div>
+-            </template>
+-
+-        </div>
+-
+-        <!-- ---- 空状態 ---- -->
+-        <div x-show="filteredQuestions.length === 0" x-transition:enter="transition ease-out duration-300"
+-            x-transition:enter-start="opacity-0 translate-y-2" x-transition:enter-end="opacity-100 translate-y-0"
+-            x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100"
+-            x-transition:leave-end="opacity-0"
+-            class="flex flex-col items-center justify-center py-24 gap-4 text-base-content/40">
+-
+-            <!-- フィルター結果なし -->
+-            <template x-if="filter !== ''">
+-                <div class="flex flex-col items-center gap-2">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-30" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="1.5">
+-                        <path stroke-linecap="round" stroke-linejoin="round"
+-                            d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
+-                    </svg>
+-                    <p class="text-sm">「<span class="font-semibold" x-text="filter"></span>」に一致する Question が見つかりません</p>
+-                    <button @click="filter = ''" class="btn btn-ghost btn-xs mt-1">
+-                        フィルターをクリア
+-                    </button>
+-                </div>
+-            </template>
+-
+-            <!-- SSE 待機中 -->
+-            <template x-if="filter === '' && questions.length === 0">
+-                <div class="flex flex-col items-center gap-3">
+-                    <span class="loading loading-ring loading-lg text-primary opacity-50"></span>
+-                    <p class="text-sm">SSE から Question を待機中...</p>
+-                </div>
+-            </template>
+-
+-        </div>
+-
+-        <!-- ---- 質問入力モーダル ---- -->
+-        <dialog id="question-form-modal" class="modal modal-bottom sm:modal-middle">
+-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
+-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
+-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave="transition ease-in duration-150"
+-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
+-
+-                <!-- ヘッダー -->
+-                <div class="flex items-center justify-between mb-5">
+-                    <h3 class="font-bold text-lg">質問を追加</h3>
+-                    <button @click="closeForm()" class="btn btn-ghost btn-sm btn-circle" aria-label="閉じる">
+-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
+-                            stroke="currentColor" stroke-width="2.5">
+-                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
+-                        </svg>
+-                    </button>
+-                </div>
+-
+-                <!-- フォーム -->
+-                <form @submit.prevent="submitForm()" class="flex flex-col gap-4" novalidate>
+-                    <!-- タイトル -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">
+-                                タイトル
+-                                <span class="text-error ml-0.5">*</span>
+-                            </span>
+-                        </div>
+-                        <textarea x-model="form.title" placeholder="質問のタイトルを入力..." rows="3"
+-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
+-                            :class="formError && !form.title.trim() ? 'textarea-error' : ''"
+-                            :disabled="submitting"></textarea>
+-                    </label>
+-
+-                    <!-- 内容 -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">
+-                                内容
+-                                <span class="text-error ml-0.5">*</span>
+-                            </span>
+-                        </div>
+-                        <textarea x-model="form.content" placeholder="質問の内容を入力..." rows="3"
+-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
+-                            :class="formError && !form.content.trim() ? 'textarea-error' : ''"
+-                            :disabled="submitting"></textarea>
+-                    </label>
+-
+-                    <!-- 期日 -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">期日</span>
+-                            <span class="label-text-alt text-base-content/40">任意</span>
+-                        </div>
+-                        <input type="datetime-local" x-model="form.due"
+-                            class="input input-bordered w-full focus:outline-none" :disabled="submitting" />
+-                    </label>
+-
+-                    <!-- エラーメッセージ -->
+-                    <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
+-                        x-transition:enter-start="opacity-0 -translate-y-1"
+-                        x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
+-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
+-                            stroke="currentColor" stroke-width="2">
+-                            <path stroke-linecap="round" stroke-linejoin="round"
+-                                d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
+-                        </svg>
+-                        <span x-text="formError"></span>
+-                    </div>
+-
+-                    <!-- アクション -->
+-                    <div class="modal-action mt-1">
+-                        <button type="button" @click="closeForm()" class="btn btn-ghost" :disabled="submitting">
+-                            キャンセル
+-                        </button>
+-                        <button type="submit" class="btn btn-primary gap-2" :disabled="submitting">
+-                            <span x-show="submitting" class="loading loading-spinner loading-xs"></span>
+-                            <span x-text="submitting ? '送信中...' : '送信'"></span>
+-                        </button>
+-                    </div>
+-
+-                </form>
+-            </div>
+-
+-            <!-- バックドロップクリックで閉じる -->
+-            <form method="dialog" class="modal-backdrop">
+-                <button @click="closeForm()">close</button>
+-            </form>
+-        </dialog>
+-
+-        <!-- ---- 詳細モーダル ---- -->
+-        <dialog id="question-detail-modal" class="modal modal-bottom sm:modal-middle">
+-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
+-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
+-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave="transition ease-in duration-150"
+-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
+-
+-                <div class="flex items-center justify-between mb-5">
+-                    <span class="card-title" x-text="questionDetail.title"></span>
+-                </div>
+-                <hr />
+-
+-                <div class="flex items-center justify-between mb-2">
+-                    <span class="card-body" x-text="questionDetail.content"></span>
+-                </div>
+-
+-                <div class="flex items-center justify-between mb-5">
+-                    <textarea name="raw-json" id="raw-json" rows="30" cols="50"
+-                        x-text="JSON.stringify(questionDetail)"></textarea>
+-                </div>
+-
+-                <!-- エラーメッセージ -->
+-                <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
+-                    x-transition:enter-start="opacity-0 -translate-y-1"
+-                    x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="2">
+-                        <path stroke-linecap="round" stroke-linejoin="round"
+-                            d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
+-                    </svg>
+-                    <span x-text="formError"></span>
+-                </div>
+-
+-                <div class="flex items-end justify-between mb-5">
+-                    <button class="btn btn-circle" @click="deleteQuestion(questionDetail.id)">削除</button>
+-                </div>
+-            </div>
+-
+-            <!-- バックドロップクリックで閉じる -->
+-            <form method="dialog" class="modal-backdrop">
+-                <button @click="closeDetail()">close</button>
+-            </form>
+-        </dialog>
+-    </main>
+-
+-</body>
+-
+-=======
+@@ -479 +24 @@
+-                form: { content: '', due: '' },
++                form: {content: '', due: '' },
+@@ -488 +33 @@
+-                    const res = await fetch(`/api/question/${id}`);
++                    const res = await fetch(`/api/v1/question/${id}`);
+@@ -494 +39 @@
+-                    const res = await fetch(`/api/question/${id}`, {
++                    const res = await fetch(`/api/v1/question/${id}`, {
+@@ -508 +52,0 @@
+-                    this.drawCommunication(id)
+@@ -533 +77 @@
+-                        const res = await fetch('/api/questions', {
++                        const res = await fetch('/api/v1/question', {
+@@ -909,2 +452,0 @@
+-
+->>>>>>> Stashed changes
+diff --git a/templates/sample/mock4.5.html b/templates/sample/mock4.5.html
+index 0a513e4..7a588d6 100644
+--- a/templates/sample/mock4.5.html
++++ b/templates/sample/mock4.5.html
+@@ -31,0 +32,865 @@
++<html lang="ja" data-theme="light">
++<head>
++    <meta charset="UTF-8">
++    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
++    <title>HR FAQ Management v0.2</title>
++    
++    <!-- 1. Tailwind CSS + DaisyUI (built) -->
++    <link href="/static/css/output.css" rel="stylesheet">
++
++    <!-- 2. Lucide Icons -->
++    <script src="https://unpkg.com/lucide@latest"></script>
++
++    <!-- 3. htmx -->
++    <script src="/static/vendor/htmx.min.js"></script>
++
++    <style>
++        .no-scrollbar::-webkit-scrollbar { display: none; }
++        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
++        [x-cloak] { display: none !important; }
++        .transition-all { transition: all 0.3s ease; }
++        .sidebar-transition { transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
++    </style>
++
++    <script>
++        document.addEventListener('alpine:init', () => {
++            Alpine.data('hrAppData', () => ({
++                isLoggedIn: false,
++                isSidebarOpen: true,
++                currentView: 'home',
++                viewMode: 'card',
++                isManager: true,
++                showAddUserModal: false,
++                unreadNotices: 1,
++                activeQuestion: {},
++                isConnect: false,
++
++                selectedTags: [],
++                availableTags: ['諸手当', '休暇', '規程', '健康診断'],
++                searchQuery: '',
++                statusFilter: 'all',
++                sortOrder: 'date_desc',
++
++                relatedSearchQuery: '',
++
++                titleMap: {
++                    home: '質問一覧',
++                    detail: '質問詳細',
++                    notice: '通知センター',
++                    users: 'ユーザー管理',
++                    categories: 'カテゴリ管理',
++                    settings: 'プロフィール設定'
++                },
++
++                statusColorMap: {
++                    '未対応': 'bg-red-100 text-red-800',
++                    '対応中': 'bg-blue-100 text-blue-800',
++                    '完了': 'bg-gray-100 text-gray-600'
++                },
++
++                questions: [
++                    { id: 1002, sender: '鈴木 一郎', department: 'ここに部署名', title: '通勤手当の経路変更について', content: '引越しに伴い、通勤経路が変更になります。申請手順と必要な書類を教えてください。', status: '未対応', tags: ['諸手当'], daysLeft: 1, date: '2026-04-08 09:30', dueDate: '2026-04-10', relatedQuestions: [] },
++                    { id: 1003, sender: '田中 花子', department: 'ここに部署名', title: '育児休業の延長申請', content: '現在取得中の育休を半年間延長したいと考えています。手続きの流れを教えてください。', status: '対応中', tags: ['休暇'], daysLeft: 5, date: '2026-04-04 14:00', dueDate: '2026-04-14', relatedQuestions: [] },
++                    { id: 1004, sender: '佐藤 次郎', department: 'ここに部署名', title: '健康診断の受診日変更', content: '指定された健康診断の日程ですが、出張と重なってしまいました。', status: '完了', tags: ['健康診断'], daysLeft: 0, date: '2026-04-01 11:15', dueDate: '2026-04-09', relatedQuestions: [] },
++                    { id: 1005, sender: '高橋 三郎', department: 'ここに部署名', title: '慶弔休暇の適用範囲', content: '配偶者の祖父母が亡くなった場合、忌引休暇の対象になりますでしょうか？', status: '未対応', tags: ['規程'], daysLeft: 2, date: '2026-04-07 16:45', dueDate: '2026-04-11', relatedQuestions: [] }
++                ],
++
++                notices: [
++                    { id: 1, type: 'alert', title: '期日接近: 質問 #1002', time: '10分前', content: '「通勤手当の経路変更」の期日が明日です。早急な対応をお願いします。' },
++                    { id: 2, type: 'info', title: 'システムアップデート完了', time: '1時間前', content: 'FAQ検索エンジンの精度を向上させるアップデートを適用しました。' }
++                ],
++
++                users: [
++                    { name: '自分', email: 'jinji.taro@example.com', role: 'Manager' },
++                    { name: '鈴木 一郎', email: 'suzuki@example.com', role: 'Creator' },
++                    { name: '田中 花子', email: 'tanaka@example.com', role: 'Creator' }
++                ],
++
++                get filteredQuestions() {
++                    let result = this.questions.filter(q => {
++                        const matchesTag = this.selectedTags.length === 0 || q.tags.some(tag => this.selectedTags.includes(tag));
++                        const matchesSearch = q.title.includes(this.searchQuery) || q.content.includes(this.searchQuery) || q.sender.includes(this.searchQuery);
++                        let matchesStatus = true;
++                        if (this.statusFilter === 'unanswered') matchesStatus = q.status === '未対応';
++                        else if (this.statusFilter === 'progress') matchesStatus = q.status === '対応中';
++                        else if (this.statusFilter === 'open') matchesStatus = (q.status === '未対応' || q.status === '対応中');
++                        else if (this.statusFilter === 'completed') matchesStatus = q.status === '完了';
++                        return matchesTag && matchesSearch && matchesStatus;
++                    });
++
++                    result.sort((a, b) => {
++                        if (this.sortOrder === 'date_desc') {
++                            return new Date(b.date) - new Date(a.date);
++                        } else if (this.sortOrder === 'date_asc') {
++                            return new Date(a.date) - new Date(b.date);
++                        } else if (this.sortOrder === 'dueDate_asc') {
++                            return new Date(a.dueDate) - new Date(b.dueDate);
++                        } else if (this.sortOrder === 'dueDate_desc') {
++                            return new Date(b.dueDate) - new Date(a.dueDate);
++                        }
++                        return 0;
++                    });
++
++                    return result;
++                },
++
++                get availableRelatedQuestions() {
++                    if (!this.activeQuestion || !this.activeQuestion.id) return [];
++                    let qs = this.questions.filter(q => q.id !== this.activeQuestion.id);
++                    if (this.relatedSearchQuery) {
++                        const query = this.relatedSearchQuery.toLowerCase();
++                        qs = qs.filter(q => q.title.toLowerCase().includes(query) || q.id.toString().includes(query));
++                    }
++                    return qs;
++                },
++
++                init() {
++                    this.refreshIcons();
++
++                    document.addEventListener('connect disconnect', (event) => {
++                        this.isConnect = event.detail;
++                    })
++
++                    document.addEventListener('sse-notice', (event) => {
++                        const notice = event.detail;
++                        this.notices.unshift(notice);
++                        if (notice.type === 'alert') this.unreadNotices += 1;
++                        this.refreshIcons();
++                    });
++
++                    document.addEventListener('sse-question', (event) => {
++                        const question = this.toQuestionViewModel(event.detail);
++                        const index = this.questions.findIndex(q => q.id === question.id);
++                        if (index === -1) {
++                            this.questions.unshift(question);
++                        } else {
++                            this.questions.splice(index, 1, {
++                                ...this.questions[index],
++                                ...question,
++                            });
++                        }
++
++                        if (question.status === '完了') {
++                            const before = this.notices.length;
++                            this.notices = this.notices.filter(
++                                n => !(n.type === 'alert' && n.title.includes(`#${question.id}`))
++                            );
++                            this.unreadNotices = Math.max(0, this.unreadNotices - (before - this.notices.length));
++                        }
++
++                        if (this.activeQuestion?.id === question.id) {
++                            this.activeQuestion = {
++                                ...this.activeQuestion,
++                                ...question,
++                            };
++                        }
++
++                        this.refreshIcons();
++                    });
++                },
++
++                toQuestionViewModel(question) {
++                    const createdAt = question.createdAt instanceof Date
++                        ? question.createdAt
++                        : new Date(question.createdAt);
++                    const due = question.due instanceof Date
++                        ? question.due
++                        : (question.due ? new Date(question.due) : null);
++                    const daysLeft = due
++                        ? Math.max(0, Math.ceil((due.getTime() - Date.now()) / (1000 * 60 * 60 * 24)))
++                        : 0;
++
++                    return {
++                        id: question.id,
++                        sender: question.support?.user?.name ?? '不明',
++                        department: 'ここに部署名',
++                        title: question.title ?? '',
++                        content: question.content ?? '',
++                        status: question.support?.supportStatus?.title ?? '未対応',
++                        tags: (question.tags ?? []).map(tag => tag.title),
++                        daysLeft,
++                        date: this.formatDateTime(createdAt),
++                        dueDate: due ? this.formatDate(due) : '',
++                        relatedQuestions: (question.subQuestions ?? []).map(q => q.id),
++                    };
++                },
++
++                formatDateTime(date) {
++                    const year = date.getFullYear();
++                    const month = String(date.getMonth() + 1).padStart(2, '0');
++                    const day = String(date.getDate()).padStart(2, '0');
++                    const hour = String(date.getHours()).padStart(2, '0');
++                    const minute = String(date.getMinutes()).padStart(2, '0');
++                    return `${year}-${month}-${day} ${hour}:${minute}`;
++                },
++
++                formatDate(date) {
++                    const year = date.getFullYear();
++                    const month = String(date.getMonth() + 1).padStart(2, '0');
++                    const day = String(date.getDate()).padStart(2, '0');
++                    return `${year}-${month}-${day}`;
++                },
++
++                login() {
++                    this.isLoggedIn = true;
++                    this.refreshIcons();
++                },
++
++                logout() {
++                    if(confirm('ログアウトしますか？')) {
++                        this.isLoggedIn = false;
++                        this.currentView = 'home';
++                        this.refreshIcons();
++                    }
++                },
++
++                setView(view) {
++                    this.currentView = view;
++                    this.refreshIcons();
++                },
++
++                openDetail(q) {
++                    this.activeQuestion = q;
++                    this.relatedSearchQuery = '';
++                    this.setView('detail');
++                },
++
++                toggleTag(tag) {
++                    if (this.selectedTags.includes(tag)) {
++                        this.selectedTags = this.selectedTags.filter(t => t !== tag);
++                    } else {
++                        this.selectedTags.push(tag);
++                    }
++                },
++
++                toggleQuestionTag(tag) {
++                    if (!this.activeQuestion.tags) {
++                        this.activeQuestion.tags = [];
++                    }
++                    if (this.activeQuestion.tags.includes(tag)) {
++                        this.activeQuestion.tags = this.activeQuestion.tags.filter(t => t !== tag);
++                    } else {
++                        this.activeQuestion.tags.push(tag);
++                    }
++                },
++
++                toggleRelatedQuestion(qId) {
++                    if (!this.activeQuestion.relatedQuestions) {
++                        this.activeQuestion.relatedQuestions = [];
++                    }
++                    if (this.activeQuestion.relatedQuestions.includes(qId)) {
++                        this.activeQuestion.relatedQuestions = this.activeQuestion.relatedQuestions.filter(id => id !== qId);
++                    } else {
++                        this.activeQuestion.relatedQuestions.push(qId);
++                    }
++                },
++
++                toggleSidebar() {
++                    this.isSidebarOpen = !this.isSidebarOpen;
++                },
++
++                refreshIcons() {
++                    this.$nextTick(() => {
++                        if (typeof lucide !== 'undefined') {
++                            lucide.createIcons();
++                        }
++                    });
++                }
++            }));
++        });
++    </script>
++
++    <script type="module">
++        import { Question } from '/static/js/model.js';
++
++        document.addEventListener('DOMContentLoaded', () => {
++            const es = new EventSource('/sse');
++
++            es.onopen = (_, event) => {
++                document.dispatchEvent(new CustomeEvent('connect', {
++                    detail: true,
++                }))
++            }
++
++            es.onerror = (_, event) => {
++                document.dispatchEvent(new CustomeEvent('disconnect', {
++                    detail: false,
++                }))
++            }
++
++            es.addEventListener('notice', (event) => {
++                try {
++                    document.dispatchEvent(new CustomEvent('sse-notice', {
++                        detail: JSON.parse(event.data),
++                    }));
++                } catch (_) {}
++            });
++
++            es.addEventListener('question', (event) => {
++                try {
++                    const question = Question.fromJSON(JSON.parse(event.data));
++                    document.dispatchEvent(new CustomEvent('sse-question', {
++                        detail: question,
++                    }));
++                } catch (_) {}
++            });
++        });
++    </script>
++
++    <!-- 4. Alpine.js -->
++    <script defer src="/static/vendor/alpine.min.js"></script>
++</head>
++
++<body class="bg-gray-50 text-gray-900 font-sans antialiased overflow-x-hidden" x-data="hrAppData">
++
++    <!-- 1. ログインビュー -->
++    <div x-show="!isLoggedIn" class="min-h-screen flex items-center justify-center p-4 bg-gray-100">
++        <div class="w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden transition-all">
++            <div class="bg-slate-50 p-8 text-center border-b border-gray-100">
++                <div class="inline-flex items-center justify-center w-16 h-16 bg-slate-100 rounded-full mb-4 shadow-inner">
++                    <i data-lucide="boxes" class="w-8 h-8 text-slate-700"></i>
++                </div>
++                <h1 class="text-2xl font-bold text-gray-900 tracking-tight">HR Assist</h1>
++                <p class="text-sm text-gray-500 mt-2 font-medium">総務人事FAQ管理システム</p>
++            </div>
++            <div class="p-8 space-y-6">
++                <button @click="login()" class="w-full flex items-center justify-center gap-3 bg-gray-900 hover:bg-gray-800 text-white font-bold py-3 px-4 rounded-xl transition-all shadow-sm">
++                    <svg class="w-5 h-5" viewBox="0 0 21 21" fill="none" xmlns="http://www.w3.org/2000/svg">
++                        <rect x="1" y="1" width="9" height="9" fill="#F25022"/><rect x="11" y="1" width="9" height="9" fill="#7FBA00"/><rect x="1" y="11" width="9" height="9" fill="#00A4EF"/><rect x="11" y="11" width="9" height="9" fill="#FFB900"/>
++                    </svg>
++                    社内アカウントでログイン
++                </button>
++                <div class="divider text-xs text-gray-400 font-bold uppercase">または</div>
++                <form @submit.prevent="login()" class="space-y-4">
++                    <input type="email" placeholder="メールアドレス" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
++                    <input type="password" placeholder="パスワード" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
++                    <button type="submit" class="btn btn-primary w-full bg-slate-800 border-none hover:bg-slate-900 font-bold shadow-lg transition-transform active:scale-95">ログイン</button>
++                </form>
++            </div>
++        </div>
++    </div>
++
++    <!-- 2. メインアプリケーションビュー -->
++    <div x-show="isLoggedIn" class="flex min-h-screen relative w-full overflow-hidden" x-cloak>
++        
++        <!-- サイドバー -->
++        <aside class="sidebar-transition bg-white border-r border-gray-200 flex flex-col shrink-0 overflow-hidden shadow-sm"
++               :class="isSidebarOpen ? 'w-64' : 'w-20'">
++            
++            <div class="h-16 flex items-center border-b border-gray-200 shrink-0 px-6">
++                <i data-lucide="boxes" class="w-6 h-6 text-slate-800 shrink-0"></i>
++                <span class="text-slate-800 font-bold text-lg tracking-tight ml-2 whitespace-nowrap transition-opacity duration-200"
++                      :class="isSidebarOpen ? 'opacity-100' : 'opacity-0'">HR Assist</span>
++            </div>
++
++            <nav class="flex-1 p-4 space-y-2 overflow-y-auto no-scrollbar">
++                <button @click="setView('home')" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'home' || currentView === 'detail' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="home" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">質問一覧</span>
++                </button>
++                <button @click="setView('notice')" class="w-full flex items-center justify-between py-3 rounded-lg text-sm font-bold transition-colors text-gray-600 hover:bg-gray-50"
++                        :class="[currentView === 'notice' ? 'bg-slate-100 text-slate-800' : '', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <div class="flex items-center gap-3">
++                        <i data-lucide="bell" class="w-5 h-5 shrink-0"></i>
++                        <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">通知</span>
++                    </div>
++                    <span x-show="isSidebarOpen" class="bg-red-500 text-white text-xs px-2 py-0.5 rounded-full" x-text="unreadNotices"></span>
++                </button>
++                
++                <div class="my-4 border-t border-gray-100" x-show="isManager"></div>
++
++                <button @click="setView('categories')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'categories' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="tags" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">カテゴリ管理</span>
++                </button>
++
++                <button @click="setView('users')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'users' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="users" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">ユーザー管理</span>
++                </button>
++            </nav>
++
++            <!-- プロフィールフッター -->
++            <div class="p-4 border-t border-gray-200 flex items-center gap-3 bg-gray-50 cursor-pointer hover:bg-gray-100 transition-colors" 
++                 :class="isSidebarOpen ? 'px-4' : 'justify-center'"
++                 @click="setView('settings')">
++                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-10 h-10 rounded-full border border-gray-200 bg-white object-cover shadow-sm shrink-0" alt="Avatar" />
++                <div class="min-w-0" x-show="isSidebarOpen">
++                    <div class="text-sm font-bold text-gray-800 truncate">自分</div>
++                    <div class="text-xs text-gray-500 truncate font-medium uppercase tracking-wider" x-text="isManager ? 'Manager' : 'Creator'"></div>
++                </div>
++            </div>
++        </aside>
++
++        <!-- メインエリア -->
++        <div class="flex-1 flex flex-col min-w-0 w-full overflow-hidden">
++            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-6 sticky top-0 z-30 shadow-sm shrink-0">
++                <div class="flex items-center gap-4">
++                    <button @click="toggleSidebar()" class="p-2 rounded-lg hover:bg-gray-100 text-gray-600 transition-colors">
++                        <i data-lucide="menu" class="w-5 h-5"></i>
++                    </button>
++                    <h1 class="text-lg font-bold text-gray-800 tracking-tight" x-text="titleMap[currentView] || 'HR Assist'"></h1>
++                </div>
++                <div class="flex items-center gap-4">
++                    <label class="flex items-center gap-2 cursor-pointer bg-gray-100 px-3 py-1.5 rounded-full hover:bg-gray-200 transition-colors">
++                        <span class="text-[10px] font-bold text-gray-500 uppercase tracking-widest" x-text="isManager ? 'Manager Mode' : 'Creator Mode'"></span>
++                        <input type="checkbox" x-model="isManager" class="toggle toggle-sm toggle-success" />
++                    </label>
++                    <button @click="logout()" class="text-xs font-bold text-gray-400 hover:text-red-600 transition-colors uppercase tracking-widest">ログアウト</button>
++                </div>
++            </header>
++
++            <main class="flex-1 overflow-y-auto p-6 w-full max-w-6xl mx-auto no-scrollbar">
++
++                <!-- [View: HOME] -->
++                <div x-show="currentView === 'home'" class="space-y-6">
++                    <!-- ツールバー -->
++                    <div class="flex flex-wrap items-center justify-between gap-4">
++                        <div class="flex items-center gap-2 flex-1 min-w-[300px]">
++                            <div class="relative flex-1">
++                                <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                                <input type="text" x-model="searchQuery" placeholder="キーワード検索..." class="input input-bordered w-full pl-10 h-10 text-sm border-gray-300 focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-sm" />
++                            </div>
++                            
++                            <div class="dropdown dropdown-end">
++                                <label tabindex="0" class="btn btn-outline btn-sm border-gray-300 normal-case h-10 px-4 font-bold text-gray-600 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800">
++                                    <i data-lucide="filter" class="w-4 h-4 mr-2"></i>
++                                    カテゴリ
++                                    <span x-show="selectedTags.length > 0" class="ml-2 badge bg-slate-800 text-white border-none badge-xs" x-text="selectedTags.length"></span>
++                                </label>
++                                <div tabindex="0" class="dropdown-content z-[50] card card-compact w-64 p-2 shadow-xl bg-white border border-gray-200 mt-2">
++                                    <div class="card-body">
++                                        <h3 class="font-bold text-xs uppercase text-gray-400 tracking-widest mb-2 border-b pb-1">カテゴリ絞り込み</h3>
++                                        <div class="flex flex-col gap-1 max-h-64 overflow-y-auto no-scrollbar">
++                                            <template x-for="tag in availableTags" :key="tag">
++                                                <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                    <input type="checkbox" :checked="selectedTags.includes(tag)" @change="toggleTag(tag)" class="checkbox checkbox-sm checkbox-primary border-gray-300" />
++                                                    <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
++                                                </label>
++                                            </template>
++                                        </div>
++                                        <div class="divider my-1"></div>
++                                        <button @click="selectedTags = []" class="btn btn-ghost btn-xs text-red-500 font-bold" :disabled="selectedTags.length === 0">リセット</button>
++                                    </div>
++                                </div>
++                            </div>
++                        </div>
++
++                        <div class="bg-gray-200 p-1 rounded-lg flex items-center shadow-inner">
++                            <button @click="viewMode = 'card'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'card' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
++                                <i data-lucide="layout-grid" class="w-4 h-4"></i>
++                            </button>
++                            <button @click="viewMode = 'list'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'list' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
++                                <i data-lucide="list" class="w-4 h-4"></i>
++                            </button>
++                        </div>
++                    </div>
++
++                    <!-- ステータス・クイックフィルタと並び替え -->
++                    <div class="flex flex-wrap items-center justify-between gap-4">
++                        <div class="flex flex-wrap items-center gap-2">
++                            <div class="flex items-center gap-1 bg-gray-200 p-1 rounded-xl w-max shadow-inner overflow-x-auto no-scrollbar">
++                                <button @click="statusFilter = 'all'" :class="statusFilter === 'all' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">すべて</button>
++                                <button @click="statusFilter = 'unanswered'" :class="statusFilter === 'unanswered' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応のみ</button>
++                                <button @click="statusFilter = 'progress'" :class="statusFilter === 'progress' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">対応中のみ</button>
++                                <button @click="statusFilter = 'open'" :class="statusFilter === 'open' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応と対応中</button>
++                                <button @click="statusFilter = 'completed'" :class="statusFilter === 'completed' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">完了のみ</button>
++                            </div>
++                            
++                            <div x-show="selectedTags.length > 0" class="flex flex-wrap gap-1 items-center ml-2">
++                                <template x-for="tag in selectedTags" :key="tag">
++                                    <span class="badge bg-slate-100 border-slate-200 text-slate-800 font-bold gap-1 px-2 py-2 text-[10px] shadow-sm">
++                                        <span x-text="tag"></span>
++                                        <i data-lucide="x" class="w-2.5 h-2.5 cursor-pointer hover:text-red-500" @click="toggleTag(tag); refreshIcons()"></i>
++                                    </span>
++                                </template>
++                            </div>
++                        </div>
++
++                        <!-- 並び替えドロップダウン -->
++                        <select x-model="sortOrder" class="select select-bordered select-sm text-xs font-bold text-gray-600 focus:outline-none focus:border-slate-800 shadow-sm bg-white">
++                            <option value="date_desc">質問日時 (新しい順)</option>
++                            <option value="date_asc">質問日時 (古い順)</option>
++                            <option value="dueDate_asc">回答期日 (近い順)</option>
++                            <option value="dueDate_desc">回答期日 (遠い順)</option>
++                        </select>
++                    </div>
++
++                    <!-- カード表示 -->
++                    <div x-show="viewMode === 'card'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
++                        <template x-for="q in filteredQuestions" :key="q.id">
++                            <div class="bg-white rounded-xl p-5 shadow-sm border border-gray-200 cursor-pointer hover:shadow-md hover:border-slate-300 transition-all flex flex-col group" @click="openDetail(q)">
++                                <div class="flex justify-between mb-3 items-start">
++                                    <div class="flex items-center gap-3 min-w-0">
++                                        <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-600 text-sm group-hover:bg-slate-100 group-hover:text-slate-800 transition-colors shrink-0" x-text="q.sender.charAt(0)"></div>
++                                        <div class="min-w-0">
++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="q.sender"></div>
++                                            <div class="text-[10px] text-gray-400 font-bold uppercase" x-text="q.department"></div>
++                                        </div>
++                                    </div>
++                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold shadow-sm" :class="statusColorMap[q.status]" x-text="q.status"></span>
++                                </div>
++                                <h3 class="text-base font-bold text-gray-800 mb-1 group-hover:text-slate-900" x-text="q.title"></h3>
++                                <p class="text-sm text-gray-500 mb-4 flex-1 line-clamp-2" x-text="q.content"></p>
++                                <div class="flex items-center justify-between pt-4 border-t border-gray-50 text-[10px] font-bold">
++                                    <div class="flex gap-1 flex-wrap">
++                                        <template x-for="tag in (q.tags || [])" :key="tag">
++                                            <span class="bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
++                                        </template>
++                                    </div>
++                                    <div class="flex items-center gap-1 shrink-0" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                                        <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
++                                        <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                                    </div>
++                                </div>
++                            </div>
++                        </template>
++                        <div x-show="filteredQuestions.length === 0" class="col-span-full py-16 text-center">
++                            <i data-lucide="search-x" class="w-12 h-12 text-gray-200 mx-auto mb-4"></i>
++                            <p class="text-gray-400 font-bold">条件に一致する質問は見つかりませんでした。</p>
++                        </div>
++                    </div>
++
++                    <!-- リスト表示 -->
++                    <div x-show="viewMode === 'list'" class="flex flex-col gap-2 w-full" style="display: none;">
++                        <template x-for="q in filteredQuestions" :key="q.id">
++                            <div class="bg-white rounded-lg p-3 shadow-sm border border-gray-200 cursor-pointer hover:border-slate-300 hover:shadow-md transition-all flex items-center gap-4 group" @click="openDetail(q)">
++                                <span class="px-2 py-0.5 rounded text-[10px] font-bold shrink-0 w-16 text-center" :class="statusColorMap[q.status]" x-text="q.status"></span>
++                                <h3 class="text-sm font-bold text-gray-800 truncate w-48 shrink-0 group-hover:text-slate-900" x-text="q.title"></h3>
++                                <p class="text-xs text-gray-500 truncate flex-1 font-medium" x-text="q.content"></p>
++                                <div class="flex gap-1 shrink-0">
++                                    <template x-for="tag in (q.tags || [])" :key="tag">
++                                        <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
++                                    </template>
++                                </div>
++                                <div class="text-[10px] font-bold shrink-0 flex items-center gap-1 w-16 justify-end" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                                    <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
++                                    <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                                </div>
++                            </div>
++                        </template>
++                    </div>
++                </div>
++
++                <!-- [View: DETAIL] 質問詳細 -->
++                <div x-show="currentView === 'detail'" class="space-y-6" style="display: none;">
++                    <div class="flex items-center justify-between">
++                        <button class="btn btn-ghost btn-sm gap-2 font-bold text-gray-500 hover:text-gray-800" @click="setView('home')">
++                            <i data-lucide="arrow-left" class="w-4 h-4"></i> 一覧に戻る
++                        </button>
++                    </div>
++                    <div class="flex flex-col lg:flex-row gap-6">
++                        
++                        <!-- 左側：チャットエリア -->
++                        <div class="flex-1 bg-white rounded-xl shadow-sm border border-gray-200 flex flex-col overflow-hidden min-h-[500px]">
++                            <div class="bg-gray-50 p-6 flex-1 space-y-6 overflow-y-auto">
++                                <div class="flex gap-4">
++                                    <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 shrink-0 shadow-sm" x-text="activeQuestion.sender?.charAt(0)"></div>
++                                    <div class="space-y-1 max-w-[80%]">
++                                        <div class="text-[10px] font-bold text-gray-400 uppercase tracking-widest" x-text="(activeQuestion.sender || '') + ' • ' + (activeQuestion.department || '')"></div>
++                                        <div class="bg-white p-4 rounded-2xl rounded-tl-none border border-gray-100 shadow-sm text-sm leading-relaxed" x-text="activeQuestion.content"></div>
++                                    </div>
++                                </div>
++                                <div class="flex gap-4">
++                                    <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix" class="w-10 h-10 rounded-full border bg-white shrink-0 shadow-sm" />
++                                    <div class="space-y-1 max-w-[80%]">
++                                        <div class="text-[10px] font-bold text-yellow-600 uppercase tracking-widest">自分 • 内部メモ</div>
++                                        <div class="bg-yellow-50 p-4 rounded-2xl rounded-tl-none border border-yellow-100 text-sm text-yellow-900 italic">前回の事例（#1001）を参考に回答を作成中...</div>
++                                    </div>
++                                </div>
++                            </div>
++                            <div class="p-4 border-t border-gray-100 bg-white">
++                                <div class="flex gap-2 mb-3">
++                                    <button class="text-[10px] font-bold bg-slate-800 text-white px-3 py-1 rounded shadow-sm">質問者に返信</button>
++                                    <button class="text-[10px] font-bold bg-gray-100 text-gray-500 px-3 py-1 rounded">内部メモ</button>
++                                </div>
++                                <textarea class="textarea textarea-bordered w-full h-24 text-sm focus:border-slate-800 focus:outline-none" placeholder="回答またはメモを入力..."></textarea>
++                                <div class="mt-3 flex justify-end">
++                                    <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md" @click="alert('送信しました'); setView('home')">送信</button>
++                                </div>
++                            </div>
++                        </div>
++
++                        <!-- 右側：プロパティパネル -->
++                        <div class="w-full lg:w-80 space-y-4 shrink-0">
++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm space-y-6">
++                                
++                                <div>
++                                    <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-3">質問者情報</h4>
++                                    <div class="flex items-center gap-3">
++                                        <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 text-sm shadow-inner shrink-0" x-text="activeQuestion.sender?.charAt(0)"></div>
++                                        <div class="min-w-0">
++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.sender"></div>
++                                            <div class="text-[10px] text-gray-500 font-bold uppercase tracking-wider" x-text="activeQuestion.department"></div>
++                                        </div>
++                                    </div>
++                                </div>
++
++                                <div class="divider my-0"></div>
++
++                                <div class="space-y-4">
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">タイトル</h4>
++                                        <input type="text" x-model="activeQuestion.title" class="input input-bordered input-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" />
++                                    </div>
++
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">ステータス</h4>
++                                        <select class="select select-bordered select-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" x-model="activeQuestion.status">
++                                            <option>未対応</option>
++                                            <option>対応中</option>
++                                            <option>完了</option>
++                                        </select>
++                                    </div>
++
++                                    <!-- カテゴリ (マルチセレクト) -->
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">カテゴリ</h4>
++                                        <div class="relative w-full" x-data="{ isCategoryOpen: false }" @click.outside="isCategoryOpen = false">
++                                            <button type="button" @click="isCategoryOpen = !isCategoryOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
++                                                <span class="truncate text-xs" x-text="activeQuestion.tags && activeQuestion.tags.length > 0 ? activeQuestion.tags.join(', ') : 'カテゴリを選択'"></span>
++                                                <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
++                                            </button>
++                                            <div x-show="isCategoryOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
++                                                <div class="card-body p-2">
++                                                    <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
++                                                        <template x-for="tag in availableTags" :key="tag">
++                                                            <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                                <input type="checkbox" 
++                                                                       :checked="activeQuestion.tags && activeQuestion.tags.includes(tag)" 
++                                                                       @change="toggleQuestionTag(tag)"
++                                                                       class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
++                                                                <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
++                                                            </label>
++                                                        </template>
++                                                    </div>
++                                                </div>
++                                            </div>
++                                        </div>
++                                    </div>
++
++                                    <div class="grid grid-cols-2 gap-3">
++                                        <div>
++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">質問日時</h4>
++                                            <div class="text-xs font-bold text-gray-700 bg-gray-50 px-2 rounded-lg border border-gray-200 flex items-center h-8 shadow-inner truncate" x-text="activeQuestion.date || '---'"></div>
++                                        </div>
++                                        <div>
++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">期日</h4>
++                                            <input type="date" x-model="activeQuestion.dueDate" class="input input-bordered input-sm w-full text-xs font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner px-2" />
++                                        </div>
++                                    </div>
++                                </div>
++                            </div>
++                            
++                            <!-- 関連する質問 -->
++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm mt-4">
++                                <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-4">関連する質問</h4>
++                                
++                                <div class="relative w-full" x-data="{ isRelatedOpen: false }" @click.outside="isRelatedOpen = false">
++                                    <button type="button" @click="isRelatedOpen = !isRelatedOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
++                                        <span class="truncate text-xs">
++                                            <template x-if="!activeQuestion.relatedQuestions || activeQuestion.relatedQuestions.length === 0">
++                                                <span>質問を選択</span>
++                                            </template>
++                                            <template x-if="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
++                                                <span x-text="activeQuestion.relatedQuestions.length + '件選択中'"></span>
++                                            </template>
++                                        </span>
++                                        <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
++                                    </button>
++                                    
++                                    <div x-show="isRelatedOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
++                                        <div class="card-body p-2">
++                                            <!-- 検索入力 -->
++                                            <div class="relative mb-2">
++                                                <i data-lucide="search" class="w-3 h-3 absolute left-2 top-2.5 text-gray-400"></i>
++                                                <input type="text" x-model="relatedSearchQuery" placeholder="検索..." class="input input-bordered input-sm w-full pl-7 text-xs focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800" />
++                                            </div>
++                                            
++                                            <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
++                                                <template x-for="rq in availableRelatedQuestions" :key="rq.id">
++                                                    <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                        <input type="checkbox" 
++                                                               :checked="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.includes(rq.id)" 
++                                                               @change="toggleRelatedQuestion(rq.id)"
++                                                               class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
++                                                        <div class="flex flex-col min-w-0">
++                                                            <span class="text-xs font-bold text-gray-700 group-hover:text-slate-900 truncate" x-text="'#' + rq.id + ' ' + rq.title"></span>
++                                                        </div>
++                                                    </label>
++                                                </template>
++                                                <div x-show="availableRelatedQuestions.length === 0" class="text-center py-4 text-xs text-gray-400 font-bold">
++                                                    該当なし
++                                                </div>
++                                            </div>
++                                        </div>
++                                    </div>
++                                </div>
++                                
++                                <!-- 選択された関連質問のリスト -->
++                                <div class="mt-3 space-y-2" x-show="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
++                                    <template x-for="rId in activeQuestion.relatedQuestions" :key="rId">
++                                        <div class="p-2 bg-gray-50 rounded border text-xs font-bold hover:bg-slate-50 transition-colors flex justify-between items-center group">
++                                            <span class="truncate pr-2 cursor-pointer hover:text-blue-600" x-text="'#' + rId + ' ' + (questions.find(q => q.id === rId)?.title || '')" @click="openDetail(questions.find(q => q.id === rId))"></span>
++                                            <i data-lucide="x" class="w-3 h-3 text-gray-400 hover:text-red-500 cursor-pointer shrink-0" @click="toggleRelatedQuestion(rId); refreshIcons()"></i>
++                                        </div>
++                                    </template>
++                                </div>
++                            </div>
++                        </div>
++                    </div>
++                </div>
++
++                <!-- [View: NOTICE] -->
++                <div x-show="currentView === 'notice'" class="max-w-3xl mx-auto space-y-4" style="display: none;">
++                    <template x-for="n in notices" :key="n.id">
++                        <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm flex gap-4 items-start hover:bg-gray-50 transition-colors cursor-pointer" @click="setView('home')">
++                            <div class="w-10 h-10 rounded-lg flex items-center justify-center shrink-0" :class="n.type === 'alert' ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
++                                <i :data-lucide="n.type === 'alert' ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++                            </div>
++                            <div class="flex-1 min-w-0">
++                                <div class="flex justify-between items-center mb-1">
++                                    <h3 class="font-bold text-sm text-gray-800" x-text="n.title"></h3>
++                                    <span class="text-[10px] font-bold text-gray-400" x-text="n.time"></span>
++                                </div>
++                                <p class="text-xs text-gray-500 line-clamp-2" x-text="n.content"></p>
++                            </div>
++                        </div>
++                    </template>
++                </div>
++
++                <!-- [View: CATEGORIES] -->
++                <div x-show="currentView === 'categories'" class="space-y-6" style="display: none;">
++                    <div class="flex justify-between items-center">
++                        <div class="relative w-80">
++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                            <input type="text" placeholder="カテゴリ検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
++                        </div>
++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="alert('カテゴリを追加します（モック）')">+ カテゴリ追加</button>
++                    </div>
++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
++                        <table class="table w-full">
++                            <thead>
++                                <tr class="bg-gray-50">
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">カテゴリ名</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">使用数</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
++                                </tr>
++                            </thead>
++                            <tbody>
++                                <template x-for="tag in availableTags" :key="tag">
++                                    <tr class="hover:bg-gray-50 transition-colors">
++                                        <td>
++                                            <div class="flex items-center gap-3">
++                                                <div class="w-8 h-8 rounded-lg bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs">
++                                                    <i data-lucide="tag" class="w-4 h-4"></i>
++                                                </div>
++                                                <div class="font-bold text-sm" x-text="tag"></div>
++                                            </div>
++                                        </td>
++                                        <td>
++                                            <span class="badge badge-sm font-bold bg-slate-100 text-slate-800 border-slate-200" x-text="questions.filter(q => q.tags && q.tags.includes(tag)).length + '件'"></span>
++                                        </td>
++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
++                                    </tr>
++                                </template>
++                            </tbody>
++                        </table>
++                    </div>
++                </div>
++
++                <!-- [View: USERS] -->
++                <div x-show="currentView === 'users'" class="space-y-6" style="display: none;">
++                    <div class="flex justify-between items-center">
++                        <div class="relative w-80">
++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                            <input type="text" placeholder="ユーザー検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
++                        </div>
++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="showAddUserModal = true">+ ユーザー追加</button>
++                    </div>
++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
++                        <table class="table w-full">
++                            <thead>
++                                <tr class="bg-gray-50">
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">氏名</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">役割</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
++                                </tr>
++                            </thead>
++                            <tbody>
++                                <template x-for="u in users" :key="u.email">
++                                    <tr class="hover:bg-gray-50 transition-colors">
++                                        <td>
++                                            <div class="flex items-center gap-3">
++                                                <div class="w-8 h-8 rounded-full bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs" x-text="u.name.charAt(0)"></div>
++                                                <div>
++                                                    <div class="font-bold text-sm" x-text="u.name"></div>
++                                                    <div class="text-[10px] text-gray-400" x-text="u.email"></div>
++                                                </div>
++                                            </div>
++                                        </td>
++                                        <td><span class="badge badge-sm font-bold" :class="u.role === 'Manager' ? 'badge-info bg-slate-100 text-slate-800 border-slate-200' : 'badge-ghost'" x-text="u.role"></span></td>
++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
++                                    </tr>
++                                </template>
++                            </tbody>
++                        </table>
++                    </div>
++                </div>
++
++                <!-- [View: SETTINGS] -->
++                <div x-show="currentView === 'settings'" class="max-w-2xl mx-auto" style="display: none;">
++                    <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
++                        <div class="p-6 border-b border-gray-100 bg-gray-50/50 text-center lg:text-left">
++                            <h2 class="text-lg font-bold text-gray-800 tracking-tight">プロフィール設定</h2>
++                        </div>
++                        <div class="p-8 space-y-6">
++                            <div class="flex flex-col sm:flex-row items-center gap-8">
++                                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-24 h-24 rounded-2xl border-2 border-white shadow-lg" />
++                                <button class="btn btn-sm btn-outline font-bold shadow-sm">画像をアップロード</button>
++                            </div>
++                            <div class="space-y-4">
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">氏名</label><input type="text" value="自分" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">メールアドレス</label><input type="email" value="jinji.taro@example.com" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">新しいパスワード</label><input type="password" placeholder="変更する場合のみ入力" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">自己紹介・メモ</label><textarea class="textarea textarea-bordered w-full h-24 text-sm font-bold shadow-inner" placeholder="管理担当のメモなど..."></textarea></div>
++                            </div>
++                        </div>
++                        <div class="p-6 bg-gray-50 border-t border-gray-100 flex justify-end gap-3">
++                            <button class="btn btn-ghost font-bold text-gray-500" @click="setView('home')">キャンセル</button>
++                            <button class="btn bg-slate-800 text-white border-none font-bold px-10 shadow-md" @click="setView('home'); alert('設定を保存しました')">保存する</button>
++                        </div>
++                    </div>
++                </div>
++
++            </main>
++        </div>
++    </div>
++
++    <!-- ユーザー招待モーダル -->
++    <div class="modal" :class="{'modal-open': showAddUserModal}">
++        <div class="modal-box bg-white rounded-2xl shadow-2xl">
++            <h3 class="font-bold text-lg mb-6 tracking-tight">新規ユーザーを招待</h3>
++            <div class="space-y-4">
++                <input type="text" placeholder="氏名" class="input input-bordered w-full text-sm shadow-inner" />
++                <input type="email" placeholder="メールアドレス" class="input input-bordered w-full text-sm shadow-inner" />
++                <select class="select select-bordered w-full text-sm font-bold shadow-inner">
++                    <option disabled selected>役割を選択</option>
++                    <option>Manager</option>
++                    <option>Creator</option>
++                </select>
++            </div>
++            <div class="modal-action">
++                <button class="btn btn-ghost font-bold" @click="showAddUserModal = false">キャンセル</button>
++                <button class="btn bg-slate-800 border-none font-bold px-8 shadow-md" @click="showAddUserModal = false; alert('ユーザーを招待しました')">招待を送信</button>
++            </div>
++        </div>
++        <div class="modal-backdrop bg-gray-900 bg-opacity-50" @click="showAddUserModal = false"></div>
++    </div>
++
++</body>
++</html>
++<!DOCTYPE html>
diff --git a/diff_20260424.md b/diff_20260424.md
new file mode 100644
index 0000000..e0f1a17
--- /dev/null
+++ b/diff_20260424.md
@@ -0,0 +1,5642 @@
+diff --git a/.air.toml b/.air.toml
+index 26f31a0..188df86 100644
+--- a/.air.toml
++++ b/.air.toml
+@@ -10 +10 @@
+-  include_file = ["frontend/package.json", "./.env"]
++  include_file = ["frontend/package.json", "./.env", "./.air.toml"]
+diff --git a/_mac_infrastructure/converter.go b/_mac_infrastructure/converter.go
+index 636f58a..3d6f195 100644
+--- a/_mac_infrastructure/converter.go
++++ b/_mac_infrastructure/converter.go
+@@ -239,0 +240,8 @@ func tagFromEntityShallow(e Tag) TagForm {
++func TagFromEntities(e []Tag) []TagForm {
++	forms := []TagForm{}
++	for _, tag := range e {
++		forms = append(forms, TagFromEntity(tag))
++	}
++	return forms
++}
++
+@@ -272,0 +281,22 @@ func TagToEntity(f TagForm) Tag {
++func TagToEntity(f TagForm) Tag {
++	e := Tag{
++		Title:      f.Title,
++		Usage:      f.Usage,
++		CategoryID: f.CategoryIDInt64(),
++	}
++	if f.Category != nil {
++		e.Category = CategoryToEntity(*f.Category)
++	}
++	for _, qf := range f.Questions {
++		tm := TagManager{
++			TagID:      f.ID,
++			QuestionID: qf.ID,
++		}
++		if qf.ID != 0 {
++			tm.Question = Question{ID: qf.ID}
++		}
++		e.TagManagers = append(e.TagManagers, tm)
++	}
++	return e
++}
++
+@@ -493,0 +524,7 @@ func NoticeToEntity(f NoticeForm) Notice {
++func QuestionFromEntities(e []Question) []QuestionForm {
++	forms := []QuestionForm{}
++	for _, q := range e {
++		forms = append(forms, QuestionFromEntity(q))
++	}
++	return forms
++}
+@@ -519,3 +555,0 @@ func QuestionFromEntity(e Question) QuestionForm {
+-	for _, n := range e.Notices {
+-		f.Notices = append(f.Notices, NoticeFromEntity(n))
+-	}
+@@ -570,8 +603,0 @@ func QuestionToEntity(f QuestionForm) Question {
+-	for _, nf := range f.Notices {
+-		n := NoticeToEntity(nf)
+-		if n.QuestionID == nil && qid != 0 {
+-			qidCopy := qid
+-			n.QuestionID = &qidCopy
+-		}
+-		e.Notices = append(e.Notices, n)
+-	}
+diff --git a/_mac_infrastructure/db.go b/_mac_infrastructure/db.go
+index f158477..180e4a9 100644
+--- a/_mac_infrastructure/db.go
++++ b/_mac_infrastructure/db.go
+@@ -115 +114,0 @@ func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) e
+-		ref := Question{ID: q.ID}
+@@ -159,16 +158,16 @@ func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) e
+-		// 6) 通知
+-		for i := range q.Notices {
+-			if q.Notices[i].QuestionID == nil {
+-				qid := q.ID
+-				q.Notices[i].QuestionID = &qid
+-			}
+-		}
+-		if len(q.Notices) == 0 {
+-			if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
+-				return err
+-			}
+-		} else {
+-			if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
+-				return err
+-			}
+-		}
++		// // 6) 通知
++		// for i := range q.Notices {
++		// 	if q.Notices[i].QuestionID == nil {
++		// 		qid := q.ID
++		// 		q.Notices[i].QuestionID = &qid
++		// 	}
++		// }
++		// if len(q.Notices) == 0 {
++		// 	if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
++		// 		return err
++		// 	}
++		// } else {
++		// 	if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
++		// 		return err
++		// 	}
++		// }
+@@ -282,0 +282,8 @@ func GetTags(ctx context.Context, db *gorm.DB) (models []Tag, err error) {
++func GetTagByID(ctx context.Context, db *gorm.DB, id int64) (models Tag, err error) {
++	models, err = gorm.G[Tag](db).
++		Preload("Category", nil).
++		Where("id = ?", id)
++	Find(ctx)
++	return
++}
++
+diff --git a/_mac_infrastructure/entity.go b/_mac_infrastructure/entity.go
+index 86b015f..e8b4dd4 100644
+--- a/_mac_infrastructure/entity.go
++++ b/_mac_infrastructure/entity.go
+@@ -13,2 +13 @@ type Role struct {
+-
+-	Users []User `gorm:"foreignKey:RoleID;references:ID"`
++	Users    []User `gorm:"foreignKey:RoleID;references:ID"`
+@@ -43,2 +42 @@ type Category struct {
+-
+-	Tags []Tag `gorm:"foreignKey:CategoryID;references:ID"`
++	Tags         []Tag  `gorm:"foreignKey:CategoryID;references:ID"`
+@@ -55,5 +53,4 @@ type Tag struct {
+-	ID         int64  `gorm:"column:id;primaryKey"`
+-	Title      string `gorm:"column:title"`
+-	Usage      int    `gorm:"column:usage"`
+-	CategoryID int64  `gorm:"column:category_id"`
+-
++	ID          int64        `gorm:"column:id;primaryKey"`
++	Title       string       `gorm:"column:title"`
++	Usage       int          `gorm:"column:usage"`
++	CategoryID  int64        `gorm:"column:category_id"`
+@@ -72,15 +69,13 @@ type Question struct {
+-	ID               int64      `gorm:"column:id;primaryKey"`
+-	OriginQuestionID *int64     `gorm:"column:origin_question_id"`
+-	AnswerID         *int64     `gorm:"column:answer_id"`
+-	SupportID        *int64     `gorm:"column:support_id"`
+-	Title            string     `gorm:"column:title"`
+-	Content          string     `gorm:"column:content"`
+-	Deleted          bool       `gorm:"column:deleted"`
+-	Due              *time.Time `gorm:"column:due"`
+-	CreatedAt        time.Time  `gorm:"column:created_at"`
+-
+-	Answer      *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
+-	Memos       []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
+-	TagManagers []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
+-	Notices     []Notice     `gorm:"foreignKey:QuestionID;references:ID"`
+-	Support     *Support     `gorm:"foreignKey:SupportID;references:ID"`
++	ID               int64        `gorm:"column:id;primaryKey"`
++	OriginQuestionID *int64       `gorm:"column:origin_question_id"`
++	AnswerID         *int64       `gorm:"column:answer_id"`
++	SupportID        *int64       `gorm:"column:support_id"`
++	Title            string       `gorm:"column:title"`
++	Content          string       `gorm:"column:content"`
++	Deleted          bool         `gorm:"column:deleted"`
++	Due              *time.Time   `gorm:"column:due"`
++	CreatedAt        time.Time    `gorm:"column:created_at"`
++	Answer           *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
++	Memos            []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
++	TagManagers      []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
++	Support          *Support     `gorm:"foreignKey:SupportID;references:ID"`
+@@ -97,6 +92,5 @@ type Answer struct {
+-	ID         int64      `gorm:"column:id;primaryKey"`
+-	UserID     int64      `gorm:"column:user_id"`
+-	Content    string     `gorm:"column:content"`
+-	AnsweredAt *time.Time `gorm:"column:answered_at"`
+-	CreatedAt  time.Time  `gorm:"column:created_at"`
+-
++	ID            int64          `gorm:"column:id;primaryKey"`
++	UserID        int64          `gorm:"column:user_id"`
++	Content       string         `gorm:"column:content"`
++	AnsweredAt    *time.Time     `gorm:"column:answered_at"`
++	CreatedAt     time.Time      `gorm:"column:created_at"`
+@@ -131,4 +125,3 @@ type Refer struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-	URL   string `gorm:"column:url"`
+-
++	ID            int64          `gorm:"column:id;primaryKey"`
++	Title         string         `gorm:"column:title"`
++	URL           string         `gorm:"column:url"`
+@@ -146,6 +139,5 @@ type ReferManager struct {
+-	ID       int64 `gorm:"column:id;primaryKey"`
+-	AnswerID int64 `gorm:"column:answer_id"`
+-	ReferID  int64 `gorm:"column:refer_id"`
+-
+-	Answer Answer `gorm:"foreignKey:AnswerID;references:ID"`
+-	Refer  Refer  `gorm:"foreignKey:ReferID;references:ID"`
++	ID       int64  `gorm:"column:id;primaryKey"`
++	AnswerID int64  `gorm:"column:answer_id"`
++	ReferID  int64  `gorm:"column:refer_id"`
++	Answer   Answer `gorm:"foreignKey:AnswerID;references:ID"`
++	Refer    Refer  `gorm:"foreignKey:ReferID;references:ID"`
+@@ -162,6 +154,5 @@ type TagManager struct {
+-	ID         int64 `gorm:"column:id;primaryKey"`
+-	TagID      int64 `gorm:"column:tag_id"`
+-	QuestionID int64 `gorm:"column:question_id"`
+-
+-	Tag      Tag      `gorm:"foreignKey:TagID;references:ID"`
+-	Question Question `gorm:"foreignKey:QuestionID;references:ID"`
++	ID         int64    `gorm:"column:id;primaryKey"`
++	TagID      int64    `gorm:"column:tag_id"`
++	QuestionID int64    `gorm:"column:question_id"`
++	Tag        Tag      `gorm:"foreignKey:TagID;references:ID"`
++	Question   Question `gorm:"foreignKey:QuestionID;references:ID"`
+@@ -182,3 +173,2 @@ type Escalation struct {
+-
+-	FromQuestion Question `gorm:"foreignKey:FromQuestionID;references:ID"`
+-	ToQuestion   Question `gorm:"foreignKey:ToQuestionID;references:ID"`
++	FromQuestion   Question  `gorm:"foreignKey:FromQuestionID;references:ID"`
++	ToQuestion     Question  `gorm:"foreignKey:ToQuestionID;references:ID"`
+@@ -195,3 +185,2 @@ type SupportStatus struct {
+-	ID    int64  `gorm:"column:id;primaryKey"`
+-	Title string `gorm:"column:title"`
+-
++	ID       int64     `gorm:"column:id;primaryKey"`
++	Title    string    `gorm:"column:title"`
+@@ -209,6 +198,5 @@ type Support struct {
+-	ID              int64 `gorm:"column:id;primaryKey"`
+-	UserID          int64 `gorm:"column:user_id"`
+-	SupportStatusID int64 `gorm:"column:support_status_id"`
+-
+-	User          User          `gorm:"foreignKey:UserID;references:ID"`
+-	SupportStatus SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
++	ID              int64         `gorm:"column:id;primaryKey"`
++	UserID          int64         `gorm:"column:user_id"`
++	SupportStatusID int64         `gorm:"column:support_status_id"`
++	User            User          `gorm:"foreignKey:UserID;references:ID"`
++	SupportStatus   SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
+@@ -243 +230,0 @@ type Notice struct {
+-
+diff --git a/_mac_infrastructure/form.go b/_mac_infrastructure/form.go
+index 91b092e..efcd10e 100644
+--- a/_mac_infrastructure/form.go
++++ b/_mac_infrastructure/form.go
+@@ -208 +207,0 @@ type QuestionForm struct {
+-	Notices          []NoticeForm     `json:"notices,omitempty"`
+diff --git a/bash.exe.stackdump b/bash.exe.stackdump
+new file mode 100644
+index 0000000..6036ff8
+--- /dev/null
++++ b/bash.exe.stackdump
+@@ -0,0 +1,29 @@
++Stack trace:
++Frame         Function      Args
++0007FFFF9EF0  00021006118E (00021028DEE8, 000210272B3E, 000000000000, 0007FFFF8DF0) msys-2.0.dll+0x2118E
++0007FFFF9EF0  0002100469BA (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x69BA
++0007FFFF9EF0  0002100469F2 (00021028DF99, 0007FFFF9DA8, 000000000000, 000000000000) msys-2.0.dll+0x69F2
++0007FFFF9EF0  00021006A41E (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A41E
++0007FFFF9EF0  00021006A545 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A545
++0001004F94B7  00021006B9A5 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2B9A5
++End of stack trace
++Loaded modules:
++000100400000 bash.exe
++7FFCFB4B0000 ntdll.dll
++7FFCFA0D0000 KERNEL32.DLL
++7FFCF8900000 KERNELBASE.dll
++7FFCFA6B0000 USER32.dll
++7FFCF8CD0000 win32u.dll
++7FFCF9260000 GDI32.dll
++7FFCF87E0000 gdi32full.dll
++7FFCF8650000 msvcp_win.dll
++7FFCF8E40000 ucrtbase.dll
++000210040000 msys-2.0.dll
++7FFCFA010000 advapi32.dll
++7FFCFA920000 msvcrt.dll
++7FFCFA870000 sechost.dll
++7FFCF86F0000 bcrypt.dll
++7FFCF9C50000 RPCRT4.dll
++7FFCF7DD0000 CRYPTBASE.DLL
++7FFCF8F60000 bcryptPrimitives.dll
++7FFCF9FD0000 IMM32.DLL
+diff --git a/bun.lock b/bun.lock
+deleted file mode 100644
+index 0074c27..0000000
+--- a/bun.lock
++++ /dev/null
+@@ -1,16 +0,0 @@
+-{
+-  "lockfileVersion": 1,
+-  "configVersion": 1,
+-  "workspaces": {
+-    "": {
+-      "dependencies": {
+-        "htmx-ext-sse": "^2.2.4",
+-      },
+-    },
+-  },
+-  "packages": {
+-    "htmx-ext-sse": ["htmx-ext-sse@2.2.4", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-LJmxVhykyflBWgh5PvbRidcyuqMHlgfajmmzumvKctv9puvsufeH6OaejSMZTNTnEI8O2wXXn4ZZtBdpEMqmEQ=="],
+-
+-    "htmx.org": ["htmx.org@2.0.8", "", {}, "sha512-fm297iru0iWsNJlBrjvtN7V9zjaxd+69Oqjh4F/Vq9Wwi2kFisLcrLCiv5oBX0KLfOX/zG8AUo9ROMU5XUB44Q=="],
+-  }
+-}
+diff --git a/diff_20260423.md b/diff_20260423.md
+new file mode 100644
+index 0000000..92bde9f
+--- /dev/null
++++ b/diff_20260423.md
+@@ -0,0 +1,3102 @@
++diff --git a/.air.toml b/.air.toml
++index 26f31a0..188df86 100644
++--- a/.air.toml
+++++ b/.air.toml
++@@ -10 +10 @@
++-  include_file = ["frontend/package.json", "./.env"]
+++  include_file = ["frontend/package.json", "./.env", "./.air.toml"]
++diff --git a/_mac_infrastructure/converter.go b/_mac_infrastructure/converter.go
++index ba85bda..033bcb9 100644
++--- a/_mac_infrastructure/converter.go
+++++ b/_mac_infrastructure/converter.go
++@@ -462 +462,5 @@ func NoticeFromEntity(e Notice) NoticeForm {
++-		qf := QuestionForm{ID: e.Question.ID, Title: e.Question.Title, Content: e.Question.Content}
+++		// due := e.Question.Due.Format(time.DateTime)
+++
+++		// const tag = TagFromEntity(e.Question.TagManagers)
+++		// qf := QuestionForm{ID: e.Question.ID, Title: e.Question.Title, Content: e.Question.Content, Due: &due, Tags: tag}
+++		qf := QuestionFromEntity(*e.Question)
++@@ -523,0 +528,3 @@ func QuestionFromEntity(e Question) QuestionForm {
+++	for _, n := range e.Notices {
+++		f.Notices = append(f.Notices, NoticeFromEntity(n))
+++	}
++@@ -584,0 +592,8 @@ func QuestionToEntity(f QuestionForm) Question {
+++	for _, nf := range f.Notices {
+++		n := NoticeToEntity(nf)
+++		if n.QuestionID == nil && qid != 0 {
+++			qidCopy := qid
+++			n.QuestionID = &qidCopy
+++		}
+++		e.Notices = append(e.Notices, n)
+++	}
++diff --git a/_mac_infrastructure/db.go b/_mac_infrastructure/db.go
++new file mode 100644
++index 0000000..d825d58
++--- /dev/null
+++++ b/_mac_infrastructure/db.go
++@@ -0,0 +1,312 @@
+++package infrastructure
+++
+++import (
+++	"context"
+++
+++	"gorm.io/gorm"
+++	"gorm.io/gorm/logger"
+++)
+++
+++func GetUsers(ctx context.Context, db *gorm.DB) (models []User, err error) {
+++	models, err = gorm.G[User](db).
+++		Where("role_id <> ?", 1).
+++		Preload("Role", nil).
+++		Not("role_id = 1").
+++		Select("id, name, email, role_id").Find(ctx)
+++	return
+++}
+++
+++func Register[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) error {
+++	var v = gorm.G[T](db)
+++	for _, preload := range preloads {
+++		v.Preload(preload, nil)
+++	}
+++	return v.Create(ctx, &model)
+++}
+++
+++func Updates[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) (int, error) {
+++	var v = gorm.G[T](db)
+++	for _, preload := range preloads {
+++		v.Preload(preload, nil)
+++	}
+++	return v.Updates(ctx, model)
+++}
+++
+++// updateInTransaction は単一の DB トランザクション内で gorm.Updates を実行する。
+++// omit に関連名を渡し、Role/Category など中間テーブル向けの関連を更新対象から外す。
+++// 既存の updates と違い、こちらは更新用。事前読み込みは行わない。
+++func UpdateInTransaction[T any](ctx context.Context, db *gorm.DB, model T, omit ...string) (rowsAffected int, err error) {
+++	err = db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
+++		m := model
+++		res := tx.Omit(omit...).Model(&m).Updates(&m)
+++		rowsAffected = int(res.RowsAffected)
+++		return res.Error
+++	})
+++	return
+++}
+++
+++// updateQuestionInTransaction は Question の1行更新と、QuestionToEntity で組み立てた
+++// 1対多の関連（Answer, Support, TagManagers, Memos, Notices）の同期を1トランザクションで行う。
+++// フォーム上の下位の質問（SubQuestions）やエスカレーション等は本関数では永続化しない。
+++func UpdateQuestionInTransaction(ctx context.Context, db *gorm.DB, q Question) error {
+++	return db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
+++		// 1) Answer: 新規作成 or 既存更新し、親の answer_id を整合させる
+++		if q.Answer != nil {
+++			a := *q.Answer
+++			if a.ID == 0 {
+++				if err := tx.Create(&a).Error; err != nil {
+++					return err
+++				}
+++				aid := a.ID
+++				q.AnswerID = &aid
+++				q.Answer = &a
+++			} else {
+++				if _, err := gorm.G[Answer](tx).
+++					Omit("User", "ReferManagers").
+++					Where("id = ?", a.ID).
+++					Updates(ctx, a); err != nil {
+++					return err
+++				}
+++				if q.AnswerID == nil {
+++					aid := a.ID
+++					q.AnswerID = &aid
+++				}
+++			}
+++		}
+++		// 2) Support: 新規作成 or 既存更新
+++		if q.Support != nil {
+++			s := *q.Support
+++			if s.ID == 0 {
+++				if err := tx.Create(&s).Error; err != nil {
+++					return err
+++				}
+++				sid := s.ID
+++				q.SupportID = &sid
+++				q.Support = &s
+++			} else {
+++				if err := tx.Model(&s).Omit("User", "SupportStatus").Updates(&s).Error; err != nil {
+++					return err
+++				}
+++				if q.SupportID == nil {
+++					sid := s.ID
+++					q.SupportID = &sid
+++					q.Support = &s
+++				}
+++			}
+++		}
+++		// 3) 親の questions: スカラ列と FK のみ（CreatedAt は更新で変えない）
+++		if _, err := gorm.G[Question](tx).
+++			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "CreatedAt").
+++			Where("id = ?", q.ID).
+++			Updates(ctx, q); err != nil {
+++			return err
+++		}
+++		ref := Question{ID: q.ID}
+++		// 4) タグ紐づけ（tag_managers）
+++		for i := range q.TagManagers {
+++			if q.TagManagers[i].QuestionID == 0 {
+++				q.TagManagers[i].QuestionID = q.ID
+++			}
+++		}
+++		if len(q.TagManagers) == 0 {
+++			if err := tx.Model(&ref).Association("TagManagers").Clear(); err != nil {
+++				return err
+++			}
+++		} else {
+++			if err := tx.Model(&ref).Association("TagManagers").Replace(q.TagManagers); err != nil {
+++				return err
+++			}
+++		}
+++		// 5) メモ
+++		for i := range q.Memos {
+++			if q.Memos[i].QuestionID == 0 {
+++				q.Memos[i].QuestionID = q.ID
+++			}
+++		}
+++		if len(q.Memos) == 0 {
+++			if err := tx.Model(&ref).Association("Memos").Clear(); err != nil {
+++				return err
+++			}
+++		} else {
+++			if err := tx.Model(&ref).Association("Memos").Replace(q.Memos); err != nil {
+++				return err
+++			}
+++		}
+++		// 6) 通知
+++		for i := range q.Notices {
+++			if q.Notices[i].QuestionID == nil {
+++				qid := q.ID
+++				q.Notices[i].QuestionID = &qid
+++			}
+++		}
+++		if len(q.Notices) == 0 {
+++			if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
+++				return err
+++			}
+++		} else {
+++			if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
+++				return err
+++			}
+++		}
+++		return nil
+++	})
+++}
+++
+++func DeleteQuestionByID(ctx context.Context, db *gorm.DB, id int64) error {
+++	if _, err := gorm.G[Question](db).
+++		Preload("Answer", nil).
+++		Preload("Answer.User", nil).
+++		Preload("Answer.User.Role", nil).
+++		Preload("Answer.ReferManagers", nil).
+++		Preload("Answer.ReferManagers.Refer", nil).
+++		Preload("Memos", nil).
+++		Preload("Memos.User", nil).
+++		Preload("Memos.User.Role", nil).
+++		Preload("TagManagers", nil).
+++		Preload("TagManagers.Tag", nil).
+++		Preload("TagManagers.Tag.Category", nil).
+++		Preload("Support", nil).
+++		Preload("Support.User", nil).
+++		Preload("Support.User.Role", nil).
+++		Preload("Support.SupportStatus", nil).
+++		Where("id = ?", id).
+++		Limit(1).
+++		Delete(ctx); err != nil {
+++		return err
+++	}
+++	return nil
+++}
+++
+++func DeleteUserByID(ctx context.Context, db *gorm.DB, id int64) error {
+++	if _, err := gorm.G[User](db).
+++		Preload("Role", nil).
+++		Where("id = ?", id).
+++		Limit(1).
+++		Delete(ctx); err != nil {
+++		return err
+++	}
+++	return nil
+++}
+++
+++func GetQuestion(ctx context.Context, db *gorm.DB, id int64) (model Question, err error) {
+++	model, err = gorm.G[Question](db).
+++		Preload("Answer", nil).
+++		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Answer.User.Role", nil).
+++		Preload("Answer.ReferManagers", nil).
+++		Preload("Answer.ReferManagers.Refer", nil).
+++		Preload("Memos", nil).
+++		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Memos.User.Role", nil).
+++		Preload("TagManagers", nil).
+++		Preload("TagManagers.Tag", nil).
+++		Preload("TagManagers.Tag.Category", nil).
+++		Preload("Support", nil).
+++		Preload("Support.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Support.User.Role", nil).
+++		Preload("Support.SupportStatus", nil).
+++		Where("id = ?", id).
+++		First(ctx)
+++	return
+++}
+++
+++func GetQuestions(ctx context.Context, db *gorm.DB) (models []Question, err error) {
+++	models, err = gorm.G[Question](db).
+++		Preload("Answer", nil).
+++		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Answer.User.Role", nil).
+++		Preload("Answer.ReferManagers", nil).
+++		Preload("Answer.ReferManagers.Refer", nil).
+++		Preload("Memos", nil).
+++		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Memos.User.Role", nil).
+++		Preload("TagManagers", nil).
+++		Preload("TagManagers.Tag", nil).
+++		Preload("TagManagers.Tag.Category", nil).
+++		Preload("Support", nil).
+++		Preload("Support.User", func(db gorm.PreloadBuilder) error {
+++			db.Select("id", "name", "email", "role_id")
+++			return nil
+++		}).
+++		Preload("Support.User.Role", nil).
+++		Preload("Support.SupportStatus", nil).
+++		Find(ctx)
+++	return
+++}
+++
+++func GetTags(ctx context.Context, db *gorm.DB) (models []Tag, err error) {
+++	models, err = gorm.G[Tag](db).
+++		Preload("Category", nil).
+++		Find(ctx)
+++	return
+++}
+++
+++func DeleteTagByID(ctx context.Context, db *gorm.DB, id int64) error {
+++	if _, err := gorm.G[Tag](db).Where("id = ?", id).Limit(1).Delete(ctx); err != nil {
+++		return err
+++	}
+++	return nil
+++}
+++
+++func GetNoticeByQuestionIDs(ctx context.Context, db *gorm.DB, questionIDs []int64) (models []Notice, err error) {
+++	if len(questionIDs) > 0 {
+++		models, err = gorm.G[Notice](db).Where("question_id IN ?", questionIDs).Find(ctx)
+++	}
+++	return
+++}
+++
+++func GetNoticeByQuestion(ctx context.Context, db *gorm.DB, question Question) (models Notice, err error) {
+++	models, err = gorm.G[Notice](db).Where("question_id = ?", question.ID).First(ctx)
+++	return
+++}
+++
+++func GetNotice(ctx context.Context, db *gorm.DB) (models []Notice, err error) {
+++	models, err = gorm.G[Notice](db).
+++		Preload("NoticeType", nil).
+++		Preload("Question", nil).
+++		Preload("Question.Support", nil).
+++		Preload("Question.Support.SupportStatus", nil).
+++		Preload("Question.TagManagers", nil).
+++		Preload("Question.TagManagers.Tag", nil).
+++		Find(ctx)
+++	return
+++}
+++
+++func GetNoticeByQuestionSilent(ctx context.Context, db *gorm.DB, question Question) (models Notice, err error) {
+++	models, err = gorm.G[Notice](db.Session(&gorm.Session{
+++		Logger: db.Logger.LogMode(logger.Silent),
+++	})).Where("question_id = ?", question.ID).First(ctx)
+++	return
+++}
+++
+++func RegisterNoticeByQuestionID(ctx context.Context, db *gorm.DB, questionID int64) error {
+++	var content = "質問の回答期日が近づいています。"
+++	notice := Notice{
+++		TypeID:     3,
+++		QuestionID: &questionID,
+++		Content:    &content,
+++	}
+++	return gorm.G[Notice](db).Create(ctx, &notice)
+++}
+++
+++func DeleteNoticeByID(ctx context.Context, db *gorm.DB, id int64) error {
+++	if _, err := gorm.G[Notice](db).Where("id = ?", id).Delete(ctx); err != nil {
+++		return err
+++	}
+++	return nil
+++}
++diff --git a/_mac_infrastructure/entity.go b/_mac_infrastructure/entity.go
++index 86b015f..49ae6ac 100644
++--- a/_mac_infrastructure/entity.go
+++++ b/_mac_infrastructure/entity.go
++@@ -13,2 +13 @@ type Role struct {
++-
++-	Users []User `gorm:"foreignKey:RoleID;references:ID"`
+++	Users    []User `gorm:"foreignKey:RoleID;references:ID"`
++@@ -43,2 +42 @@ type Category struct {
++-
++-	Tags []Tag `gorm:"foreignKey:CategoryID;references:ID"`
+++	Tags         []Tag  `gorm:"foreignKey:CategoryID;references:ID"`
++@@ -55,5 +53,4 @@ type Tag struct {
++-	ID         int64  `gorm:"column:id;primaryKey"`
++-	Title      string `gorm:"column:title"`
++-	Usage      int    `gorm:"column:usage"`
++-	CategoryID int64  `gorm:"column:category_id"`
++-
+++	ID          int64        `gorm:"column:id;primaryKey"`
+++	Title       string       `gorm:"column:title"`
+++	Usage       int          `gorm:"column:usage"`
+++	CategoryID  int64        `gorm:"column:category_id"`
++@@ -72,15 +69,14 @@ type Question struct {
++-	ID               int64      `gorm:"column:id;primaryKey"`
++-	OriginQuestionID *int64     `gorm:"column:origin_question_id"`
++-	AnswerID         *int64     `gorm:"column:answer_id"`
++-	SupportID        *int64     `gorm:"column:support_id"`
++-	Title            string     `gorm:"column:title"`
++-	Content          string     `gorm:"column:content"`
++-	Deleted          bool       `gorm:"column:deleted"`
++-	Due              *time.Time `gorm:"column:due"`
++-	CreatedAt        time.Time  `gorm:"column:created_at"`
++-
++-	Answer      *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
++-	Memos       []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
++-	TagManagers []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
++-	Notices     []Notice     `gorm:"foreignKey:QuestionID;references:ID"`
++-	Support     *Support     `gorm:"foreignKey:SupportID;references:ID"`
+++	ID               int64        `gorm:"column:id;primaryKey"`
+++	OriginQuestionID *int64       `gorm:"column:origin_question_id"`
+++	AnswerID         *int64       `gorm:"column:answer_id"`
+++	SupportID        *int64       `gorm:"column:support_id"`
+++	Title            string       `gorm:"column:title"`
+++	Content          string       `gorm:"column:content"`
+++	Deleted          bool         `gorm:"column:deleted"`
+++	Due              *time.Time   `gorm:"column:due"`
+++	CreatedAt        time.Time    `gorm:"column:created_at"`
+++	Answer           *Answer      `gorm:"foreignKey:AnswerID;references:ID"`
+++	Memos            []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
+++	TagManagers      []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
+++	Notices          []Notice     `gorm:"foreignKey:QuestionID;references:ID"`
+++	Support          *Support     `gorm:"foreignKey:SupportID;references:ID"`
++@@ -97,6 +93,5 @@ type Answer struct {
++-	ID         int64      `gorm:"column:id;primaryKey"`
++-	UserID     int64      `gorm:"column:user_id"`
++-	Content    string     `gorm:"column:content"`
++-	AnsweredAt *time.Time `gorm:"column:answered_at"`
++-	CreatedAt  time.Time  `gorm:"column:created_at"`
++-
+++	ID            int64          `gorm:"column:id;primaryKey"`
+++	UserID        int64          `gorm:"column:user_id"`
+++	Content       string         `gorm:"column:content"`
+++	AnsweredAt    *time.Time     `gorm:"column:answered_at"`
+++	CreatedAt     time.Time      `gorm:"column:created_at"`
++@@ -131,4 +126,3 @@ type Refer struct {
++-	ID    int64  `gorm:"column:id;primaryKey"`
++-	Title string `gorm:"column:title"`
++-	URL   string `gorm:"column:url"`
++-
+++	ID            int64          `gorm:"column:id;primaryKey"`
+++	Title         string         `gorm:"column:title"`
+++	URL           string         `gorm:"column:url"`
++@@ -146,6 +140,5 @@ type ReferManager struct {
++-	ID       int64 `gorm:"column:id;primaryKey"`
++-	AnswerID int64 `gorm:"column:answer_id"`
++-	ReferID  int64 `gorm:"column:refer_id"`
++-
++-	Answer Answer `gorm:"foreignKey:AnswerID;references:ID"`
++-	Refer  Refer  `gorm:"foreignKey:ReferID;references:ID"`
+++	ID       int64  `gorm:"column:id;primaryKey"`
+++	AnswerID int64  `gorm:"column:answer_id"`
+++	ReferID  int64  `gorm:"column:refer_id"`
+++	Answer   Answer `gorm:"foreignKey:AnswerID;references:ID"`
+++	Refer    Refer  `gorm:"foreignKey:ReferID;references:ID"`
++@@ -162,6 +155,5 @@ type TagManager struct {
++-	ID         int64 `gorm:"column:id;primaryKey"`
++-	TagID      int64 `gorm:"column:tag_id"`
++-	QuestionID int64 `gorm:"column:question_id"`
++-
++-	Tag      Tag      `gorm:"foreignKey:TagID;references:ID"`
++-	Question Question `gorm:"foreignKey:QuestionID;references:ID"`
+++	ID         int64    `gorm:"column:id;primaryKey"`
+++	TagID      int64    `gorm:"column:tag_id"`
+++	QuestionID int64    `gorm:"column:question_id"`
+++	Tag        Tag      `gorm:"foreignKey:TagID;references:ID"`
+++	Question   Question `gorm:"foreignKey:QuestionID;references:ID"`
++@@ -182,3 +174,2 @@ type Escalation struct {
++-
++-	FromQuestion Question `gorm:"foreignKey:FromQuestionID;references:ID"`
++-	ToQuestion   Question `gorm:"foreignKey:ToQuestionID;references:ID"`
+++	FromQuestion   Question  `gorm:"foreignKey:FromQuestionID;references:ID"`
+++	ToQuestion     Question  `gorm:"foreignKey:ToQuestionID;references:ID"`
++@@ -195,3 +186,2 @@ type SupportStatus struct {
++-	ID    int64  `gorm:"column:id;primaryKey"`
++-	Title string `gorm:"column:title"`
++-
+++	ID       int64     `gorm:"column:id;primaryKey"`
+++	Title    string    `gorm:"column:title"`
++@@ -209,6 +199,5 @@ type Support struct {
++-	ID              int64 `gorm:"column:id;primaryKey"`
++-	UserID          int64 `gorm:"column:user_id"`
++-	SupportStatusID int64 `gorm:"column:support_status_id"`
++-
++-	User          User          `gorm:"foreignKey:UserID;references:ID"`
++-	SupportStatus SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
+++	ID              int64         `gorm:"column:id;primaryKey"`
+++	UserID          int64         `gorm:"column:user_id"`
+++	SupportStatusID int64         `gorm:"column:support_status_id"`
+++	User            User          `gorm:"foreignKey:UserID;references:ID"`
+++	SupportStatus   SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
++@@ -243 +231,0 @@ type Notice struct {
++-
++diff --git a/_mac_infrastructure/entity.txt b/_mac_infrastructure/entity.txt
++deleted file mode 100644
++index 1f086b7..0000000
++--- a/_mac_infrastructure/entity.txt
+++++ /dev/null
++@@ -1,208 +0,0 @@
++-package infrastructure
++-
++-import (
++-	"time"
++-)
++-
++-// =====================
++-// ROLE
++-// =====================
++-type Role struct {
++-	ID       int64  `gorm:"column:id;primaryKey"`
++-	RoleName string `gorm:"column:role_name"`
++-
++-	Users []User `gorm:"foreignKey:RoleID;references:ID"`
++-}
++-
++-func (Role) TableName() string {
++-	return "roles"
++-}
++-
++-// =====================
++-// USER
++-// =====================
++-type User struct {
++-	ID        int64  `gorm:"column:id;primaryKey"`
++-	Name      string `gorm:"column:name"`
++-	Passwordd string `gorm:"column:passwordd"`
++-	Email     string `gorm:"column:email"`
++-	RoleID    int64  `gorm:"column:role_id"`
++-
++-	Role Role `gorm:"foreignKey:RoleID;references:ID"`
++-}
++-
++-func (User) TableName() string {
++-	return "users"
++-}
++-
++-// =====================
++-// CATEGORY
++-// =====================
++-type Category struct {
++-	ID           int64  `gorm:"column:id;primaryKey"`
++-	CategoryName string `gorm:"column:category_name"`
++-
++-	Tags []Tag `gorm:"foreignKey:CategoryID;references:ID"`
++-}
++-
++-func (Category) TableName() string {
++-	return "categories"
++-}
++-
++-// =====================
++-// TAG
++-// =====================
++-type Tag struct {
++-	ID         int64  `gorm:"column:id;primaryKey"`
++-	Title      string `gorm:"column:title"`
++-	Usage      int    `gorm:"column:usage"`
++-	CategoryID int64  `gorm:"column:category_id"`
++-
++-	Category    Category     `gorm:"foreignKey:CategoryID;references:ID"`
++-	TagManagers []TagManager `gorm:"foreignKey:TagID;references:ID"`
++-}
++-
++-func (Tag) TableName() string {
++-	return "tags"
++-}
++-
++-// =====================
++-// QUESTION
++-// =====================
++-type Question struct {
++-	ID               int64      `gorm:"column:id;primaryKey"`
++-	OriginQuestionID *int64     `gorm:"column:origin_question_id"`
++-	SupportID        *int64     `gorm:"foreignKey:SupportID;references:ID"`
++-	Title            string     `gorm:"column:title"`
++-	Content          string     `gorm:"column:content"`
++-	Due              *time.Time `gorm:"column:due"`
++-	CreatedAt        time.Time  `gorm:"column:created_at"`
++-
++-	Answers     []Answer     `gorm:"foreignKey:QuestionID;references:ID"`
++-	Memos       []Memo       `gorm:"foreignKey:QuestionID;references:ID"`
++-	TagManagers []TagManager `gorm:"foreignKey:QuestionID;references:ID"`
++-	Support     Support      `gorm:"foreignKey:SupportID;references:ID"`
++-}
++-
++-func (Question) TableName() string {
++-	return "questions"
++-}
++-
++-// =====================
++-// ANSWER
++-// =====================
++-type Answer struct {
++-	ID         int64      `gorm:"column:id;primaryKey"`
++-	UserID     int64      `gorm:"column:user_id"`
++-	QuestionID int64      `gorm:"column:question_id"`
++-	Content    string     `gorm:"column:content"`
++-	AnsweredAt *time.Time `gorm:"column:answered_at"`
++-	CreatedAt  time.Time  `gorm:"column:created_at"`
++-
++-	User          User           `gorm:"foreignKey:UserID;references:ID"`
++-	ReferManagers []ReferManager `gorm:"foreignKey:AnswerID;references:ID"`
++-}
++-
++-func (Answer) TableName() string {
++-	return "answers"
++-}
++-
++-// =====================
++-// MEMO
++-// =====================
++-type Memo struct {
++-	ID         int64  `gorm:"column:id;primaryKey"`
++-	UserID     int64  `gorm:"foreignKey:UserID;references:ID"`
++-	Content    string `gorm:"column:content"`
++-	QuestionID int64  `gorm:"foreignKey:QuestionID;references:ID"`
++-	User       User   `gorm:"foreignKey:UserID;references:ID"`
++-}
++-
++-func (Memo) TableName() string {
++-	return "memos"
++-}
++-
++-// =====================
++-// REFER
++-// =====================
++-type Refer struct {
++-	ID    int64  `gorm:"column:id;primaryKey"`
++-	Title string `gorm:"column:title"`
++-	URL   string `gorm:"column:url"`
++-
++-	ReferManagers []ReferManager `gorm:"foreignKey:ReferID;references:ID"`
++-}
++-
++-func (Refer) TableName() string {
++-	return "refers"
++-}
++-
++-// =====================
++-// REFER_MANAGER
++-// =====================
++-type ReferManager struct {
++-	ID       int64 `gorm:"column:id;primaryKey"`
++-	AnswerID int64 `gorm:"column:answer_id"`
++-	ReferID  int64 `gorm:"column:refer_id"`
++-
++-	Answer Answer `gorm:"foreignKey:AnswerID;references:ID"`
++-	Refer  Refer  `gorm:"foreignKey:ReferID;references:ID"`
++-}
++-
++-func (ReferManager) TableName() string {
++-	return "refer_managers"
++-}
++-
++-// =====================
++-// TAG_MANAGER
++-// =====================
++-type TagManager struct {
++-	ID         int64 `gorm:"column:id;primaryKey"`
++-	TagID      int64 `gorm:"column:tag_id"`
++-	QuestionID int64 `gorm:"column:question_id"`
++-
++-	Tag      Tag      `gorm:"foreignKey:TagID;references:ID"`
++-	Question Question `gorm:"foreignKey:QuestionID;references:ID"`
++-}
++-
++-func (TagManager) TableName() string {
++-	return "tag_managers"
++-}
++-
++-// =====================
++-// ESCALATION
++-// =====================
++-type Escalation struct {
++-	ID             int64     `gorm:"column:id;primaryKey"`
++-	FromQuestionID int64     `gorm:"column:from_question_id"`
++-	ToQuestionID   int64     `gorm:"column:to_question_id"`
++-	EscalatedAt    time.Time `gorm:"column:escalated_at"`
++-}
++-
++-func (Escalation) TableName() string {
++-	return "escalations"
++-}
++-
++-type SupportStatus struct {
++-	ID    int64  `gorm:"column:id;primaryKey"`
++-	Title string `gorm:"column:title"`
++-
++-	Supports []Support `gorm:"foreignKey:SupportStatusID;references:ID"`
++-}
++-
++-func (SupportStatus) TableName() string {
++-	return "support_statuses"
++-}
++-
++-type Support struct {
++-	ID              int64 `gorm:"column:id;primaryKey"`
++-	UserID          int64 `gorm:"column:user_id"`
++-	SupportStatusID int64 `gorm:"column:support_status_id"`
++-
++-	User          User          `gorm:"foreignKey:UserID;references:ID"`
++-	SupportStatus SupportStatus `gorm:"foreignKey:SupportStatusID;references:ID"`
++-}
++-
++-func (Support) TableName() string {
++-	return "supports"
++-}
++diff --git a/bash.exe.stackdump b/bash.exe.stackdump
++new file mode 100644
++index 0000000..6036ff8
++--- /dev/null
+++++ b/bash.exe.stackdump
++@@ -0,0 +1,29 @@
+++Stack trace:
+++Frame         Function      Args
+++0007FFFF9EF0  00021006118E (00021028DEE8, 000210272B3E, 000000000000, 0007FFFF8DF0) msys-2.0.dll+0x2118E
+++0007FFFF9EF0  0002100469BA (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x69BA
+++0007FFFF9EF0  0002100469F2 (00021028DF99, 0007FFFF9DA8, 000000000000, 000000000000) msys-2.0.dll+0x69F2
+++0007FFFF9EF0  00021006A41E (000000000000, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A41E
+++0007FFFF9EF0  00021006A545 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2A545
+++0001004F94B7  00021006B9A5 (0007FFFF9F00, 000000000000, 000000000000, 000000000000) msys-2.0.dll+0x2B9A5
+++End of stack trace
+++Loaded modules:
+++000100400000 bash.exe
+++7FFCFB4B0000 ntdll.dll
+++7FFCFA0D0000 KERNEL32.DLL
+++7FFCF8900000 KERNELBASE.dll
+++7FFCFA6B0000 USER32.dll
+++7FFCF8CD0000 win32u.dll
+++7FFCF9260000 GDI32.dll
+++7FFCF87E0000 gdi32full.dll
+++7FFCF8650000 msvcp_win.dll
+++7FFCF8E40000 ucrtbase.dll
+++000210040000 msys-2.0.dll
+++7FFCFA010000 advapi32.dll
+++7FFCFA920000 msvcrt.dll
+++7FFCFA870000 sechost.dll
+++7FFCF86F0000 bcrypt.dll
+++7FFCF9C50000 RPCRT4.dll
+++7FFCF7DD0000 CRYPTBASE.DLL
+++7FFCF8F60000 bcryptPrimitives.dll
+++7FFCF9FD0000 IMM32.DLL
++diff --git a/bun.lock b/bun.lock
++deleted file mode 100644
++index 0074c27..0000000
++--- a/bun.lock
+++++ /dev/null
++@@ -1,16 +0,0 @@
++-{
++-  "lockfileVersion": 1,
++-  "configVersion": 1,
++-  "workspaces": {
++-    "": {
++-      "dependencies": {
++-        "htmx-ext-sse": "^2.2.4",
++-      },
++-    },
++-  },
++-  "packages": {
++-    "htmx-ext-sse": ["htmx-ext-sse@2.2.4", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-LJmxVhykyflBWgh5PvbRidcyuqMHlgfajmmzumvKctv9puvsufeH6OaejSMZTNTnEI8O2wXXn4ZZtBdpEMqmEQ=="],
++-
++-    "htmx.org": ["htmx.org@2.0.8", "", {}, "sha512-fm297iru0iWsNJlBrjvtN7V9zjaxd+69Oqjh4F/Vq9Wwi2kFisLcrLCiv5oBX0KLfOX/zG8AUo9ROMU5XUB44Q=="],
++-  }
++-}
++diff --git a/doc/db/INIT.sql b/doc/db/INIT.sql
++index e6389bb..2c23f3e 100644
++--- a/doc/db/INIT.sql
+++++ b/doc/db/INIT.sql
++@@ -0,0 +1,5 @@
+++DROP SCHEMA public CASCADE;
+++CREATE SCHEMA public;
+++GRANT ALL ON SCHEMA public TO postgres;
+++GRANT ALL ON SCHEMA public TO public;
+++
++@@ -245,0 +251,5 @@ INSERT INTO users (name, password, email, role_id) VALUES
+++<<<<<<< HEAD
+++('Admin', 'Admin', 'Admin', 1),
+++('Taro Yamada', 'hashed_password_1', 'taro@example.com', 2),
+++('Hanako Suzuki', 'hashed_password_2', 'hanako@example.com', 3),
+++=======
++@@ -247,0 +258 @@ INSERT INTO users (name, password, email, role_id) VALUES
+++>>>>>>> add-mock
++@@ -314 +325,2 @@ INSERT INTO notice_types (name) VALUES
++-('DUE');
+++('ALERT'),
+++('QUESTION');
++@@ -319 +331 @@ INSERT INTO notices (type_id, question_id, content, display_due) VALUES
++-(1, NULL, 'システムメンテナンスのお知らせ', NULL);
++\ No newline at end of file
+++(1, NULL, 'システムメンテナンスのお知らせ', NULL);
++diff --git a/doc/db/er.md b/doc/db/er.md
++index d6c8234..31e7de4 100644
++--- a/doc/db/er.md
+++++ b/doc/db/er.md
++@@ -21 +20,0 @@ erDiagram
++-        number support_id FK
++@@ -23,0 +23 @@ erDiagram
+++        number support_id FK
++@@ -88 +88 @@ erDiagram
++-        string title
+++        string name
++@@ -120 +120,16 @@ erDiagram
++-```
+++
+++    NOTICE {
+++        number id PK
+++        number type_id FK
+++        number question_id FK
+++        string content
+++        datetime displayDue
+++    }
+++    NOTICE ||--|| NOTICE_TYPE: "通知は必ず１つの通知タイプを持つ"
+++    NOTICE ||--o| QUESTION: "質問期限通知の場合は質問を持っている場合もある"
+++
+++    NOTICE_TYPE {
+++        number id PK
+++        string name "SYSTEM|DUE"
+++    }
+++```
++\ No newline at end of file
++diff --git a/env/env.go b/env/env.go
++index a706925..80bf249 100644
++--- a/env/env.go
+++++ b/env/env.go
++@@ -9,0 +10,9 @@ import (
+++const (
+++	host     = "PSQL_HOST"
+++	port     = "PSQL_PORT"
+++	user     = "PSQL_USER"
+++	password = "PSQL_PASSWORD"
+++	dbname   = "PSQL_DBNAME"
+++	sslmode  = "PSQL_SSLMODE"
+++)
+++
++diff --git a/frontend/bun.lock b/frontend/bun.lock
++index fc464ba..2d8d80a 100644
++--- a/frontend/bun.lock
+++++ b/frontend/bun.lock
++@@ -43 +43 @@
++-    "@types/bun": ["@types/bun@1.3.11", "", { "dependencies": { "bun-types": "1.3.11" } }, "sha512-5vPne5QvtpjGpsGYXiFyycfpDF2ECyPcTSsFBMa0fraoxiQyMJ3SmuQIGhzPg2WJuWxVBoxWJ2kClYTcw/4fAg=="],
+++    "@types/bun": ["@types/bun@1.3.12", "", { "dependencies": { "bun-types": "1.3.12" } }, "sha512-DBv81elK+/VSwXHDlnH3Qduw+KxkTIWi7TXkAeh24zpi5l0B2kUg9Ga3tb4nJaPcOFswflgi/yAvMVBPrxMB+A=="],
++@@ -45 +45 @@
++-    "@types/node": ["@types/node@25.5.0", "", { "dependencies": { "undici-types": "~7.18.0" } }, "sha512-jp2P3tQMSxWugkCUKLRPVUpGaL5MVFwF8RDuSRztfwgN1wmqJeMSbKlnEtQqU8UrhTmzEmZdu2I6v2dpp7XIxw=="],
+++    "@types/node": ["@types/node@25.6.0", "", { "dependencies": { "undici-types": "~7.19.0" } }, "sha512-+qIYRKdNYJwY3vRCZMdJbPLJAtGjQBudzZzdzwQYkEPQd+PJGixUL5QfvCLDaULoLv+RhT3LDkwEfKaAkgSmNQ=="],
++@@ -61 +61 @@
++-    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.13", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-BL2sTuHOdy0YT1lYieUxTw/QMtPBC3pmlJC6xk8BBYVv6vcw3SGdKemQ+Xsx9ik2F/lYDO9tqsFQH1r9PFuHKw=="],
+++    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.20", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-1AaXxEPfXT+GvTBJFuy4yXVHWJBXa4OdbIebGN/wX5DlsIkU0+wzGnd2lOzokSk51d5LUmqjgBLRLlypLUqInQ=="],
++@@ -69 +69 @@
++-    "bun-types": ["bun-types@1.3.11", "", { "dependencies": { "@types/node": "*" } }, "sha512-1KGPpoxQWl9f6wcZh57LvrPIInQMn2TQ7jsgxqpRzg+l0QPOFvJVH7HmvHo/AiPgwXy+/Thf6Ov3EdVn1vOabg=="],
+++    "bun-types": ["bun-types@1.3.12", "", { "dependencies": { "@types/node": "*" } }, "sha512-HqOLj5PoFajAQciOMRiIZGNoKxDJSr6qigAttOX40vJuSp6DN/CxWp9s3C1Xwm4oH7ybueITwiaOcWXoYVoRkA=="],
++@@ -73 +73 @@
++-    "caniuse-lite": ["caniuse-lite@1.0.30001788", "", {}, "sha512-6q8HFp+lOQtcf7wBK+uEenxymVWkGKkjFpCvw5W25cmMwEDU45p1xQFBQv8JDlMMry7eNxyBaR+qxgmTUZkIRQ=="],
+++    "caniuse-lite": ["caniuse-lite@1.0.30001790", "", {}, "sha512-bOoxfJPyYo+ds6W0YfptaCWbFnJYjh2Y1Eow5lRv+vI2u8ganPZqNm1JwNh0t2ELQCqIWg4B3dWEusgAmsoyOw=="],
++@@ -91 +91,3 @@
++-    "electron-to-chromium": ["electron-to-chromium@1.5.331", "", {}, "sha512-IbxXrsTlD3hRodkLnbxAPP4OuJYdWCeM3IOdT+CpcMoIwIoDfCmRpEtSPfwBXxVkg9xmBeY7Lz2Eo2TDn/HC3Q=="],
+++    "electron-to-chromium": ["electron-to-chromium@1.5.343", "", {}, "sha512-YHnQ3MXI08icvL9ZKnEBy05F2EQ8ob01UaMOuMbM8l+4UcAq6MPPbBTJBbsBUg3H8JeZNt+O4fjsoWth3p6IFg=="],
+++
+++    "es-errors": ["es-errors@1.3.0", "", {}, "sha512-Zf5H2Kxt2xjTvbJvP2ZWLEICxA6j+hAmMzIlypy4xcBg1vKVnx89Wy0GbS+kf5cwCVFFzdCFh2XSCFNULS6csw=="],
++@@ -115,3 +117 @@
++-    "hasown": ["hasown@2.0.2", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-0hJU9SCPvmMzIBdZFqNPXWa6dqh7WdH0cII9y+CyS8rG3nL48Bclra9HmKhVVUHyPWNH5Y7xDwAB7bfgSjkUMQ=="],
++-
++-    "htmx-ext-client-side-templates": ["htmx-ext-client-side-templates@2.0.2", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-d2dA5HOJuMPN+QmAa4MtriTc1OZaaidG/UK/uwDsKJzstMFae2M7tFgykFXOjuFoV2XNCcexr3p+a9p5Jb54Dg=="],
+++    "hasown": ["hasown@2.0.3", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-ej4AhfhfL2Q2zpMmLo7U1Uv9+PyhIZpgQLGT1F9miIGmiCJIoCgSmczFdrc97mWT4kVY72KA+WnnhJ5pghSvSg=="],
++@@ -149 +149 @@
++-    "node-releases": ["node-releases@2.0.37", "", {}, "sha512-1h5gKZCF+pO/o3Iqt5Jp7wc9rH3eJJ0+nh/CIoiRwjRxde/hAHyLPXYN4V3CqKAbiZPSeJFSWHmJsbkicta0Eg=="],
+++    "node-releases": ["node-releases@2.0.38", "", {}, "sha512-3qT/88Y3FbH/Kx4szpQQ4HzUbVrHPKTLVpVocKiLfoYvw9XSGOX2FmD2d6DrXbVYyAQTF2HeF6My8jmzx7/CRw=="],
++@@ -187 +187 @@
++-    "resolve": ["resolve@1.22.11", "", { "dependencies": { "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-RfqAvLnMl313r7c9oclB1HhUEAezcpLjz95wFH4LVuhk9JF/r22qmVP9AMmOU4vMX7Q8pN8jwNg/CSpdFnMjTQ=="],
+++    "resolve": ["resolve@1.22.12", "", { "dependencies": { "es-errors": "^1.3.0", "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-TyeJ1zif53BPfHootBGwPRYT1RUt6oGWsaQr8UyZW/eAm9bKoijtvruSDEmZHm92CwS9nj7/fWttqPCgzep8CA=="],
++@@ -205 +205 @@
++-    "tinyglobby": ["tinyglobby@0.2.15", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.3" } }, "sha512-j2Zq4NyQYG5XMST4cbs02Ak8iJUdxRM0XI5QyxXuZOzKOINmWurp3smXu3y5wDcJrptwpSjgXHzIQxR0omXljQ=="],
+++    "tinyglobby": ["tinyglobby@0.2.16", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.4" } }, "sha512-pn99VhoACYR8nFHhxqix+uvsbXineAasWm5ojXoN8xEwK5Kd3/TrhNn1wByuD52UxWRLy8pu+kRMniEi6Eq9Zg=="],
++@@ -213 +213 @@
++-    "undici-types": ["undici-types@7.18.2", "", {}, "sha512-AsuCzffGHJybSaRrmr5eHr81mwJU3kjw6M+uprWvCXiNeN9SOGwQ3Jn8jb8m3Z6izVgknn1R0FTCEAP2QrLY/w=="],
+++    "undici-types": ["undici-types@7.19.2", "", {}, "sha512-qYVnV5OEm2AW8cJMCpdV20CDyaN3g0AjDlOGf1OW4iaDEx8MwdtChUp4zu4H0VP3nDRF/8RKWH+IPp9uW0YGZg=="],
++@@ -221,2 +220,0 @@
++-    "browserslist/caniuse-lite": ["caniuse-lite@1.0.30001784", "", {}, "sha512-WU346nBTklUV9YfUl60fqRbU5ZqyXlqvo1SgigE1OAXK5bFL8LL9q1K7aap3N739l4BvNqnkm3YrGHiY9sfUQw=="],
++-
++@@ -227,2 +224,0 @@
++-    "tailwindcss/postcss": ["postcss@8.5.8", "", { "dependencies": { "nanoid": "^3.3.11", "picocolors": "^1.1.1", "source-map-js": "^1.2.1" } }, "sha512-OW/rX8O/jXnm82Ey1k44pObPtdblfiuWnrd8X7GJ7emImCOstunGbXUpp7HdBrFQX6rJzn3sPT397Wp5aCwCHg=="],
++-
++diff --git a/handler.go b/handler.go
++deleted file mode 100644
++index 06ab7d0..0000000
++--- a/handler.go
+++++ /dev/null
++@@ -1 +0,0 @@
++-package main
++diff --git a/handler/apiHandler.go b/handler/apiHandler.go
++index f7d4288..d09faae 100644
++--- a/handler/apiHandler.go
+++++ b/handler/apiHandler.go
++@@ -4 +3,0 @@ import (
++-	"context"
++@@ -11 +9,0 @@ import (
++-	"time"
++@@ -14 +11,0 @@ import (
++-	"gorm.io/gorm"
++@@ -77,2 +74 @@ func (hm *HandlerManager) registerUser() echo.HandlerFunc {
++-		fmt.Println(data)
++-		return register(c.Request().Context(), hm.db, &data)
+++		return infrastructure.Register(c.Request().Context(), hm.db, &data)
++@@ -96 +92 @@ func (hm *HandlerManager) registerQuestion() echo.HandlerFunc {
++-		return register(c.Request().Context(), hm.db, &data)
+++		return infrastructure.Register(c.Request().Context(), hm.db, &data)
++@@ -113 +109 @@ func (hm *HandlerManager) registerTag() echo.HandlerFunc {
++-		return register(c.Request().Context(), hm.db, &model)
+++		return infrastructure.Register(c.Request().Context(), hm.db, &model)
++@@ -130,2 +126 @@ func (hm *HandlerManager) updateTag() echo.HandlerFunc {
++-		fmt.Println(model)
++-		if _, err := updateInTransaction(c.Request().Context(), hm.db, model, "Category", "TagManagers"); err != nil {
+++		if _, err := infrastructure.UpdateInTransaction(c.Request().Context(), hm.db, model, "Category", "TagManagers"); err != nil {
++@@ -147 +142 @@ func (hm *HandlerManager) deleteTagByID() echo.HandlerFunc {
++-		if err := deleteTag(c.Request().Context(), hm.db, id); err != nil {
+++		if err := infrastructure.DeleteTagByID(c.Request().Context(), hm.db, id); err != nil {
++@@ -156 +151 @@ func (hm *HandlerManager) getUsers() echo.HandlerFunc {
++-		users, err := getUsers(c.Request().Context(), hm.db)
+++		users, err := infrastructure.GetUsers(c.Request().Context(), hm.db)
++@@ -173 +168 @@ func (hm *HandlerManager) getQuestionByID() echo.HandlerFunc {
++-		model, err := getQuestion(c.Request().Context(), hm.db, id)
+++		model, err := infrastructure.GetQuestion(c.Request().Context(), hm.db, id)
++@@ -193 +188 @@ func (hm *HandlerManager) updateQuestionByID() echo.HandlerFunc {
++-		if err := updateQuestionInTransaction(c.Request().Context(), hm.db, updatedModel); err != nil {
+++		if err := infrastructure.UpdateQuestionInTransaction(c.Request().Context(), hm.db, updatedModel); err != nil {
++@@ -208 +203 @@ func (hm *HandlerManager) deleteQuestionByID() echo.HandlerFunc {
++-		if err := deleteQuestion(c.Request().Context(), hm.db, id); err != nil {
+++		if err := infrastructure.DeleteQuestionByID(c.Request().Context(), hm.db, id); err != nil {
++@@ -222 +217 @@ func (hm *HandlerManager) deleteUserByID() echo.HandlerFunc {
++-		if err := deleteUser(c.Request().Context(), hm.db, id); err != nil {
+++		if err := infrastructure.DeleteUserByID(c.Request().Context(), hm.db, id); err != nil {
++@@ -243,2 +238 @@ func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
++-		fmt.Println(updatedModel)
++-		if _, err := updateInTransaction(c.Request().Context(), hm.db, updatedModel, "Role"); err != nil {
+++		if _, err := infrastructure.UpdateInTransaction(c.Request().Context(), hm.db, updatedModel, "Role"); err != nil {
++@@ -250,273 +243,0 @@ func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
++-
++-func getUsers(ctx context.Context, db *gorm.DB) (models []infrastructure.User, err error) {
++-	models, err = gorm.G[infrastructure.User](db).
++-		Where("role_id <> ?", 1).
++-		Preload("Role", nil).
++-		Select("id, name, email, role_id").Find(ctx)
++-	return
++-}
++-
++-func register[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) error {
++-	var v = gorm.G[T](db)
++-	for _, preload := range preloads {
++-		v.Preload(preload, nil)
++-	}
++-	return v.Create(ctx, &model)
++-}
++-
++-func updates[T any](ctx context.Context, db *gorm.DB, model T, preloads ...string) (int, error) {
++-	var v = gorm.G[T](db)
++-	for _, preload := range preloads {
++-		v.Preload(preload, nil)
++-	}
++-	return v.Updates(ctx, model)
++-}
++-
++-// updateInTransaction は単一の DB トランザクション内で gorm.Updates を実行する。
++-// omit に関連名を渡し、Role/Category など中間テーブル向けの関連を更新対象から外す。
++-// 既存の updates と違い、こちらは更新用。事前読み込みは行わない。
++-func updateInTransaction[T any](ctx context.Context, db *gorm.DB, model T, omit ...string) (rowsAffected int, err error) {
++-	err = db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
++-		m := model
++-		res := tx.Omit(omit...).Model(&m).Updates(&m)
++-		rowsAffected = int(res.RowsAffected)
++-		return res.Error
++-	})
++-	return
++-}
++-
++-// updateQuestionInTransaction は Question の1行更新と、QuestionToEntity で組み立てた
++-// 1対多の関連（Answer, Support, TagManagers, Memos, Notices）の同期を1トランザクションで行う。
++-// フォーム上の下位の質問（SubQuestions）やエスカレーション等は本関数では永続化しない。
++-func updateQuestionInTransaction(ctx context.Context, db *gorm.DB, q infrastructure.Question) error {
++-	return db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
++-		// 1) Answer: 新規作成 or 既存更新し、親の answer_id を整合させる
++-		if q.Answer != nil {
++-			a := *q.Answer
++-			if a.ID == 0 {
++-				if err := tx.Create(&a).Error; err != nil {
++-					return err
++-				}
++-				aid := a.ID
++-				q.AnswerID = &aid
++-				q.Answer = &a
++-			} else {
++-				if _, err := gorm.G[infrastructure.Answer](tx).
++-					Omit("User", "ReferManagers").
++-					Where("id = ?", a.ID).
++-					Updates(ctx, a); err != nil {
++-					return err
++-				}
++-				if q.AnswerID == nil {
++-					aid := a.ID
++-					q.AnswerID = &aid
++-				}
++-			}
++-		}
++-		// 2) Support: 新規作成 or 既存更新
++-		if q.Support != nil {
++-			s := *q.Support
++-			if s.ID == 0 {
++-				if err := tx.Create(&s).Error; err != nil {
++-					return err
++-				}
++-				sid := s.ID
++-				q.SupportID = &sid
++-				q.Support = &s
++-			} else {
++-				if err := tx.Model(&s).Omit("User", "SupportStatus").Updates(&s).Error; err != nil {
++-					return err
++-				}
++-				if q.SupportID == nil {
++-					sid := s.ID
++-					q.SupportID = &sid
++-				}
++-			}
++-		}
++-		// 3) 親の questions: スカラ列と FK のみ（CreatedAt は更新で変えない）
++-		if _, err := gorm.G[infrastructure.Question](tx).
++-			Omit("Answer", "Memos", "Notices", "TagManagers", "Support", "CreatedAt").
++-			Where("id = ?", q.ID).
++-			Updates(ctx, q); err != nil {
++-			return err
++-		}
++-		ref := infrastructure.Question{ID: q.ID}
++-		// 4) タグ紐づけ（tag_managers）
++-		for i := range q.TagManagers {
++-			if q.TagManagers[i].QuestionID == 0 {
++-				q.TagManagers[i].QuestionID = q.ID
++-			}
++-		}
++-		if len(q.TagManagers) == 0 {
++-			if err := tx.Model(&ref).Association("TagManagers").Clear(); err != nil {
++-				return err
++-			}
++-		} else {
++-			if err := tx.Model(&ref).Association("TagManagers").Replace(q.TagManagers); err != nil {
++-				return err
++-			}
++-		}
++-		// 5) メモ
++-		for i := range q.Memos {
++-			if q.Memos[i].QuestionID == 0 {
++-				q.Memos[i].QuestionID = q.ID
++-			}
++-		}
++-		if len(q.Memos) == 0 {
++-			if err := tx.Model(&ref).Association("Memos").Clear(); err != nil {
++-				return err
++-			}
++-		} else {
++-			if err := tx.Model(&ref).Association("Memos").Replace(q.Memos); err != nil {
++-				return err
++-			}
++-		}
++-		// 6) 通知
++-		for i := range q.Notices {
++-			if q.Notices[i].QuestionID == nil {
++-				qid := q.ID
++-				q.Notices[i].QuestionID = &qid
++-			}
++-		}
++-		if len(q.Notices) == 0 {
++-			if err := tx.Model(&ref).Association("Notices").Clear(); err != nil {
++-				return err
++-			}
++-		} else {
++-			if err := tx.Model(&ref).Association("Notices").Replace(q.Notices); err != nil {
++-				return err
++-			}
++-		}
++-		return nil
++-	})
++-}
++-
++-func deleteQuestion(ctx context.Context, db *gorm.DB, id int64) error {
++-	if _, err := gorm.G[infrastructure.Question](db).
++-		Preload("Answer", nil).
++-		Preload("Answer.User", nil).
++-		Preload("Answer.User.Role", nil).
++-		Preload("Answer.ReferManagers", nil).
++-		Preload("Answer.ReferManagers.Refer", nil).
++-		Preload("Memos", nil).
++-		Preload("Memos.User", nil).
++-		Preload("Memos.User.Role", nil).
++-		Preload("TagManagers", nil).
++-		Preload("TagManagers.Tag", nil).
++-		Preload("TagManagers.Tag.Category", nil).
++-		Preload("Support", nil).
++-		Preload("Support.User", nil).
++-		Preload("Support.User.Role", nil).
++-		Preload("Support.SupportStatus", nil).
++-		Where("id = ?", id).
++-		Delete(ctx); err != nil {
++-		return err
++-	}
++-	return nil
++-}
++-
++-func deleteUser(ctx context.Context, db *gorm.DB, id int64) error {
++-	if _, err := gorm.G[infrastructure.User](db).
++-		Preload("Role", nil).
++-		Where("id = ?", id).
++-		Delete(ctx); err != nil {
++-		return err
++-	}
++-	return nil
++-}
++-
++-func getQuestion(ctx context.Context, db *gorm.DB, id int64) (model infrastructure.Question, err error) {
++-	model, err = gorm.G[infrastructure.Question](db).
++-		Preload("Answer", nil).
++-		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Answer.User.Role", nil).
++-		Preload("Answer.ReferManagers", nil).
++-		Preload("Answer.ReferManagers.Refer", nil).
++-		Preload("Memos", nil).
++-		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Memos.User.Role", nil).
++-		Preload("TagManagers", nil).
++-		Preload("TagManagers.Tag", nil).
++-		Preload("TagManagers.Tag.Category", nil).
++-		Preload("Support", nil).
++-		Preload("Support.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Support.User.Role", nil).
++-		Preload("Support.SupportStatus", nil).
++-		Where("id = ?", id).
++-		First(ctx)
++-	return
++-}
++-
++-func getQuestions(ctx context.Context, db *gorm.DB) (models []infrastructure.Question, err error) {
++-	models, err = gorm.G[infrastructure.Question](db).
++-		Preload("Answer", nil).
++-		Preload("Answer.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Answer.User.Role", nil).
++-		Preload("Answer.ReferManagers", nil).
++-		Preload("Answer.ReferManagers.Refer", nil).
++-		Preload("Memos", nil).
++-		Preload("Memos.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Memos.User.Role", nil).
++-		Preload("TagManagers", nil).
++-		Preload("TagManagers.Tag", nil).
++-		Preload("TagManagers.Tag.Category", nil).
++-		Preload("Support", nil).
++-		Preload("Support.User", func(db gorm.PreloadBuilder) error {
++-			db.Select("id", "name", "email", "role_id")
++-			return nil
++-		}).
++-		Preload("Support.User.Role", nil).
++-		Preload("Support.SupportStatus", nil).
++-		Find(ctx)
++-	return
++-}
++-
++-func getTags(ctx context.Context, db *gorm.DB) (models []infrastructure.Tag, err error) {
++-	models, err = gorm.G[infrastructure.Tag](db).
++-		Preload("Category", nil).
++-		Find(ctx)
++-	return
++-}
++-
++-func deleteTag(ctx context.Context, db *gorm.DB, id int64) error {
++-	if _, err := gorm.G[infrastructure.Tag](db).Where("id = ?", id).Delete(ctx); err != nil {
++-		return err
++-	}
++-	return nil
++-}
++-
++-func getNotice(ctx context.Context, db *gorm.DB) ([]infrastructure.Question, error) {
++-	questions, err := gorm.G[infrastructure.Question](db).Find(ctx)
++-	if err != nil {
++-		return nil, err
++-	}
++-
++-	now := time.Now()
++-	limit := now.Add(3 * 24 * time.Hour)
++-	var recent []infrastructure.Question
++-	for _, q := range questions {
++-		if q.Due == nil {
++-			continue
++-		}
++-		d := *q.Due
++-		if !d.Before(now) && d.Before(limit) {
++-			recent = append(recent, q)
++-		}
++-	}
++-	return recent, nil
++-}
++diff --git a/handler/authHandler.go b/handler/authHandler.go
++index 53bcd92..6111eec 100644
++--- a/handler/authHandler.go
+++++ b/handler/authHandler.go
++@@ -6 +5,0 @@ import (
++-	"fmt"
++@@ -58 +56,0 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
++-			fmt.Println("User not found")
++@@ -70 +67,0 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
++-			fmt.Println("Signed Error")
++diff --git a/handler/sseHub.go b/handler/eventHub.go
++similarity index 80%
++rename from handler/sseHub.go
++rename to handler/eventHub.go
++index bd6838e..a63cf74 100644
++--- a/handler/sseHub.go
+++++ b/handler/eventHub.go
++@@ -50 +49,0 @@ func (h *Hub) Run() {
++-			fmt.Println(*client)
++@@ -82 +81 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++-			hub.Broadcast <- Event{Event: "time-tick", Data: fmt.Sprintf(`<div>%s</div>`, t.In(local).Format("15:04:05"))}
+++			hub.Broadcast <- Event{Event: "time-tick", Data: t.In(local).Format(time.DateTime)}
++@@ -88 +87 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++-			models, err := getQuestions(ctx, db)
+++			models, err := infrastructure.GetQuestions(ctx, db)
++@@ -107 +106 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++-			models, err := getUsers(ctx, db)
+++			models, err := infrastructure.GetUsers(ctx, db)
++@@ -126 +125 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++-			models, err := getTags(ctx, db)
+++			models, err := infrastructure.GetTags(ctx, db)
++@@ -142,0 +142,18 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+++	go func() {
+++		for range tickHalfSecond.C {
+++			models, err := infrastructure.GetNotice(ctx, db)
+++			if err != nil {
+++				hub.Broadcast <- Event{Event: "error", Data: fmt.Sprintf(`Error: %v\n`, err)}
+++				continue
+++			}
+++			for _, model := range models {
+++				nf := infrastructure.NoticeFromEntity(model)
+++				if data, err := json.Marshal(nf); err != nil {
+++					hub.Broadcast <- Event{Event: "error", Data: fmt.Sprintf(`Error: %v\n`, err)}
+++					continue
+++				} else {
+++					hub.Broadcast <- Event{Event: "notice", Data: string(data)}
+++				}
+++			}
+++		}
+++	}()
++diff --git a/handler/notice.go b/handler/notice.go
++new file mode 100644
++index 0000000..a52bb45
++--- /dev/null
+++++ b/handler/notice.go
++@@ -0,0 +1,51 @@
+++package handler
+++
+++import (
+++	"context"
+++	"errors"
+++	"fmt"
+++	infrastructure "meetup/_mac_infrastructure"
+++	"time"
+++
+++	"gorm.io/gorm"
+++)
+++
+++// Questionを定期監視し、期限が近づいたものをNoticeへと登録する
+++func (hm *HandlerManager) PollingStart(ctx context.Context) error {
+++	return hm.checkQuesiton(ctx)
+++}
+++
+++func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
+++	var ticker = time.NewTicker(30 * time.Minute)
+++	var checkedQuestions map[int64]any = make(map[int64]any)
+++
+++	for {
+++		select {
+++		case <-ticker.C:
+++			questions, err := infrastructure.GetQuestions(context.Background(), hm.db)
+++			if err != nil {
+++				return err
+++			}
+++
+++			for _, question := range questions {
+++				if _, ok := checkedQuestions[question.ID]; ok {
+++					continue
+++				}
+++				if question.Due.Add(-24 * time.Hour).Before(time.Now()) {
+++					// question_idでの検索でRecordNotFoundが出る想定のため、ログ出力を抑えた関数を呼びだしています
+++					if _, err := infrastructure.GetNoticeByQuestionSilent(context.Background(), hm.db, question); err != nil {
+++						if errors.Is(err, gorm.ErrRecordNotFound) {
+++							infrastructure.RegisterNoticeByQuestionID(ctx, hm.db, question.ID)
+++							checkedQuestions[question.ID] = struct{}{}
+++							continue
+++						}
+++						fmt.Println(err.Error())
+++					}
+++				}
+++				checkedQuestions[question.ID] = struct{}{}
+++			}
+++		case <-ctx.Done():
+++			return nil
+++		}
+++	}
+++}
++diff --git a/main.go b/main.go
++index 162346f..4ce15f8 100644
++--- a/main.go
+++++ b/main.go
++@@ -3,0 +4 @@ import (
+++	"context"
++@@ -31,0 +33 @@ func main() {
+++	go handlerManager.PollingStart(context.Background())
++diff --git a/package.json b/package.json
++deleted file mode 100644
++index 8d7055f..0000000
++--- a/package.json
+++++ /dev/null
++@@ -1,5 +0,0 @@
++-{
++-  "dependencies": {
++-    "htmx-ext-sse": "^2.2.4"
++-  }
++-}
++\ No newline at end of file
++diff --git a/static/js/hrAppData.js b/static/js/hrAppData.js
++index e6718ef..124bbbc 100644
++--- a/static/js/hrAppData.js
+++++ b/static/js/hrAppData.js
++@@ -1,0 +2 @@ import { Question, User, Tag } from '/static/js/model.js';
+++import { SSE_ADD_NOTICE, SSE_ADD_QUESTION, SSE_ADD_TAG, SSE_ADD_USER, SSE_TIME_TICKER } from './sse.js';
++@@ -4,0 +6 @@ document.addEventListener('alpine:init', () => {
+++        timeNow: new Date(),
++@@ -50,4 +52 @@ document.addEventListener('alpine:init', () => {
++-        notices: [
++-            { id: 1, type: 'alert', title: '期日接近: 質問 #1002', time: '10分前', content: '「通勤手当の経路変更」の期日が明日です。早急な対応をお願いします。' },
++-            { id: 2, type: 'info', title: 'システムアップデート完了', time: '1時間前', content: 'FAQ検索エンジンの精度を向上させるアップデートを適用しました。' }
++-        ],
+++        notices: [],
++@@ -104,0 +104 @@ document.addEventListener('alpine:init', () => {
+++            this.activeQuestion = { support: { supportStatusID: "", user: { name: "" } } };
++@@ -113 +113,5 @@ document.addEventListener('alpine:init', () => {
++-            document.addEventListener('sse-notice', (event) => {
+++            document.addEventListener(SSE_TIME_TICKER, (event) => {
+++                this.timeNow = event.detail
+++            });
+++
+++            document.addEventListener(SSE_ADD_NOTICE, (event) => {
++@@ -115,3 +119,14 @@ document.addEventListener('alpine:init', () => {
++-                this.notices.unshift(notice);
++-                if (notice.type === 'alert') this.unreadNotices += 1;
++-                this.refreshIcons();
+++                const index = this.notices.findIndex(n => n.id === notice.id)
+++                if (index === -1) {
+++                    this.notices.unshift(notice);
+++                    this.refreshIcons();
+++                } else {
+++                    if (!_.isEqual(this.notices[index], notice)) {
+++                        this.notices.splice(index, 1, {
+++                            ...this.notices[index],
+++                            ...notice,
+++                        });
+++                        this.refreshIcons();
+++                    }
+++                }
+++                if (notice.noticeType.name === 'ALERT') this.unreadNotices += 1;
++@@ -120 +135 @@ document.addEventListener('alpine:init', () => {
++-            document.addEventListener('sse-question', (event) => {
+++            document.addEventListener(SSE_ADD_QUESTION, (event) => {
++@@ -122 +136,0 @@ document.addEventListener('alpine:init', () => {
++-                console.log(this.questions)
++@@ -141 +155,2 @@ document.addEventListener('alpine:init', () => {
++-                                const originalVal = _.get(this.originalQuestion, key);
+++                                const originalVal = _.get(this.originalQuestion, key)
+++                                // ローカル変更されてたら優先
++@@ -143 +158 @@ document.addEventListener('alpine:init', () => {
++-                                    return localVal;
+++                                    return localVal
++@@ -145 +160,2 @@ document.addEventListener('alpine:init', () => {
++-                                return serverVal;
+++                                // server採用
+++                                return serverVal
++@@ -147,2 +163,2 @@ document.addEventListener('alpine:init', () => {
++-                        );
++-                        this.originalQuestion = _.cloneDeep(question);
+++                        )
+++                        this.originalQuestion = _.cloneDeep(question)
++@@ -152,2 +167,0 @@ document.addEventListener('alpine:init', () => {
++-
++-                this.refreshIcons();
++@@ -156 +170 @@ document.addEventListener('alpine:init', () => {
++-            document.addEventListener('sse-user', (event) => {
+++            document.addEventListener(SSE_ADD_USER, (event) => {
++@@ -173 +187 @@ document.addEventListener('alpine:init', () => {
++-            document.addEventListener('sse-tag', (event) => {
+++            document.addEventListener(SSE_ADD_TAG, (event) => {
++@@ -192,0 +207,8 @@ document.addEventListener('alpine:init', () => {
+++        currentTimeFormatted(dt = new Date()) {
+++            return new Intl.DateTimeFormat('ja-JP', {
+++                hour: '2-digit',
+++                minute: '2-digit',
+++                second: '2-digit'
+++            }).format(dt);
+++        },
+++
++@@ -206 +227,0 @@ document.addEventListener('alpine:init', () => {
++-
++@@ -272,2 +293 @@ document.addEventListener('alpine:init', () => {
++-            const idx = this.activeQuestion.tags.findIndex(t => t.id === tag.id);
++-            if (idx !== -1) {
+++            if (this.activeQuestion.tags.filter(t => t.id === tag.id).length === 1) {
++@@ -296 +315,0 @@ document.addEventListener('alpine:init', () => {
++-                console.log(Question.toModel(this.activeQuestion));
++@@ -350,2 +368,0 @@ document.addEventListener('alpine:init', () => {
++-            const json = await res.json();
++-            console.log(json);
++@@ -360,2 +377,2 @@ document.addEventListener('alpine:init', () => {
++-                    const newTags = [...this.tags.filter(t => t.id !== id)];
++-                    this.tags = newTags;
+++                    const newTags = [...this.tags.filter(t => t.id !== id)]
+++                    this.tags = newTags
++@@ -395,0 +413,14 @@ document.addEventListener('alpine:init', () => {
+++        calcRemainingTimeAndString(question) {
+++            const [hours, minutes] = this.calcRemainingTime(new Date(question.due), this.timeNow)
+++            return `${hours}時間${minutes}分`;
+++        },
+++
+++
+++        calcRemainingTime(baseDate, d = new Date()) {
+++            const diff = baseDate - d;
+++            const hours = Math.floor(diff / (1000 * 60 * 60));
+++            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
+++
+++            return [hours, minutes];
+++        },
+++
++diff --git a/static/js/model.js b/static/js/model.js
++index eb4f303..1fef66b 100644
++--- a/static/js/model.js
+++++ b/static/js/model.js
++@@ -41 +41 @@ export class Role {
++-    this.id       = id;
+++    this.id = id;
++@@ -43 +43 @@ export class Role {
++-    this.users    = users;
+++    this.users = users;
++@@ -71,2 +71,2 @@ export class SupportStatus {
++-    this.id       = id;
++-    this.title    = title;
+++    this.id = id;
+++    this.title = title;
++@@ -97,2 +97,2 @@ export class Support {
++-   * @param {number|string} userId
++-   * @param {number|string} supportStatusId
+++   * @param {number}        userId
+++   * @param {string}        supportStatusId
++@@ -104,2 +104,2 @@ export class Support {
++-    this.id              = id;
++-    this.userId          = userId;
+++    this.id = id;
+++    this.userId = userId;
++@@ -107,3 +107,3 @@ export class Support {
++-    this.user            = user;
++-    this.supportStatus   = supportStatus;
++-    this.question        = question;
+++    this.user = user;
+++    this.supportStatus = supportStatus;
+++    this.question = question;
++@@ -145,5 +145,5 @@ export class User {
++-    this.id       = id;
++-    this.name     = name;
++-    this.email    = email;
++-    this.roleId   = roleId;
++-    this.role     = role;
+++    this.id = id;
+++    this.name = name;
+++    this.email = email;
+++    this.roleId = roleId;
+++    this.role = role;
++@@ -151,2 +151,2 @@ export class User {
++-    this.answers  = answers;
++-    this.memos    = memos;
+++    this.answers = answers;
+++    this.memos = memos;
++@@ -158 +158 @@ export class User {
++-    // const roleIdNum =
+++    // const roleIdNum = 
++@@ -192 +192 @@ export class Category {
++-    this.id           = id;
+++    this.id = id;
++@@ -194 +194 @@ export class Category {
++-    this.tags         = tags;
+++    this.tags = tags;
++@@ -225,3 +225,3 @@ export class Tag {
++-    this.id         = id;
++-    this.title      = title;
++-    this.usage      = usage;
+++    this.id = id;
+++    this.title = title;
+++    this.usage = usage;
++@@ -229,2 +229,2 @@ export class Tag {
++-    this.category   = category;
++-    this.questions  = questions;
+++    this.category = category;
+++    this.questions = questions;
++@@ -269,3 +269,3 @@ export class Refer {
++-    this.id      = id;
++-    this.title   = title;
++-    this.url     = url;
+++    this.id = id;
+++    this.title = title;
+++    this.url = url;
++@@ -304 +304 @@ export class Memo {
++-    this.id         = id;
+++    this.id = id;
++@@ -306,4 +306,4 @@ export class Memo {
++-    this.userId     = userId;
++-    this.content    = content;
++-    this.question   = question;
++-    this.user       = user;
+++    this.userId = userId;
+++    this.content = content;
+++    this.question = question;
+++    this.user = user;
++@@ -344,3 +344,3 @@ export class Answer {
++-    this.id         = id;
++-    this.userId     = userId;
++-    this.content    = content;
+++    this.id = id;
+++    this.userId = userId;
+++    this.content = content;
++@@ -348,3 +348,3 @@ export class Answer {
++-    this.createdAt  = new Date(createdAt);
++-    this.user       = user;
++-    this.refers     = refers;
+++    this.createdAt = new Date(createdAt);
+++    this.user = user;
+++    this.refers = refers;
++@@ -385 +385 @@ export class Escalation {
++-    this.id             = id;
+++    this.id = id;
++@@ -387,4 +387,4 @@ export class Escalation {
++-    this.toQuestionId   = toQuestionId;
++-    this.escalatedAt    = new Date(escalatedAt);
++-    this.fromQuestion   = fromQuestion;
++-    this.toQuestion     = toQuestion;
+++    this.toQuestionId = toQuestionId;
+++    this.escalatedAt = new Date(escalatedAt);
+++    this.fromQuestion = fromQuestion;
+++    this.toQuestion = toQuestion;
++@@ -421,2 +421,2 @@ export class NoticeType {
++-    this.id      = id;
++-    this.name    = name;
+++    this.id = id;
+++    this.name = name;
++@@ -455,2 +455,2 @@ export class Notice {
++-    this.id         = id;
++-    this.typeId     = typeId;
+++    this.id = id;
+++    this.typeId = typeId;
++@@ -458 +458 @@ export class Notice {
++-    this.content    = content;
+++    this.content = content;
++@@ -461 +461 @@ export class Notice {
++-    this.question   = question;
+++    this.question = question;
++@@ -488,13 +487,0 @@ export class Question {
++-<<<<<<< HEAD
++-<<<<<<< Updated upstream
++- * @param {number}         id
++- * @param {number|null}    originQuestionId
++- * @param {number|null}    supportId
++- * @param {string}         title
++- * @param {string}         content
++- * @param {string|null}    due              ISO8601 文字列 or null
++- * @param {string}         createdAt        ISO8601 文字列
++-=======
++-   * @param {number}         id
++-   * @param {number|null}    messageId
++-=======
++@@ -502 +488,0 @@ export class Question {
++->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
++@@ -511,4 +496,0 @@ export class Question {
++-<<<<<<< HEAD
++->>>>>>> Stashed changes
++-=======
++->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
++@@ -531 +513 @@ export class Question {
++-    this.id               = id;
+++    this.id = id;
++@@ -533,16 +515,16 @@ export class Question {
++-    this.answerId         = answerId;
++-    this.supportId        = supportId;
++-    this.title            = title;
++-    this.content          = content;
++-    this.deleted          = deleted;
++-    this.due              = due ? new Date(due) : null;
++-    this.createdAt        = new Date(createdAt);
++-    this.originQuestion   = originQuestion;
++-    this.subQuestions     = subQuestions;
++-    this.support          = support;
++-    this.answer           = answer;
++-    this.memos            = memos;
++-    this.tags             = tags;
++-    this.notices          = notices;
++-    this.escalationsFrom  = escalationsFrom;
++-    this.escalationsTo    = escalationsTo;
+++    this.answerId = answerId;
+++    this.supportId = supportId;
+++    this.title = title;
+++    this.content = content;
+++    this.deleted = deleted;
+++    this.due = due ? new Date(due) : null;
+++    this.createdAt = new Date(createdAt);
+++    this.originQuestion = originQuestion;
+++    this.subQuestions = subQuestions;
+++    this.support = support;
+++    this.answer = answer;
+++    this.memos = memos;
+++    this.tags = tags;
+++    this.notices = notices;
+++    this.escalationsFrom = escalationsFrom;
+++    this.escalationsTo = escalationsTo;
++@@ -597 +579 @@ export class ReferManager {
++-    this.id       = id;
+++    this.id = id;
++@@ -599,3 +581,3 @@ export class ReferManager {
++-    this.referId  = referId;
++-    this.answer   = answer;
++-    this.refer    = refer;
+++    this.referId = referId;
+++    this.answer = answer;
+++    this.refer = refer;
++@@ -633,2 +615,2 @@ export class TagManager {
++-    this.id         = id;
++-    this.tagId      = tagId;
+++    this.id = id;
+++    this.tagId = tagId;
++@@ -636,2 +618,2 @@ export class TagManager {
++-    this.tag        = tag;
++-    this.question   = question;
+++    this.tag = tag;
+++    this.question = question;
++diff --git a/static/js/sse.js b/static/js/sse.js
++index 4ae6276..08f84e7 100644
++--- a/static/js/sse.js
+++++ b/static/js/sse.js
++@@ -1 +1,7 @@
++-import { Question, User, Tag } from '/static/js/model.js';
+++import { Question, User, Tag, Notice } from '/static/js/model.js';
+++
+++export const SSE_TIME_TICKER = "time-ticker";
+++export const SSE_ADD_NOTICE = "sse-notice";
+++export const SSE_ADD_USER = "sse-user";
+++export const SSE_ADD_QUESTION = "sse-question";
+++export const SSE_ADD_TAG = "sse-tag";
++@@ -17,0 +24,6 @@ document.addEventListener('DOMContentLoaded', () => {
+++    es.addEventListener('time-tick', (event) => {
+++        document.dispatchEvent(new CustomEvent(SSE_TIME_TICKER, {
+++            detail: new Date(event.data),
+++        }))
+++    })
+++
++@@ -20,2 +32,4 @@ document.addEventListener('DOMContentLoaded', () => {
++-            document.dispatchEvent(new CustomEvent('sse-notice', {
++-                detail: JSON.parse(event.data),
+++            const json = JSON.parse(event.data);
+++            const notice = Notice.fromJSON(json);
+++            document.dispatchEvent(new CustomEvent(SSE_ADD_NOTICE, {
+++                detail: notice,
++@@ -29,2 +43 @@ document.addEventListener('DOMContentLoaded', () => {
++-            console.log(question);
++-            document.dispatchEvent(new CustomEvent('sse-question', {
+++            document.dispatchEvent(new CustomEvent(SSE_ADD_QUESTION, {
++@@ -39 +52 @@ document.addEventListener('DOMContentLoaded', () => {
++-            document.dispatchEvent(new CustomEvent('sse-user', {
+++            document.dispatchEvent(new CustomEvent(SSE_ADD_USER, {
++@@ -48 +61 @@ document.addEventListener('DOMContentLoaded', () => {
++-            document.dispatchEvent(new CustomEvent('sse-tag', {
+++            document.dispatchEvent(new CustomEvent(SSE_ADD_TAG, {
++diff --git a/templates/components/login_head.html b/templates/components/login_head.html
++index 352793f..b2fad6e 100644
++--- a/templates/components/login_head.html
+++++ b/templates/components/login_head.html
++@@ -23,0 +24,6 @@
+++            init() {
+++                if (typeof lucide !== 'undefined') {
+++                    lucide.createIcons();
+++                }
+++            },
+++
++@@ -51 +57 @@
++-{{end}}
+++{{end}}
++\ No newline at end of file
++diff --git a/templates/components/mock5_header.html b/templates/components/mock5_header.html
++index 84a5236..1b7a60f 100644
++--- a/templates/components/mock5_header.html
+++++ b/templates/components/mock5_header.html
++@@ -20,2 +20,2 @@
++-        <div hx-ext="sse" sse-connect="/sse">
++-            <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
+++        <div>
+++            <span class="font-mono text-sm text-base-content/60" x-text="currentTimeFormatted(timeNow)">
++diff --git a/templates/components/mock5_view_detail.html b/templates/components/mock5_view_detail.html
++index f7c342d..5e244f4 100644
++--- a/templates/components/mock5_view_detail.html
+++++ b/templates/components/mock5_view_detail.html
++@@ -62,0 +63 @@
+++                <div class="divider my-0"></div>
++@@ -68 +69 @@
++-                            x-text="activeQuestion.support?.user?.name?.charAt(0)"></div>
+++                            x-text="activeQuestion.support.user.name?.charAt(0)"></div>
++@@ -70 +71 @@
++-                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.support?.user?.name"></div>
+++                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.support.user.name"></div>
++@@ -75,2 +75,0 @@
++-                <div class="divider my-0"></div>
++-
++diff --git a/templates/components/mock5_view_home.html b/templates/components/mock5_view_home.html
++index 72467df..d0815ae 100644
++--- a/templates/components/mock5_view_home.html
+++++ b/templates/components/mock5_view_home.html
++@@ -113 +113 @@
++-                            :class="statusColorMap[q.support?.supportStatus?.title]" x-text="q.support?.supportStatus?.title"></span>
+++                            :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
++@@ -125 +125 @@
++-                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+++                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++@@ -127 +127 @@
++-                            <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+++                            <span x-text="q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++@@ -143 +143 @@
++-                        :class="statusColorMap[q.support?.supportStatus?.title]" x-text="q.support?.supportStatus?.title"></span>
+++                        :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
++@@ -155 +155 @@
++-                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+++                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++@@ -157 +157 @@
++-                        <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+++                        <span x-text="q.support ?? q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++diff --git a/templates/components/mock5_view_notice.html b/templates/components/mock5_view_notice.html
++index 484f0e6..c95b602 100644
++--- a/templates/components/mock5_view_notice.html
+++++ b/templates/components/mock5_view_notice.html
++@@ -7,2 +7,2 @@
++-                :class="n.type === 'alert' ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
++-                <i :data-lucide="n.type === 'alert' ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
+++                :class="[2, 3].includes(n.typeId) ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
+++                <i :data-lucide="[2, 3].includes(n.typeId) ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++@@ -12,2 +12,11 @@
++-                    <h3 class="font-bold text-sm text-gray-800" x-text="n.title"></h3>
++-                    <span class="text-[10px] font-bold text-gray-400" x-text="n.time"></span>
+++                    <h3 class="font-bold text-sm text-gray-800" x-show="[1, 2].includes(n.typeId)" x-text="n.content"></h3>
+++                    <h3 class="font-bold text-sm text-gray-800" x-show="n.typeId === 3" x-text="n.question && (n.question.title ? `回答期限接近中: ${n.question.title}` : `回答期限接近中: 質問ID#${n.question.id}`)"></h3>
+++                    <div class="justify-between">
+++                        <span class="text-[10px] font-bold text-gray-400" x-text="n.question && n.question.due && `残り時間: ${calcRemainingTimeAndString(n.question)}`"></span>
+++                        <div class="flex gap-1 shrink-0">
+++                            <template x-for="tag in (n.question.tags || [])" :key="tag.id">
+++                                <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase"
+++                                    x-text="tag.title"></span>
+++                            </template>
+++                        </div>
+++                    </div>
++@@ -15 +24 @@
++-                <p class="text-xs text-gray-500 line-clamp-2" x-text="n.content"></p>
+++                <p class="text-xs text-gray-500 line-clamp-2" x-show="n.question" x-text="n.question && n.question.content"></p>
++diff --git a/templates/sample/index.html b/templates/sample/index.html
++index dea253f..0f7e8d2 100644
++--- a/templates/sample/index.html
+++++ b/templates/sample/index.html
++@@ -1,455 +0,0 @@
++-<<<<<<< Updated upstream
++-<!DOCTYPE html>
++-<html lang="ja" data-theme="cupcake">
++-
++-<head>
++-    <meta charset="UTF-8">
++-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
++-    <title>{{.Title}}</title>
++-    <link href="/static/css/output.css" rel="stylesheet">
++-
++-    <!--
++-        module スクリプトは defer 扱いのため、後続の defer Alpine より先に実行される。
++-        alpine:init リスナーを登録 → Alpine が初期化時に発火 → Alpine.data() が登録される。
++-    -->
++-    <script type="module">
++-        import { Question } from '/static/js/model.js';
++-
++-        document.addEventListener('alpine:init', () => {
++-            Alpine.data('questionBoard', () => ({
++-                questions: [],
++-                questionIDMap: new Map([]),
++-                filter: '',
++-                connected: false,
++-
++-                form: {content: '', due: '' },
++-                submitting: false,
++-                formError: '',
++-
++-                currentView: 'home',
++-
++-                questionDetail: {},
++-
++-                async getQuestionDetail(id) {
++-                    const res = await fetch(`/api/v1/question/${id}`);
++-                    const body = await res.json();
++-                    this.questionDetail = Question.fromJSON(body);
++-                },
++-
++-                async deleteQuestion(id) {
++-                    const res = await fetch(`/api/v1/question/${id}`, {
++-                        method: "DELETE"
++-                    }).then(v => {
++-                        if (v.status == 200) {
++-                            this.questions = this.questions.filter(q => q.id != id);
++-                            this.closeDetail()
++-                        } else {
++-                            this.formError = '質問データの削除に失敗しました。'
++-                        }
++-                    });
++-                },
++-
++-                openDetail(id) {
++-                    this.getQuestionDetail(id)
++-                    document.getElementById('question-detail-modal').showModal();
++-                },
++-                closeDetail() {
++-                    document.getElementById('question-detail-modal').close();
++-                },
++-
++-                openForm() {
++-                    this.form = { title: '', content: '', due: '' };
++-                    this.formError = '';
++-                    document.getElementById('question-form-modal').showModal();
++-                },
++-                closeForm() {
++-                    document.getElementById('question-form-modal').close();
++-                },
++-                async submitForm() {
++-                    if (!this.form.content.trim()) {
++-                        this.formError = '内容を入力してください';
++-                        return;
++-                    }
++-                    this.submitting = true;
++-                    this.formError = '';
++-                    try {
++-                        const body = { content: this.form.content.trim(), title: this.form.title.trim() };
++-                        if (this.form.due) body.due = `${this.form.due}:00+09:00`;
++-                        const res = await fetch('/api/v1/question', {
++-                            method: 'POST',
++-                            headers: { 'Content-Type': 'application/json' },
++-                            body: JSON.stringify(body),
++-                        });
++-                        if (!res.ok) throw new Error(`HTTP ${res.status}`);
++-                        this.closeForm();
++-                    } catch (err) {
++-                        this.formError = `送信に失敗しました (${err.message})`;
++-                    } finally {
++-                        this.submitting = false;
++-                    }
++-                },
++-
++-                get filteredQuestions() {
++-                    const f = this.filter.toLowerCase().trim();
++-                    if (!f) return this.questions;
++-                    return this.questions.filter(q =>
++-                        q.title.toLowerCase().includes(f) ||
++-                        (q.tags ?? []).some(t => t.title.toLowerCase().includes(f))
++-                    );
++-                },
++-
++-                isIncludeMap(id) {
++-                    return this.questionIDMap[id] == undefined ? false : true;
++-                },
++-
++-                init() {
++-                    const es = new EventSource('/sse');
++-                    es.onopen = () => { this.connected = true; };
++-                    es.onerror = () => { this.connected = false; };
++-                    es.addEventListener('question', (e) => {
++-                        try {
++-                            const q = Question.fromJSON(JSON.parse(e.data));
++-                            if (!this.isIncludeMap(q.id)) {
++-                                this.questionIDMap[q.id] = true;
++-                                this.questions.unshift(q);
++-                            }
++-                        } catch (err) {
++-                            console.error('Failed to parse question:', err);
++-                        }
++-                    });
++-                },
++-
++-                formatDate(d) {
++-                    if (!d) return null;
++-                    return new Intl.DateTimeFormat('ja-JP', {
++-                        year: 'numeric', month: '2-digit', day: '2-digit',
++-                        hour: '2-digit', minute: '2-digit',
++-                    }).format(new Date(d));
++-                },
++-            }));
++-        });
++-    </script>
++-
++-    <script defer src="/static/vendor/htmx.min.js"></script>
++-    <script defer src="/static/vendor/sse.min.js"></script>
++-    <script defer src="/static/vendor/alpine.min.js"></script>
++-</head>
++-
++-<body class="min-h-screen bg-base-200">
++-
++-    <!-- ========== ヘッダー ========== -->
++-    <header class="sticky top-0 z-10 bg-base-100 shadow-sm">
++-        <div class="max-w-6xl mx-auto px-6 py-3 flex items-center justify-between gap-4">
++-
++-            <h1 class="text-xl font-bold text-primary tracking-tight">Question Board</h1>
++-
++-            <div class="flex items-center gap-3">
++-                <!-- 接続状態インジケーター -->
++-                <div x-data x-cloak class="hidden"><!-- Alpine 読み込み前の FOUC 防止 --></div>
++-
++-                <!-- サーバー時刻 (htmx SSE) -->
++-                <div hx-ext="sse" sse-connect="/sse">
++-                    <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
++-                        --:--:--
++-                    </span>
++-                </div>
++-            </div>
++-
++-        </div>
++-    </header>
++-
++-    <!-- ========== メインコンテンツ ========== -->
++-    <main class="max-w-6xl mx-auto px-6 py-8" x-data="questionBoard()" x-init="init()">
++-        <!-- ---- フィルターバー ---- -->
++-        <div class="mb-6 flex flex-col sm:flex-row items-stretch sm:items-center gap-3 flex-wrap">
++-
++-            <!-- 検索ボックス -->
++-            <label class="flex-1 input input-bordered flex items-center gap-2">
++-                <!-- search icon -->
++-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 opacity-50" fill="none"
++-                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
++-                    <path stroke-linecap="round" stroke-linejoin="round"
++-                        d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
++-                </svg>
++-                <input type="text" x-model="filter" placeholder="タイトル・タグで絞り込み..."
++-                    class="grow outline-none bg-transparent text-sm" />
++-                <!-- クリアボタン -->
++-                <button x-show="filter !== ''" @click="filter = ''" class="btn btn-ghost btn-xs btn-circle"
++-                    aria-label="クリア">
++-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24"
++-                        stroke="currentColor" stroke-width="2.5">
++-                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
++-                    </svg>
++-                </button>
++-            </label>
++-
++-            <!-- 質問追加ボタン -->
++-            <button @click="openForm()" class="btn btn-primary btn-sm gap-1.5 shrink-0">
++-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
++-                    stroke="currentColor" stroke-width="2.5">
++-                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
++-                </svg>
++-                質問を追加
++-            </button>
++-
++-            <!-- 件数バッジ + 接続状態 -->
++-            <div class="flex items-center gap-2 shrink-0">
++-                <span class="badge badge-primary badge-outline font-mono"
++-                    x-text="`${filteredQuestions.length} 件`"></span>
++-
++-                <!-- 接続インジケーター -->
++-                <div class="flex items-center gap-1 text-xs" :class="connected ? 'text-success' : 'text-error'">
++-                    <span class="inline-block w-2 h-2 rounded-full animate-pulse"
++-                        :class="connected ? 'bg-success' : 'bg-error'"></span>
++-                    <span x-text="connected ? 'LIVE' : 'disconnected'"></span>
++-                </div>
++-            </div>
++-
++-        </div>
++-
++-        <!-- ---- カードグリッド ---- -->
++-        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
++-
++-            <template x-for="q in filteredQuestions" :key="q.id">
++-                <div x-transition:enter="transition ease-out duration-300"
++-                    x-transition:enter-start="opacity-0 scale-95 translate-y-2"
++-                    x-transition:enter-end="opacity-100 scale-100 translate-y-0"
++-                    x-transition:leave="transition ease-in duration-200"
++-                    x-transition:leave-start="opacity-100 scale-100 translate-y-0"
++-                    x-transition:leave-end="opacity-0 scale-95 translate-y-2"
++-                    class="card bg-base-100 shadow hover:shadow-lg transition-shadow duration-200 cursor-pointer"
++-                    @click="openDetail(q.id)">
++-                    <div class="card-body gap-3 p-5">
++-
++-                        <!-- タイトル + ID -->
++-                        <div class="flex items-start justify-between gap-2">
++-                            <h2 class="card-title text-sm leading-snug" x-text="q.title"></h2>
++-                            <span class="badge badge-ghost badge-sm shrink-0 font-mono" x-text="`#${q.id}`"></span>
++-                        </div>
++-
++-                        <div class="flex items-start gap-2">
++-                            <span x-text="q.content"></span>
++-                        </div>
++-
++-                        <!-- 期限 -->
++-                        <template x-if="q.due">
++-                            <div class="flex items-center gap-1 text-xs text-warning font-medium">
++-                                <!-- clock icon -->
++-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0" fill="none"
++-                                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
++-                                    <path stroke-linecap="round" stroke-linejoin="round"
++-                                        d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />
++-                                </svg>
++-                                <span x-text="`期限: ${formatDate(q.due)}`"></span>
++-                            </div>
++-                        </template>
++-
++-                        <!-- サポートステータス -->
++-                        <template x-if="q.support && q.support.supportStatus">
++-                            <div class="flex items-center gap-1 text-xs">
++-                                <!-- tag icon -->
++-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0 opacity-60"
++-                                    fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
++-                                    <path stroke-linecap="round" stroke-linejoin="round"
++-                                        d="M7 7h.01M7 3h14a1 1 0 0 1 1 1v9.586a1 1 0 0 1-.293.707l-7 7a1 1 0 0 1-1.414 0l-9.586-9.586A1 1 0 0 1 3 11V4a1 1 0 0 1 1-1h3z" />
++-                                </svg>
++-                                <span class="badge badge-secondary badge-sm"
++-                                    x-text="q.support.supportStatus.title"></span>
++-                            </div>
++-                        </template>
++-
++-                        <!-- タグ一覧 -->
++-                        <template x-if="q.tags && q.tags.length > 0">
++-                            <div class="flex flex-wrap gap-1">
++-                                <template x-for="tag in q.tags" :key="tag.id">
++-                                    <span class="badge badge-outline badge-xs" x-text="tag.title"></span>
++-                                </template>
++-                            </div>
++-                        </template>
++-
++-                        <!-- フッター: 作成日時 -->
++-                        <div class="mt-auto pt-2 border-t border-base-200 flex justify-end">
++-                            <span class="text-xs text-base-content/40 font-mono"
++-                                x-text="formatDate(q.createdAt)"></span>
++-                        </div>
++-
++-                    </div>
++-                </div>
++-            </template>
++-
++-        </div>
++-
++-        <!-- ---- 空状態 ---- -->
++-        <div x-show="filteredQuestions.length === 0" x-transition:enter="transition ease-out duration-300"
++-            x-transition:enter-start="opacity-0 translate-y-2" x-transition:enter-end="opacity-100 translate-y-0"
++-            x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100"
++-            x-transition:leave-end="opacity-0"
++-            class="flex flex-col items-center justify-center py-24 gap-4 text-base-content/40">
++-
++-            <!-- フィルター結果なし -->
++-            <template x-if="filter !== ''">
++-                <div class="flex flex-col items-center gap-2">
++-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-30" fill="none" viewBox="0 0 24 24"
++-                        stroke="currentColor" stroke-width="1.5">
++-                        <path stroke-linecap="round" stroke-linejoin="round"
++-                            d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
++-                    </svg>
++-                    <p class="text-sm">「<span class="font-semibold" x-text="filter"></span>」に一致する Question が見つかりません</p>
++-                    <button @click="filter = ''" class="btn btn-ghost btn-xs mt-1">
++-                        フィルターをクリア
++-                    </button>
++-                </div>
++-            </template>
++-
++-            <!-- SSE 待機中 -->
++-            <template x-if="filter === '' && questions.length === 0">
++-                <div class="flex flex-col items-center gap-3">
++-                    <span class="loading loading-ring loading-lg text-primary opacity-50"></span>
++-                    <p class="text-sm">SSE から Question を待機中...</p>
++-                </div>
++-            </template>
++-
++-        </div>
++-
++-        <!-- ---- 質問入力モーダル ---- -->
++-        <dialog id="question-form-modal" class="modal modal-bottom sm:modal-middle">
++-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
++-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
++-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
++-                x-transition:leave="transition ease-in duration-150"
++-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
++-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
++-
++-                <!-- ヘッダー -->
++-                <div class="flex items-center justify-between mb-5">
++-                    <h3 class="font-bold text-lg">質問を追加</h3>
++-                    <button @click="closeForm()" class="btn btn-ghost btn-sm btn-circle" aria-label="閉じる">
++-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
++-                            stroke="currentColor" stroke-width="2.5">
++-                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
++-                        </svg>
++-                    </button>
++-                </div>
++-
++-                <!-- フォーム -->
++-                <form @submit.prevent="submitForm()" class="flex flex-col gap-4" novalidate>
++-                    <!-- タイトル -->
++-                    <label class="form-control">
++-                        <div class="label pb-1">
++-                            <span class="label-text font-medium">
++-                                タイトル
++-                                <span class="text-error ml-0.5">*</span>
++-                            </span>
++-                        </div>
++-                        <textarea x-model="form.title" placeholder="質問のタイトルを入力..." rows="3"
++-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
++-                            :class="formError && !form.title.trim() ? 'textarea-error' : ''"
++-                            :disabled="submitting"></textarea>
++-                    </label>
++-
++-                    <!-- 内容 -->
++-                    <label class="form-control">
++-                        <div class="label pb-1">
++-                            <span class="label-text font-medium">
++-                                内容
++-                                <span class="text-error ml-0.5">*</span>
++-                            </span>
++-                        </div>
++-                        <textarea x-model="form.content" placeholder="質問の内容を入力..." rows="3"
++-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
++-                            :class="formError && !form.content.trim() ? 'textarea-error' : ''"
++-                            :disabled="submitting"></textarea>
++-                    </label>
++-
++-                    <!-- 期日 -->
++-                    <label class="form-control">
++-                        <div class="label pb-1">
++-                            <span class="label-text font-medium">期日</span>
++-                            <span class="label-text-alt text-base-content/40">任意</span>
++-                        </div>
++-                        <input type="datetime-local" x-model="form.due"
++-                            class="input input-bordered w-full focus:outline-none" :disabled="submitting" />
++-                    </label>
++-
++-                    <!-- エラーメッセージ -->
++-                    <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
++-                        x-transition:enter-start="opacity-0 -translate-y-1"
++-                        x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
++-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
++-                            stroke="currentColor" stroke-width="2">
++-                            <path stroke-linecap="round" stroke-linejoin="round"
++-                                d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
++-                        </svg>
++-                        <span x-text="formError"></span>
++-                    </div>
++-
++-                    <!-- アクション -->
++-                    <div class="modal-action mt-1">
++-                        <button type="button" @click="closeForm()" class="btn btn-ghost" :disabled="submitting">
++-                            キャンセル
++-                        </button>
++-                        <button type="submit" class="btn btn-primary gap-2" :disabled="submitting">
++-                            <span x-show="submitting" class="loading loading-spinner loading-xs"></span>
++-                            <span x-text="submitting ? '送信中...' : '送信'"></span>
++-                        </button>
++-                    </div>
++-
++-                </form>
++-            </div>
++-
++-            <!-- バックドロップクリックで閉じる -->
++-            <form method="dialog" class="modal-backdrop">
++-                <button @click="closeForm()">close</button>
++-            </form>
++-        </dialog>
++-
++-        <!-- ---- 詳細モーダル ---- -->
++-        <dialog id="question-detail-modal" class="modal modal-bottom sm:modal-middle">
++-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
++-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
++-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
++-                x-transition:leave="transition ease-in duration-150"
++-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
++-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
++-
++-                <div class="flex items-center justify-between mb-5">
++-                    <span class="card-title" x-text="questionDetail.title"></span>
++-                </div>
++-                <hr />
++-
++-                <div class="flex items-center justify-between mb-2">
++-                    <span class="card-body" x-text="questionDetail.content"></span>
++-                </div>
++-
++-                <div class="flex items-center justify-between mb-5">
++-                    <textarea name="raw-json" id="raw-json" rows="30" cols="50"
++-                        x-text="JSON.stringify(questionDetail)"></textarea>
++-                </div>
++-
++-                <!-- エラーメッセージ -->
++-                <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
++-                    x-transition:enter-start="opacity-0 -translate-y-1"
++-                    x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
++-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
++-                        stroke="currentColor" stroke-width="2">
++-                        <path stroke-linecap="round" stroke-linejoin="round"
++-                            d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
++-                    </svg>
++-                    <span x-text="formError"></span>
++-                </div>
++-
++-                <div class="flex items-end justify-between mb-5">
++-                    <button class="btn btn-circle" @click="deleteQuestion(questionDetail.id)">削除</button>
++-                </div>
++-            </div>
++-
++-            <!-- バックドロップクリックで閉じる -->
++-            <form method="dialog" class="modal-backdrop">
++-                <button @click="closeDetail()">close</button>
++-            </form>
++-        </dialog>
++-    </main>
++-
++-</body>
++-
++-=======
++@@ -479 +24 @@
++-                form: { content: '', due: '' },
+++                form: {content: '', due: '' },
++@@ -488 +33 @@
++-                    const res = await fetch(`/api/question/${id}`);
+++                    const res = await fetch(`/api/v1/question/${id}`);
++@@ -494 +39 @@
++-                    const res = await fetch(`/api/question/${id}`, {
+++                    const res = await fetch(`/api/v1/question/${id}`, {
++@@ -508 +52,0 @@
++-                    this.drawCommunication(id)
++@@ -533 +77 @@
++-                        const res = await fetch('/api/questions', {
+++                        const res = await fetch('/api/v1/question', {
++@@ -909,2 +452,0 @@
++-
++->>>>>>> Stashed changes
++diff --git a/templates/sample/mock4.5.html b/templates/sample/mock4.5.html
++index 0a513e4..7a588d6 100644
++--- a/templates/sample/mock4.5.html
+++++ b/templates/sample/mock4.5.html
++@@ -31,0 +32,865 @@
+++<html lang="ja" data-theme="light">
+++<head>
+++    <meta charset="UTF-8">
+++    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
+++    <title>HR FAQ Management v0.2</title>
+++    
+++    <!-- 1. Tailwind CSS + DaisyUI (built) -->
+++    <link href="/static/css/output.css" rel="stylesheet">
+++
+++    <!-- 2. Lucide Icons -->
+++    <script src="https://unpkg.com/lucide@latest"></script>
+++
+++    <!-- 3. htmx -->
+++    <script src="/static/vendor/htmx.min.js"></script>
+++
+++    <style>
+++        .no-scrollbar::-webkit-scrollbar { display: none; }
+++        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
+++        [x-cloak] { display: none !important; }
+++        .transition-all { transition: all 0.3s ease; }
+++        .sidebar-transition { transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
+++    </style>
+++
+++    <script>
+++        document.addEventListener('alpine:init', () => {
+++            Alpine.data('hrAppData', () => ({
+++                isLoggedIn: false,
+++                isSidebarOpen: true,
+++                currentView: 'home',
+++                viewMode: 'card',
+++                isManager: true,
+++                showAddUserModal: false,
+++                unreadNotices: 1,
+++                activeQuestion: {},
+++                isConnect: false,
+++
+++                selectedTags: [],
+++                availableTags: ['諸手当', '休暇', '規程', '健康診断'],
+++                searchQuery: '',
+++                statusFilter: 'all',
+++                sortOrder: 'date_desc',
+++
+++                relatedSearchQuery: '',
+++
+++                titleMap: {
+++                    home: '質問一覧',
+++                    detail: '質問詳細',
+++                    notice: '通知センター',
+++                    users: 'ユーザー管理',
+++                    categories: 'カテゴリ管理',
+++                    settings: 'プロフィール設定'
+++                },
+++
+++                statusColorMap: {
+++                    '未対応': 'bg-red-100 text-red-800',
+++                    '対応中': 'bg-blue-100 text-blue-800',
+++                    '完了': 'bg-gray-100 text-gray-600'
+++                },
+++
+++                questions: [
+++                    { id: 1002, sender: '鈴木 一郎', department: 'ここに部署名', title: '通勤手当の経路変更について', content: '引越しに伴い、通勤経路が変更になります。申請手順と必要な書類を教えてください。', status: '未対応', tags: ['諸手当'], daysLeft: 1, date: '2026-04-08 09:30', dueDate: '2026-04-10', relatedQuestions: [] },
+++                    { id: 1003, sender: '田中 花子', department: 'ここに部署名', title: '育児休業の延長申請', content: '現在取得中の育休を半年間延長したいと考えています。手続きの流れを教えてください。', status: '対応中', tags: ['休暇'], daysLeft: 5, date: '2026-04-04 14:00', dueDate: '2026-04-14', relatedQuestions: [] },
+++                    { id: 1004, sender: '佐藤 次郎', department: 'ここに部署名', title: '健康診断の受診日変更', content: '指定された健康診断の日程ですが、出張と重なってしまいました。', status: '完了', tags: ['健康診断'], daysLeft: 0, date: '2026-04-01 11:15', dueDate: '2026-04-09', relatedQuestions: [] },
+++                    { id: 1005, sender: '高橋 三郎', department: 'ここに部署名', title: '慶弔休暇の適用範囲', content: '配偶者の祖父母が亡くなった場合、忌引休暇の対象になりますでしょうか？', status: '未対応', tags: ['規程'], daysLeft: 2, date: '2026-04-07 16:45', dueDate: '2026-04-11', relatedQuestions: [] }
+++                ],
+++
+++                notices: [
+++                    { id: 1, type: 'alert', title: '期日接近: 質問 #1002', time: '10分前', content: '「通勤手当の経路変更」の期日が明日です。早急な対応をお願いします。' },
+++                    { id: 2, type: 'info', title: 'システムアップデート完了', time: '1時間前', content: 'FAQ検索エンジンの精度を向上させるアップデートを適用しました。' }
+++                ],
+++
+++                users: [
+++                    { name: '自分', email: 'jinji.taro@example.com', role: 'Manager' },
+++                    { name: '鈴木 一郎', email: 'suzuki@example.com', role: 'Creator' },
+++                    { name: '田中 花子', email: 'tanaka@example.com', role: 'Creator' }
+++                ],
+++
+++                get filteredQuestions() {
+++                    let result = this.questions.filter(q => {
+++                        const matchesTag = this.selectedTags.length === 0 || q.tags.some(tag => this.selectedTags.includes(tag));
+++                        const matchesSearch = q.title.includes(this.searchQuery) || q.content.includes(this.searchQuery) || q.sender.includes(this.searchQuery);
+++                        let matchesStatus = true;
+++                        if (this.statusFilter === 'unanswered') matchesStatus = q.status === '未対応';
+++                        else if (this.statusFilter === 'progress') matchesStatus = q.status === '対応中';
+++                        else if (this.statusFilter === 'open') matchesStatus = (q.status === '未対応' || q.status === '対応中');
+++                        else if (this.statusFilter === 'completed') matchesStatus = q.status === '完了';
+++                        return matchesTag && matchesSearch && matchesStatus;
+++                    });
+++
+++                    result.sort((a, b) => {
+++                        if (this.sortOrder === 'date_desc') {
+++                            return new Date(b.date) - new Date(a.date);
+++                        } else if (this.sortOrder === 'date_asc') {
+++                            return new Date(a.date) - new Date(b.date);
+++                        } else if (this.sortOrder === 'dueDate_asc') {
+++                            return new Date(a.dueDate) - new Date(b.dueDate);
+++                        } else if (this.sortOrder === 'dueDate_desc') {
+++                            return new Date(b.dueDate) - new Date(a.dueDate);
+++                        }
+++                        return 0;
+++                    });
+++
+++                    return result;
+++                },
+++
+++                get availableRelatedQuestions() {
+++                    if (!this.activeQuestion || !this.activeQuestion.id) return [];
+++                    let qs = this.questions.filter(q => q.id !== this.activeQuestion.id);
+++                    if (this.relatedSearchQuery) {
+++                        const query = this.relatedSearchQuery.toLowerCase();
+++                        qs = qs.filter(q => q.title.toLowerCase().includes(query) || q.id.toString().includes(query));
+++                    }
+++                    return qs;
+++                },
+++
+++                init() {
+++                    this.refreshIcons();
+++
+++                    document.addEventListener('connect disconnect', (event) => {
+++                        this.isConnect = event.detail;
+++                    })
+++
+++                    document.addEventListener('sse-notice', (event) => {
+++                        const notice = event.detail;
+++                        this.notices.unshift(notice);
+++                        if (notice.type === 'alert') this.unreadNotices += 1;
+++                        this.refreshIcons();
+++                    });
+++
+++                    document.addEventListener('sse-question', (event) => {
+++                        const question = this.toQuestionViewModel(event.detail);
+++                        const index = this.questions.findIndex(q => q.id === question.id);
+++                        if (index === -1) {
+++                            this.questions.unshift(question);
+++                        } else {
+++                            this.questions.splice(index, 1, {
+++                                ...this.questions[index],
+++                                ...question,
+++                            });
+++                        }
+++
+++                        if (question.status === '完了') {
+++                            const before = this.notices.length;
+++                            this.notices = this.notices.filter(
+++                                n => !(n.type === 'alert' && n.title.includes(`#${question.id}`))
+++                            );
+++                            this.unreadNotices = Math.max(0, this.unreadNotices - (before - this.notices.length));
+++                        }
+++
+++                        if (this.activeQuestion?.id === question.id) {
+++                            this.activeQuestion = {
+++                                ...this.activeQuestion,
+++                                ...question,
+++                            };
+++                        }
+++
+++                        this.refreshIcons();
+++                    });
+++                },
+++
+++                toQuestionViewModel(question) {
+++                    const createdAt = question.createdAt instanceof Date
+++                        ? question.createdAt
+++                        : new Date(question.createdAt);
+++                    const due = question.due instanceof Date
+++                        ? question.due
+++                        : (question.due ? new Date(question.due) : null);
+++                    const daysLeft = due
+++                        ? Math.max(0, Math.ceil((due.getTime() - Date.now()) / (1000 * 60 * 60 * 24)))
+++                        : 0;
+++
+++                    return {
+++                        id: question.id,
+++                        sender: question.support?.user?.name ?? '不明',
+++                        department: 'ここに部署名',
+++                        title: question.title ?? '',
+++                        content: question.content ?? '',
+++                        status: question.support?.supportStatus?.title ?? '未対応',
+++                        tags: (question.tags ?? []).map(tag => tag.title),
+++                        daysLeft,
+++                        date: this.formatDateTime(createdAt),
+++                        dueDate: due ? this.formatDate(due) : '',
+++                        relatedQuestions: (question.subQuestions ?? []).map(q => q.id),
+++                    };
+++                },
+++
+++                formatDateTime(date) {
+++                    const year = date.getFullYear();
+++                    const month = String(date.getMonth() + 1).padStart(2, '0');
+++                    const day = String(date.getDate()).padStart(2, '0');
+++                    const hour = String(date.getHours()).padStart(2, '0');
+++                    const minute = String(date.getMinutes()).padStart(2, '0');
+++                    return `${year}-${month}-${day} ${hour}:${minute}`;
+++                },
+++
+++                formatDate(date) {
+++                    const year = date.getFullYear();
+++                    const month = String(date.getMonth() + 1).padStart(2, '0');
+++                    const day = String(date.getDate()).padStart(2, '0');
+++                    return `${year}-${month}-${day}`;
+++                },
+++
+++                login() {
+++                    this.isLoggedIn = true;
+++                    this.refreshIcons();
+++                },
+++
+++                logout() {
+++                    if(confirm('ログアウトしますか？')) {
+++                        this.isLoggedIn = false;
+++                        this.currentView = 'home';
+++                        this.refreshIcons();
+++                    }
+++                },
+++
+++                setView(view) {
+++                    this.currentView = view;
+++                    this.refreshIcons();
+++                },
+++
+++                openDetail(q) {
+++                    this.activeQuestion = q;
+++                    this.relatedSearchQuery = '';
+++                    this.setView('detail');
+++                },
+++
+++                toggleTag(tag) {
+++                    if (this.selectedTags.includes(tag)) {
+++                        this.selectedTags = this.selectedTags.filter(t => t !== tag);
+++                    } else {
+++                        this.selectedTags.push(tag);
+++                    }
+++                },
+++
+++                toggleQuestionTag(tag) {
+++                    if (!this.activeQuestion.tags) {
+++                        this.activeQuestion.tags = [];
+++                    }
+++                    if (this.activeQuestion.tags.includes(tag)) {
+++                        this.activeQuestion.tags = this.activeQuestion.tags.filter(t => t !== tag);
+++                    } else {
+++                        this.activeQuestion.tags.push(tag);
+++                    }
+++                },
+++
+++                toggleRelatedQuestion(qId) {
+++                    if (!this.activeQuestion.relatedQuestions) {
+++                        this.activeQuestion.relatedQuestions = [];
+++                    }
+++                    if (this.activeQuestion.relatedQuestions.includes(qId)) {
+++                        this.activeQuestion.relatedQuestions = this.activeQuestion.relatedQuestions.filter(id => id !== qId);
+++                    } else {
+++                        this.activeQuestion.relatedQuestions.push(qId);
+++                    }
+++                },
+++
+++                toggleSidebar() {
+++                    this.isSidebarOpen = !this.isSidebarOpen;
+++                },
+++
+++                refreshIcons() {
+++                    this.$nextTick(() => {
+++                        if (typeof lucide !== 'undefined') {
+++                            lucide.createIcons();
+++                        }
+++                    });
+++                }
+++            }));
+++        });
+++    </script>
+++
+++    <script type="module">
+++        import { Question } from '/static/js/model.js';
+++
+++        document.addEventListener('DOMContentLoaded', () => {
+++            const es = new EventSource('/sse');
+++
+++            es.onopen = (_, event) => {
+++                document.dispatchEvent(new CustomeEvent('connect', {
+++                    detail: true,
+++                }))
+++            }
+++
+++            es.onerror = (_, event) => {
+++                document.dispatchEvent(new CustomeEvent('disconnect', {
+++                    detail: false,
+++                }))
+++            }
+++
+++            es.addEventListener('notice', (event) => {
+++                try {
+++                    document.dispatchEvent(new CustomEvent('sse-notice', {
+++                        detail: JSON.parse(event.data),
+++                    }));
+++                } catch (_) {}
+++            });
+++
+++            es.addEventListener('question', (event) => {
+++                try {
+++                    const question = Question.fromJSON(JSON.parse(event.data));
+++                    document.dispatchEvent(new CustomEvent('sse-question', {
+++                        detail: question,
+++                    }));
+++                } catch (_) {}
+++            });
+++        });
+++    </script>
+++
+++    <!-- 4. Alpine.js -->
+++    <script defer src="/static/vendor/alpine.min.js"></script>
+++</head>
+++
+++<body class="bg-gray-50 text-gray-900 font-sans antialiased overflow-x-hidden" x-data="hrAppData">
+++
+++    <!-- 1. ログインビュー -->
+++    <div x-show="!isLoggedIn" class="min-h-screen flex items-center justify-center p-4 bg-gray-100">
+++        <div class="w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden transition-all">
+++            <div class="bg-slate-50 p-8 text-center border-b border-gray-100">
+++                <div class="inline-flex items-center justify-center w-16 h-16 bg-slate-100 rounded-full mb-4 shadow-inner">
+++                    <i data-lucide="boxes" class="w-8 h-8 text-slate-700"></i>
+++                </div>
+++                <h1 class="text-2xl font-bold text-gray-900 tracking-tight">HR Assist</h1>
+++                <p class="text-sm text-gray-500 mt-2 font-medium">総務人事FAQ管理システム</p>
+++            </div>
+++            <div class="p-8 space-y-6">
+++                <button @click="login()" class="w-full flex items-center justify-center gap-3 bg-gray-900 hover:bg-gray-800 text-white font-bold py-3 px-4 rounded-xl transition-all shadow-sm">
+++                    <svg class="w-5 h-5" viewBox="0 0 21 21" fill="none" xmlns="http://www.w3.org/2000/svg">
+++                        <rect x="1" y="1" width="9" height="9" fill="#F25022"/><rect x="11" y="1" width="9" height="9" fill="#7FBA00"/><rect x="1" y="11" width="9" height="9" fill="#00A4EF"/><rect x="11" y="11" width="9" height="9" fill="#FFB900"/>
+++                    </svg>
+++                    社内アカウントでログイン
+++                </button>
+++                <div class="divider text-xs text-gray-400 font-bold uppercase">または</div>
+++                <form @submit.prevent="login()" class="space-y-4">
+++                    <input type="email" placeholder="メールアドレス" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
+++                    <input type="password" placeholder="パスワード" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
+++                    <button type="submit" class="btn btn-primary w-full bg-slate-800 border-none hover:bg-slate-900 font-bold shadow-lg transition-transform active:scale-95">ログイン</button>
+++                </form>
+++            </div>
+++        </div>
+++    </div>
+++
+++    <!-- 2. メインアプリケーションビュー -->
+++    <div x-show="isLoggedIn" class="flex min-h-screen relative w-full overflow-hidden" x-cloak>
+++        
+++        <!-- サイドバー -->
+++        <aside class="sidebar-transition bg-white border-r border-gray-200 flex flex-col shrink-0 overflow-hidden shadow-sm"
+++               :class="isSidebarOpen ? 'w-64' : 'w-20'">
+++            
+++            <div class="h-16 flex items-center border-b border-gray-200 shrink-0 px-6">
+++                <i data-lucide="boxes" class="w-6 h-6 text-slate-800 shrink-0"></i>
+++                <span class="text-slate-800 font-bold text-lg tracking-tight ml-2 whitespace-nowrap transition-opacity duration-200"
+++                      :class="isSidebarOpen ? 'opacity-100' : 'opacity-0'">HR Assist</span>
+++            </div>
+++
+++            <nav class="flex-1 p-4 space-y-2 overflow-y-auto no-scrollbar">
+++                <button @click="setView('home')" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
+++                        :class="[currentView === 'home' || currentView === 'detail' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
+++                    <i data-lucide="home" class="w-5 h-5 shrink-0"></i>
+++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">質問一覧</span>
+++                </button>
+++                <button @click="setView('notice')" class="w-full flex items-center justify-between py-3 rounded-lg text-sm font-bold transition-colors text-gray-600 hover:bg-gray-50"
+++                        :class="[currentView === 'notice' ? 'bg-slate-100 text-slate-800' : '', isSidebarOpen ? 'px-3' : 'justify-center']">
+++                    <div class="flex items-center gap-3">
+++                        <i data-lucide="bell" class="w-5 h-5 shrink-0"></i>
+++                        <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">通知</span>
+++                    </div>
+++                    <span x-show="isSidebarOpen" class="bg-red-500 text-white text-xs px-2 py-0.5 rounded-full" x-text="unreadNotices"></span>
+++                </button>
+++                
+++                <div class="my-4 border-t border-gray-100" x-show="isManager"></div>
+++
+++                <button @click="setView('categories')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
+++                        :class="[currentView === 'categories' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
+++                    <i data-lucide="tags" class="w-5 h-5 shrink-0"></i>
+++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">カテゴリ管理</span>
+++                </button>
+++
+++                <button @click="setView('users')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
+++                        :class="[currentView === 'users' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
+++                    <i data-lucide="users" class="w-5 h-5 shrink-0"></i>
+++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">ユーザー管理</span>
+++                </button>
+++            </nav>
+++
+++            <!-- プロフィールフッター -->
+++            <div class="p-4 border-t border-gray-200 flex items-center gap-3 bg-gray-50 cursor-pointer hover:bg-gray-100 transition-colors" 
+++                 :class="isSidebarOpen ? 'px-4' : 'justify-center'"
+++                 @click="setView('settings')">
+++                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-10 h-10 rounded-full border border-gray-200 bg-white object-cover shadow-sm shrink-0" alt="Avatar" />
+++                <div class="min-w-0" x-show="isSidebarOpen">
+++                    <div class="text-sm font-bold text-gray-800 truncate">自分</div>
+++                    <div class="text-xs text-gray-500 truncate font-medium uppercase tracking-wider" x-text="isManager ? 'Manager' : 'Creator'"></div>
+++                </div>
+++            </div>
+++        </aside>
+++
+++        <!-- メインエリア -->
+++        <div class="flex-1 flex flex-col min-w-0 w-full overflow-hidden">
+++            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-6 sticky top-0 z-30 shadow-sm shrink-0">
+++                <div class="flex items-center gap-4">
+++                    <button @click="toggleSidebar()" class="p-2 rounded-lg hover:bg-gray-100 text-gray-600 transition-colors">
+++                        <i data-lucide="menu" class="w-5 h-5"></i>
+++                    </button>
+++                    <h1 class="text-lg font-bold text-gray-800 tracking-tight" x-text="titleMap[currentView] || 'HR Assist'"></h1>
+++                </div>
+++                <div class="flex items-center gap-4">
+++                    <label class="flex items-center gap-2 cursor-pointer bg-gray-100 px-3 py-1.5 rounded-full hover:bg-gray-200 transition-colors">
+++                        <span class="text-[10px] font-bold text-gray-500 uppercase tracking-widest" x-text="isManager ? 'Manager Mode' : 'Creator Mode'"></span>
+++                        <input type="checkbox" x-model="isManager" class="toggle toggle-sm toggle-success" />
+++                    </label>
+++                    <button @click="logout()" class="text-xs font-bold text-gray-400 hover:text-red-600 transition-colors uppercase tracking-widest">ログアウト</button>
+++                </div>
+++            </header>
+++
+++            <main class="flex-1 overflow-y-auto p-6 w-full max-w-6xl mx-auto no-scrollbar">
+++
+++                <!-- [View: HOME] -->
+++                <div x-show="currentView === 'home'" class="space-y-6">
+++                    <!-- ツールバー -->
+++                    <div class="flex flex-wrap items-center justify-between gap-4">
+++                        <div class="flex items-center gap-2 flex-1 min-w-[300px]">
+++                            <div class="relative flex-1">
+++                                <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
+++                                <input type="text" x-model="searchQuery" placeholder="キーワード検索..." class="input input-bordered w-full pl-10 h-10 text-sm border-gray-300 focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-sm" />
+++                            </div>
+++                            
+++                            <div class="dropdown dropdown-end">
+++                                <label tabindex="0" class="btn btn-outline btn-sm border-gray-300 normal-case h-10 px-4 font-bold text-gray-600 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800">
+++                                    <i data-lucide="filter" class="w-4 h-4 mr-2"></i>
+++                                    カテゴリ
+++                                    <span x-show="selectedTags.length > 0" class="ml-2 badge bg-slate-800 text-white border-none badge-xs" x-text="selectedTags.length"></span>
+++                                </label>
+++                                <div tabindex="0" class="dropdown-content z-[50] card card-compact w-64 p-2 shadow-xl bg-white border border-gray-200 mt-2">
+++                                    <div class="card-body">
+++                                        <h3 class="font-bold text-xs uppercase text-gray-400 tracking-widest mb-2 border-b pb-1">カテゴリ絞り込み</h3>
+++                                        <div class="flex flex-col gap-1 max-h-64 overflow-y-auto no-scrollbar">
+++                                            <template x-for="tag in availableTags" :key="tag">
+++                                                <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
+++                                                    <input type="checkbox" :checked="selectedTags.includes(tag)" @change="toggleTag(tag)" class="checkbox checkbox-sm checkbox-primary border-gray-300" />
+++                                                    <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
+++                                                </label>
+++                                            </template>
+++                                        </div>
+++                                        <div class="divider my-1"></div>
+++                                        <button @click="selectedTags = []" class="btn btn-ghost btn-xs text-red-500 font-bold" :disabled="selectedTags.length === 0">リセット</button>
+++                                    </div>
+++                                </div>
+++                            </div>
+++                        </div>
+++
+++                        <div class="bg-gray-200 p-1 rounded-lg flex items-center shadow-inner">
+++                            <button @click="viewMode = 'card'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'card' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
+++                                <i data-lucide="layout-grid" class="w-4 h-4"></i>
+++                            </button>
+++                            <button @click="viewMode = 'list'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'list' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
+++                                <i data-lucide="list" class="w-4 h-4"></i>
+++                            </button>
+++                        </div>
+++                    </div>
+++
+++                    <!-- ステータス・クイックフィルタと並び替え -->
+++                    <div class="flex flex-wrap items-center justify-between gap-4">
+++                        <div class="flex flex-wrap items-center gap-2">
+++                            <div class="flex items-center gap-1 bg-gray-200 p-1 rounded-xl w-max shadow-inner overflow-x-auto no-scrollbar">
+++                                <button @click="statusFilter = 'all'" :class="statusFilter === 'all' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">すべて</button>
+++                                <button @click="statusFilter = 'unanswered'" :class="statusFilter === 'unanswered' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応のみ</button>
+++                                <button @click="statusFilter = 'progress'" :class="statusFilter === 'progress' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">対応中のみ</button>
+++                                <button @click="statusFilter = 'open'" :class="statusFilter === 'open' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応と対応中</button>
+++                                <button @click="statusFilter = 'completed'" :class="statusFilter === 'completed' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">完了のみ</button>
+++                            </div>
+++                            
+++                            <div x-show="selectedTags.length > 0" class="flex flex-wrap gap-1 items-center ml-2">
+++                                <template x-for="tag in selectedTags" :key="tag">
+++                                    <span class="badge bg-slate-100 border-slate-200 text-slate-800 font-bold gap-1 px-2 py-2 text-[10px] shadow-sm">
+++                                        <span x-text="tag"></span>
+++                                        <i data-lucide="x" class="w-2.5 h-2.5 cursor-pointer hover:text-red-500" @click="toggleTag(tag); refreshIcons()"></i>
+++                                    </span>
+++                                </template>
+++                            </div>
+++                        </div>
+++
+++                        <!-- 並び替えドロップダウン -->
+++                        <select x-model="sortOrder" class="select select-bordered select-sm text-xs font-bold text-gray-600 focus:outline-none focus:border-slate-800 shadow-sm bg-white">
+++                            <option value="date_desc">質問日時 (新しい順)</option>
+++                            <option value="date_asc">質問日時 (古い順)</option>
+++                            <option value="dueDate_asc">回答期日 (近い順)</option>
+++                            <option value="dueDate_desc">回答期日 (遠い順)</option>
+++                        </select>
+++                    </div>
+++
+++                    <!-- カード表示 -->
+++                    <div x-show="viewMode === 'card'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
+++                        <template x-for="q in filteredQuestions" :key="q.id">
+++                            <div class="bg-white rounded-xl p-5 shadow-sm border border-gray-200 cursor-pointer hover:shadow-md hover:border-slate-300 transition-all flex flex-col group" @click="openDetail(q)">
+++                                <div class="flex justify-between mb-3 items-start">
+++                                    <div class="flex items-center gap-3 min-w-0">
+++                                        <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-600 text-sm group-hover:bg-slate-100 group-hover:text-slate-800 transition-colors shrink-0" x-text="q.sender.charAt(0)"></div>
+++                                        <div class="min-w-0">
+++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="q.sender"></div>
+++                                            <div class="text-[10px] text-gray-400 font-bold uppercase" x-text="q.department"></div>
+++                                        </div>
+++                                    </div>
+++                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold shadow-sm" :class="statusColorMap[q.status]" x-text="q.status"></span>
+++                                </div>
+++                                <h3 class="text-base font-bold text-gray-800 mb-1 group-hover:text-slate-900" x-text="q.title"></h3>
+++                                <p class="text-sm text-gray-500 mb-4 flex-1 line-clamp-2" x-text="q.content"></p>
+++                                <div class="flex items-center justify-between pt-4 border-t border-gray-50 text-[10px] font-bold">
+++                                    <div class="flex gap-1 flex-wrap">
+++                                        <template x-for="tag in (q.tags || [])" :key="tag">
+++                                            <span class="bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
+++                                        </template>
+++                                    </div>
+++                                    <div class="flex items-center gap-1 shrink-0" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
+++                                        <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
+++                                        <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+++                                    </div>
+++                                </div>
+++                            </div>
+++                        </template>
+++                        <div x-show="filteredQuestions.length === 0" class="col-span-full py-16 text-center">
+++                            <i data-lucide="search-x" class="w-12 h-12 text-gray-200 mx-auto mb-4"></i>
+++                            <p class="text-gray-400 font-bold">条件に一致する質問は見つかりませんでした。</p>
+++                        </div>
+++                    </div>
+++
+++                    <!-- リスト表示 -->
+++                    <div x-show="viewMode === 'list'" class="flex flex-col gap-2 w-full" style="display: none;">
+++                        <template x-for="q in filteredQuestions" :key="q.id">
+++                            <div class="bg-white rounded-lg p-3 shadow-sm border border-gray-200 cursor-pointer hover:border-slate-300 hover:shadow-md transition-all flex items-center gap-4 group" @click="openDetail(q)">
+++                                <span class="px-2 py-0.5 rounded text-[10px] font-bold shrink-0 w-16 text-center" :class="statusColorMap[q.status]" x-text="q.status"></span>
+++                                <h3 class="text-sm font-bold text-gray-800 truncate w-48 shrink-0 group-hover:text-slate-900" x-text="q.title"></h3>
+++                                <p class="text-xs text-gray-500 truncate flex-1 font-medium" x-text="q.content"></p>
+++                                <div class="flex gap-1 shrink-0">
+++                                    <template x-for="tag in (q.tags || [])" :key="tag">
+++                                        <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
+++                                    </template>
+++                                </div>
+++                                <div class="text-[10px] font-bold shrink-0 flex items-center gap-1 w-16 justify-end" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
+++                                    <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
+++                                    <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+++                                </div>
+++                            </div>
+++                        </template>
+++                    </div>
+++                </div>
+++
+++                <!-- [View: DETAIL] 質問詳細 -->
+++                <div x-show="currentView === 'detail'" class="space-y-6" style="display: none;">
+++                    <div class="flex items-center justify-between">
+++                        <button class="btn btn-ghost btn-sm gap-2 font-bold text-gray-500 hover:text-gray-800" @click="setView('home')">
+++                            <i data-lucide="arrow-left" class="w-4 h-4"></i> 一覧に戻る
+++                        </button>
+++                    </div>
+++                    <div class="flex flex-col lg:flex-row gap-6">
+++                        
+++                        <!-- 左側：チャットエリア -->
+++                        <div class="flex-1 bg-white rounded-xl shadow-sm border border-gray-200 flex flex-col overflow-hidden min-h-[500px]">
+++                            <div class="bg-gray-50 p-6 flex-1 space-y-6 overflow-y-auto">
+++                                <div class="flex gap-4">
+++                                    <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 shrink-0 shadow-sm" x-text="activeQuestion.sender?.charAt(0)"></div>
+++                                    <div class="space-y-1 max-w-[80%]">
+++                                        <div class="text-[10px] font-bold text-gray-400 uppercase tracking-widest" x-text="(activeQuestion.sender || '') + ' • ' + (activeQuestion.department || '')"></div>
+++                                        <div class="bg-white p-4 rounded-2xl rounded-tl-none border border-gray-100 shadow-sm text-sm leading-relaxed" x-text="activeQuestion.content"></div>
+++                                    </div>
+++                                </div>
+++                                <div class="flex gap-4">
+++                                    <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix" class="w-10 h-10 rounded-full border bg-white shrink-0 shadow-sm" />
+++                                    <div class="space-y-1 max-w-[80%]">
+++                                        <div class="text-[10px] font-bold text-yellow-600 uppercase tracking-widest">自分 • 内部メモ</div>
+++                                        <div class="bg-yellow-50 p-4 rounded-2xl rounded-tl-none border border-yellow-100 text-sm text-yellow-900 italic">前回の事例（#1001）を参考に回答を作成中...</div>
+++                                    </div>
+++                                </div>
+++                            </div>
+++                            <div class="p-4 border-t border-gray-100 bg-white">
+++                                <div class="flex gap-2 mb-3">
+++                                    <button class="text-[10px] font-bold bg-slate-800 text-white px-3 py-1 rounded shadow-sm">質問者に返信</button>
+++                                    <button class="text-[10px] font-bold bg-gray-100 text-gray-500 px-3 py-1 rounded">内部メモ</button>
+++                                </div>
+++                                <textarea class="textarea textarea-bordered w-full h-24 text-sm focus:border-slate-800 focus:outline-none" placeholder="回答またはメモを入力..."></textarea>
+++                                <div class="mt-3 flex justify-end">
+++                                    <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md" @click="alert('送信しました'); setView('home')">送信</button>
+++                                </div>
+++                            </div>
+++                        </div>
+++
+++                        <!-- 右側：プロパティパネル -->
+++                        <div class="w-full lg:w-80 space-y-4 shrink-0">
+++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm space-y-6">
+++                                
+++                                <div>
+++                                    <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-3">質問者情報</h4>
+++                                    <div class="flex items-center gap-3">
+++                                        <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 text-sm shadow-inner shrink-0" x-text="activeQuestion.sender?.charAt(0)"></div>
+++                                        <div class="min-w-0">
+++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.sender"></div>
+++                                            <div class="text-[10px] text-gray-500 font-bold uppercase tracking-wider" x-text="activeQuestion.department"></div>
+++                                        </div>
+++                                    </div>
+++                                </div>
+++
+++                                <div class="divider my-0"></div>
+++
+++                                <div class="space-y-4">
+++                                    <div>
+++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">タイトル</h4>
+++                                        <input type="text" x-model="activeQuestion.title" class="input input-bordered input-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" />
+++                                    </div>
+++
+++                                    <div>
+++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">ステータス</h4>
+++                                        <select class="select select-bordered select-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" x-model="activeQuestion.status">
+++                                            <option>未対応</option>
+++                                            <option>対応中</option>
+++                                            <option>完了</option>
+++                                        </select>
+++                                    </div>
+++
+++                                    <!-- カテゴリ (マルチセレクト) -->
+++                                    <div>
+++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">カテゴリ</h4>
+++                                        <div class="relative w-full" x-data="{ isCategoryOpen: false }" @click.outside="isCategoryOpen = false">
+++                                            <button type="button" @click="isCategoryOpen = !isCategoryOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
+++                                                <span class="truncate text-xs" x-text="activeQuestion.tags && activeQuestion.tags.length > 0 ? activeQuestion.tags.join(', ') : 'カテゴリを選択'"></span>
+++                                                <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
+++                                            </button>
+++                                            <div x-show="isCategoryOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
+++                                                <div class="card-body p-2">
+++                                                    <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
+++                                                        <template x-for="tag in availableTags" :key="tag">
+++                                                            <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
+++                                                                <input type="checkbox" 
+++                                                                       :checked="activeQuestion.tags && activeQuestion.tags.includes(tag)" 
+++                                                                       @change="toggleQuestionTag(tag)"
+++                                                                       class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
+++                                                                <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
+++                                                            </label>
+++                                                        </template>
+++                                                    </div>
+++                                                </div>
+++                                            </div>
+++                                        </div>
+++                                    </div>
+++
+++                                    <div class="grid grid-cols-2 gap-3">
+++                                        <div>
+++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">質問日時</h4>
+++                                            <div class="text-xs font-bold text-gray-700 bg-gray-50 px-2 rounded-lg border border-gray-200 flex items-center h-8 shadow-inner truncate" x-text="activeQuestion.date || '---'"></div>
+++                                        </div>
+++                                        <div>
+++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">期日</h4>
+++                                            <input type="date" x-model="activeQuestion.dueDate" class="input input-bordered input-sm w-full text-xs font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner px-2" />
+++                                        </div>
+++                                    </div>
+++                                </div>
+++                            </div>
+++                            
+++                            <!-- 関連する質問 -->
+++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm mt-4">
+++                                <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-4">関連する質問</h4>
+++                                
+++                                <div class="relative w-full" x-data="{ isRelatedOpen: false }" @click.outside="isRelatedOpen = false">
+++                                    <button type="button" @click="isRelatedOpen = !isRelatedOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
+++                                        <span class="truncate text-xs">
+++                                            <template x-if="!activeQuestion.relatedQuestions || activeQuestion.relatedQuestions.length === 0">
+++                                                <span>質問を選択</span>
+++                                            </template>
+++                                            <template x-if="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
+++                                                <span x-text="activeQuestion.relatedQuestions.length + '件選択中'"></span>
+++                                            </template>
+++                                        </span>
+++                                        <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
+++                                    </button>
+++                                    
+++                                    <div x-show="isRelatedOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
+++                                        <div class="card-body p-2">
+++                                            <!-- 検索入力 -->
+++                                            <div class="relative mb-2">
+++                                                <i data-lucide="search" class="w-3 h-3 absolute left-2 top-2.5 text-gray-400"></i>
+++                                                <input type="text" x-model="relatedSearchQuery" placeholder="検索..." class="input input-bordered input-sm w-full pl-7 text-xs focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800" />
+++                                            </div>
+++                                            
+++                                            <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
+++                                                <template x-for="rq in availableRelatedQuestions" :key="rq.id">
+++                                                    <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
+++                                                        <input type="checkbox" 
+++                                                               :checked="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.includes(rq.id)" 
+++                                                               @change="toggleRelatedQuestion(rq.id)"
+++                                                               class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
+++                                                        <div class="flex flex-col min-w-0">
+++                                                            <span class="text-xs font-bold text-gray-700 group-hover:text-slate-900 truncate" x-text="'#' + rq.id + ' ' + rq.title"></span>
+++                                                        </div>
+++                                                    </label>
+++                                                </template>
+++                                                <div x-show="availableRelatedQuestions.length === 0" class="text-center py-4 text-xs text-gray-400 font-bold">
+++                                                    該当なし
+++                                                </div>
+++                                            </div>
+++                                        </div>
+++                                    </div>
+++                                </div>
+++                                
+++                                <!-- 選択された関連質問のリスト -->
+++                                <div class="mt-3 space-y-2" x-show="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
+++                                    <template x-for="rId in activeQuestion.relatedQuestions" :key="rId">
+++                                        <div class="p-2 bg-gray-50 rounded border text-xs font-bold hover:bg-slate-50 transition-colors flex justify-between items-center group">
+++                                            <span class="truncate pr-2 cursor-pointer hover:text-blue-600" x-text="'#' + rId + ' ' + (questions.find(q => q.id === rId)?.title || '')" @click="openDetail(questions.find(q => q.id === rId))"></span>
+++                                            <i data-lucide="x" class="w-3 h-3 text-gray-400 hover:text-red-500 cursor-pointer shrink-0" @click="toggleRelatedQuestion(rId); refreshIcons()"></i>
+++                                        </div>
+++                                    </template>
+++                                </div>
+++                            </div>
+++                        </div>
+++                    </div>
+++                </div>
+++
+++                <!-- [View: NOTICE] -->
+++                <div x-show="currentView === 'notice'" class="max-w-3xl mx-auto space-y-4" style="display: none;">
+++                    <template x-for="n in notices" :key="n.id">
+++                        <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm flex gap-4 items-start hover:bg-gray-50 transition-colors cursor-pointer" @click="setView('home')">
+++                            <div class="w-10 h-10 rounded-lg flex items-center justify-center shrink-0" :class="n.type === 'alert' ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
+++                                <i :data-lucide="n.type === 'alert' ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
+++                            </div>
+++                            <div class="flex-1 min-w-0">
+++                                <div class="flex justify-between items-center mb-1">
+++                                    <h3 class="font-bold text-sm text-gray-800" x-text="n.title"></h3>
+++                                    <span class="text-[10px] font-bold text-gray-400" x-text="n.time"></span>
+++                                </div>
+++                                <p class="text-xs text-gray-500 line-clamp-2" x-text="n.content"></p>
+++                            </div>
+++                        </div>
+++                    </template>
+++                </div>
+++
+++                <!-- [View: CATEGORIES] -->
+++                <div x-show="currentView === 'categories'" class="space-y-6" style="display: none;">
+++                    <div class="flex justify-between items-center">
+++                        <div class="relative w-80">
+++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
+++                            <input type="text" placeholder="カテゴリ検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
+++                        </div>
+++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="alert('カテゴリを追加します（モック）')">+ カテゴリ追加</button>
+++                    </div>
+++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
+++                        <table class="table w-full">
+++                            <thead>
+++                                <tr class="bg-gray-50">
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">カテゴリ名</th>
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">使用数</th>
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
+++                                </tr>
+++                            </thead>
+++                            <tbody>
+++                                <template x-for="tag in availableTags" :key="tag">
+++                                    <tr class="hover:bg-gray-50 transition-colors">
+++                                        <td>
+++                                            <div class="flex items-center gap-3">
+++                                                <div class="w-8 h-8 rounded-lg bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs">
+++                                                    <i data-lucide="tag" class="w-4 h-4"></i>
+++                                                </div>
+++                                                <div class="font-bold text-sm" x-text="tag"></div>
+++                                            </div>
+++                                        </td>
+++                                        <td>
+++                                            <span class="badge badge-sm font-bold bg-slate-100 text-slate-800 border-slate-200" x-text="questions.filter(q => q.tags && q.tags.includes(tag)).length + '件'"></span>
+++                                        </td>
+++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
+++                                    </tr>
+++                                </template>
+++                            </tbody>
+++                        </table>
+++                    </div>
+++                </div>
+++
+++                <!-- [View: USERS] -->
+++                <div x-show="currentView === 'users'" class="space-y-6" style="display: none;">
+++                    <div class="flex justify-between items-center">
+++                        <div class="relative w-80">
+++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
+++                            <input type="text" placeholder="ユーザー検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
+++                        </div>
+++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="showAddUserModal = true">+ ユーザー追加</button>
+++                    </div>
+++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
+++                        <table class="table w-full">
+++                            <thead>
+++                                <tr class="bg-gray-50">
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">氏名</th>
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">役割</th>
+++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
+++                                </tr>
+++                            </thead>
+++                            <tbody>
+++                                <template x-for="u in users" :key="u.email">
+++                                    <tr class="hover:bg-gray-50 transition-colors">
+++                                        <td>
+++                                            <div class="flex items-center gap-3">
+++                                                <div class="w-8 h-8 rounded-full bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs" x-text="u.name.charAt(0)"></div>
+++                                                <div>
+++                                                    <div class="font-bold text-sm" x-text="u.name"></div>
+++                                                    <div class="text-[10px] text-gray-400" x-text="u.email"></div>
+++                                                </div>
+++                                            </div>
+++                                        </td>
+++                                        <td><span class="badge badge-sm font-bold" :class="u.role === 'Manager' ? 'badge-info bg-slate-100 text-slate-800 border-slate-200' : 'badge-ghost'" x-text="u.role"></span></td>
+++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
+++                                    </tr>
+++                                </template>
+++                            </tbody>
+++                        </table>
+++                    </div>
+++                </div>
+++
+++                <!-- [View: SETTINGS] -->
+++                <div x-show="currentView === 'settings'" class="max-w-2xl mx-auto" style="display: none;">
+++                    <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
+++                        <div class="p-6 border-b border-gray-100 bg-gray-50/50 text-center lg:text-left">
+++                            <h2 class="text-lg font-bold text-gray-800 tracking-tight">プロフィール設定</h2>
+++                        </div>
+++                        <div class="p-8 space-y-6">
+++                            <div class="flex flex-col sm:flex-row items-center gap-8">
+++                                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-24 h-24 rounded-2xl border-2 border-white shadow-lg" />
+++                                <button class="btn btn-sm btn-outline font-bold shadow-sm">画像をアップロード</button>
+++                            </div>
+++                            <div class="space-y-4">
+++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">氏名</label><input type="text" value="自分" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
+++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">メールアドレス</label><input type="email" value="jinji.taro@example.com" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
+++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">新しいパスワード</label><input type="password" placeholder="変更する場合のみ入力" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
+++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">自己紹介・メモ</label><textarea class="textarea textarea-bordered w-full h-24 text-sm font-bold shadow-inner" placeholder="管理担当のメモなど..."></textarea></div>
+++                            </div>
+++                        </div>
+++                        <div class="p-6 bg-gray-50 border-t border-gray-100 flex justify-end gap-3">
+++                            <button class="btn btn-ghost font-bold text-gray-500" @click="setView('home')">キャンセル</button>
+++                            <button class="btn bg-slate-800 text-white border-none font-bold px-10 shadow-md" @click="setView('home'); alert('設定を保存しました')">保存する</button>
+++                        </div>
+++                    </div>
+++                </div>
+++
+++            </main>
+++        </div>
+++    </div>
+++
+++    <!-- ユーザー招待モーダル -->
+++    <div class="modal" :class="{'modal-open': showAddUserModal}">
+++        <div class="modal-box bg-white rounded-2xl shadow-2xl">
+++            <h3 class="font-bold text-lg mb-6 tracking-tight">新規ユーザーを招待</h3>
+++            <div class="space-y-4">
+++                <input type="text" placeholder="氏名" class="input input-bordered w-full text-sm shadow-inner" />
+++                <input type="email" placeholder="メールアドレス" class="input input-bordered w-full text-sm shadow-inner" />
+++                <select class="select select-bordered w-full text-sm font-bold shadow-inner">
+++                    <option disabled selected>役割を選択</option>
+++                    <option>Manager</option>
+++                    <option>Creator</option>
+++                </select>
+++            </div>
+++            <div class="modal-action">
+++                <button class="btn btn-ghost font-bold" @click="showAddUserModal = false">キャンセル</button>
+++                <button class="btn bg-slate-800 border-none font-bold px-8 shadow-md" @click="showAddUserModal = false; alert('ユーザーを招待しました')">招待を送信</button>
+++            </div>
+++        </div>
+++        <div class="modal-backdrop bg-gray-900 bg-opacity-50" @click="showAddUserModal = false"></div>
+++    </div>
+++
+++</body>
+++</html>
+++<!DOCTYPE html>
+diff --git a/doc/db/INIT.sql b/doc/db/INIT.sql
+index b9518ac..2c23f3e 100644
+--- a/doc/db/INIT.sql
++++ b/doc/db/INIT.sql
+@@ -0,0 +1,5 @@
++DROP SCHEMA public CASCADE;
++CREATE SCHEMA public;
++GRANT ALL ON SCHEMA public TO postgres;
++GRANT ALL ON SCHEMA public TO public;
++
+@@ -245,0 +251,5 @@ INSERT INTO users (name, password, email, role_id) VALUES
++<<<<<<< HEAD
++('Admin', 'Admin', 'Admin', 1),
++('Taro Yamada', 'hashed_password_1', 'taro@example.com', 2),
++('Hanako Suzuki', 'hashed_password_2', 'hanako@example.com', 3),
++=======
+@@ -247,0 +258 @@ INSERT INTO users (name, password, email, role_id) VALUES
++>>>>>>> add-mock
+@@ -320 +331 @@ INSERT INTO notices (type_id, question_id, content, display_due) VALUES
+-(1, NULL, 'システムメンテナンスのお知らせ', NULL);
+\ No newline at end of file
++(1, NULL, 'システムメンテナンスのお知らせ', NULL);
+diff --git a/doc/db/er.md b/doc/db/er.md
+index 59a0289..0683e71 100644
+--- a/doc/db/er.md
++++ b/doc/db/er.md
+@@ -21 +20,0 @@ erDiagram
+-        number support_id FK
+@@ -23,0 +23 @@ erDiagram
++        number support_id FK
+@@ -88 +88 @@ erDiagram
+-        string title
++        string name
+@@ -120 +120,16 @@ erDiagram
+-```
++
++    NOTICE {
++        number id PK
++        number type_id FK
++        number question_id FK
++        string content
++        datetime displayDue
++    }
++    NOTICE ||--|| NOTICE_TYPE: "通知は必ず１つの通知タイプを持つ"
++    NOTICE ||--o| QUESTION: "質問期限通知の場合は質問を持っている場合もある"
++
++    NOTICE_TYPE {
++        number id PK
++        string name "SYSTEM|DUE"
++    }
++```
+\ No newline at end of file
+diff --git a/frontend/bun.lock b/frontend/bun.lock
+index 6a3c676..c74d58a 100644
+--- a/frontend/bun.lock
++++ b/frontend/bun.lock
+@@ -45 +45 @@
+-    "@types/node": ["@types/node@25.5.0", "", { "dependencies": { "undici-types": "~7.18.0" } }, "sha512-jp2P3tQMSxWugkCUKLRPVUpGaL5MVFwF8RDuSRztfwgN1wmqJeMSbKlnEtQqU8UrhTmzEmZdu2I6v2dpp7XIxw=="],
++    "@types/node": ["@types/node@25.6.0", "", { "dependencies": { "undici-types": "~7.19.0" } }, "sha512-+qIYRKdNYJwY3vRCZMdJbPLJAtGjQBudzZzdzwQYkEPQd+PJGixUL5QfvCLDaULoLv+RhT3LDkwEfKaAkgSmNQ=="],
+@@ -61 +61 @@
+-    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.13", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-BL2sTuHOdy0YT1lYieUxTw/QMtPBC3pmlJC6xk8BBYVv6vcw3SGdKemQ+Xsx9ik2F/lYDO9tqsFQH1r9PFuHKw=="],
++    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.20", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-1AaXxEPfXT+GvTBJFuy4yXVHWJBXa4OdbIebGN/wX5DlsIkU0+wzGnd2lOzokSk51d5LUmqjgBLRLlypLUqInQ=="],
+@@ -73 +73 @@
+-    "caniuse-lite": ["caniuse-lite@1.0.30001788", "", {}, "sha512-6q8HFp+lOQtcf7wBK+uEenxymVWkGKkjFpCvw5W25cmMwEDU45p1xQFBQv8JDlMMry7eNxyBaR+qxgmTUZkIRQ=="],
++    "caniuse-lite": ["caniuse-lite@1.0.30001790", "", {}, "sha512-bOoxfJPyYo+ds6W0YfptaCWbFnJYjh2Y1Eow5lRv+vI2u8ganPZqNm1JwNh0t2ELQCqIWg4B3dWEusgAmsoyOw=="],
+@@ -91 +91,3 @@
+-    "electron-to-chromium": ["electron-to-chromium@1.5.331", "", {}, "sha512-IbxXrsTlD3hRodkLnbxAPP4OuJYdWCeM3IOdT+CpcMoIwIoDfCmRpEtSPfwBXxVkg9xmBeY7Lz2Eo2TDn/HC3Q=="],
++    "electron-to-chromium": ["electron-to-chromium@1.5.343", "", {}, "sha512-YHnQ3MXI08icvL9ZKnEBy05F2EQ8ob01UaMOuMbM8l+4UcAq6MPPbBTJBbsBUg3H8JeZNt+O4fjsoWth3p6IFg=="],
++
++    "es-errors": ["es-errors@1.3.0", "", {}, "sha512-Zf5H2Kxt2xjTvbJvP2ZWLEICxA6j+hAmMzIlypy4xcBg1vKVnx89Wy0GbS+kf5cwCVFFzdCFh2XSCFNULS6csw=="],
+@@ -115,3 +117 @@
+-    "hasown": ["hasown@2.0.2", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-0hJU9SCPvmMzIBdZFqNPXWa6dqh7WdH0cII9y+CyS8rG3nL48Bclra9HmKhVVUHyPWNH5Y7xDwAB7bfgSjkUMQ=="],
+-
+-    "htmx-ext-client-side-templates": ["htmx-ext-client-side-templates@2.0.2", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-d2dA5HOJuMPN+QmAa4MtriTc1OZaaidG/UK/uwDsKJzstMFae2M7tFgykFXOjuFoV2XNCcexr3p+a9p5Jb54Dg=="],
++    "hasown": ["hasown@2.0.3", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-ej4AhfhfL2Q2zpMmLo7U1Uv9+PyhIZpgQLGT1F9miIGmiCJIoCgSmczFdrc97mWT4kVY72KA+WnnhJ5pghSvSg=="],
+@@ -149 +149 @@
+-    "node-releases": ["node-releases@2.0.37", "", {}, "sha512-1h5gKZCF+pO/o3Iqt5Jp7wc9rH3eJJ0+nh/CIoiRwjRxde/hAHyLPXYN4V3CqKAbiZPSeJFSWHmJsbkicta0Eg=="],
++    "node-releases": ["node-releases@2.0.38", "", {}, "sha512-3qT/88Y3FbH/Kx4szpQQ4HzUbVrHPKTLVpVocKiLfoYvw9XSGOX2FmD2d6DrXbVYyAQTF2HeF6My8jmzx7/CRw=="],
+@@ -187 +187 @@
+-    "resolve": ["resolve@1.22.11", "", { "dependencies": { "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-RfqAvLnMl313r7c9oclB1HhUEAezcpLjz95wFH4LVuhk9JF/r22qmVP9AMmOU4vMX7Q8pN8jwNg/CSpdFnMjTQ=="],
++    "resolve": ["resolve@1.22.12", "", { "dependencies": { "es-errors": "^1.3.0", "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-TyeJ1zif53BPfHootBGwPRYT1RUt6oGWsaQr8UyZW/eAm9bKoijtvruSDEmZHm92CwS9nj7/fWttqPCgzep8CA=="],
+@@ -205 +205 @@
+-    "tinyglobby": ["tinyglobby@0.2.15", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.3" } }, "sha512-j2Zq4NyQYG5XMST4cbs02Ak8iJUdxRM0XI5QyxXuZOzKOINmWurp3smXu3y5wDcJrptwpSjgXHzIQxR0omXljQ=="],
++    "tinyglobby": ["tinyglobby@0.2.16", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.4" } }, "sha512-pn99VhoACYR8nFHhxqix+uvsbXineAasWm5ojXoN8xEwK5Kd3/TrhNn1wByuD52UxWRLy8pu+kRMniEi6Eq9Zg=="],
+@@ -213 +213 @@
+-    "undici-types": ["undici-types@7.18.2", "", {}, "sha512-AsuCzffGHJybSaRrmr5eHr81mwJU3kjw6M+uprWvCXiNeN9SOGwQ3Jn8jb8m3Z6izVgknn1R0FTCEAP2QrLY/w=="],
++    "undici-types": ["undici-types@7.19.2", "", {}, "sha512-qYVnV5OEm2AW8cJMCpdV20CDyaN3g0AjDlOGf1OW4iaDEx8MwdtChUp4zu4H0VP3nDRF/8RKWH+IPp9uW0YGZg=="],
+@@ -221,2 +220,0 @@
+-    "browserslist/caniuse-lite": ["caniuse-lite@1.0.30001784", "", {}, "sha512-WU346nBTklUV9YfUl60fqRbU5ZqyXlqvo1SgigE1OAXK5bFL8LL9q1K7aap3N739l4BvNqnkm3YrGHiY9sfUQw=="],
+-
+@@ -227,2 +224,0 @@
+-    "tailwindcss/postcss": ["postcss@8.5.8", "", { "dependencies": { "nanoid": "^3.3.11", "picocolors": "^1.1.1", "source-map-js": "^1.2.1" } }, "sha512-OW/rX8O/jXnm82Ey1k44pObPtdblfiuWnrd8X7GJ7emImCOstunGbXUpp7HdBrFQX6rJzn3sPT397Wp5aCwCHg=="],
+-
+diff --git a/handler.go b/handler.go
+deleted file mode 100644
+index 06ab7d0..0000000
+--- a/handler.go
++++ /dev/null
+@@ -1 +0,0 @@
+-package main
+diff --git a/handler/apiHandler.go b/handler/apiHandler.go
+index 11c73b8..ab9bf32 100644
+--- a/handler/apiHandler.go
++++ b/handler/apiHandler.go
+@@ -20,3 +20,4 @@ func (hm *HandlerManager) SetAPIHandler() (routeInfos []echo.RouteInfo) {
+-	routeInfos = append(routeInfos, hm.setupUserHandler()...)
+-	routeInfos = append(routeInfos, hm.setupQuestionHandler()...)
+-	routeInfos = append(routeInfos, hm.setupTagHandler()...)
++	group := hm.e.Group(apiPath, GetJWTConfig())
++	routeInfos = append(routeInfos, hm.setupUserHandler(group)...)
++	routeInfos = append(routeInfos, hm.setupQuestionHandler(group)...)
++	routeInfos = append(routeInfos, hm.setupTagHandler(group)...)
+@@ -27 +28,5 @@ func (hm *HandlerManager) SetAPIHandler() (routeInfos []echo.RouteInfo) {
+-func (hm *HandlerManager) setupUserHandler() (routeInfos []echo.RouteInfo) {
++// TODO; sseeventの名前の一覧をJSに渡すかどうか、画面側に渡す値を入力データのまま（Form）のまま他ユーザへ配信するか、DBの情報と一致したものを受け取ったほうがいいため、Entityを返却してフロントエンドで変換をかける、もしくはフロントの変換ロジックをサーバ側へと移動させ、フロントエンドの変換ロジックは使わないようにする。
++// TODO; 通知→質問の場合は一覧へ戻るではなく、通知に戻ったほうがUX的に正解だと思う
++// TODO; 更新→入力データを他ユーザへ配信、この流れだとIDが未付与の状態が送信されてしまいオブジェクトのキー（undefined）で打消しあってしまう。入力データを取得後DBからIDのもっとも大きいものを取得してきて、それに置換。それを返却して対処することができる。
++// TODO; 各CRUDのタイミングでのSSE処理の粒度があやふやになっている。登録・更新は同じイベント内の処理でよさそう。削除は削除対象のIDを送信し対象の配列内にIDを持つオブジェクトが存在すればDELETE
++func (hm *HandlerManager) setupUserHandler(group *echo.Group) (routeInfos []echo.RouteInfo) {
+@@ -30 +35 @@ func (hm *HandlerManager) setupUserHandler() (routeInfos []echo.RouteInfo) {
+-	var api = hm.e.Group(apiPath, GetJWTConfig())
++	const api = "user"
+@@ -32,3 +37,3 @@ func (hm *HandlerManager) setupUserHandler() (routeInfos []echo.RouteInfo) {
+-	routeInfos = append(routeInfos, api.POST(uri, hm.registerUser()))
+-	routeInfos = append(routeInfos, api.PUT(uri, hm.updateUserByID()))
+-	routeInfos = append(routeInfos, api.DELETE(uriWithID, hm.deleteUserByID()))
++	routeInfos = append(routeInfos, group.POST(uri, hm.registerUser(api, hm.hub.sendCreateEvent)))
++	routeInfos = append(routeInfos, group.PUT(uri, hm.updateUserByID(api, hm.hub.sendUpdateEvent)))
++	routeInfos = append(routeInfos, group.DELETE(uriWithID, hm.deleteUserByID(api, hm.hub.sendDeleteEvent)))
+@@ -38 +43 @@ func (hm *HandlerManager) setupUserHandler() (routeInfos []echo.RouteInfo) {
+-func (hm *HandlerManager) setupQuestionHandler() (routeInfos []echo.RouteInfo) {
++func (hm *HandlerManager) setupQuestionHandler(group *echo.Group) (routeInfos []echo.RouteInfo) {
+@@ -41 +46 @@ func (hm *HandlerManager) setupQuestionHandler() (routeInfos []echo.RouteInfo) {
+-	var api = hm.e.Group(apiPath, GetJWTConfig())
++	const api = "question"
+@@ -43,4 +48,5 @@ func (hm *HandlerManager) setupQuestionHandler() (routeInfos []echo.RouteInfo) {
+-	routeInfos = append(routeInfos, api.POST(uri, hm.registerQuestion()))
+-	routeInfos = append(routeInfos, api.GET(uriWithID, hm.getQuestionByID()))
+-	routeInfos = append(routeInfos, api.DELETE(uriWithID, hm.deleteQuestionByID()))
+-	routeInfos = append(routeInfos, api.PUT(uri, hm.updateQuestionByID()))
++	routeInfos = append(routeInfos, group.POST(uri, hm.registerQuestion(api, hm.hub.sendCreateEvent)))
++	routeInfos = append(routeInfos, group.GET(uri, hm.getQuestions()))
++	routeInfos = append(routeInfos, group.GET(uriWithID, hm.getQuestionByID()))
++	routeInfos = append(routeInfos, group.DELETE(uriWithID, hm.deleteQuestionByID(api, hm.hub.sendDeleteEvent)))
++	routeInfos = append(routeInfos, group.PUT(uri, hm.updateQuestionByID(api, hm.hub.sendUpdateEvent)))
+@@ -50 +56 @@ func (hm *HandlerManager) setupQuestionHandler() (routeInfos []echo.RouteInfo) {
+-func (hm *HandlerManager) setupTagHandler() (routeInfos []echo.RouteInfo) {
++func (hm *HandlerManager) setupTagHandler(group *echo.Group) (routeInfos []echo.RouteInfo) {
+@@ -53 +59 @@ func (hm *HandlerManager) setupTagHandler() (routeInfos []echo.RouteInfo) {
+-	var api = hm.e.Group(apiPath, GetJWTConfig())
++	const api = "tag"
+@@ -55,3 +61,4 @@ func (hm *HandlerManager) setupTagHandler() (routeInfos []echo.RouteInfo) {
+-	routeInfos = append(routeInfos, api.POST(uri, hm.registerTag()))
+-	routeInfos = append(routeInfos, api.PUT(uri, hm.updateTag()))
+-	routeInfos = append(routeInfos, api.DELETE(uriWithID, hm.deleteTagByID()))
++	routeInfos = append(routeInfos, group.GET(uri, hm.getTags()))
++	routeInfos = append(routeInfos, group.POST(uri, hm.registerTag(api, hm.hub.sendCreateEvent)))
++	routeInfos = append(routeInfos, group.PUT(uri, hm.updateTag(api, hm.hub.sendUpdateEvent)))
++	routeInfos = append(routeInfos, group.DELETE(uriWithID, hm.deleteTagByID(api, hm.hub.sendDeleteEvent)))
+@@ -61 +68 @@ func (hm *HandlerManager) setupTagHandler() (routeInfos []echo.RouteInfo) {
+-func (hm *HandlerManager) registerUser() echo.HandlerFunc {
++func (hm *HandlerManager) registerUser(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -74 +81,6 @@ func (hm *HandlerManager) registerUser() echo.HandlerFunc {
+-		return infrastructure.Register(c.Request().Context(), hm.db, &data)
++		if err := infrastructure.Register(c.Request().Context(), hm.db, &data); err != nil {
++			return c.JSON(http.StatusInternalServerError, fmt.Errorf("Create user server error %w", err))
++		}
++
++		sendEvent(api, string(body))
++		return c.JSON(http.StatusOK, nil)
+@@ -78 +90 @@ func (hm *HandlerManager) registerUser() echo.HandlerFunc {
+-func (hm *HandlerManager) registerQuestion() echo.HandlerFunc {
++func (hm *HandlerManager) registerQuestion(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -91 +103,5 @@ func (hm *HandlerManager) registerQuestion() echo.HandlerFunc {
+-		return infrastructure.Register(c.Request().Context(), hm.db, &data)
++		if err := infrastructure.Register(c.Request().Context(), hm.db, &data); err != nil {
++			return c.JSON(http.StatusInternalServerError, fmt.Errorf("Create question server error %w", err))
++		}
++		sendEvent(api, string(body))
++		return c.JSON(http.StatusOK, nil)
+@@ -95 +111 @@ func (hm *HandlerManager) registerQuestion() echo.HandlerFunc {
+-func (hm *HandlerManager) registerTag() echo.HandlerFunc {
++func (hm *HandlerManager) registerTag(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -108 +124,6 @@ func (hm *HandlerManager) registerTag() echo.HandlerFunc {
+-		return infrastructure.Register(c.Request().Context(), hm.db, &model)
++		if err := infrastructure.Register(c.Request().Context(), hm.db, &model); err != nil {
++			return c.JSON(http.StatusInternalServerError, fmt.Errorf("Create new tag error %w\n", err))
++		}
++
++		sendEvent(api, string(body))
++		return c.JSON(http.StatusOK, nil)
+@@ -112 +133 @@ func (hm *HandlerManager) registerTag() echo.HandlerFunc {
+-func (hm *HandlerManager) updateTag() echo.HandlerFunc {
++func (hm *HandlerManager) updateTag(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -127,0 +149 @@ func (hm *HandlerManager) updateTag() echo.HandlerFunc {
++		sendEvent(api, string(body))
+@@ -132 +154 @@ func (hm *HandlerManager) updateTag() echo.HandlerFunc {
+-func (hm *HandlerManager) deleteTagByID() echo.HandlerFunc {
++func (hm *HandlerManager) deleteTagByID(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -142,0 +165 @@ func (hm *HandlerManager) deleteTagByID() echo.HandlerFunc {
++		sendEvent(api, idStr)
+@@ -156,0 +180,10 @@ func (hm *HandlerManager) getUsers() echo.HandlerFunc {
++func (hm *HandlerManager) getQuestions() echo.HandlerFunc {
++	return func(c *echo.Context) error {
++		questions, err := infrastructure.GetQuestions(c.Request().Context(), hm.db)
++		if err != nil {
++			return err
++		}
++		return c.JSON(http.StatusOK, infrastructure.QuestionFromEntities(questions))
++	}
++}
++
+@@ -174 +207,11 @@ func (hm *HandlerManager) getQuestionByID() echo.HandlerFunc {
+-func (hm *HandlerManager) updateQuestionByID() echo.HandlerFunc {
++func (hm *HandlerManager) getTags() echo.HandlerFunc {
++	return func(c *echo.Context) error {
++		tags, err := infrastructure.GetTags(c.Request().Context(), hm.db)
++		if err != nil {
++			return err
++		}
++		return c.JSON(http.StatusOK, infrastructure.TagFromEntities(tags))
++	}
++}
++
++func (hm *HandlerManager) updateQuestionByID(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -188,0 +232 @@ func (hm *HandlerManager) updateQuestionByID() echo.HandlerFunc {
++		sendEvent(api, string(body))
+@@ -193 +237 @@ func (hm *HandlerManager) updateQuestionByID() echo.HandlerFunc {
+-func (hm *HandlerManager) deleteQuestionByID() echo.HandlerFunc {
++func (hm *HandlerManager) deleteQuestionByID(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -203,0 +248 @@ func (hm *HandlerManager) deleteQuestionByID() echo.HandlerFunc {
++		sendEvent(api, idStr)
+@@ -208 +253 @@ func (hm *HandlerManager) deleteQuestionByID() echo.HandlerFunc {
+-func (hm *HandlerManager) deleteUserByID() echo.HandlerFunc {
++func (hm *HandlerManager) deleteUserByID(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -218,0 +264 @@ func (hm *HandlerManager) deleteUserByID() echo.HandlerFunc {
++		sendEvent(api, idStr)
+@@ -223 +269 @@ func (hm *HandlerManager) deleteUserByID() echo.HandlerFunc {
+-func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
++func (hm *HandlerManager) updateUserByID(api string, sendEvent func(string, string)) echo.HandlerFunc {
+@@ -238,0 +285 @@ func (hm *HandlerManager) updateUserByID() echo.HandlerFunc {
++		sendEvent(api, string(body))
+diff --git a/handler/eventHub.go b/handler/eventHub.go
+index ced6adf..4f000a9 100644
+--- a/handler/eventHub.go
++++ b/handler/eventHub.go
+@@ -13,0 +14,9 @@ import (
++const (
++	sseTimeTick = "time-tick"
++	sseNotice   = "notice"
++	sseError    = "error"
++	sseCreate   = "create"
++	sseUpdate   = "update"
++	sseDelete   = "delete"
++)
++
+@@ -43,0 +53,10 @@ func NewHub() *Hub {
++func (h *Hub) Send(data, event string) {
++	h.Broadcast <- Event{Event: event, Data: data}
++}
++
++func (h *Hub) Sends(data string, events ...string) {
++	for _, event := range events {
++		h.Send(data, event)
++	}
++}
++
+@@ -73 +92 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-	tickHalfSecond := time.NewTicker(500 * time.Millisecond)
++	tickHalfSecond := time.NewTicker(1 * time.Hour)
+@@ -81 +100 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-			hub.Broadcast <- Event{Event: "time-tick", Data: t.In(local).Format(time.DateTime)}
++			hub.sendTimeTicker(t.In(local).Format(time.DateTime))
+@@ -160 +178,0 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
+-
+@@ -163,0 +182,34 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
++
++func (h *Hub) sendTimeTicker(data string) {
++	h.Send(data, sseTimeTick)
++}
++
++func (h *Hub) sendError(api, data string) {
++	event := fmt.Sprintf("%s-%s", sseError, api)
++	fmt.Println(time.Now().Format("2006/01/02 15:04:05"), event)
++	h.Send(data, event)
++}
++
++func (h *Hub) sendCreateEvent(api, data string) {
++	event := fmt.Sprintf("%s-%s", sseCreate, api)
++	fmt.Println(time.Now().Format("2006/01/02 15:04:05"), event)
++	h.Send(data, event)
++}
++
++func (h *Hub) sendUpdateEvent(api, data string) {
++	event := fmt.Sprintf("%s-%s", sseUpdate, api)
++	fmt.Println(time.Now().Format("2006/01/02 15:04:05"), event)
++	h.Send(data, event)
++}
++
++func (h *Hub) sendDeleteEvent(api, data string) {
++	event := fmt.Sprintf("%s-%s", sseDelete, api)
++	fmt.Println(time.Now().Format("2006/01/02 15:04:05"), event)
++	h.Send(data, event)
++}
++
++func (h *Hub) sendNotice(api, data string) {
++	event := fmt.Sprintf("%s-%s", sseNotice, api)
++	fmt.Println(time.Now().Format("2006/01/02 15:04:05"), event)
++	h.Send(data, event)
++}
+diff --git a/handler/notice.go b/handler/notice.go
+index 11c0b07..08ff8dc 100644
+--- a/handler/notice.go
++++ b/handler/notice.go
+@@ -41 +41 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
+-						if errors.Is(err, gorm.ErrRecordNotFound) {
++						if errors.Is(err, gorm.ErrRecordNotFound) && question.Support.SupportStatusID != 1 {
+diff --git a/package.json b/package.json
+deleted file mode 100644
+index 8d7055f..0000000
+--- a/package.json
++++ /dev/null
+@@ -1,5 +0,0 @@
+-{
+-  "dependencies": {
+-    "htmx-ext-sse": "^2.2.4"
+-  }
+-}
+\ No newline at end of file
+diff --git a/static/js/hrAppData.js b/static/js/hrAppData.js
+index 4c98210..0b356af 100644
+--- a/static/js/hrAppData.js
++++ b/static/js/hrAppData.js
+@@ -2 +2 @@ import { Question, User, Tag } from '/static/js/model.js';
+-import { SSE_ADD_NOTICE, SSE_ADD_QUESTION, SSE_ADD_TAG, SSE_ADD_USER, SSE_TIME_TICKER } from './sse.js';
++import { SSE_KEY } from './sse.js';
+@@ -5,0 +6 @@ document.addEventListener('alpine:init', () => {
++        timeNow: new Date(),
+@@ -15 +16 @@ document.addEventListener('alpine:init', () => {
+-        unreadNotices: 1,
++        unreadNotices: 0,
+@@ -113 +114 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener(SSE_TIME_TICKER, (event) => {
++            document.addEventListener(SSE_KEY.system.timeTick, (event) => {
+@@ -117 +118,2 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener(SSE_ADD_NOTICE, (event) => {
++            // TODO; ADD,UPDATE,DELETEのSSEイベントを受け取りリアルタイム反映を行う。初期表示のGETについてはRESTAPIとして受け取るため不要。
++            document.addEventListener(SSE_KEY.notice(), (event) => {
+@@ -121,0 +124 @@ document.addEventListener('alpine:init', () => {
++                    if (notice.typeId !== 1) this.unreadNotices++;
+@@ -123,0 +127 @@ document.addEventListener('alpine:init', () => {
++                    // TODO; SSE_UPDATE_NOTICEへと変更
+@@ -132 +135,0 @@ document.addEventListener('alpine:init', () => {
+-                if (notice.noticeType?.name === 'ALERT') this.unreadNotices += 1;
+@@ -135 +138 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener(SSE_ADD_QUESTION, (event) => {
++            document.addEventListener(SSE_KEY.question(), (event) => {
+@@ -140,0 +144 @@ document.addEventListener('alpine:init', () => {
++                    // TODO; SSE_UPDATE_QUESTIONへと変更
+@@ -148,0 +153 @@ document.addEventListener('alpine:init', () => {
++                // 更新対象が現在開いている質問だった場合の処理
+@@ -150 +155 @@ document.addEventListener('alpine:init', () => {
+-                    if (!_.isEqual(this.activeQuestion, question)) {
++                    if (!_.isEqual(this.activeQuestion, question)) { // お互いに差分があった場合、リモートのものをローカルへ差分コピー
+@@ -163,2 +168,2 @@ document.addEventListener('alpine:init', () => {
+-                        );
+-                        this.originalQuestion = _.cloneDeep(question);
++                        )
++                        this.originalQuestion = _.cloneDeep(question)
+@@ -170 +175 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener(SSE_ADD_USER, (event) => {
++            document.addEventListener(SSE_KEY.user(), (event) => {
+@@ -176,0 +182 @@ document.addEventListener('alpine:init', () => {
++                    // TODO; SSE_UPDATE_USERへと変更
+@@ -187 +193 @@ document.addEventListener('alpine:init', () => {
+-            document.addEventListener(SSE_ADD_TAG, (event) => {
++            document.addEventListener(SSE_KEY.data.create.tag, (event) => {
+@@ -189 +195 @@ document.addEventListener('alpine:init', () => {
+-                const index = this.tags.findIndex(q => q.id === tag.id);
++                const index = this.tags.findIndex(t => t.id === tag.id);
+@@ -194,0 +201 @@ document.addEventListener('alpine:init', () => {
++                    // TODO; SSE_UPDATE_TAGへと変更
+@@ -204,0 +212,10 @@ document.addEventListener('alpine:init', () => {
++
++            this.getTags()
++        },
++
++        currentTimeFormatted(dt = new Date()) {
++            return new Intl.DateTimeFormat('ja-JP', {
++                hour: '2-digit',
++                minute: '2-digit',
++                second: '2-digit'
++            }).format(dt);
+@@ -228 +244,0 @@ document.addEventListener('alpine:init', () => {
+-
+@@ -280,0 +297,11 @@ document.addEventListener('alpine:init', () => {
++        openDetailByID(id = 0) {
++            if (id === 0) {
++                window.alert(`invalid support id is ${id}.`)
++            }
++            const index = this.questions.findIndex(q => q.id == id)
++            console.log(index)
++            if (index !== -1) {
++                this.openDetail(this.questions[index])
++            }
++        },
++
+@@ -363,2 +390,2 @@ document.addEventListener('alpine:init', () => {
+-        async registerTag(name, cateogryId) {
+-            const data = Tag.toModel({ title: name, categoryId: cateogryId, usage: 0 });
++        async registerTag(name, cateogryId, categoryText) {
++            const data = Tag.toModel({ title: name, categoryId: cateogryId, usage: 0, category: {categoryName: categoryText}});
+@@ -427,0 +455,14 @@ document.addEventListener('alpine:init', () => {
++        calcRemainingTimeAndString(question) {
++            const [hours, minutes] = this.calcRemainingTime(new Date(question.due), this.timeNow)
++            return `${hours}時間${minutes}分`;
++        },
++
++
++        calcRemainingTime(baseDate, d = new Date()) {
++            const diff = baseDate - d;
++            const hours = Math.floor(diff / (1000 * 60 * 60));
++            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
++
++            return [hours, minutes];
++        },
++
+diff --git a/static/js/model.js b/static/js/model.js
+index eb4f303..1fef66b 100644
+--- a/static/js/model.js
++++ b/static/js/model.js
+@@ -41 +41 @@ export class Role {
+-    this.id       = id;
++    this.id = id;
+@@ -43 +43 @@ export class Role {
+-    this.users    = users;
++    this.users = users;
+@@ -71,2 +71,2 @@ export class SupportStatus {
+-    this.id       = id;
+-    this.title    = title;
++    this.id = id;
++    this.title = title;
+@@ -97,2 +97,2 @@ export class Support {
+-   * @param {number|string} userId
+-   * @param {number|string} supportStatusId
++   * @param {number}        userId
++   * @param {string}        supportStatusId
+@@ -104,2 +104,2 @@ export class Support {
+-    this.id              = id;
+-    this.userId          = userId;
++    this.id = id;
++    this.userId = userId;
+@@ -107,3 +107,3 @@ export class Support {
+-    this.user            = user;
+-    this.supportStatus   = supportStatus;
+-    this.question        = question;
++    this.user = user;
++    this.supportStatus = supportStatus;
++    this.question = question;
+@@ -145,5 +145,5 @@ export class User {
+-    this.id       = id;
+-    this.name     = name;
+-    this.email    = email;
+-    this.roleId   = roleId;
+-    this.role     = role;
++    this.id = id;
++    this.name = name;
++    this.email = email;
++    this.roleId = roleId;
++    this.role = role;
+@@ -151,2 +151,2 @@ export class User {
+-    this.answers  = answers;
+-    this.memos    = memos;
++    this.answers = answers;
++    this.memos = memos;
+@@ -158 +158 @@ export class User {
+-    // const roleIdNum =
++    // const roleIdNum = 
+@@ -192 +192 @@ export class Category {
+-    this.id           = id;
++    this.id = id;
+@@ -194 +194 @@ export class Category {
+-    this.tags         = tags;
++    this.tags = tags;
+@@ -225,3 +225,3 @@ export class Tag {
+-    this.id         = id;
+-    this.title      = title;
+-    this.usage      = usage;
++    this.id = id;
++    this.title = title;
++    this.usage = usage;
+@@ -229,2 +229,2 @@ export class Tag {
+-    this.category   = category;
+-    this.questions  = questions;
++    this.category = category;
++    this.questions = questions;
+@@ -269,3 +269,3 @@ export class Refer {
+-    this.id      = id;
+-    this.title   = title;
+-    this.url     = url;
++    this.id = id;
++    this.title = title;
++    this.url = url;
+@@ -304 +304 @@ export class Memo {
+-    this.id         = id;
++    this.id = id;
+@@ -306,4 +306,4 @@ export class Memo {
+-    this.userId     = userId;
+-    this.content    = content;
+-    this.question   = question;
+-    this.user       = user;
++    this.userId = userId;
++    this.content = content;
++    this.question = question;
++    this.user = user;
+@@ -344,3 +344,3 @@ export class Answer {
+-    this.id         = id;
+-    this.userId     = userId;
+-    this.content    = content;
++    this.id = id;
++    this.userId = userId;
++    this.content = content;
+@@ -348,3 +348,3 @@ export class Answer {
+-    this.createdAt  = new Date(createdAt);
+-    this.user       = user;
+-    this.refers     = refers;
++    this.createdAt = new Date(createdAt);
++    this.user = user;
++    this.refers = refers;
+@@ -385 +385 @@ export class Escalation {
+-    this.id             = id;
++    this.id = id;
+@@ -387,4 +387,4 @@ export class Escalation {
+-    this.toQuestionId   = toQuestionId;
+-    this.escalatedAt    = new Date(escalatedAt);
+-    this.fromQuestion   = fromQuestion;
+-    this.toQuestion     = toQuestion;
++    this.toQuestionId = toQuestionId;
++    this.escalatedAt = new Date(escalatedAt);
++    this.fromQuestion = fromQuestion;
++    this.toQuestion = toQuestion;
+@@ -421,2 +421,2 @@ export class NoticeType {
+-    this.id      = id;
+-    this.name    = name;
++    this.id = id;
++    this.name = name;
+@@ -455,2 +455,2 @@ export class Notice {
+-    this.id         = id;
+-    this.typeId     = typeId;
++    this.id = id;
++    this.typeId = typeId;
+@@ -458 +458 @@ export class Notice {
+-    this.content    = content;
++    this.content = content;
+@@ -461 +461 @@ export class Notice {
+-    this.question   = question;
++    this.question = question;
+@@ -488,13 +487,0 @@ export class Question {
+-<<<<<<< HEAD
+-<<<<<<< Updated upstream
+- * @param {number}         id
+- * @param {number|null}    originQuestionId
+- * @param {number|null}    supportId
+- * @param {string}         title
+- * @param {string}         content
+- * @param {string|null}    due              ISO8601 文字列 or null
+- * @param {string}         createdAt        ISO8601 文字列
+-=======
+-   * @param {number}         id
+-   * @param {number|null}    messageId
+-=======
+@@ -502 +488,0 @@ export class Question {
+->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
+@@ -511,4 +496,0 @@ export class Question {
+-<<<<<<< HEAD
+->>>>>>> Stashed changes
+-=======
+->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
+@@ -531 +513 @@ export class Question {
+-    this.id               = id;
++    this.id = id;
+@@ -533,16 +515,16 @@ export class Question {
+-    this.answerId         = answerId;
+-    this.supportId        = supportId;
+-    this.title            = title;
+-    this.content          = content;
+-    this.deleted          = deleted;
+-    this.due              = due ? new Date(due) : null;
+-    this.createdAt        = new Date(createdAt);
+-    this.originQuestion   = originQuestion;
+-    this.subQuestions     = subQuestions;
+-    this.support          = support;
+-    this.answer           = answer;
+-    this.memos            = memos;
+-    this.tags             = tags;
+-    this.notices          = notices;
+-    this.escalationsFrom  = escalationsFrom;
+-    this.escalationsTo    = escalationsTo;
++    this.answerId = answerId;
++    this.supportId = supportId;
++    this.title = title;
++    this.content = content;
++    this.deleted = deleted;
++    this.due = due ? new Date(due) : null;
++    this.createdAt = new Date(createdAt);
++    this.originQuestion = originQuestion;
++    this.subQuestions = subQuestions;
++    this.support = support;
++    this.answer = answer;
++    this.memos = memos;
++    this.tags = tags;
++    this.notices = notices;
++    this.escalationsFrom = escalationsFrom;
++    this.escalationsTo = escalationsTo;
+@@ -597 +579 @@ export class ReferManager {
+-    this.id       = id;
++    this.id = id;
+@@ -599,3 +581,3 @@ export class ReferManager {
+-    this.referId  = referId;
+-    this.answer   = answer;
+-    this.refer    = refer;
++    this.referId = referId;
++    this.answer = answer;
++    this.refer = refer;
+@@ -633,2 +615,2 @@ export class TagManager {
+-    this.id         = id;
+-    this.tagId      = tagId;
++    this.id = id;
++    this.tagId = tagId;
+@@ -636,2 +618,2 @@ export class TagManager {
+-    this.tag        = tag;
+-    this.question   = question;
++    this.tag = tag;
++    this.question = question;
+diff --git a/static/js/sse.js b/static/js/sse.js
+index 1887376..6205a35 100644
+--- a/static/js/sse.js
++++ b/static/js/sse.js
+@@ -3,5 +3,52 @@ import { Question, User, Tag, Notice } from '/static/js/model.js';
+-export const SSE_TIME_TICKER = "time-ticker";
+-export const SSE_ADD_NOTICE = "sse-notice";
+-export const SSE_ADD_USER = "sse-user";
+-export const SSE_ADD_QUESTION = "sse-question";
+-export const SSE_ADD_TAG = "sse-tag";
++export const SSE_KEY = {
++    system: {
++        timeTick: "time-tick",
++    },
++    data: {
++        create: {
++            user: "create-user",
++            question: "create-question",
++            tag: "create-tag",
++            notice: "create-notice",
++        },
++        update: {
++            user: "update-user",
++            question: "update-question",
++            tag: "update-tag",
++            notice: "update-notice",
++        },
++        delete: {
++            user: "delete-user",
++            question: "delete-question",
++            tag: "delete-tag",
++            notice: "delete-notice",
++        },
++        /** 取得の定期実行はしない想定。全体取得は初回読み込み時の１度で、他はSSE取得による部分更新で画面描画します。 */
++        get: {
++            user: "get-user",
++            question: "get-question",
++            tag: "get-tag",
++            notice: "get-notice",
++        }
++    },
++    compose(...keys) {
++        if (keys.length == 1) {
++            return keys[0]
++        } else if (keys.length == 0) {
++            return undefined
++        }
++        return keys.join(",")
++    },
++    user(includeDelete = false) {
++        return this.compose(this.data.create.user, this.data.update.user, this.data.get.user, includeDelete ? this.data.delete.user : "").split(",")
++    },
++    question(includeDelete = false) {
++        return this.compose(this.data.create.question, this.data.update.question, this.data.get.question, includeDelete ? this.data.delete.question : "").split(",")
++    },
++    tag(includeDelete = false) {
++        return this.compose(this.data.create.tag, this.data.update.tag, this.data.get.tag, includeDelete ? this.data.delete.tag : "").split(",")
++    },
++    notice(includeDelete = false) {
++        return this.compose(this.data.create.notice, this.data.update.notice, this.data.get.notice, includeDelete ? this.data.delete.notice : "").split(",")
++    },
++}
+@@ -24,2 +71,2 @@ document.addEventListener('DOMContentLoaded', () => {
+-    es.addEventListener('time-tick', (event) => {
+-        document.dispatchEvent(new CustomEvent(SSE_TIME_TICKER, {
++    es.addEventListener(SSE_KEY.system.timeTick, (event) => {
++        document.dispatchEvent(new CustomEvent(SSE_KEY.system.timeTick, {
+@@ -30,9 +77,11 @@ document.addEventListener('DOMContentLoaded', () => {
+-    es.addEventListener('notice', (event) => {
+-        try {
+-            const json = JSON.parse(event.data);
+-            const notice = Notice.fromJSON(json);
+-            document.dispatchEvent(new CustomEvent(SSE_ADD_NOTICE, {
+-                detail: notice,
+-            }));
+-        } catch (_) { }
+-    });
++    SSE_KEY.notice().forEach(eventName => {
++        es.addEventListener(eventName, (event) => {
++            try {
++                const json = JSON.parse(event.data);
++                const notice = Notice.fromJSON(json);
++                document.dispatchEvent(new CustomEvent(SSE_KEY.data.create.notice, {
++                    detail: notice,
++                }));
++            } catch (_) { }
++        });
++    })
+@@ -40,8 +89,10 @@ document.addEventListener('DOMContentLoaded', () => {
+-    es.addEventListener('question', (event) => {
+-        try {
+-            const question = Question.fromJSON(JSON.parse(event.data));
+-            document.dispatchEvent(new CustomEvent(SSE_ADD_QUESTION, {
+-                detail: question,
+-            }));
+-        } catch (_) { }
+-    });
++    SSE_KEY.question().forEach(eventName => {
++        es.addEventListener(eventName, (event) => {
++            try {
++                const question = Question.fromJSON(JSON.parse(event.data));
++                document.dispatchEvent(new CustomEvent(SSE_KEY.data.create.question, {
++                    detail: question,
++                }));
++            } catch (_) { }
++        });
++    })
+@@ -49,8 +100,10 @@ document.addEventListener('DOMContentLoaded', () => {
+-    es.addEventListener('user', (event) => {
+-        try {
+-            const user = User.fromJSON(JSON.parse(event.data));
+-            document.dispatchEvent(new CustomEvent(SSE_ADD_USER, {
+-                detail: user,
+-            }));
+-        } catch (fail) { console.log(fail) }
+-    });
++    SSE_KEY.user().forEach(eventName => {
++        es.addEventListener(eventName, (event) => {
++            try {
++                const user = User.fromJSON(JSON.parse(event.data));
++                document.dispatchEvent(new CustomEvent(SSE_KEY.data.create.user, {
++                    detail: user,
++                }));
++            } catch (fail) { console.log(fail) }
++        });
++    })
+@@ -58,8 +111,11 @@ document.addEventListener('DOMContentLoaded', () => {
+-    es.addEventListener('tag', (event) => {
+-        try {
+-            const tag = Tag.fromJSON(JSON.parse(event.data));
+-            document.dispatchEvent(new CustomEvent(SSE_ADD_TAG, {
+-                detail: tag,
+-            }));
+-        } catch (fail) { console.log(fail) }
+-    });
++    SSE_KEY.tag().forEach(eventName => {
++        es.addEventListener(eventName, (event) => {
++            try {
++                const tag = Tag.fromJSON(JSON.parse(event.data));
++                console.log(eventName, tag)
++                document.dispatchEvent(new CustomEvent(SSE_KEY.data.create.tag, {
++                    detail: tag,
++                }));
++            } catch (fail) { console.log(fail) }
++        });
++    })
+diff --git a/templates/components/login_head.html b/templates/components/login_head.html
+index 352793f..b2fad6e 100644
+--- a/templates/components/login_head.html
++++ b/templates/components/login_head.html
+@@ -23,0 +24,6 @@
++            init() {
++                if (typeof lucide !== 'undefined') {
++                    lucide.createIcons();
++                }
++            },
++
+@@ -51 +57 @@
+-{{end}}
++{{end}}
+\ No newline at end of file
+diff --git a/templates/components/mock5_modal_tag.html b/templates/components/mock5_modal_tag.html
+index 1c0b695..014287e 100644
+--- a/templates/components/mock5_modal_tag.html
++++ b/templates/components/mock5_modal_tag.html
+@@ -3 +3,3 @@
+-    x-data="{ title: '', categoryId: '0' }">
++    x-data="{ title: '', categoryId: '0', categoryText: '', selectOptionTitle(event) {
++        this.categoryText = event.target.options[event.target.selectedIndex].text;
++    }}">
+@@ -6 +8 @@
+-        <div class="space-y-4">
++        <div class="space-y-4" x-data="">
+@@ -9 +11 @@
+-            <select class="select select-bordered w-full text-sm shadow-inner" x-model="categoryId">
++            <select class="select select-bordered w-full text-sm shadow-inner" x-model="categoryId" @change="selectOptionTitle($event)">
+@@ -19 +21 @@
+-                @click="showTagModal = false; registerTag(title, categoryId); alert('タグを登録しました'); title=''; categoryId='0'">タグを追加</button>
++                @click="showTagModal = false; registerTag(title, categoryId, categoryText); alert('タグを登録しました'); title=''; categoryId='0'">タグを追加</button>
+diff --git a/templates/components/mock5_view_detail.html b/templates/components/mock5_view_detail.html
+index d40c538..15ab701 100644
+--- a/templates/components/mock5_view_detail.html
++++ b/templates/components/mock5_view_detail.html
+@@ -63 +62,0 @@
+-
+@@ -127 +126 @@
+-                        <div>
++                        <div class="col-span-4">
+@@ -132 +131 @@
+-                        <div>
++                        <div class="col-span-full">
+diff --git a/templates/components/mock5_view_home.html b/templates/components/mock5_view_home.html
+index 18b3dd3..b9d089c 100644
+--- a/templates/components/mock5_view_home.html
++++ b/templates/components/mock5_view_home.html
+@@ -113 +113 @@
+-                            :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
++                            :class="q.support && q.support.supportStatus && q.support.supportStatus.title && statusColorMap[q.support.supportStatus.title]" x-text="q.support && q.support.supportStatus && q.support.supportStatus.title"></span>
+@@ -125 +125 @@
+-                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                            :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+@@ -127 +127 @@
+-                            <span x-text="q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                            <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+@@ -143 +143 @@
+-                        :class="statusColorMap[q.support.supportStatus.title]" x-text="q.support.supportStatus.title"></span>
++                        :class="q.support && q.support.supportStatus && q.support.supportStatus.title && statusColorMap[q.support.supportStatus.title]" x-text="q.support && q.support.supportStatus && q.support.supportStatus.title && q.support.supportStatus.title"></span>
+@@ -155 +155 @@
+-                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                        :class="q.daysLeft <= 1 && q.support && q.support.supportStatus && q.support.supportStatus.title !== '完了' ? 'text-red-500' : 'text-gray-400'">
+@@ -157 +157 @@
+-                        <span x-text="q.support && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                        <span x-text="q.support && q.support.supportStatus && q.support.supportStatus.title === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
+diff --git a/templates/components/mock5_view_notice.html b/templates/components/mock5_view_notice.html
+index c0d0a53..82bee3a 100644
+--- a/templates/components/mock5_view_notice.html
++++ b/templates/components/mock5_view_notice.html
+@@ -5,4 +5,10 @@
+-            @click="setView('home')">
+-            <div class="w-10 h-10 rounded-lg flex items-center justify-center shrink-0"
+-                :class="[2, 3].includes(n.typeId) ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
+-                <i :data-lucide="[2, 3].includes(n.typeId) ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++            @click="n.question && openDetailByID(n.questionId)">
++            <div class="flex items-center" :class="(n.question && n.question.support) ? 'flex-col' : ''">
++                <div class="w-10 h-10 rounded-lg flex items-center justify-center shrink-0"
++                    :class="[2, 3].includes(n.typeId) ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
++                    <i :data-lucide="[2, 3].includes(n.typeId) ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++                </div>
++                <div>
++                    <span class="px-2 py-0.5 rounded text-[10px] font-bold shrink-0 w-16 text-center"
++                    :class="n.question && n.question.support && n.question.support.supportStatus && n.question.support.supportStatus.title && statusColorMap[n.question.support.supportStatus.title]" x-text="n.question && n.question.support && n.question.support.supportStatus && n.question.support.supportStatus.title && n.question.support.supportStatus.title"></span>
++                </div>
+@@ -12,2 +18,4 @@
+-                    <h3 class="font-bold text-sm text-gray-800" x-show="[1, 2].includes(n.typeId)" x-text="n.content"></h3>
+-                    <h3 class="font-bold text-sm text-gray-800" x-show="n.typeId === 3" x-text="n.question && (n.question.title ? `回答期限接近中: ${n.question.title}` : `回答期限接近中: 質問ID#${n.question.id}`)"></h3>
++                    <h2 class="font-bold text-sm text-gray-800 align-middle" x-show="[1, 2].includes(n.typeId)" x-text="n.content"></h2>
++                    <h3 class="font-bold text-sm text-gray-800" x-show="n.typeId === 3"
++                        x-text="n.question && (n.question.title ? `回答期限接近中: ${n.question.title}` : `回答期限接近中: 質問ID#${n.question.id}`)">
++                    </h3>
+@@ -15 +23,2 @@
+-                        <span class="text-[10px] font-bold text-gray-400" x-text="n.question && n.question.due && `残り時間: ${calcRemainingTimeAndString(n.question)}`"></span>
++                        <span class="text-[10px] font-bold text-gray-400"
++                            x-text="n.question && n.question.due && `残り時間: ${calcRemainingTimeAndString(n.question)}`"></span>
+@@ -17,2 +26,3 @@
+-                            <template x-for="tag in (n.question.tags || [])" :key="tag.id">
+-                                <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase"
++                            <template x-for="tag in (n.question && n.question.tags || [])" :key="tag.id">
++                                <span
++                                    class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase"
+@@ -24 +34,2 @@
+-                <p class="text-xs text-gray-500 line-clamp-2" x-show="n.question" x-text="n.question && n.question.content"></p>
++                <p class="text-xs text-gray-500 line-clamp-2" x-show="n.question"
++                    x-text="n.question && n.question.content"></p>
+@@ -29 +40 @@
+-{{end}}
++{{end}}
+\ No newline at end of file
+diff --git a/templates/sample/index.html b/templates/sample/index.html
+index dea253f..0f7e8d2 100644
+--- a/templates/sample/index.html
++++ b/templates/sample/index.html
+@@ -1,455 +0,0 @@
+-<<<<<<< Updated upstream
+-<!DOCTYPE html>
+-<html lang="ja" data-theme="cupcake">
+-
+-<head>
+-    <meta charset="UTF-8">
+-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+-    <title>{{.Title}}</title>
+-    <link href="/static/css/output.css" rel="stylesheet">
+-
+-    <!--
+-        module スクリプトは defer 扱いのため、後続の defer Alpine より先に実行される。
+-        alpine:init リスナーを登録 → Alpine が初期化時に発火 → Alpine.data() が登録される。
+-    -->
+-    <script type="module">
+-        import { Question } from '/static/js/model.js';
+-
+-        document.addEventListener('alpine:init', () => {
+-            Alpine.data('questionBoard', () => ({
+-                questions: [],
+-                questionIDMap: new Map([]),
+-                filter: '',
+-                connected: false,
+-
+-                form: {content: '', due: '' },
+-                submitting: false,
+-                formError: '',
+-
+-                currentView: 'home',
+-
+-                questionDetail: {},
+-
+-                async getQuestionDetail(id) {
+-                    const res = await fetch(`/api/v1/question/${id}`);
+-                    const body = await res.json();
+-                    this.questionDetail = Question.fromJSON(body);
+-                },
+-
+-                async deleteQuestion(id) {
+-                    const res = await fetch(`/api/v1/question/${id}`, {
+-                        method: "DELETE"
+-                    }).then(v => {
+-                        if (v.status == 200) {
+-                            this.questions = this.questions.filter(q => q.id != id);
+-                            this.closeDetail()
+-                        } else {
+-                            this.formError = '質問データの削除に失敗しました。'
+-                        }
+-                    });
+-                },
+-
+-                openDetail(id) {
+-                    this.getQuestionDetail(id)
+-                    document.getElementById('question-detail-modal').showModal();
+-                },
+-                closeDetail() {
+-                    document.getElementById('question-detail-modal').close();
+-                },
+-
+-                openForm() {
+-                    this.form = { title: '', content: '', due: '' };
+-                    this.formError = '';
+-                    document.getElementById('question-form-modal').showModal();
+-                },
+-                closeForm() {
+-                    document.getElementById('question-form-modal').close();
+-                },
+-                async submitForm() {
+-                    if (!this.form.content.trim()) {
+-                        this.formError = '内容を入力してください';
+-                        return;
+-                    }
+-                    this.submitting = true;
+-                    this.formError = '';
+-                    try {
+-                        const body = { content: this.form.content.trim(), title: this.form.title.trim() };
+-                        if (this.form.due) body.due = `${this.form.due}:00+09:00`;
+-                        const res = await fetch('/api/v1/question', {
+-                            method: 'POST',
+-                            headers: { 'Content-Type': 'application/json' },
+-                            body: JSON.stringify(body),
+-                        });
+-                        if (!res.ok) throw new Error(`HTTP ${res.status}`);
+-                        this.closeForm();
+-                    } catch (err) {
+-                        this.formError = `送信に失敗しました (${err.message})`;
+-                    } finally {
+-                        this.submitting = false;
+-                    }
+-                },
+-
+-                get filteredQuestions() {
+-                    const f = this.filter.toLowerCase().trim();
+-                    if (!f) return this.questions;
+-                    return this.questions.filter(q =>
+-                        q.title.toLowerCase().includes(f) ||
+-                        (q.tags ?? []).some(t => t.title.toLowerCase().includes(f))
+-                    );
+-                },
+-
+-                isIncludeMap(id) {
+-                    return this.questionIDMap[id] == undefined ? false : true;
+-                },
+-
+-                init() {
+-                    const es = new EventSource('/sse');
+-                    es.onopen = () => { this.connected = true; };
+-                    es.onerror = () => { this.connected = false; };
+-                    es.addEventListener('question', (e) => {
+-                        try {
+-                            const q = Question.fromJSON(JSON.parse(e.data));
+-                            if (!this.isIncludeMap(q.id)) {
+-                                this.questionIDMap[q.id] = true;
+-                                this.questions.unshift(q);
+-                            }
+-                        } catch (err) {
+-                            console.error('Failed to parse question:', err);
+-                        }
+-                    });
+-                },
+-
+-                formatDate(d) {
+-                    if (!d) return null;
+-                    return new Intl.DateTimeFormat('ja-JP', {
+-                        year: 'numeric', month: '2-digit', day: '2-digit',
+-                        hour: '2-digit', minute: '2-digit',
+-                    }).format(new Date(d));
+-                },
+-            }));
+-        });
+-    </script>
+-
+-    <script defer src="/static/vendor/htmx.min.js"></script>
+-    <script defer src="/static/vendor/sse.min.js"></script>
+-    <script defer src="/static/vendor/alpine.min.js"></script>
+-</head>
+-
+-<body class="min-h-screen bg-base-200">
+-
+-    <!-- ========== ヘッダー ========== -->
+-    <header class="sticky top-0 z-10 bg-base-100 shadow-sm">
+-        <div class="max-w-6xl mx-auto px-6 py-3 flex items-center justify-between gap-4">
+-
+-            <h1 class="text-xl font-bold text-primary tracking-tight">Question Board</h1>
+-
+-            <div class="flex items-center gap-3">
+-                <!-- 接続状態インジケーター -->
+-                <div x-data x-cloak class="hidden"><!-- Alpine 読み込み前の FOUC 防止 --></div>
+-
+-                <!-- サーバー時刻 (htmx SSE) -->
+-                <div hx-ext="sse" sse-connect="/sse">
+-                    <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
+-                        --:--:--
+-                    </span>
+-                </div>
+-            </div>
+-
+-        </div>
+-    </header>
+-
+-    <!-- ========== メインコンテンツ ========== -->
+-    <main class="max-w-6xl mx-auto px-6 py-8" x-data="questionBoard()" x-init="init()">
+-        <!-- ---- フィルターバー ---- -->
+-        <div class="mb-6 flex flex-col sm:flex-row items-stretch sm:items-center gap-3 flex-wrap">
+-
+-            <!-- 検索ボックス -->
+-            <label class="flex-1 input input-bordered flex items-center gap-2">
+-                <!-- search icon -->
+-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 opacity-50" fill="none"
+-                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                    <path stroke-linecap="round" stroke-linejoin="round"
+-                        d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
+-                </svg>
+-                <input type="text" x-model="filter" placeholder="タイトル・タグで絞り込み..."
+-                    class="grow outline-none bg-transparent text-sm" />
+-                <!-- クリアボタン -->
+-                <button x-show="filter !== ''" @click="filter = ''" class="btn btn-ghost btn-xs btn-circle"
+-                    aria-label="クリア">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="2.5">
+-                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
+-                    </svg>
+-                </button>
+-            </label>
+-
+-            <!-- 質問追加ボタン -->
+-            <button @click="openForm()" class="btn btn-primary btn-sm gap-1.5 shrink-0">
+-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
+-                    stroke="currentColor" stroke-width="2.5">
+-                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
+-                </svg>
+-                質問を追加
+-            </button>
+-
+-            <!-- 件数バッジ + 接続状態 -->
+-            <div class="flex items-center gap-2 shrink-0">
+-                <span class="badge badge-primary badge-outline font-mono"
+-                    x-text="`${filteredQuestions.length} 件`"></span>
+-
+-                <!-- 接続インジケーター -->
+-                <div class="flex items-center gap-1 text-xs" :class="connected ? 'text-success' : 'text-error'">
+-                    <span class="inline-block w-2 h-2 rounded-full animate-pulse"
+-                        :class="connected ? 'bg-success' : 'bg-error'"></span>
+-                    <span x-text="connected ? 'LIVE' : 'disconnected'"></span>
+-                </div>
+-            </div>
+-
+-        </div>
+-
+-        <!-- ---- カードグリッド ---- -->
+-        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
+-
+-            <template x-for="q in filteredQuestions" :key="q.id">
+-                <div x-transition:enter="transition ease-out duration-300"
+-                    x-transition:enter-start="opacity-0 scale-95 translate-y-2"
+-                    x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                    x-transition:leave="transition ease-in duration-200"
+-                    x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                    x-transition:leave-end="opacity-0 scale-95 translate-y-2"
+-                    class="card bg-base-100 shadow hover:shadow-lg transition-shadow duration-200 cursor-pointer"
+-                    @click="openDetail(q.id)">
+-                    <div class="card-body gap-3 p-5">
+-
+-                        <!-- タイトル + ID -->
+-                        <div class="flex items-start justify-between gap-2">
+-                            <h2 class="card-title text-sm leading-snug" x-text="q.title"></h2>
+-                            <span class="badge badge-ghost badge-sm shrink-0 font-mono" x-text="`#${q.id}`"></span>
+-                        </div>
+-
+-                        <div class="flex items-start gap-2">
+-                            <span x-text="q.content"></span>
+-                        </div>
+-
+-                        <!-- 期限 -->
+-                        <template x-if="q.due">
+-                            <div class="flex items-center gap-1 text-xs text-warning font-medium">
+-                                <!-- clock icon -->
+-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0" fill="none"
+-                                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                                    <path stroke-linecap="round" stroke-linejoin="round"
+-                                        d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />
+-                                </svg>
+-                                <span x-text="`期限: ${formatDate(q.due)}`"></span>
+-                            </div>
+-                        </template>
+-
+-                        <!-- サポートステータス -->
+-                        <template x-if="q.support && q.support.supportStatus">
+-                            <div class="flex items-center gap-1 text-xs">
+-                                <!-- tag icon -->
+-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0 opacity-60"
+-                                    fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
+-                                    <path stroke-linecap="round" stroke-linejoin="round"
+-                                        d="M7 7h.01M7 3h14a1 1 0 0 1 1 1v9.586a1 1 0 0 1-.293.707l-7 7a1 1 0 0 1-1.414 0l-9.586-9.586A1 1 0 0 1 3 11V4a1 1 0 0 1 1-1h3z" />
+-                                </svg>
+-                                <span class="badge badge-secondary badge-sm"
+-                                    x-text="q.support.supportStatus.title"></span>
+-                            </div>
+-                        </template>
+-
+-                        <!-- タグ一覧 -->
+-                        <template x-if="q.tags && q.tags.length > 0">
+-                            <div class="flex flex-wrap gap-1">
+-                                <template x-for="tag in q.tags" :key="tag.id">
+-                                    <span class="badge badge-outline badge-xs" x-text="tag.title"></span>
+-                                </template>
+-                            </div>
+-                        </template>
+-
+-                        <!-- フッター: 作成日時 -->
+-                        <div class="mt-auto pt-2 border-t border-base-200 flex justify-end">
+-                            <span class="text-xs text-base-content/40 font-mono"
+-                                x-text="formatDate(q.createdAt)"></span>
+-                        </div>
+-
+-                    </div>
+-                </div>
+-            </template>
+-
+-        </div>
+-
+-        <!-- ---- 空状態 ---- -->
+-        <div x-show="filteredQuestions.length === 0" x-transition:enter="transition ease-out duration-300"
+-            x-transition:enter-start="opacity-0 translate-y-2" x-transition:enter-end="opacity-100 translate-y-0"
+-            x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100"
+-            x-transition:leave-end="opacity-0"
+-            class="flex flex-col items-center justify-center py-24 gap-4 text-base-content/40">
+-
+-            <!-- フィルター結果なし -->
+-            <template x-if="filter !== ''">
+-                <div class="flex flex-col items-center gap-2">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-30" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="1.5">
+-                        <path stroke-linecap="round" stroke-linejoin="round"
+-                            d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
+-                    </svg>
+-                    <p class="text-sm">「<span class="font-semibold" x-text="filter"></span>」に一致する Question が見つかりません</p>
+-                    <button @click="filter = ''" class="btn btn-ghost btn-xs mt-1">
+-                        フィルターをクリア
+-                    </button>
+-                </div>
+-            </template>
+-
+-            <!-- SSE 待機中 -->
+-            <template x-if="filter === '' && questions.length === 0">
+-                <div class="flex flex-col items-center gap-3">
+-                    <span class="loading loading-ring loading-lg text-primary opacity-50"></span>
+-                    <p class="text-sm">SSE から Question を待機中...</p>
+-                </div>
+-            </template>
+-
+-        </div>
+-
+-        <!-- ---- 質問入力モーダル ---- -->
+-        <dialog id="question-form-modal" class="modal modal-bottom sm:modal-middle">
+-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
+-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
+-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave="transition ease-in duration-150"
+-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
+-
+-                <!-- ヘッダー -->
+-                <div class="flex items-center justify-between mb-5">
+-                    <h3 class="font-bold text-lg">質問を追加</h3>
+-                    <button @click="closeForm()" class="btn btn-ghost btn-sm btn-circle" aria-label="閉じる">
+-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
+-                            stroke="currentColor" stroke-width="2.5">
+-                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
+-                        </svg>
+-                    </button>
+-                </div>
+-
+-                <!-- フォーム -->
+-                <form @submit.prevent="submitForm()" class="flex flex-col gap-4" novalidate>
+-                    <!-- タイトル -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">
+-                                タイトル
+-                                <span class="text-error ml-0.5">*</span>
+-                            </span>
+-                        </div>
+-                        <textarea x-model="form.title" placeholder="質問のタイトルを入力..." rows="3"
+-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
+-                            :class="formError && !form.title.trim() ? 'textarea-error' : ''"
+-                            :disabled="submitting"></textarea>
+-                    </label>
+-
+-                    <!-- 内容 -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">
+-                                内容
+-                                <span class="text-error ml-0.5">*</span>
+-                            </span>
+-                        </div>
+-                        <textarea x-model="form.content" placeholder="質問の内容を入力..." rows="3"
+-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
+-                            :class="formError && !form.content.trim() ? 'textarea-error' : ''"
+-                            :disabled="submitting"></textarea>
+-                    </label>
+-
+-                    <!-- 期日 -->
+-                    <label class="form-control">
+-                        <div class="label pb-1">
+-                            <span class="label-text font-medium">期日</span>
+-                            <span class="label-text-alt text-base-content/40">任意</span>
+-                        </div>
+-                        <input type="datetime-local" x-model="form.due"
+-                            class="input input-bordered w-full focus:outline-none" :disabled="submitting" />
+-                    </label>
+-
+-                    <!-- エラーメッセージ -->
+-                    <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
+-                        x-transition:enter-start="opacity-0 -translate-y-1"
+-                        x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
+-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
+-                            stroke="currentColor" stroke-width="2">
+-                            <path stroke-linecap="round" stroke-linejoin="round"
+-                                d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
+-                        </svg>
+-                        <span x-text="formError"></span>
+-                    </div>
+-
+-                    <!-- アクション -->
+-                    <div class="modal-action mt-1">
+-                        <button type="button" @click="closeForm()" class="btn btn-ghost" :disabled="submitting">
+-                            キャンセル
+-                        </button>
+-                        <button type="submit" class="btn btn-primary gap-2" :disabled="submitting">
+-                            <span x-show="submitting" class="loading loading-spinner loading-xs"></span>
+-                            <span x-text="submitting ? '送信中...' : '送信'"></span>
+-                        </button>
+-                    </div>
+-
+-                </form>
+-            </div>
+-
+-            <!-- バックドロップクリックで閉じる -->
+-            <form method="dialog" class="modal-backdrop">
+-                <button @click="closeForm()">close</button>
+-            </form>
+-        </dialog>
+-
+-        <!-- ---- 詳細モーダル ---- -->
+-        <dialog id="question-detail-modal" class="modal modal-bottom sm:modal-middle">
+-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
+-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
+-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave="transition ease-in duration-150"
+-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
+-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
+-
+-                <div class="flex items-center justify-between mb-5">
+-                    <span class="card-title" x-text="questionDetail.title"></span>
+-                </div>
+-                <hr />
+-
+-                <div class="flex items-center justify-between mb-2">
+-                    <span class="card-body" x-text="questionDetail.content"></span>
+-                </div>
+-
+-                <div class="flex items-center justify-between mb-5">
+-                    <textarea name="raw-json" id="raw-json" rows="30" cols="50"
+-                        x-text="JSON.stringify(questionDetail)"></textarea>
+-                </div>
+-
+-                <!-- エラーメッセージ -->
+-                <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
+-                    x-transition:enter-start="opacity-0 -translate-y-1"
+-                    x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
+-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
+-                        stroke="currentColor" stroke-width="2">
+-                        <path stroke-linecap="round" stroke-linejoin="round"
+-                            d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
+-                    </svg>
+-                    <span x-text="formError"></span>
+-                </div>
+-
+-                <div class="flex items-end justify-between mb-5">
+-                    <button class="btn btn-circle" @click="deleteQuestion(questionDetail.id)">削除</button>
+-                </div>
+-            </div>
+-
+-            <!-- バックドロップクリックで閉じる -->
+-            <form method="dialog" class="modal-backdrop">
+-                <button @click="closeDetail()">close</button>
+-            </form>
+-        </dialog>
+-    </main>
+-
+-</body>
+-
+-=======
+@@ -479 +24 @@
+-                form: { content: '', due: '' },
++                form: {content: '', due: '' },
+@@ -488 +33 @@
+-                    const res = await fetch(`/api/question/${id}`);
++                    const res = await fetch(`/api/v1/question/${id}`);
+@@ -494 +39 @@
+-                    const res = await fetch(`/api/question/${id}`, {
++                    const res = await fetch(`/api/v1/question/${id}`, {
+@@ -508 +52,0 @@
+-                    this.drawCommunication(id)
+@@ -533 +77 @@
+-                        const res = await fetch('/api/questions', {
++                        const res = await fetch('/api/v1/question', {
+@@ -909,2 +452,0 @@
+-
+->>>>>>> Stashed changes
+diff --git a/templates/sample/mock4.5.html b/templates/sample/mock4.5.html
+index 0a513e4..7a588d6 100644
+--- a/templates/sample/mock4.5.html
++++ b/templates/sample/mock4.5.html
+@@ -31,0 +32,865 @@
++<html lang="ja" data-theme="light">
++<head>
++    <meta charset="UTF-8">
++    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
++    <title>HR FAQ Management v0.2</title>
++    
++    <!-- 1. Tailwind CSS + DaisyUI (built) -->
++    <link href="/static/css/output.css" rel="stylesheet">
++
++    <!-- 2. Lucide Icons -->
++    <script src="https://unpkg.com/lucide@latest"></script>
++
++    <!-- 3. htmx -->
++    <script src="/static/vendor/htmx.min.js"></script>
++
++    <style>
++        .no-scrollbar::-webkit-scrollbar { display: none; }
++        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
++        [x-cloak] { display: none !important; }
++        .transition-all { transition: all 0.3s ease; }
++        .sidebar-transition { transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
++    </style>
++
++    <script>
++        document.addEventListener('alpine:init', () => {
++            Alpine.data('hrAppData', () => ({
++                isLoggedIn: false,
++                isSidebarOpen: true,
++                currentView: 'home',
++                viewMode: 'card',
++                isManager: true,
++                showAddUserModal: false,
++                unreadNotices: 1,
++                activeQuestion: {},
++                isConnect: false,
++
++                selectedTags: [],
++                availableTags: ['諸手当', '休暇', '規程', '健康診断'],
++                searchQuery: '',
++                statusFilter: 'all',
++                sortOrder: 'date_desc',
++
++                relatedSearchQuery: '',
++
++                titleMap: {
++                    home: '質問一覧',
++                    detail: '質問詳細',
++                    notice: '通知センター',
++                    users: 'ユーザー管理',
++                    categories: 'カテゴリ管理',
++                    settings: 'プロフィール設定'
++                },
++
++                statusColorMap: {
++                    '未対応': 'bg-red-100 text-red-800',
++                    '対応中': 'bg-blue-100 text-blue-800',
++                    '完了': 'bg-gray-100 text-gray-600'
++                },
++
++                questions: [
++                    { id: 1002, sender: '鈴木 一郎', department: 'ここに部署名', title: '通勤手当の経路変更について', content: '引越しに伴い、通勤経路が変更になります。申請手順と必要な書類を教えてください。', status: '未対応', tags: ['諸手当'], daysLeft: 1, date: '2026-04-08 09:30', dueDate: '2026-04-10', relatedQuestions: [] },
++                    { id: 1003, sender: '田中 花子', department: 'ここに部署名', title: '育児休業の延長申請', content: '現在取得中の育休を半年間延長したいと考えています。手続きの流れを教えてください。', status: '対応中', tags: ['休暇'], daysLeft: 5, date: '2026-04-04 14:00', dueDate: '2026-04-14', relatedQuestions: [] },
++                    { id: 1004, sender: '佐藤 次郎', department: 'ここに部署名', title: '健康診断の受診日変更', content: '指定された健康診断の日程ですが、出張と重なってしまいました。', status: '完了', tags: ['健康診断'], daysLeft: 0, date: '2026-04-01 11:15', dueDate: '2026-04-09', relatedQuestions: [] },
++                    { id: 1005, sender: '高橋 三郎', department: 'ここに部署名', title: '慶弔休暇の適用範囲', content: '配偶者の祖父母が亡くなった場合、忌引休暇の対象になりますでしょうか？', status: '未対応', tags: ['規程'], daysLeft: 2, date: '2026-04-07 16:45', dueDate: '2026-04-11', relatedQuestions: [] }
++                ],
++
++                notices: [
++                    { id: 1, type: 'alert', title: '期日接近: 質問 #1002', time: '10分前', content: '「通勤手当の経路変更」の期日が明日です。早急な対応をお願いします。' },
++                    { id: 2, type: 'info', title: 'システムアップデート完了', time: '1時間前', content: 'FAQ検索エンジンの精度を向上させるアップデートを適用しました。' }
++                ],
++
++                users: [
++                    { name: '自分', email: 'jinji.taro@example.com', role: 'Manager' },
++                    { name: '鈴木 一郎', email: 'suzuki@example.com', role: 'Creator' },
++                    { name: '田中 花子', email: 'tanaka@example.com', role: 'Creator' }
++                ],
++
++                get filteredQuestions() {
++                    let result = this.questions.filter(q => {
++                        const matchesTag = this.selectedTags.length === 0 || q.tags.some(tag => this.selectedTags.includes(tag));
++                        const matchesSearch = q.title.includes(this.searchQuery) || q.content.includes(this.searchQuery) || q.sender.includes(this.searchQuery);
++                        let matchesStatus = true;
++                        if (this.statusFilter === 'unanswered') matchesStatus = q.status === '未対応';
++                        else if (this.statusFilter === 'progress') matchesStatus = q.status === '対応中';
++                        else if (this.statusFilter === 'open') matchesStatus = (q.status === '未対応' || q.status === '対応中');
++                        else if (this.statusFilter === 'completed') matchesStatus = q.status === '完了';
++                        return matchesTag && matchesSearch && matchesStatus;
++                    });
++
++                    result.sort((a, b) => {
++                        if (this.sortOrder === 'date_desc') {
++                            return new Date(b.date) - new Date(a.date);
++                        } else if (this.sortOrder === 'date_asc') {
++                            return new Date(a.date) - new Date(b.date);
++                        } else if (this.sortOrder === 'dueDate_asc') {
++                            return new Date(a.dueDate) - new Date(b.dueDate);
++                        } else if (this.sortOrder === 'dueDate_desc') {
++                            return new Date(b.dueDate) - new Date(a.dueDate);
++                        }
++                        return 0;
++                    });
++
++                    return result;
++                },
++
++                get availableRelatedQuestions() {
++                    if (!this.activeQuestion || !this.activeQuestion.id) return [];
++                    let qs = this.questions.filter(q => q.id !== this.activeQuestion.id);
++                    if (this.relatedSearchQuery) {
++                        const query = this.relatedSearchQuery.toLowerCase();
++                        qs = qs.filter(q => q.title.toLowerCase().includes(query) || q.id.toString().includes(query));
++                    }
++                    return qs;
++                },
++
++                init() {
++                    this.refreshIcons();
++
++                    document.addEventListener('connect disconnect', (event) => {
++                        this.isConnect = event.detail;
++                    })
++
++                    document.addEventListener('sse-notice', (event) => {
++                        const notice = event.detail;
++                        this.notices.unshift(notice);
++                        if (notice.type === 'alert') this.unreadNotices += 1;
++                        this.refreshIcons();
++                    });
++
++                    document.addEventListener('sse-question', (event) => {
++                        const question = this.toQuestionViewModel(event.detail);
++                        const index = this.questions.findIndex(q => q.id === question.id);
++                        if (index === -1) {
++                            this.questions.unshift(question);
++                        } else {
++                            this.questions.splice(index, 1, {
++                                ...this.questions[index],
++                                ...question,
++                            });
++                        }
++
++                        if (question.status === '完了') {
++                            const before = this.notices.length;
++                            this.notices = this.notices.filter(
++                                n => !(n.type === 'alert' && n.title.includes(`#${question.id}`))
++                            );
++                            this.unreadNotices = Math.max(0, this.unreadNotices - (before - this.notices.length));
++                        }
++
++                        if (this.activeQuestion?.id === question.id) {
++                            this.activeQuestion = {
++                                ...this.activeQuestion,
++                                ...question,
++                            };
++                        }
++
++                        this.refreshIcons();
++                    });
++                },
++
++                toQuestionViewModel(question) {
++                    const createdAt = question.createdAt instanceof Date
++                        ? question.createdAt
++                        : new Date(question.createdAt);
++                    const due = question.due instanceof Date
++                        ? question.due
++                        : (question.due ? new Date(question.due) : null);
++                    const daysLeft = due
++                        ? Math.max(0, Math.ceil((due.getTime() - Date.now()) / (1000 * 60 * 60 * 24)))
++                        : 0;
++
++                    return {
++                        id: question.id,
++                        sender: question.support?.user?.name ?? '不明',
++                        department: 'ここに部署名',
++                        title: question.title ?? '',
++                        content: question.content ?? '',
++                        status: question.support?.supportStatus?.title ?? '未対応',
++                        tags: (question.tags ?? []).map(tag => tag.title),
++                        daysLeft,
++                        date: this.formatDateTime(createdAt),
++                        dueDate: due ? this.formatDate(due) : '',
++                        relatedQuestions: (question.subQuestions ?? []).map(q => q.id),
++                    };
++                },
++
++                formatDateTime(date) {
++                    const year = date.getFullYear();
++                    const month = String(date.getMonth() + 1).padStart(2, '0');
++                    const day = String(date.getDate()).padStart(2, '0');
++                    const hour = String(date.getHours()).padStart(2, '0');
++                    const minute = String(date.getMinutes()).padStart(2, '0');
++                    return `${year}-${month}-${day} ${hour}:${minute}`;
++                },
++
++                formatDate(date) {
++                    const year = date.getFullYear();
++                    const month = String(date.getMonth() + 1).padStart(2, '0');
++                    const day = String(date.getDate()).padStart(2, '0');
++                    return `${year}-${month}-${day}`;
++                },
++
++                login() {
++                    this.isLoggedIn = true;
++                    this.refreshIcons();
++                },
++
++                logout() {
++                    if(confirm('ログアウトしますか？')) {
++                        this.isLoggedIn = false;
++                        this.currentView = 'home';
++                        this.refreshIcons();
++                    }
++                },
++
++                setView(view) {
++                    this.currentView = view;
++                    this.refreshIcons();
++                },
++
++                openDetail(q) {
++                    this.activeQuestion = q;
++                    this.relatedSearchQuery = '';
++                    this.setView('detail');
++                },
++
++                toggleTag(tag) {
++                    if (this.selectedTags.includes(tag)) {
++                        this.selectedTags = this.selectedTags.filter(t => t !== tag);
++                    } else {
++                        this.selectedTags.push(tag);
++                    }
++                },
++
++                toggleQuestionTag(tag) {
++                    if (!this.activeQuestion.tags) {
++                        this.activeQuestion.tags = [];
++                    }
++                    if (this.activeQuestion.tags.includes(tag)) {
++                        this.activeQuestion.tags = this.activeQuestion.tags.filter(t => t !== tag);
++                    } else {
++                        this.activeQuestion.tags.push(tag);
++                    }
++                },
++
++                toggleRelatedQuestion(qId) {
++                    if (!this.activeQuestion.relatedQuestions) {
++                        this.activeQuestion.relatedQuestions = [];
++                    }
++                    if (this.activeQuestion.relatedQuestions.includes(qId)) {
++                        this.activeQuestion.relatedQuestions = this.activeQuestion.relatedQuestions.filter(id => id !== qId);
++                    } else {
++                        this.activeQuestion.relatedQuestions.push(qId);
++                    }
++                },
++
++                toggleSidebar() {
++                    this.isSidebarOpen = !this.isSidebarOpen;
++                },
++
++                refreshIcons() {
++                    this.$nextTick(() => {
++                        if (typeof lucide !== 'undefined') {
++                            lucide.createIcons();
++                        }
++                    });
++                }
++            }));
++        });
++    </script>
++
++    <script type="module">
++        import { Question } from '/static/js/model.js';
++
++        document.addEventListener('DOMContentLoaded', () => {
++            const es = new EventSource('/sse');
++
++            es.onopen = (_, event) => {
++                document.dispatchEvent(new CustomeEvent('connect', {
++                    detail: true,
++                }))
++            }
++
++            es.onerror = (_, event) => {
++                document.dispatchEvent(new CustomeEvent('disconnect', {
++                    detail: false,
++                }))
++            }
++
++            es.addEventListener('notice', (event) => {
++                try {
++                    document.dispatchEvent(new CustomEvent('sse-notice', {
++                        detail: JSON.parse(event.data),
++                    }));
++                } catch (_) {}
++            });
++
++            es.addEventListener('question', (event) => {
++                try {
++                    const question = Question.fromJSON(JSON.parse(event.data));
++                    document.dispatchEvent(new CustomEvent('sse-question', {
++                        detail: question,
++                    }));
++                } catch (_) {}
++            });
++        });
++    </script>
++
++    <!-- 4. Alpine.js -->
++    <script defer src="/static/vendor/alpine.min.js"></script>
++</head>
++
++<body class="bg-gray-50 text-gray-900 font-sans antialiased overflow-x-hidden" x-data="hrAppData">
++
++    <!-- 1. ログインビュー -->
++    <div x-show="!isLoggedIn" class="min-h-screen flex items-center justify-center p-4 bg-gray-100">
++        <div class="w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden transition-all">
++            <div class="bg-slate-50 p-8 text-center border-b border-gray-100">
++                <div class="inline-flex items-center justify-center w-16 h-16 bg-slate-100 rounded-full mb-4 shadow-inner">
++                    <i data-lucide="boxes" class="w-8 h-8 text-slate-700"></i>
++                </div>
++                <h1 class="text-2xl font-bold text-gray-900 tracking-tight">HR Assist</h1>
++                <p class="text-sm text-gray-500 mt-2 font-medium">総務人事FAQ管理システム</p>
++            </div>
++            <div class="p-8 space-y-6">
++                <button @click="login()" class="w-full flex items-center justify-center gap-3 bg-gray-900 hover:bg-gray-800 text-white font-bold py-3 px-4 rounded-xl transition-all shadow-sm">
++                    <svg class="w-5 h-5" viewBox="0 0 21 21" fill="none" xmlns="http://www.w3.org/2000/svg">
++                        <rect x="1" y="1" width="9" height="9" fill="#F25022"/><rect x="11" y="1" width="9" height="9" fill="#7FBA00"/><rect x="1" y="11" width="9" height="9" fill="#00A4EF"/><rect x="11" y="11" width="9" height="9" fill="#FFB900"/>
++                    </svg>
++                    社内アカウントでログイン
++                </button>
++                <div class="divider text-xs text-gray-400 font-bold uppercase">または</div>
++                <form @submit.prevent="login()" class="space-y-4">
++                    <input type="email" placeholder="メールアドレス" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
++                    <input type="password" placeholder="パスワード" class="input input-bordered w-full bg-gray-50 focus:bg-white text-sm border-gray-300 shadow-inner" />
++                    <button type="submit" class="btn btn-primary w-full bg-slate-800 border-none hover:bg-slate-900 font-bold shadow-lg transition-transform active:scale-95">ログイン</button>
++                </form>
++            </div>
++        </div>
++    </div>
++
++    <!-- 2. メインアプリケーションビュー -->
++    <div x-show="isLoggedIn" class="flex min-h-screen relative w-full overflow-hidden" x-cloak>
++        
++        <!-- サイドバー -->
++        <aside class="sidebar-transition bg-white border-r border-gray-200 flex flex-col shrink-0 overflow-hidden shadow-sm"
++               :class="isSidebarOpen ? 'w-64' : 'w-20'">
++            
++            <div class="h-16 flex items-center border-b border-gray-200 shrink-0 px-6">
++                <i data-lucide="boxes" class="w-6 h-6 text-slate-800 shrink-0"></i>
++                <span class="text-slate-800 font-bold text-lg tracking-tight ml-2 whitespace-nowrap transition-opacity duration-200"
++                      :class="isSidebarOpen ? 'opacity-100' : 'opacity-0'">HR Assist</span>
++            </div>
++
++            <nav class="flex-1 p-4 space-y-2 overflow-y-auto no-scrollbar">
++                <button @click="setView('home')" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'home' || currentView === 'detail' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="home" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">質問一覧</span>
++                </button>
++                <button @click="setView('notice')" class="w-full flex items-center justify-between py-3 rounded-lg text-sm font-bold transition-colors text-gray-600 hover:bg-gray-50"
++                        :class="[currentView === 'notice' ? 'bg-slate-100 text-slate-800' : '', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <div class="flex items-center gap-3">
++                        <i data-lucide="bell" class="w-5 h-5 shrink-0"></i>
++                        <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">通知</span>
++                    </div>
++                    <span x-show="isSidebarOpen" class="bg-red-500 text-white text-xs px-2 py-0.5 rounded-full" x-text="unreadNotices"></span>
++                </button>
++                
++                <div class="my-4 border-t border-gray-100" x-show="isManager"></div>
++
++                <button @click="setView('categories')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'categories' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="tags" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">カテゴリ管理</span>
++                </button>
++
++                <button @click="setView('users')" x-show="isManager" class="w-full flex items-center gap-3 py-3 rounded-lg text-sm font-bold transition-colors"
++                        :class="[currentView === 'users' ? 'bg-slate-100 text-slate-800' : 'text-gray-600 hover:bg-gray-50', isSidebarOpen ? 'px-3' : 'justify-center']">
++                    <i data-lucide="users" class="w-5 h-5 shrink-0"></i>
++                    <span class="whitespace-nowrap transition-opacity duration-200" x-show="isSidebarOpen">ユーザー管理</span>
++                </button>
++            </nav>
++
++            <!-- プロフィールフッター -->
++            <div class="p-4 border-t border-gray-200 flex items-center gap-3 bg-gray-50 cursor-pointer hover:bg-gray-100 transition-colors" 
++                 :class="isSidebarOpen ? 'px-4' : 'justify-center'"
++                 @click="setView('settings')">
++                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-10 h-10 rounded-full border border-gray-200 bg-white object-cover shadow-sm shrink-0" alt="Avatar" />
++                <div class="min-w-0" x-show="isSidebarOpen">
++                    <div class="text-sm font-bold text-gray-800 truncate">自分</div>
++                    <div class="text-xs text-gray-500 truncate font-medium uppercase tracking-wider" x-text="isManager ? 'Manager' : 'Creator'"></div>
++                </div>
++            </div>
++        </aside>
++
++        <!-- メインエリア -->
++        <div class="flex-1 flex flex-col min-w-0 w-full overflow-hidden">
++            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-6 sticky top-0 z-30 shadow-sm shrink-0">
++                <div class="flex items-center gap-4">
++                    <button @click="toggleSidebar()" class="p-2 rounded-lg hover:bg-gray-100 text-gray-600 transition-colors">
++                        <i data-lucide="menu" class="w-5 h-5"></i>
++                    </button>
++                    <h1 class="text-lg font-bold text-gray-800 tracking-tight" x-text="titleMap[currentView] || 'HR Assist'"></h1>
++                </div>
++                <div class="flex items-center gap-4">
++                    <label class="flex items-center gap-2 cursor-pointer bg-gray-100 px-3 py-1.5 rounded-full hover:bg-gray-200 transition-colors">
++                        <span class="text-[10px] font-bold text-gray-500 uppercase tracking-widest" x-text="isManager ? 'Manager Mode' : 'Creator Mode'"></span>
++                        <input type="checkbox" x-model="isManager" class="toggle toggle-sm toggle-success" />
++                    </label>
++                    <button @click="logout()" class="text-xs font-bold text-gray-400 hover:text-red-600 transition-colors uppercase tracking-widest">ログアウト</button>
++                </div>
++            </header>
++
++            <main class="flex-1 overflow-y-auto p-6 w-full max-w-6xl mx-auto no-scrollbar">
++
++                <!-- [View: HOME] -->
++                <div x-show="currentView === 'home'" class="space-y-6">
++                    <!-- ツールバー -->
++                    <div class="flex flex-wrap items-center justify-between gap-4">
++                        <div class="flex items-center gap-2 flex-1 min-w-[300px]">
++                            <div class="relative flex-1">
++                                <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                                <input type="text" x-model="searchQuery" placeholder="キーワード検索..." class="input input-bordered w-full pl-10 h-10 text-sm border-gray-300 focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-sm" />
++                            </div>
++                            
++                            <div class="dropdown dropdown-end">
++                                <label tabindex="0" class="btn btn-outline btn-sm border-gray-300 normal-case h-10 px-4 font-bold text-gray-600 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800">
++                                    <i data-lucide="filter" class="w-4 h-4 mr-2"></i>
++                                    カテゴリ
++                                    <span x-show="selectedTags.length > 0" class="ml-2 badge bg-slate-800 text-white border-none badge-xs" x-text="selectedTags.length"></span>
++                                </label>
++                                <div tabindex="0" class="dropdown-content z-[50] card card-compact w-64 p-2 shadow-xl bg-white border border-gray-200 mt-2">
++                                    <div class="card-body">
++                                        <h3 class="font-bold text-xs uppercase text-gray-400 tracking-widest mb-2 border-b pb-1">カテゴリ絞り込み</h3>
++                                        <div class="flex flex-col gap-1 max-h-64 overflow-y-auto no-scrollbar">
++                                            <template x-for="tag in availableTags" :key="tag">
++                                                <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                    <input type="checkbox" :checked="selectedTags.includes(tag)" @change="toggleTag(tag)" class="checkbox checkbox-sm checkbox-primary border-gray-300" />
++                                                    <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
++                                                </label>
++                                            </template>
++                                        </div>
++                                        <div class="divider my-1"></div>
++                                        <button @click="selectedTags = []" class="btn btn-ghost btn-xs text-red-500 font-bold" :disabled="selectedTags.length === 0">リセット</button>
++                                    </div>
++                                </div>
++                            </div>
++                        </div>
++
++                        <div class="bg-gray-200 p-1 rounded-lg flex items-center shadow-inner">
++                            <button @click="viewMode = 'card'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'card' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
++                                <i data-lucide="layout-grid" class="w-4 h-4"></i>
++                            </button>
++                            <button @click="viewMode = 'list'" class="p-1.5 rounded-md transition-all" :class="viewMode === 'list' ? 'bg-white shadow-sm text-gray-900' : 'text-gray-500 hover:text-gray-700'">
++                                <i data-lucide="list" class="w-4 h-4"></i>
++                            </button>
++                        </div>
++                    </div>
++
++                    <!-- ステータス・クイックフィルタと並び替え -->
++                    <div class="flex flex-wrap items-center justify-between gap-4">
++                        <div class="flex flex-wrap items-center gap-2">
++                            <div class="flex items-center gap-1 bg-gray-200 p-1 rounded-xl w-max shadow-inner overflow-x-auto no-scrollbar">
++                                <button @click="statusFilter = 'all'" :class="statusFilter === 'all' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">すべて</button>
++                                <button @click="statusFilter = 'unanswered'" :class="statusFilter === 'unanswered' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応のみ</button>
++                                <button @click="statusFilter = 'progress'" :class="statusFilter === 'progress' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">対応中のみ</button>
++                                <button @click="statusFilter = 'open'" :class="statusFilter === 'open' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">未対応と対応中</button>
++                                <button @click="statusFilter = 'completed'" :class="statusFilter === 'completed' ? 'bg-white shadow-sm text-slate-800' : 'text-gray-500 hover:text-gray-700'" class="px-4 py-1.5 rounded-lg text-[10px] font-bold transition-all whitespace-nowrap">完了のみ</button>
++                            </div>
++                            
++                            <div x-show="selectedTags.length > 0" class="flex flex-wrap gap-1 items-center ml-2">
++                                <template x-for="tag in selectedTags" :key="tag">
++                                    <span class="badge bg-slate-100 border-slate-200 text-slate-800 font-bold gap-1 px-2 py-2 text-[10px] shadow-sm">
++                                        <span x-text="tag"></span>
++                                        <i data-lucide="x" class="w-2.5 h-2.5 cursor-pointer hover:text-red-500" @click="toggleTag(tag); refreshIcons()"></i>
++                                    </span>
++                                </template>
++                            </div>
++                        </div>
++
++                        <!-- 並び替えドロップダウン -->
++                        <select x-model="sortOrder" class="select select-bordered select-sm text-xs font-bold text-gray-600 focus:outline-none focus:border-slate-800 shadow-sm bg-white">
++                            <option value="date_desc">質問日時 (新しい順)</option>
++                            <option value="date_asc">質問日時 (古い順)</option>
++                            <option value="dueDate_asc">回答期日 (近い順)</option>
++                            <option value="dueDate_desc">回答期日 (遠い順)</option>
++                        </select>
++                    </div>
++
++                    <!-- カード表示 -->
++                    <div x-show="viewMode === 'card'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
++                        <template x-for="q in filteredQuestions" :key="q.id">
++                            <div class="bg-white rounded-xl p-5 shadow-sm border border-gray-200 cursor-pointer hover:shadow-md hover:border-slate-300 transition-all flex flex-col group" @click="openDetail(q)">
++                                <div class="flex justify-between mb-3 items-start">
++                                    <div class="flex items-center gap-3 min-w-0">
++                                        <div class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-600 text-sm group-hover:bg-slate-100 group-hover:text-slate-800 transition-colors shrink-0" x-text="q.sender.charAt(0)"></div>
++                                        <div class="min-w-0">
++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="q.sender"></div>
++                                            <div class="text-[10px] text-gray-400 font-bold uppercase" x-text="q.department"></div>
++                                        </div>
++                                    </div>
++                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold shadow-sm" :class="statusColorMap[q.status]" x-text="q.status"></span>
++                                </div>
++                                <h3 class="text-base font-bold text-gray-800 mb-1 group-hover:text-slate-900" x-text="q.title"></h3>
++                                <p class="text-sm text-gray-500 mb-4 flex-1 line-clamp-2" x-text="q.content"></p>
++                                <div class="flex items-center justify-between pt-4 border-t border-gray-50 text-[10px] font-bold">
++                                    <div class="flex gap-1 flex-wrap">
++                                        <template x-for="tag in (q.tags || [])" :key="tag">
++                                            <span class="bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
++                                        </template>
++                                    </div>
++                                    <div class="flex items-center gap-1 shrink-0" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                                        <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
++                                        <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                                    </div>
++                                </div>
++                            </div>
++                        </template>
++                        <div x-show="filteredQuestions.length === 0" class="col-span-full py-16 text-center">
++                            <i data-lucide="search-x" class="w-12 h-12 text-gray-200 mx-auto mb-4"></i>
++                            <p class="text-gray-400 font-bold">条件に一致する質問は見つかりませんでした。</p>
++                        </div>
++                    </div>
++
++                    <!-- リスト表示 -->
++                    <div x-show="viewMode === 'list'" class="flex flex-col gap-2 w-full" style="display: none;">
++                        <template x-for="q in filteredQuestions" :key="q.id">
++                            <div class="bg-white rounded-lg p-3 shadow-sm border border-gray-200 cursor-pointer hover:border-slate-300 hover:shadow-md transition-all flex items-center gap-4 group" @click="openDetail(q)">
++                                <span class="px-2 py-0.5 rounded text-[10px] font-bold shrink-0 w-16 text-center" :class="statusColorMap[q.status]" x-text="q.status"></span>
++                                <h3 class="text-sm font-bold text-gray-800 truncate w-48 shrink-0 group-hover:text-slate-900" x-text="q.title"></h3>
++                                <p class="text-xs text-gray-500 truncate flex-1 font-medium" x-text="q.content"></p>
++                                <div class="flex gap-1 shrink-0">
++                                    <template x-for="tag in (q.tags || [])" :key="tag">
++                                        <span class="text-[10px] font-bold bg-gray-100 border px-1.5 py-0.5 rounded text-gray-500 uppercase" x-text="tag"></span>
++                                    </template>
++                                </div>
++                                <div class="text-[10px] font-bold shrink-0 flex items-center gap-1 w-16 justify-end" :class="q.daysLeft <= 1 && q.status !== '完了' ? 'text-red-500' : 'text-gray-400'">
++                                    <i data-lucide="clock" class="w-3.5 h-3.5"></i> 
++                                    <span x-text="q.status === '完了' ? '完了' : `あと${q.daysLeft}日`"></span>
++                                </div>
++                            </div>
++                        </template>
++                    </div>
++                </div>
++
++                <!-- [View: DETAIL] 質問詳細 -->
++                <div x-show="currentView === 'detail'" class="space-y-6" style="display: none;">
++                    <div class="flex items-center justify-between">
++                        <button class="btn btn-ghost btn-sm gap-2 font-bold text-gray-500 hover:text-gray-800" @click="setView('home')">
++                            <i data-lucide="arrow-left" class="w-4 h-4"></i> 一覧に戻る
++                        </button>
++                    </div>
++                    <div class="flex flex-col lg:flex-row gap-6">
++                        
++                        <!-- 左側：チャットエリア -->
++                        <div class="flex-1 bg-white rounded-xl shadow-sm border border-gray-200 flex flex-col overflow-hidden min-h-[500px]">
++                            <div class="bg-gray-50 p-6 flex-1 space-y-6 overflow-y-auto">
++                                <div class="flex gap-4">
++                                    <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 shrink-0 shadow-sm" x-text="activeQuestion.sender?.charAt(0)"></div>
++                                    <div class="space-y-1 max-w-[80%]">
++                                        <div class="text-[10px] font-bold text-gray-400 uppercase tracking-widest" x-text="(activeQuestion.sender || '') + ' • ' + (activeQuestion.department || '')"></div>
++                                        <div class="bg-white p-4 rounded-2xl rounded-tl-none border border-gray-100 shadow-sm text-sm leading-relaxed" x-text="activeQuestion.content"></div>
++                                    </div>
++                                </div>
++                                <div class="flex gap-4">
++                                    <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix" class="w-10 h-10 rounded-full border bg-white shrink-0 shadow-sm" />
++                                    <div class="space-y-1 max-w-[80%]">
++                                        <div class="text-[10px] font-bold text-yellow-600 uppercase tracking-widest">自分 • 内部メモ</div>
++                                        <div class="bg-yellow-50 p-4 rounded-2xl rounded-tl-none border border-yellow-100 text-sm text-yellow-900 italic">前回の事例（#1001）を参考に回答を作成中...</div>
++                                    </div>
++                                </div>
++                            </div>
++                            <div class="p-4 border-t border-gray-100 bg-white">
++                                <div class="flex gap-2 mb-3">
++                                    <button class="text-[10px] font-bold bg-slate-800 text-white px-3 py-1 rounded shadow-sm">質問者に返信</button>
++                                    <button class="text-[10px] font-bold bg-gray-100 text-gray-500 px-3 py-1 rounded">内部メモ</button>
++                                </div>
++                                <textarea class="textarea textarea-bordered w-full h-24 text-sm focus:border-slate-800 focus:outline-none" placeholder="回答またはメモを入力..."></textarea>
++                                <div class="mt-3 flex justify-end">
++                                    <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md" @click="alert('送信しました'); setView('home')">送信</button>
++                                </div>
++                            </div>
++                        </div>
++
++                        <!-- 右側：プロパティパネル -->
++                        <div class="w-full lg:w-80 space-y-4 shrink-0">
++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm space-y-6">
++                                
++                                <div>
++                                    <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-3">質問者情報</h4>
++                                    <div class="flex items-center gap-3">
++                                        <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 text-sm shadow-inner shrink-0" x-text="activeQuestion.sender?.charAt(0)"></div>
++                                        <div class="min-w-0">
++                                            <div class="text-sm font-bold text-gray-900 truncate" x-text="activeQuestion.sender"></div>
++                                            <div class="text-[10px] text-gray-500 font-bold uppercase tracking-wider" x-text="activeQuestion.department"></div>
++                                        </div>
++                                    </div>
++                                </div>
++
++                                <div class="divider my-0"></div>
++
++                                <div class="space-y-4">
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">タイトル</h4>
++                                        <input type="text" x-model="activeQuestion.title" class="input input-bordered input-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" />
++                                    </div>
++
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">ステータス</h4>
++                                        <select class="select select-bordered select-sm w-full text-sm font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner" x-model="activeQuestion.status">
++                                            <option>未対応</option>
++                                            <option>対応中</option>
++                                            <option>完了</option>
++                                        </select>
++                                    </div>
++
++                                    <!-- カテゴリ (マルチセレクト) -->
++                                    <div>
++                                        <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">カテゴリ</h4>
++                                        <div class="relative w-full" x-data="{ isCategoryOpen: false }" @click.outside="isCategoryOpen = false">
++                                            <button type="button" @click="isCategoryOpen = !isCategoryOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
++                                                <span class="truncate text-xs" x-text="activeQuestion.tags && activeQuestion.tags.length > 0 ? activeQuestion.tags.join(', ') : 'カテゴリを選択'"></span>
++                                                <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
++                                            </button>
++                                            <div x-show="isCategoryOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
++                                                <div class="card-body p-2">
++                                                    <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
++                                                        <template x-for="tag in availableTags" :key="tag">
++                                                            <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                                <input type="checkbox" 
++                                                                       :checked="activeQuestion.tags && activeQuestion.tags.includes(tag)" 
++                                                                       @change="toggleQuestionTag(tag)"
++                                                                       class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
++                                                                <span class="text-sm font-bold text-gray-700 group-hover:text-slate-900" x-text="tag"></span>
++                                                            </label>
++                                                        </template>
++                                                    </div>
++                                                </div>
++                                            </div>
++                                        </div>
++                                    </div>
++
++                                    <div class="grid grid-cols-2 gap-3">
++                                        <div>
++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">質問日時</h4>
++                                            <div class="text-xs font-bold text-gray-700 bg-gray-50 px-2 rounded-lg border border-gray-200 flex items-center h-8 shadow-inner truncate" x-text="activeQuestion.date || '---'"></div>
++                                        </div>
++                                        <div>
++                                            <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1.5">期日</h4>
++                                            <input type="date" x-model="activeQuestion.dueDate" class="input input-bordered input-sm w-full text-xs font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner px-2" />
++                                        </div>
++                                    </div>
++                                </div>
++                            </div>
++                            
++                            <!-- 関連する質問 -->
++                            <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm mt-4">
++                                <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-4">関連する質問</h4>
++                                
++                                <div class="relative w-full" x-data="{ isRelatedOpen: false }" @click.outside="isRelatedOpen = false">
++                                    <button type="button" @click="isRelatedOpen = !isRelatedOpen" class="btn btn-outline btn-sm border-gray-300 normal-case w-full justify-between font-bold text-gray-700 hover:bg-slate-50 hover:text-slate-800 hover:border-slate-800 shadow-sm bg-gray-50">
++                                        <span class="truncate text-xs">
++                                            <template x-if="!activeQuestion.relatedQuestions || activeQuestion.relatedQuestions.length === 0">
++                                                <span>質問を選択</span>
++                                            </template>
++                                            <template x-if="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
++                                                <span x-text="activeQuestion.relatedQuestions.length + '件選択中'"></span>
++                                            </template>
++                                        </span>
++                                        <i data-lucide="chevron-down" class="w-4 h-4 ml-2 shrink-0"></i>
++                                    </button>
++                                    
++                                    <div x-show="isRelatedOpen" x-transition x-cloak class="absolute left-0 right-0 z-[50] card card-compact w-full p-2 shadow-xl bg-white border border-gray-200 mt-1">
++                                        <div class="card-body p-2">
++                                            <!-- 検索入力 -->
++                                            <div class="relative mb-2">
++                                                <i data-lucide="search" class="w-3 h-3 absolute left-2 top-2.5 text-gray-400"></i>
++                                                <input type="text" x-model="relatedSearchQuery" placeholder="検索..." class="input input-bordered input-sm w-full pl-7 text-xs focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800" />
++                                            </div>
++                                            
++                                            <div class="flex flex-col gap-1 max-h-48 overflow-y-auto no-scrollbar">
++                                                <template x-for="rq in availableRelatedQuestions" :key="rq.id">
++                                                    <label class="flex items-center gap-3 p-2 rounded-lg hover:bg-slate-50 cursor-pointer transition-colors group">
++                                                        <input type="checkbox" 
++                                                               :checked="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.includes(rq.id)" 
++                                                               @change="toggleRelatedQuestion(rq.id)"
++                                                               class="checkbox checkbox-sm checkbox-primary border-gray-300 rounded" />
++                                                        <div class="flex flex-col min-w-0">
++                                                            <span class="text-xs font-bold text-gray-700 group-hover:text-slate-900 truncate" x-text="'#' + rq.id + ' ' + rq.title"></span>
++                                                        </div>
++                                                    </label>
++                                                </template>
++                                                <div x-show="availableRelatedQuestions.length === 0" class="text-center py-4 text-xs text-gray-400 font-bold">
++                                                    該当なし
++                                                </div>
++                                            </div>
++                                        </div>
++                                    </div>
++                                </div>
++                                
++                                <!-- 選択された関連質問のリスト -->
++                                <div class="mt-3 space-y-2" x-show="activeQuestion.relatedQuestions && activeQuestion.relatedQuestions.length > 0">
++                                    <template x-for="rId in activeQuestion.relatedQuestions" :key="rId">
++                                        <div class="p-2 bg-gray-50 rounded border text-xs font-bold hover:bg-slate-50 transition-colors flex justify-between items-center group">
++                                            <span class="truncate pr-2 cursor-pointer hover:text-blue-600" x-text="'#' + rId + ' ' + (questions.find(q => q.id === rId)?.title || '')" @click="openDetail(questions.find(q => q.id === rId))"></span>
++                                            <i data-lucide="x" class="w-3 h-3 text-gray-400 hover:text-red-500 cursor-pointer shrink-0" @click="toggleRelatedQuestion(rId); refreshIcons()"></i>
++                                        </div>
++                                    </template>
++                                </div>
++                            </div>
++                        </div>
++                    </div>
++                </div>
++
++                <!-- [View: NOTICE] -->
++                <div x-show="currentView === 'notice'" class="max-w-3xl mx-auto space-y-4" style="display: none;">
++                    <template x-for="n in notices" :key="n.id">
++                        <div class="bg-white rounded-xl p-5 border border-gray-200 shadow-sm flex gap-4 items-start hover:bg-gray-50 transition-colors cursor-pointer" @click="setView('home')">
++                            <div class="w-10 h-10 rounded-lg flex items-center justify-center shrink-0" :class="n.type === 'alert' ? 'bg-red-50 text-red-500' : 'bg-slate-50 text-slate-500'">
++                                <i :data-lucide="n.type === 'alert' ? 'alert-circle' : 'info'" class="w-5 h-5"></i>
++                            </div>
++                            <div class="flex-1 min-w-0">
++                                <div class="flex justify-between items-center mb-1">
++                                    <h3 class="font-bold text-sm text-gray-800" x-text="n.title"></h3>
++                                    <span class="text-[10px] font-bold text-gray-400" x-text="n.time"></span>
++                                </div>
++                                <p class="text-xs text-gray-500 line-clamp-2" x-text="n.content"></p>
++                            </div>
++                        </div>
++                    </template>
++                </div>
++
++                <!-- [View: CATEGORIES] -->
++                <div x-show="currentView === 'categories'" class="space-y-6" style="display: none;">
++                    <div class="flex justify-between items-center">
++                        <div class="relative w-80">
++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                            <input type="text" placeholder="カテゴリ検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
++                        </div>
++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="alert('カテゴリを追加します（モック）')">+ カテゴリ追加</button>
++                    </div>
++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
++                        <table class="table w-full">
++                            <thead>
++                                <tr class="bg-gray-50">
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">カテゴリ名</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">使用数</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
++                                </tr>
++                            </thead>
++                            <tbody>
++                                <template x-for="tag in availableTags" :key="tag">
++                                    <tr class="hover:bg-gray-50 transition-colors">
++                                        <td>
++                                            <div class="flex items-center gap-3">
++                                                <div class="w-8 h-8 rounded-lg bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs">
++                                                    <i data-lucide="tag" class="w-4 h-4"></i>
++                                                </div>
++                                                <div class="font-bold text-sm" x-text="tag"></div>
++                                            </div>
++                                        </td>
++                                        <td>
++                                            <span class="badge badge-sm font-bold bg-slate-100 text-slate-800 border-slate-200" x-text="questions.filter(q => q.tags && q.tags.includes(tag)).length + '件'"></span>
++                                        </td>
++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
++                                    </tr>
++                                </template>
++                            </tbody>
++                        </table>
++                    </div>
++                </div>
++
++                <!-- [View: USERS] -->
++                <div x-show="currentView === 'users'" class="space-y-6" style="display: none;">
++                    <div class="flex justify-between items-center">
++                        <div class="relative w-80">
++                            <i data-lucide="search" class="w-5 h-5 absolute left-3 top-2.5 text-gray-400"></i>
++                            <input type="text" placeholder="ユーザー検索..." class="input input-bordered w-full pl-10 h-10 text-sm focus:border-slate-800 shadow-sm" />
++                        </div>
++                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold" @click="showAddUserModal = true">+ ユーザー追加</button>
++                    </div>
++                    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
++                        <table class="table w-full">
++                            <thead>
++                                <tr class="bg-gray-50">
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">氏名</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">役割</th>
++                                    <th class="text-[10px] font-bold text-gray-400 uppercase tracking-widest border-none">操作</th>
++                                </tr>
++                            </thead>
++                            <tbody>
++                                <template x-for="u in users" :key="u.email">
++                                    <tr class="hover:bg-gray-50 transition-colors">
++                                        <td>
++                                            <div class="flex items-center gap-3">
++                                                <div class="w-8 h-8 rounded-full bg-slate-50 flex items-center justify-center font-bold text-slate-600 text-xs" x-text="u.name.charAt(0)"></div>
++                                                <div>
++                                                    <div class="font-bold text-sm" x-text="u.name"></div>
++                                                    <div class="text-[10px] text-gray-400" x-text="u.email"></div>
++                                                </div>
++                                            </div>
++                                        </td>
++                                        <td><span class="badge badge-sm font-bold" :class="u.role === 'Manager' ? 'badge-info bg-slate-100 text-slate-800 border-slate-200' : 'badge-ghost'" x-text="u.role"></span></td>
++                                        <td><button class="text-gray-400 hover:text-slate-800"><i data-lucide="edit-2" class="w-4 h-4"></i></button></td>
++                                    </tr>
++                                </template>
++                            </tbody>
++                        </table>
++                    </div>
++                </div>
++
++                <!-- [View: SETTINGS] -->
++                <div x-show="currentView === 'settings'" class="max-w-2xl mx-auto" style="display: none;">
++                    <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
++                        <div class="p-6 border-b border-gray-100 bg-gray-50/50 text-center lg:text-left">
++                            <h2 class="text-lg font-bold text-gray-800 tracking-tight">プロフィール設定</h2>
++                        </div>
++                        <div class="p-8 space-y-6">
++                            <div class="flex flex-col sm:flex-row items-center gap-8">
++                                <img src="https://api.dicebear.com/7.x/notionists/svg?seed=Felix&backgroundColor=eff6ff" class="w-24 h-24 rounded-2xl border-2 border-white shadow-lg" />
++                                <button class="btn btn-sm btn-outline font-bold shadow-sm">画像をアップロード</button>
++                            </div>
++                            <div class="space-y-4">
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">氏名</label><input type="text" value="自分" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">メールアドレス</label><input type="email" value="jinji.taro@example.com" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">新しいパスワード</label><input type="password" placeholder="変更する場合のみ入力" class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
++                                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">自己紹介・メモ</label><textarea class="textarea textarea-bordered w-full h-24 text-sm font-bold shadow-inner" placeholder="管理担当のメモなど..."></textarea></div>
++                            </div>
++                        </div>
++                        <div class="p-6 bg-gray-50 border-t border-gray-100 flex justify-end gap-3">
++                            <button class="btn btn-ghost font-bold text-gray-500" @click="setView('home')">キャンセル</button>
++                            <button class="btn bg-slate-800 text-white border-none font-bold px-10 shadow-md" @click="setView('home'); alert('設定を保存しました')">保存する</button>
++                        </div>
++                    </div>
++                </div>
++
++            </main>
++        </div>
++    </div>
++
++    <!-- ユーザー招待モーダル -->
++    <div class="modal" :class="{'modal-open': showAddUserModal}">
++        <div class="modal-box bg-white rounded-2xl shadow-2xl">
++            <h3 class="font-bold text-lg mb-6 tracking-tight">新規ユーザーを招待</h3>
++            <div class="space-y-4">
++                <input type="text" placeholder="氏名" class="input input-bordered w-full text-sm shadow-inner" />
++                <input type="email" placeholder="メールアドレス" class="input input-bordered w-full text-sm shadow-inner" />
++                <select class="select select-bordered w-full text-sm font-bold shadow-inner">
++                    <option disabled selected>役割を選択</option>
++                    <option>Manager</option>
++                    <option>Creator</option>
++                </select>
++            </div>
++            <div class="modal-action">
++                <button class="btn btn-ghost font-bold" @click="showAddUserModal = false">キャンセル</button>
++                <button class="btn bg-slate-800 border-none font-bold px-8 shadow-md" @click="showAddUserModal = false; alert('ユーザーを招待しました')">招待を送信</button>
++            </div>
++        </div>
++        <div class="modal-backdrop bg-gray-900 bg-opacity-50" @click="showAddUserModal = false"></div>
++    </div>
++
++</body>
++</html>
++<!DOCTYPE html>
diff --git a/doc/db/INIT.sql b/doc/db/INIT.sql
index b9518ac..270175b 100644
--- a/doc/db/INIT.sql
+++ b/doc/db/INIT.sql
@@ -0,0 +1,5 @@
+DROP SCHEMA public CASCADE;
+CREATE SCHEMA public;
+GRANT ALL ON SCHEMA public TO postgres;
+GRANT ALL ON SCHEMA public TO public;
+
@@ -24,0 +30 @@ CREATE TABLE users (
+    memo TEXT DEFAULT '',
@@ -160,0 +167,28 @@ CREATE TABLE notices (
+-- ============================================
+-- RELATED_QUESTION
+-- ============================================
+
+CREATE TABLE related_questions (
+    id BIGSERIAL PRIMARY KEY,
+    question_id BIGINT NOT NULL,
+    related_question_id BIGINT NOT NULL,
+
+    CONSTRAINT fk_related_questions_question
+        FOREIGN KEY (question_id)
+        REFERENCES questions(id),
+
+    CONSTRAINT fk_related_questions_related
+        FOREIGN KEY (related_question_id)
+        REFERENCES questions(id)
+);
+
+-- 自己参照禁止
+ALTER TABLE related_questions
+ADD CONSTRAINT chk_no_self_reference
+CHECK (question_id <> related_question_id);
+
+-- 重複登録禁止
+ALTER TABLE related_questions
+ADD CONSTRAINT uq_related_questions
+UNIQUE (question_id, related_question_id);
+
@@ -218,0 +253,2 @@ ON notices(question_id);
+CREATE INDEX idx_related_questions_question
+ON related_questions(question_id);
@@ -245,0 +282,5 @@ INSERT INTO users (name, password, email, role_id) VALUES
+<<<<<<< HEAD
+('Admin', 'Admin', 'Admin', 1),
+('Taro Yamada', 'hashed_password_1', 'taro@example.com', 2),
+('Hanako Suzuki', 'hashed_password_2', 'hanako@example.com', 3),
+=======
@@ -247,0 +289 @@ INSERT INTO users (name, password, email, role_id) VALUES
+>>>>>>> add-mock
@@ -320 +362,7 @@ INSERT INTO notices (type_id, question_id, content, display_due) VALUES
-(1, NULL, 'システムメンテナンスのお知らせ', NULL);
\ No newline at end of file
+(1, NULL, 'システムメンテナンスのお知らせ', NULL);
+
+
+INSERT INTO related_questions (question_id, related_question_id) VALUES
+(2, 1),
+(3, 1),
+(3, 2);
\ No newline at end of file
diff --git a/doc/db/er.md b/doc/db/er.md
index 59a0289..5ea8a5f 100644
--- a/doc/db/er.md
+++ b/doc/db/er.md
@@ -7,0 +8 @@ erDiagram
+        string memo
@@ -21 +21,0 @@ erDiagram
-        number support_id FK
@@ -23,0 +24 @@ erDiagram
+        number support_id FK
@@ -33,0 +35,2 @@ erDiagram
+    QUESTION ||--o{ RELATED_QUESTION: "１つの質問に対して、複数の関連する質問を持つ（from_question_id）"
+    QUESTION ||--o{ RELATED_QUESTION: "１つの質問に対して、複数の関連する質問を持つ（to_question_id）"
@@ -88 +91 @@ erDiagram
-        string title
+        string name
@@ -120 +123,7 @@ erDiagram
-```
+
+    RELATED_QUESTION {
+        number id PK
+        number from_question_id FK  "関連元の質問ID"
+        number to_question_id FK    "関連先の質問ID"
+    }
+```
\ No newline at end of file
diff --git a/frontend/bun.lock b/frontend/bun.lock
index d4c80f1..ab188e8 100644
--- a/frontend/bun.lock
+++ b/frontend/bun.lock
@@ -45 +45 @@
-    "@types/node": ["@types/node@25.5.0", "", { "dependencies": { "undici-types": "~7.18.0" } }, "sha512-jp2P3tQMSxWugkCUKLRPVUpGaL5MVFwF8RDuSRztfwgN1wmqJeMSbKlnEtQqU8UrhTmzEmZdu2I6v2dpp7XIxw=="],
+    "@types/node": ["@types/node@25.6.0", "", { "dependencies": { "undici-types": "~7.19.0" } }, "sha512-+qIYRKdNYJwY3vRCZMdJbPLJAtGjQBudzZzdzwQYkEPQd+PJGixUL5QfvCLDaULoLv+RhT3LDkwEfKaAkgSmNQ=="],
@@ -61 +61 @@
-    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.13", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-BL2sTuHOdy0YT1lYieUxTw/QMtPBC3pmlJC6xk8BBYVv6vcw3SGdKemQ+Xsx9ik2F/lYDO9tqsFQH1r9PFuHKw=="],
+    "baseline-browser-mapping": ["baseline-browser-mapping@2.10.20", "", { "bin": { "baseline-browser-mapping": "dist/cli.cjs" } }, "sha512-1AaXxEPfXT+GvTBJFuy4yXVHWJBXa4OdbIebGN/wX5DlsIkU0+wzGnd2lOzokSk51d5LUmqjgBLRLlypLUqInQ=="],
@@ -73 +73 @@
-    "caniuse-lite": ["caniuse-lite@1.0.30001788", "", {}, "sha512-6q8HFp+lOQtcf7wBK+uEenxymVWkGKkjFpCvw5W25cmMwEDU45p1xQFBQv8JDlMMry7eNxyBaR+qxgmTUZkIRQ=="],
+    "caniuse-lite": ["caniuse-lite@1.0.30001790", "", {}, "sha512-bOoxfJPyYo+ds6W0YfptaCWbFnJYjh2Y1Eow5lRv+vI2u8ganPZqNm1JwNh0t2ELQCqIWg4B3dWEusgAmsoyOw=="],
@@ -91 +91,3 @@
-    "electron-to-chromium": ["electron-to-chromium@1.5.331", "", {}, "sha512-IbxXrsTlD3hRodkLnbxAPP4OuJYdWCeM3IOdT+CpcMoIwIoDfCmRpEtSPfwBXxVkg9xmBeY7Lz2Eo2TDn/HC3Q=="],
+    "electron-to-chromium": ["electron-to-chromium@1.5.343", "", {}, "sha512-YHnQ3MXI08icvL9ZKnEBy05F2EQ8ob01UaMOuMbM8l+4UcAq6MPPbBTJBbsBUg3H8JeZNt+O4fjsoWth3p6IFg=="],
+
+    "es-errors": ["es-errors@1.3.0", "", {}, "sha512-Zf5H2Kxt2xjTvbJvP2ZWLEICxA6j+hAmMzIlypy4xcBg1vKVnx89Wy0GbS+kf5cwCVFFzdCFh2XSCFNULS6csw=="],
@@ -115,3 +117 @@
-    "hasown": ["hasown@2.0.2", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-0hJU9SCPvmMzIBdZFqNPXWa6dqh7WdH0cII9y+CyS8rG3nL48Bclra9HmKhVVUHyPWNH5Y7xDwAB7bfgSjkUMQ=="],
-
-    "htmx-ext-client-side-templates": ["htmx-ext-client-side-templates@2.0.2", "", { "dependencies": { "htmx.org": "^2.0.2" } }, "sha512-d2dA5HOJuMPN+QmAa4MtriTc1OZaaidG/UK/uwDsKJzstMFae2M7tFgykFXOjuFoV2XNCcexr3p+a9p5Jb54Dg=="],
+    "hasown": ["hasown@2.0.3", "", { "dependencies": { "function-bind": "^1.1.2" } }, "sha512-ej4AhfhfL2Q2zpMmLo7U1Uv9+PyhIZpgQLGT1F9miIGmiCJIoCgSmczFdrc97mWT4kVY72KA+WnnhJ5pghSvSg=="],
@@ -149 +149 @@
-    "node-releases": ["node-releases@2.0.37", "", {}, "sha512-1h5gKZCF+pO/o3Iqt5Jp7wc9rH3eJJ0+nh/CIoiRwjRxde/hAHyLPXYN4V3CqKAbiZPSeJFSWHmJsbkicta0Eg=="],
+    "node-releases": ["node-releases@2.0.38", "", {}, "sha512-3qT/88Y3FbH/Kx4szpQQ4HzUbVrHPKTLVpVocKiLfoYvw9XSGOX2FmD2d6DrXbVYyAQTF2HeF6My8jmzx7/CRw=="],
@@ -187 +187 @@
-    "resolve": ["resolve@1.22.11", "", { "dependencies": { "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-RfqAvLnMl313r7c9oclB1HhUEAezcpLjz95wFH4LVuhk9JF/r22qmVP9AMmOU4vMX7Q8pN8jwNg/CSpdFnMjTQ=="],
+    "resolve": ["resolve@1.22.12", "", { "dependencies": { "es-errors": "^1.3.0", "is-core-module": "^2.16.1", "path-parse": "^1.0.7", "supports-preserve-symlinks-flag": "^1.0.0" }, "bin": { "resolve": "bin/resolve" } }, "sha512-TyeJ1zif53BPfHootBGwPRYT1RUt6oGWsaQr8UyZW/eAm9bKoijtvruSDEmZHm92CwS9nj7/fWttqPCgzep8CA=="],
@@ -205 +205 @@
-    "tinyglobby": ["tinyglobby@0.2.15", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.3" } }, "sha512-j2Zq4NyQYG5XMST4cbs02Ak8iJUdxRM0XI5QyxXuZOzKOINmWurp3smXu3y5wDcJrptwpSjgXHzIQxR0omXljQ=="],
+    "tinyglobby": ["tinyglobby@0.2.16", "", { "dependencies": { "fdir": "^6.5.0", "picomatch": "^4.0.4" } }, "sha512-pn99VhoACYR8nFHhxqix+uvsbXineAasWm5ojXoN8xEwK5Kd3/TrhNn1wByuD52UxWRLy8pu+kRMniEi6Eq9Zg=="],
@@ -213 +213 @@
-    "undici-types": ["undici-types@7.18.2", "", {}, "sha512-AsuCzffGHJybSaRrmr5eHr81mwJU3kjw6M+uprWvCXiNeN9SOGwQ3Jn8jb8m3Z6izVgknn1R0FTCEAP2QrLY/w=="],
+    "undici-types": ["undici-types@7.19.2", "", {}, "sha512-qYVnV5OEm2AW8cJMCpdV20CDyaN3g0AjDlOGf1OW4iaDEx8MwdtChUp4zu4H0VP3nDRF/8RKWH+IPp9uW0YGZg=="],
@@ -221,2 +220,0 @@
-    "browserslist/caniuse-lite": ["caniuse-lite@1.0.30001784", "", {}, "sha512-WU346nBTklUV9YfUl60fqRbU5ZqyXlqvo1SgigE1OAXK5bFL8LL9q1K7aap3N739l4BvNqnkm3YrGHiY9sfUQw=="],
-
@@ -227,2 +224,0 @@
-    "tailwindcss/postcss": ["postcss@8.5.8", "", { "dependencies": { "nanoid": "^3.3.11", "picocolors": "^1.1.1", "source-map-js": "^1.2.1" } }, "sha512-OW/rX8O/jXnm82Ey1k44pObPtdblfiuWnrd8X7GJ7emImCOstunGbXUpp7HdBrFQX6rJzn3sPT397Wp5aCwCHg=="],
-
diff --git a/handler.go b/handler.go
deleted file mode 100644
index 06ab7d0..0000000
--- a/handler.go
+++ /dev/null
@@ -1 +0,0 @@
-package main
diff --git a/handler/apiHandler.go b/handler/apiHandler.go
index 4aacdc2..e8757dd 100644
--- a/handler/apiHandler.go
+++ b/handler/apiHandler.go
@@ -10,0 +11 @@ import (
+	"github.com/golang-jwt/jwt/v5"
@@ -34,0 +36 @@ func (hm *HandlerManager) setupUserHandler(group *echo.Group) (routeInfos []echo
+	const uriWithToken = uri + "/t"
@@ -37,0 +40 @@ func (hm *HandlerManager) setupUserHandler(group *echo.Group) (routeInfos []echo
+	routeInfos = append(routeInfos, group.GET(uriWithToken, hm.getUserFromToken()))
@@ -170,0 +174,20 @@ func (hm *HandlerManager) deleteTagByID(api string, sendEvent func(string, strin
+func (hm *HandlerManager) getUserFromToken() echo.HandlerFunc {
+	return func(c *echo.Context) error {
+		token := c.Get("user").(*jwt.Token)
+		claims := token.Claims.(jwt.MapClaims)
+		subFloat, ok := claims["sub"].(float64)
+		if !ok {
+			return echo.NewHTTPError(http.StatusBadRequest, "Invalid ID format")
+		}
+		id := int64(subFloat)
+
+		user, err := infrastructure.GetUserByID(c.Request().Context(), hm.db, id)
+		if err != nil {
+			fmt.Println(err)
+			return c.JSON(http.StatusInternalServerError, err.Error())
+		}
+
+		return c.JSON(http.StatusOK, infrastructure.UserFromEntity(user))
+	}
+}
+
@@ -232,0 +256 @@ func (hm *HandlerManager) updateQuestionByID(api string, sendEvent func(string,
+		fmt.Println(updatedModel)
@@ -265,0 +290 @@ func (hm *HandlerManager) deleteUserByID(api string, sendEvent func(string, stri
+		sendEvent(api, idStr)
diff --git a/handler/authHandler.go b/handler/authHandler.go
index 6111eec..2d10b18 100644
--- a/handler/authHandler.go
+++ b/handler/authHandler.go
@@ -4 +3,0 @@ import (
-	"context"
@@ -15 +13,0 @@ import (
-	"gorm.io/gorm"
@@ -22 +19,0 @@ const (
-// TODO; 基本的なJWT認証の一部。まだ本実装には進まない
@@ -55 +52 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
-		user, err := getUserInfo(c.Request().Context(), hm.db, info.E, info.P)
+		user, err := infrastructure.GetUserInfo(c.Request().Context(), hm.db, info.E, info.P)
@@ -99,5 +95,0 @@ func logoutHandler() echo.HandlerFunc {
-
-func getUserInfo(ctx context.Context, db *gorm.DB, email, pass string) (user infrastructure.User, err error) {
-	user, err = gorm.G[infrastructure.User](db).Where("email = ? AND password = ?", email, pass).First(ctx)
-	return
-}
diff --git a/handler/eventHub.go b/handler/eventHub.go
index b55cee2..1c3f38e 100644
--- a/handler/eventHub.go
+++ b/handler/eventHub.go
@@ -180 +179,0 @@ func (hub *Hub) RunSSE(db *gorm.DB) error {
-
diff --git a/handler/notice.go b/handler/notice.go
index 08ff8dc..4ecf61d 100644
--- a/handler/notice.go
+++ b/handler/notice.go
@@ -19 +19,2 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-	var ticker = time.NewTicker(30 * time.Minute)
+	// var ticker = time.NewTicker(30 * time.Minute)
+	var ticker = time.NewTicker(15 * time.Second)
@@ -38 +39 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-				if question.Due.Add(-24 * time.Hour).Before(time.Now()) {
+				if question.Due.Add(-72 * time.Hour).Before(time.Now()) {
diff --git a/package.json b/package.json
deleted file mode 100644
index 8d7055f..0000000
--- a/package.json
+++ /dev/null
@@ -1,5 +0,0 @@
-{
-  "dependencies": {
-    "htmx-ext-sse": "^2.2.4"
-  }
-}
\ No newline at end of file
diff --git a/static/js/hrAppData.js b/static/js/hrAppData.js
index 89d3c95..699faa3 100644
--- a/static/js/hrAppData.js
+++ b/static/js/hrAppData.js
@@ -2,0 +3 @@ import { SSE_KEY } from './sse.js';
+import { Support, SupportStatus } from './model.js';
@@ -27,0 +29 @@ document.addEventListener('alpine:init', () => {
+        timeNow: new Date(),
@@ -60,0 +63,6 @@ document.addEventListener('alpine:init', () => {
+        statusTitleMap: {
+            1: '未対応',
+            2: '対応中',
+            3: '完了'
+        },
+
@@ -123,0 +132,7 @@ document.addEventListener('alpine:init', () => {
+        initData() {
+            this.getLoginUser();
+            this.getQuestions();
+            this.getUsers();
+            this.getTags();
+        },
+
@@ -128,0 +144 @@ document.addEventListener('alpine:init', () => {
+                this.initData()
@@ -179 +195,2 @@ document.addEventListener('alpine:init', () => {
-                if (index === -1) {
+
+                if (index !== -1) {
@@ -195 +212,2 @@ document.addEventListener('alpine:init', () => {
-										return localVal
+                                        if (key === "support") return serverVal; // support.supportStatusの更新が必ずLocalWinになってしまうため例外
+										return localVal;
@@ -276,4 +293,0 @@ document.addEventListener('alpine:init', () => {
-
-            this.getQuestions();
-            this.getUsers();
-            this.getTags();
@@ -293 +307,5 @@ document.addEventListener('alpine:init', () => {
-            return String(q.support.supportStatus.title);
+            const id = Number(q?.support?.supportStatusId);
+            const title = this.statusTitleMap[id];
+
+            this.activeQuestion.support.supportStatus = {id: id, title: title}
+            return title;
@@ -392,0 +411,17 @@ document.addEventListener('alpine:init', () => {
+        /** Form の relatedQuestions 行またはレガシーな数値IDから関連先質問IDを取り出す */
+        relatedQuestionTargetId(rowOrId) {
+            if (rowOrId == null) return 0;
+            if (typeof rowOrId === 'object' && rowOrId.relatedQuestionId != null && rowOrId.relatedQuestionId !== '') {
+                const n = Number(rowOrId.relatedQuestionId);
+                return Number.isNaN(n) ? 0 : n;
+            }
+            const n = Number(rowOrId);
+            return Number.isNaN(n) ? 0 : n;
+        },
+
+        relatedQuestionRowKey(row, idx) {
+            const rid = row && typeof row === 'object' ? String(row.relatedQuestionId ?? '') : '';
+            const idPart = row && typeof row === 'object' && row.id != null ? String(row.id) : '';
+            return `${idPart}-${rid}-${idx}`;
+        },
+
@@ -396 +431,2 @@ document.addEventListener('alpine:init', () => {
-            return rq.includes(rId);
+            const id = Number(rId);
+            return rq.some((row) => this.relatedQuestionTargetId(row) === id);
@@ -404,2 +440,6 @@ document.addEventListener('alpine:init', () => {
-        relatedQuestionLineTitle(rId) {
-            const id = rId;
+        relatedQuestionLineTitle(rowOrId) {
+            const id = this.relatedQuestionTargetId(rowOrId);
+            let title = '';
+            if (rowOrId && typeof rowOrId === 'object' && rowOrId.relatedQuestion && rowOrId.relatedQuestion.title != null) {
+                title = String(rowOrId.relatedQuestion.title);
+            }
@@ -407 +447 @@ document.addEventListener('alpine:init', () => {
-            const t = q?.title != null ? String(q.title) : '';
+            const t = title || (q?.title != null ? String(q.title) : '');
@@ -517 +556,0 @@ document.addEventListener('alpine:init', () => {
-
@@ -529,2 +568 @@ document.addEventListener('alpine:init', () => {
-                relatedQuestions: (question.subQuestions ?? []).map((sq) =>
-                    typeof sq === 'object' && sq !== null ? sq.id : sq),
+                relatedQuestions: this.normalizeRelatedQuestionsForViewModel(question),
@@ -533,0 +572,34 @@ document.addEventListener('alpine:init', () => {
+        normalizeRelatedQuestionsForViewModel(question) {
+            const raw = question.relatedQuestions;
+            if (Array.isArray(raw) && raw.length > 0) {
+                return raw.map((row) => {
+                    if (row && typeof row === 'object' && (row.relatedQuestionId != null || row.relatedQuestion)) {
+                        return {
+                            id: row.id ?? 0,
+                            questionId: row.questionId != null ? String(row.questionId) : String(question.id ?? ''),
+                            relatedQuestionId: row.relatedQuestionId != null ? String(row.relatedQuestionId) : '',
+                            question: row.question ?? null,
+                            relatedQuestion: row.relatedQuestion ?? null,
+                        };
+                    }
+                    const n = typeof row === 'number' ? row : Number(row);
+                    return {
+                        id: 0,
+                        questionId: String(question.id ?? ''),
+                        relatedQuestionId: String(Number.isNaN(n) ? 0 : n),
+                        question: null,
+                        relatedQuestion: null,
+                    };
+                });
+            }
+            return (question.subQuestions ?? []).map((sq) => ({
+                id: 0,
+                questionId: String(question.id ?? ''),
+                relatedQuestionId: String(
+                    typeof sq === 'object' && sq !== null && sq.id != null ? sq.id : sq,
+                ),
+                question: null,
+                relatedQuestion: null,
+            }));
+        },
+
@@ -573 +644,0 @@ document.addEventListener('alpine:init', () => {
-            console.log(index)
@@ -604,2 +675,7 @@ document.addEventListener('alpine:init', () => {
-            if (this.activeQuestion.relatedQuestions.includes(qId)) {
-                this.activeQuestion.relatedQuestions = this.activeQuestion.relatedQuestions.filter(id => id !== qId);
+            const q = this.activeQuestion;
+            const target = Number(qId);
+            const idx = q.relatedQuestions.findIndex(
+                (row) => this.relatedQuestionTargetId(row) === target
+            );
+            if (idx !== -1) {
+                q.relatedQuestions.splice(idx, 1);
@@ -607 +683,7 @@ document.addEventListener('alpine:init', () => {
-                this.activeQuestion.relatedQuestions.push(qId);
+                q.relatedQuestions.push({
+                    id: 0,
+                    questionId: String(q.id),
+                    relatedQuestionId: String(target),
+                    question: null,
+                    relatedQuestion: null,
+                });
@@ -613,0 +696 @@ document.addEventListener('alpine:init', () => {
+
@@ -626,0 +710 @@ document.addEventListener('alpine:init', () => {
+            console.log(list);
@@ -629,0 +714,6 @@ document.addEventListener('alpine:init', () => {
+        async getLoginUser() {
+            const res = await fetch('/api/v1/user/t', { headers: this.apiHeaders(false) });
+            const json = await res.json();
+            this.activeUser = User.fromJSON(json);
+        },
+
@@ -732,0 +823,14 @@ document.addEventListener('alpine:init', () => {
+        calcRemainingTimeAndString(question) {
+            const [hours, minutes] = this.calcRemainingTime(new Date(question.due), this.timeNow)
+            return `${hours}時間${minutes}分`;
+        },
+
+
+        calcRemainingTime(baseDate, d = new Date()) {
+            const diff = baseDate - d;
+            const hours = Math.floor(diff / (1000 * 60 * 60));
+            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
+
+            return [hours, minutes];
+        },
+
diff --git a/static/js/model.js b/static/js/model.js
index 1975569..32ea6ce 100644
--- a/static/js/model.js
+++ b/static/js/model.js
@@ -26 +26 @@ export function formToApiJson(form) {
-      value instanceof Date ? value.toString() : value,
+      value instanceof Date ? value.toISOString() : value,
@@ -137,0 +138,2 @@ export class User {
+   * @param {string}   memo
+   * @param {string}   pass
@@ -144 +146 @@ export class User {
-  constructor(id, name, email, roleId, role = null, supports = [], answers = [], memos = []) {
+  constructor(id, name, email, memo, pass, roleId, role = null, supports = [], answers = [], memos = []) {
@@ -147,0 +150,2 @@ export class User {
+    this.memo = memo;
+    this.pass = pass;
@@ -157,7 +160,0 @@ export class User {
-    // const rid = json.roleId;
-    // const roleIdNum = 
-    //   rid === null || rid === undefined || rid === ''
-    //     ? rid
-    //     : typeof rid === 'string'
-    //       ? parseInt(rid, 10)
-    //       : rid;
@@ -167,0 +165 @@ export class User {
+      json.memo ?? '',
@@ -482,0 +481,36 @@ export class Notice {
+// ---------------------------------------------------------------------------
+// RelatedQuestion  (中間テーブル related_questions)
+// ---------------------------------------------------------------------------
+export class RelatedQuestion {
+  /**
+   * @param {number} id
+   * @param {string} questionId
+   * @param {string} relatedQuestionId
+   * @param {Question|null} [question]
+   * @param {Question|null} [relatedQuestion]
+   */
+  constructor(id, questionId, relatedQuestionId, question = null, relatedQuestion = null) {
+    this.id = id;
+    this.questionId = questionId;
+    this.relatedQuestionId = relatedQuestionId;
+    this.question = question;
+    this.relatedQuestion = relatedQuestion;
+  }
+
+  /** @param {Object} json @returns {RelatedQuestion} */
+  static fromJSON(json) {
+    return new RelatedQuestion(
+      json.id ?? 0,
+      json.questionId != null ? String(json.questionId) : '',
+      json.relatedQuestionId != null ? String(json.relatedQuestionId) : '',
+      json.question ? Question.fromJSON(json.question) : null,
+      json.relatedQuestion ? Question.fromJSON(json.relatedQuestion) : null,
+    );
+  }
+
+  /** @returns {Object} */
+  static toModel(form) {
+    return formToApiJson(form);
+  }
+}
+
@@ -488,13 +521,0 @@ export class Question {
-<<<<<<< HEAD
-<<<<<<< Updated upstream
- * @param {number}         id
- * @param {number|null}    originQuestionId
- * @param {number|null}    supportId
- * @param {string}         title
- * @param {string}         content
- * @param {string|null}    due              ISO8601 文字列 or null
- * @param {string}         createdAt        ISO8601 文字列
-=======
-   * @param {number}         id
-   * @param {number|null}    messageId
-=======
@@ -502 +522,0 @@ export class Question {
->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
@@ -511,4 +530,0 @@ export class Question {
-<<<<<<< HEAD
->>>>>>> Stashed changes
-=======
->>>>>>> 732376d4ae14134f782ec669ad4e3382eae5f953
@@ -523,0 +540 @@ export class Question {
+   * @param {RelatedQuestion[]} [relatedQuestions]
@@ -529 +546 @@ export class Question {
-    escalationsFrom = [], escalationsTo = [],
+    escalationsFrom = [], escalationsTo = [], relatedQuestions = [],
@@ -548,0 +566 @@ export class Question {
+    this.relatedQuestions = relatedQuestions;
@@ -575,0 +594 @@ export class Question {
+      (json.relatedQuestions ?? []).map(RelatedQuestion.fromJSON),
diff --git a/static/js/sse.js b/static/js/sse.js
index 77bca90..370546e 100644
--- a/static/js/sse.js
+++ b/static/js/sse.js
@@ -91,11 +91,6 @@ document.addEventListener('DOMContentLoaded', () => {
-    const bindSSE = (eventNames, fromJSON, normalizedEvent, deleteEvent = null) => {
-        eventNames.forEach((eventName) => {
-            es.addEventListener(eventName, (event) => {
-                try {
-                    if (deleteEvent && eventName.startsWith('delete-')) {
-                        const id = Number(event.data);
-                        document.dispatchEvent(new CustomEvent(deleteEvent, {
-                            detail: Number.isNaN(id) ? event.data : id,
-                        }));
-                        return;
-                    }
+    bindSSE(es, SSE_KEY.notice(true), Notice.fromJSON, SSE_KEY.data.create.notice, SSE_KEY.data.delete.notice);
+    bindSSE(es, SSE_KEY.question(true), Question.fromJSON, SSE_KEY.data.create.question, SSE_KEY.data.delete.question);
+    bindSSE(es, SSE_KEY.question(true), Question.fromJSON, SSE_KEY.data.update.question, SSE_KEY.data.delete.question);
+    bindSSE(es, SSE_KEY.user(true), User.fromJSON, SSE_KEY.data.create.user, SSE_KEY.data.delete.user);
+    bindSSE(es, SSE_KEY.tag(true), Tag.fromJSON, SSE_KEY.data.create.tag, SSE_KEY.data.delete.tag);
+});
@@ -103,3 +98,8 @@ document.addEventListener('DOMContentLoaded', () => {
-                    const model = fromJSON(JSON.parse(event.data));
-                    document.dispatchEvent(new CustomEvent(normalizedEvent, {
-                        detail: model,
+const bindSSE = (eventSource, eventNames, fromJSON, normalizedEvent, deleteEvent = null) => {
+    eventNames.forEach((eventName) => {
+        eventSource.addEventListener(eventName, (event) => {
+            try {
+                if (deleteEvent && eventName.startsWith('delete-')) {
+                    const id = Number(event.data);
+                    document.dispatchEvent(new CustomEvent(deleteEvent, {
+                        detail: Number.isNaN(id) ? event.data : id,
@@ -107,2 +107 @@ document.addEventListener('DOMContentLoaded', () => {
-                } catch (fail) {
-                    console.log(fail);
+                    return;
@@ -110,3 +108,0 @@ document.addEventListener('DOMContentLoaded', () => {
-            });
-        });
-    };
@@ -114,5 +110,11 @@ document.addEventListener('DOMContentLoaded', () => {
-    bindSSE(SSE_KEY.notice(true), Notice.fromJSON, SSE_KEY.data.create.notice, SSE_KEY.data.delete.notice);
-    bindSSE(SSE_KEY.question(true), Question.fromJSON, SSE_KEY.data.create.question, SSE_KEY.data.delete.question);
-    bindSSE(SSE_KEY.user(true), User.fromJSON, SSE_KEY.data.create.user, SSE_KEY.data.delete.user);
-    bindSSE(SSE_KEY.tag(true), Tag.fromJSON, SSE_KEY.data.create.tag, SSE_KEY.data.delete.tag);
-});
+                const model = fromJSON(JSON.parse(event.data));
+                console.log(eventName, model)
+                document.dispatchEvent(new CustomEvent(normalizedEvent, {
+                    detail: model,
+                }));
+            } catch (fail) {
+                console.log(fail);
+            }
+        });
+    });
+};
\ No newline at end of file
diff --git a/templates/components/login_head.html b/templates/components/login_head.html
index 352793f..b2fad6e 100644
--- a/templates/components/login_head.html
+++ b/templates/components/login_head.html
@@ -23,0 +24,6 @@
+            init() {
+                if (typeof lucide !== 'undefined') {
+                    lucide.createIcons();
+                }
+            },
+
@@ -51 +57 @@
-{{end}}
+{{end}}
\ No newline at end of file
diff --git a/templates/components/mock5_sidebar.html b/templates/components/mock5_sidebar.html
index 856005d..5f9ccb9 100644
--- a/templates/components/mock5_sidebar.html
+++ b/templates/components/mock5_sidebar.html
@@ -49,0 +50 @@
+            :src="`https://api.dicebear.com/7.x/notionists/svg?seed=${activeUser.name}&backgroundColor=eff6ff`"
@@ -53 +54 @@
-            <div class="text-sm font-bold text-gray-800 truncate">自分</div>
+            <div class="text-sm font-bold text-gray-800 truncate" x-text="activeUser.name">自分</div>
@@ -55 +56 @@
-                x-text="isManager ? 'Manager' : 'Creator'"></div>
+                x-text="activeUser.role.roleName"></div>
diff --git a/templates/components/mock5_view_detail.html b/templates/components/mock5_view_detail.html
index 3f5c908..9d37bb8 100644
--- a/templates/components/mock5_view_detail.html
+++ b/templates/components/mock5_view_detail.html
@@ -34,5 +33,0 @@
-                <div class="flex gap-2 mb-3">
-                    <button
-                        class="text-[10px] font-bold bg-slate-800 text-white px-3 py-1 rounded shadow-sm">質問者に返信</button>
-                    <button class="text-[10px] font-bold bg-gray-100 text-gray-500 px-3 py-1 rounded">内部メモ</button>
-                </div>
@@ -42,3 +37,9 @@
-                <div class="mt-3 flex justify-end">
-                    <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md"
-                        @click="alert('送信しました'); setView('home')">送信</button>
+                <div class="flex gap-1 flex-row-reverse">
+                    <div class="mt-3 mr-5 flex justify-end">
+                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md"
+                        @click="alert('送信しました'); setView('home')">メモ登録</button>
+                    </div>
+                    <div class="mt-3 flex justify-end">
+                        <button class="btn btn-primary btn-sm bg-slate-800 border-none font-bold px-6 shadow-md"
+                        @click="alert('送信しました'); setView('home')">回答送信</button>
+                    </div>
@@ -63 +63,0 @@
-
@@ -187 +187 @@
-                    <template x-for="rId in activeQuestion.relatedQuestions" :key="rId">
+                    <template x-for="(rqRow, rqIdx) in activeQuestion.relatedQuestions" :key="relatedQuestionRowKey(rqRow, rqIdx)">
@@ -191,2 +191,2 @@
-                                x-text="relatedQuestionLineTitle(rId)"
-                                @click="openDetail(findQuestionById(rId))"></span>
+                                x-text="relatedQuestionLineTitle(rqRow)"
+                                @click="openDetail(findQuestionById(relatedQuestionTargetId(rqRow)))"></span>
@@ -194 +194 @@
-                                @click="toggleRelatedQuestion(rId); refreshIcons()"></i>
+                                @click="toggleRelatedQuestion(relatedQuestionTargetId(rqRow)); refreshIcons()"></i>
diff --git a/templates/components/mock5_view_notice.html b/templates/components/mock5_view_notice.html
index 083c952..dc21ed8 100644
--- a/templates/components/mock5_view_notice.html
+++ b/templates/components/mock5_view_notice.html
@@ -40 +40 @@
-{{end}}
+{{end}}
\ No newline at end of file
diff --git a/templates/components/mock5_view_settings.html b/templates/components/mock5_view_settings.html
index 98b713b..964831f 100644
--- a/templates/components/mock5_view_settings.html
+++ b/templates/components/mock5_view_settings.html
@@ -9,0 +10 @@
+                    :src="`https://api.dicebear.com/7.x/notionists/svg?seed=${activeUser.name}&backgroundColor=eff6ff`"
@@ -11 +12,3 @@
-                <button class="btn btn-sm btn-outline font-bold shadow-sm">画像をアップロード</button>
+                <div class="lg:tooltip-top" data-tip="現在未実装です。今後のアップデートで追加予定です。">
+                    <button class="btn btn-sm btn-outline font-bold shadow-sm disabled">画像をアップロード</button>
+                </div>
@@ -14,2 +17,35 @@
-                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">氏名</label><input
-                        type="text" value="自分" class="input input-bordered w-full text-sm font-bold shadow-inner" />
+                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">氏名</label>
+                    <input type="text" value="自分" class="input input-bordered w-full text-sm font-bold shadow-inner"
+                        x-model="activeUser.name" />
+                </div>
+                <div x-data="{ isValid: false }"><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">メールアドレス</label><input
+                        type="email" value="example@example.mail.com"
+                        @input="isValid = /^\S+@\S+\.\S+$/.test(activeUser.email)"
+                        class="input input-bordered w-full text-sm font-bold shadow-inner" x-model="activeUser.email"
+                        :class="!isValid ? 'input-error' : 'input-primary'" />
+                </div>
+                <div>
+                    <label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">新しいパスワード</label>
+                    <div x-data="{ show: false }" class="relative flex items-center mt-2">
+                        <input type="password" placeholder="変更する場合のみ入力"
+                            class="input input-bordered w-full text-sm shadow-inner" :type="show ? 'text' : 'password'"
+                            x-model="activeUser.pass" />
+                        <button type="button"
+                            class="absolute right-2 bg-transparent flex items-center justify-center hover:text-blue-600"
+                            @click="show = !show">
+                            <svg x-show="!show" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"
+                                xmlns="http://www.w3.org/2000/svg">
+                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+                                    d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21">
+                                </path>
+                            </svg>
+                            <svg x-show="show" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"
+                                xmlns="http://www.w3.org/2000/svg" style="display: none;">
+                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+                                    d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
+                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+                                    d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z">
+                                </path>
+                            </svg>
+                        </button>
+                    </div>
@@ -17,6 +52,0 @@
-                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">メールアドレス</label><input
-                        type="email" value="jinji.taro@example.com"
-                        class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
-                <div><label class="block text-[10px] font-bold text-gray-400 uppercase mb-1.5">新しいパスワード</label><input
-                        type="password" placeholder="変更する場合のみ入力"
-                        class="input input-bordered w-full text-sm font-bold shadow-inner" /></div>
@@ -24,2 +54,2 @@
-                        class="textarea textarea-bordered w-full h-24 text-sm font-bold shadow-inner"
-                        placeholder="管理担当のメモなど..."></textarea></div>
+                        class="textarea textarea-bordered w-full h-24 text-sm font-bold shadow-inner" x-model="activeUser.memo"
+                        placeholder="対応のメモなど..."></textarea></div>
@@ -31 +61 @@
-                @click="setView('home'); alert('設定を保存しました')">保存する</button>
+                @click="updateUser()">保存する</button>
diff --git a/templates/sample/index.html b/templates/sample/index.html
index 8dd5113..830fbcb 100644
--- a/templates/sample/index.html
+++ b/templates/sample/index.html
@@ -2,458 +1,0 @@
-
-<html lang="ja" data-theme="cupcake">
-
-<head>
-    <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>{{.Title}}</title>
-    <link href="/static/css/output.css" rel="stylesheet">
-
-    <!--
-        module スクリプトは defer 扱いのため、後続の defer Alpine より先に実行される。
-        alpine:init リスナーを登録 → Alpine が初期化時に発火 → Alpine.data() が登録される。
-    -->
-    <script type="module">
-        import { Question } from '/static/js/model.js';
-
-        document.addEventListener('alpine:init', () => {
-            Alpine.data('questionBoard', () => ({
-                questions: [],
-                questionIDMap: new Map([]),
-                filter: '',
-                connected: false,
-
-                form: { content: '', due: '' },
-                submitting: false,
-                formError: '',
-
-                currentView: 'home',
-
-                questionDetail: {},
-
-                async getQuestionDetail(id) {
-                    const res = await fetch(`/api/question/${id}`);
-                    const body = await res.json();
-                    this.questionDetail = Question.fromJSON(body);
-                },
-
-                async deleteQuestion(id) {
-                    const res = await fetch(`/api/question/${id}`, {
-                        method: "DELETE"
-                    }).then(v => {
-                        if (v.status == 200) {
-                            this.questions = this.questions.filter(q => q.id != id);
-                            this.closeDetail()
-                        } else {
-                            this.formError = '質問データの削除に失敗しました。'
-                        }
-                    });
-                },
-
-                openDetail(id) {
-                    this.getQuestionDetail(id)
-                    this.drawCommunication(id)
-                    document.getElementById('question-detail-modal').showModal();
-                },
-                closeDetail() {
-                    document.getElementById('question-detail-modal').close();
-                },
-
-                openForm() {
-                    this.form = { title: '', content: '', due: '' };
-                    this.formError = '';
-                    document.getElementById('question-form-modal').showModal();
-                },
-                closeForm() {
-                    document.getElementById('question-form-modal').close();
-                },
-                async submitForm() {
-                    if (!this.form.content.trim()) {
-                        this.formError = '内容を入力してください';
-                        return;
-                    }
-                    this.submitting = true;
-                    this.formError = '';
-                    try {
-                        const body = { content: this.form.content.trim(), title: this.form.title.trim() };
-                        if (this.form.due) body.due = `${this.form.due}:00+09:00`;
-                        const res = await fetch('/api/questions', {
-                            method: 'POST',
-                            headers: { 'Content-Type': 'application/json' },
-                            body: JSON.stringify(body),
-                        });
-                        if (!res.ok) throw new Error(`HTTP ${res.status}`);
-                        this.closeForm();
-                    } catch (err) {
-                        this.formError = `送信に失敗しました (${err.message})`;
-                    } finally {
-                        this.submitting = false;
-                    }
-                },
-
-                get filteredQuestions() {
-                    const f = this.filter.toLowerCase().trim();
-                    if (!f) return this.questions;
-                    return this.questions.filter(q =>
-                        q.title.toLowerCase().includes(f) ||
-                        (q.tags ?? []).some(t => t.title.toLowerCase().includes(f))
-                    );
-                },
-
-                isIncludeMap(id) {
-                    return this.questionIDMap[id] == undefined ? false : true;
-                },
-
-                init() {
-                    const es = new EventSource('/sse');
-                    es.onopen = () => { this.connected = true; };
-                    es.onerror = () => { this.connected = false; };
-                    es.addEventListener('question', (e) => {
-                        try {
-                            const q = Question.fromJSON(JSON.parse(e.data));
-                            if (!this.isIncludeMap(q.id)) {
-                                this.questionIDMap[q.id] = true;
-                                this.questions.unshift(q);
-                            }
-                        } catch (err) {
-                            console.error('Failed to parse question:', err);
-                        }
-                    });
-                },
-
-                formatDate(d) {
-                    if (!d) return null;
-                    return new Intl.DateTimeFormat('ja-JP', {
-                        year: 'numeric', month: '2-digit', day: '2-digit',
-                        hour: '2-digit', minute: '2-digit',
-                    }).format(new Date(d));
-                },
-            }));
-        });
-    </script>
-
-    <script defer src="/static/vendor/htmx.min.js"></script>
-    <script defer src="/static/vendor/sse.min.js"></script>
-    <script defer src="/static/vendor/alpine.min.js"></script>
-</head>
-
-<body class="min-h-screen bg-base-200">
-
-    <!-- ========== ヘッダー ========== -->
-    <header class="sticky top-0 z-10 bg-base-100 shadow-sm">
-        <div class="max-w-6xl mx-auto px-6 py-3 flex items-center justify-between gap-4">
-
-            <h1 class="text-xl font-bold text-primary tracking-tight">Question Board</h1>
-
-            <div class="flex items-center gap-3">
-                <!-- 接続状態インジケーター -->
-                <div x-data x-cloak class="hidden"><!-- Alpine 読み込み前の FOUC 防止 --></div>
-
-                <!-- サーバー時刻 (htmx SSE) -->
-                <div hx-ext="sse" sse-connect="/sse">
-                    <span class="font-mono text-sm text-base-content/60" sse-swap="time-tick" hx-swap="innerHTML">
-                        --:--:--
-                    </span>
-                </div>
-            </div>
-
-        </div>
-    </header>
-
-    <!-- ========== メインコンテンツ ========== -->
-    <main class="max-w-6xl mx-auto px-6 py-8" x-data="questionBoard()" x-init="init()">
-        <!-- ---- フィルターバー ---- -->
-        <div class="mb-6 flex flex-col sm:flex-row items-stretch sm:items-center gap-3 flex-wrap">
-
-            <!-- 検索ボックス -->
-            <label class="flex-1 input input-bordered flex items-center gap-2">
-                <!-- search icon -->
-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0 opacity-50" fill="none"
-                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
-                    <path stroke-linecap="round" stroke-linejoin="round"
-                        d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
-                </svg>
-                <input type="text" x-model="filter" placeholder="タイトル・タグで絞り込み..."
-                    class="grow outline-none bg-transparent text-sm" />
-                <!-- クリアボタン -->
-                <button x-show="filter !== ''" @click="filter = ''" class="btn btn-ghost btn-xs btn-circle"
-                    aria-label="クリア">
-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24"
-                        stroke="currentColor" stroke-width="2.5">
-                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
-                    </svg>
-                </button>
-            </label>
-
-            <!-- 質問追加ボタン -->
-            <button @click="openForm()" class="btn btn-primary btn-sm gap-1.5 shrink-0">
-                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
-                    stroke="currentColor" stroke-width="2.5">
-                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
-                </svg>
-                質問を追加
-            </button>
-
-            <!-- 件数バッジ + 接続状態 -->
-            <div class="flex items-center gap-2 shrink-0">
-                <span class="badge badge-primary badge-outline font-mono"
-                    x-text="`${filteredQuestions.length} 件`"></span>
-
-                <!-- 接続インジケーター -->
-                <div class="flex items-center gap-1 text-xs" :class="connected ? 'text-success' : 'text-error'">
-                    <span class="inline-block w-2 h-2 rounded-full animate-pulse"
-                        :class="connected ? 'bg-success' : 'bg-error'"></span>
-                    <span x-text="connected ? 'LIVE' : 'disconnected'"></span>
-                </div>
-            </div>
-
-        </div>
-
-        <!-- ---- カードグリッド ---- -->
-        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
-
-            <template x-for="q in filteredQuestions" :key="q.id">
-                <div x-transition:enter="transition ease-out duration-300"
-                    x-transition:enter-start="opacity-0 scale-95 translate-y-2"
-                    x-transition:enter-end="opacity-100 scale-100 translate-y-0"
-                    x-transition:leave="transition ease-in duration-200"
-                    x-transition:leave-start="opacity-100 scale-100 translate-y-0"
-                    x-transition:leave-end="opacity-0 scale-95 translate-y-2"
-                    class="card bg-base-100 shadow hover:shadow-lg transition-shadow duration-200 cursor-pointer"
-                    @click="openDetail(q.id)">
-                    <div class="card-body gap-3 p-5">
-
-                        <!-- タイトル + ID -->
-                        <div class="flex items-start justify-between gap-2">
-                            <h2 class="card-title text-sm leading-snug" x-text="q.title"></h2>
-                            <span class="badge badge-ghost badge-sm shrink-0 font-mono" x-text="`#${q.id}`"></span>
-                        </div>
-
-                        <div class="flex items-start gap-2">
-                            <span x-text="q.content"></span>
-                        </div>
-
-                        <!-- 期限 -->
-                        <template x-if="q.due">
-                            <div class="flex items-center gap-1 text-xs text-warning font-medium">
-                                <!-- clock icon -->
-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0" fill="none"
-                                    viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
-                                    <path stroke-linecap="round" stroke-linejoin="round"
-                                        d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />
-                                </svg>
-                                <span x-text="`期限: ${formatDate(q.due)}`"></span>
-                            </div>
-                        </template>
-
-                        <!-- サポートステータス -->
-                        <template x-if="q.support && q.support.supportStatus">
-                            <div class="flex items-center gap-1 text-xs">
-                                <!-- tag icon -->
-                                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 shrink-0 opacity-60"
-                                    fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
-                                    <path stroke-linecap="round" stroke-linejoin="round"
-                                        d="M7 7h.01M7 3h14a1 1 0 0 1 1 1v9.586a1 1 0 0 1-.293.707l-7 7a1 1 0 0 1-1.414 0l-9.586-9.586A1 1 0 0 1 3 11V4a1 1 0 0 1 1-1h3z" />
-                                </svg>
-                                <span class="badge badge-secondary badge-sm"
-                                    x-text="q.support.supportStatus.title"></span>
-                            </div>
-                        </template>
-
-                        <!-- タグ一覧 -->
-                        <template x-if="q.tags && q.tags.length > 0">
-                            <div class="flex flex-wrap gap-1">
-                                <template x-for="tag in q.tags" :key="tag.id">
-                                    <span class="badge badge-outline badge-xs" x-text="tag.title"></span>
-                                </template>
-                            </div>
-                        </template>
-
-                        <!-- フッター: 作成日時 -->
-                        <div class="mt-auto pt-2 border-t border-base-200 flex justify-end">
-                            <span class="text-xs text-base-content/40 font-mono"
-                                x-text="formatDate(q.createdAt)"></span>
-                        </div>
-
-                    </div>
-                </div>
-            </template>
-
-        </div>
-
-        <!-- ---- 空状態 ---- -->
-        <div x-show="filteredQuestions.length === 0" x-transition:enter="transition ease-out duration-300"
-            x-transition:enter-start="opacity-0 translate-y-2" x-transition:enter-end="opacity-100 translate-y-0"
-            x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100"
-            x-transition:leave-end="opacity-0"
-            class="flex flex-col items-center justify-center py-24 gap-4 text-base-content/40">
-
-            <!-- フィルター結果なし -->
-            <template x-if="filter !== ''">
-                <div class="flex flex-col items-center gap-2">
-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 opacity-30" fill="none" viewBox="0 0 24 24"
-                        stroke="currentColor" stroke-width="1.5">
-                        <path stroke-linecap="round" stroke-linejoin="round"
-                            d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
-                    </svg>
-                    <p class="text-sm">「<span class="font-semibold" x-text="filter"></span>」に一致する Question が見つかりません</p>
-                    <button @click="filter = ''" class="btn btn-ghost btn-xs mt-1">
-                        フィルターをクリア
-                    </button>
-                </div>
-            </template>
-
-            <!-- SSE 待機中 -->
-            <template x-if="filter === '' && questions.length === 0">
-                <div class="flex flex-col items-center gap-3">
-                    <span class="loading loading-ring loading-lg text-primary opacity-50"></span>
-                    <p class="text-sm">SSE から Question を待機中...</p>
-                </div>
-            </template>
-
-        </div>
-
-        <!-- ---- 質問入力モーダル ---- -->
-        <dialog id="question-form-modal" class="modal modal-bottom sm:modal-middle">
-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
-                x-transition:leave="transition ease-in duration-150"
-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
-
-                <!-- ヘッダー -->
-                <div class="flex items-center justify-between mb-5">
-                    <h3 class="font-bold text-lg">質問を追加</h3>
-                    <button @click="closeForm()" class="btn btn-ghost btn-sm btn-circle" aria-label="閉じる">
-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
-                            stroke="currentColor" stroke-width="2.5">
-                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
-                        </svg>
-                    </button>
-                </div>
-
-                <!-- フォーム -->
-                <form @submit.prevent="submitForm()" class="flex flex-col gap-4" novalidate>
-                    <!-- タイトル -->
-                    <label class="form-control">
-                        <div class="label pb-1">
-                            <span class="label-text font-medium">
-                                タイトル
-                                <span class="text-error ml-0.5">*</span>
-                            </span>
-                        </div>
-                        <textarea x-model="form.title" placeholder="質問のタイトルを入力..." rows="3"
-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
-                            :class="formError && !form.title.trim() ? 'textarea-error' : ''"
-                            :disabled="submitting"></textarea>
-                    </label>
-
-                    <!-- 内容 -->
-                    <label class="form-control">
-                        <div class="label pb-1">
-                            <span class="label-text font-medium">
-                                内容
-                                <span class="text-error ml-0.5">*</span>
-                            </span>
-                        </div>
-                        <textarea x-model="form.content" placeholder="質問の内容を入力..." rows="3"
-                            class="textarea textarea-bordered w-full resize-none focus:outline-none"
-                            :class="formError && !form.content.trim() ? 'textarea-error' : ''"
-                            :disabled="submitting"></textarea>
-                    </label>
-
-                    <!-- 期日 -->
-                    <label class="form-control">
-                        <div class="label pb-1">
-                            <span class="label-text font-medium">期日</span>
-                            <span class="label-text-alt text-base-content/40">任意</span>
-                        </div>
-                        <input type="datetime-local" x-model="form.due"
-                            class="input input-bordered w-full focus:outline-none" :disabled="submitting" />
-                    </label>
-
-                    <!-- エラーメッセージ -->
-                    <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
-                        x-transition:enter-start="opacity-0 -translate-y-1"
-                        x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
-                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
-                            stroke="currentColor" stroke-width="2">
-                            <path stroke-linecap="round" stroke-linejoin="round"
-                                d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
-                        </svg>
-                        <span x-text="formError"></span>
-                    </div>
-
-                    <!-- アクション -->
-                    <div class="modal-action mt-1">
-                        <button type="button" @click="closeForm()" class="btn btn-ghost" :disabled="submitting">
-                            キャンセル
-                        </button>
-                        <button type="submit" class="btn btn-primary gap-2" :disabled="submitting">
-                            <span x-show="submitting" class="loading loading-spinner loading-xs"></span>
-                            <span x-text="submitting ? '送信中...' : '送信'"></span>
-                        </button>
-                    </div>
-
-                </form>
-            </div>
-
-            <!-- バックドロップクリックで閉じる -->
-            <form method="dialog" class="modal-backdrop">
-                <button @click="closeForm()">close</button>
-            </form>
-        </dialog>
-
-        <!-- ---- 詳細モーダル ---- -->
-        <dialog id="question-detail-modal" class="modal modal-bottom sm:modal-middle">
-            <div class="modal-box" x-transition:enter="transition ease-out duration-200"
-                x-transition:enter-start="opacity-0 scale-95 translate-y-4"
-                x-transition:enter-end="opacity-100 scale-100 translate-y-0"
-                x-transition:leave="transition ease-in duration-150"
-                x-transition:leave-start="opacity-100 scale-100 translate-y-0"
-                x-transition:leave-end="opacity-0 scale-95 translate-y-4">
-
-                <div class="flex items-center justify-between mb-5">
-                    <span class="card-title" x-text="questionDetail.title"></span>
-                </div>
-                <hr />
-
-                <div class="flex items-center justify-between mb-2">
-                    <span class="card-body" x-text="questionDetail.content"></span>
-                </div>
-
-                <div class="flex items-center justify-between mb-5">
-                    <textarea name="raw-json" id="raw-json" rows="30" cols="50"
-                        x-text="JSON.stringify(questionDetail)"></textarea>
-                </div>
-
-                <!-- エラーメッセージ -->
-                <div x-show="formError !== ''" x-transition:enter="transition ease-out duration-200"
-                    x-transition:enter-start="opacity-0 -translate-y-1"
-                    x-transition:enter-end="opacity-100 translate-y-0" class="alert alert-error py-2 text-sm">
-                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24"
-                        stroke="currentColor" stroke-width="2">
-                        <path stroke-linecap="round" stroke-linejoin="round"
-                            d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
-                    </svg>
-                    <span x-text="formError"></span>
-                </div>
-
-                <div class="flex items-end justify-between mb-5">
-                    <button class="btn btn-circle" @click="deleteQuestion(questionDetail.id)">削除</button>
-                </div>
-            </div>
-
-            <!-- バックドロップクリックで閉じる -->
-            <form method="dialog" class="modal-backdrop">
-                <button @click="closeDetail()">close</button>
-            </form>
-        </dialog>
-    </main>
-
-</body>
-
->>>>>>> Stashed changes
-</html>
-=======
-<!DOCTYPE html>
diff --git a/templates/sample/mock4.5.html b/templates/sample/mock4.5.html
index ce76df5..830fbcb 100644
--- a/templates/sample/mock4.5.html
+++ b/templates/sample/mock4.5.html
@@ -30 +30 @@
-</html>
+</html>
\ No newline at end of file
diff --git a/templates/sample/questionboard.html b/templates/sample/questionboard.html
new file mode 100644
index 0000000..627f378
--- /dev/null
+++ b/templates/sample/questionboard.html
@@ -0,0 +1,135 @@
+<!DOCTYPE html>
+<html lang="ja" data-theme="cupcake">
+
+<head>
+    <meta charset="UTF-8">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <title>{{.Title}}</title>
+    <link href="/static/css/output.css" rel="stylesheet">
+
+    <!--
+        module スクリプトは defer 扱いのため、後続の defer Alpine より先に実行される。
+        alpine:init リスナーを登録 → Alpine が初期化時に発火 → Alpine.data() が登録される。
+    -->
+    <script type="module">
+        import { Question } from '/static/js/model.js';
+
+        document.addEventListener('alpine:init', () => {
+            Alpine.data('questionBoard', () => ({
+                questions: [],
+                questionIDMap: new Map([]),
+                filter: '',
+                connected: false,
+
+                form: {content: '', due: '' },
+                submitting: false,
+                formError: '',
+
+                currentView: 'home',
+
+                questionDetail: {},
+
+                async getQuestionDetail(id) {
+                    const res = await fetch(`/api/v1/question/${id}`);
+                    const body = await res.json();
+                    this.questionDetail = Question.fromJSON(body);
+                },
+
+                async deleteQuestion(id) {
+                    const res = await fetch(`/api/v1/question/${id}`, {
+                        method: "DELETE"
+                    }).then(v => {
+                        if (v.status == 200) {
+                            this.questions = this.questions.filter(q => q.id != id);
+                            this.closeDetail()
+                        } else {
+                            this.formError = '質問データの削除に失敗しました。'
+                        }
+                    });
+                },
+
+                openDetail(id) {
+                    this.getQuestionDetail(id)
+                    document.getElementById('question-detail-modal').showModal();
+                },
+                closeDetail() {
+                    document.getElementById('question-detail-modal').close();
+                },
+
+                openForm() {
+                    this.form = { title: '', content: '', due: '' };
+                    this.formError = '';
+                    document.getElementById('question-form-modal').showModal();
+                },
+                closeForm() {
+                    document.getElementById('question-form-modal').close();
+                },
+                async submitForm() {
+                    if (!this.form.content.trim()) {
+                        this.formError = '内容を入力してください';
+                        return;
+                    }
+                    this.submitting = true;
+                    this.formError = '';
+                    try {
+                        const body = { content: this.form.content.trim(), title: this.form.title.trim() };
+                        if (this.form.due) body.due = `${this.form.due}:00+09:00`;
+                        const res = await fetch('/api/v1/question', {
+                            method: 'POST',
+                            headers: { 'Content-Type': 'application/json' },
+                            body: JSON.stringify(body),
+                        });
+                        if (!res.ok) throw new Error(`HTTP ${res.status}`);
+                        this.closeForm();
+                    } catch (err) {
+                        this.formError = `送信に失敗しました (${err.message})`;
+                    } finally {
+                        this.submitting = false;
+                    }
+                },
+
+                get filteredQuestions() {
+                    const f = this.filter.toLowerCase().trim();
+                    if (!f) return this.questions;
+                    return this.questions.filter(q =>
+                        q.title.toLowerCase().includes(f) ||
+                        (q.tags ?? []).some(t => t.title.toLowerCase().includes(f))
+                    );
+                },
+
+                isIncludeMap(id) {
+                    return this.questionIDMap[id] == undefined ? false : true;
+                },
+
+                init() {
+                    const es = new EventSource('/sse');
+                    es.onopen = () => { this.connected = true; };
+                    es.onerror = () => { this.connected = false; };
+                    es.addEventListener('question', (e) => {
+                        try {
+                            const q = Question.fromJSON(JSON.parse(e.data));
+                            if (!this.isIncludeMap(q.id)) {
+                                this.questionIDMap[q.id] = true;
+                                this.questions.unshift(q);
+                            }
+                        } catch (err) {
+                            console.error('Failed to parse question:', err);
+                        }
+                    });
+                },
+
+                formatDate(d) {
+                    if (!d) return null;
+                    return new Intl.DateTimeFormat('ja-JP', {
+                        year: 'numeric', month: '2-digit', day: '2-digit',
+                        hour: '2-digit', minute: '2-digit',
+                    }).format(new Date(d));
+                },
+            }));
+        });
+    </script>
+
+    <script defer src="/static/vendor/htmx.min.js"></script>
+    <script defer src="/static/vendor/sse.min.js"></script>
+    <script defer src="/static/vendor/alpine.min.js"></script>
+</head>
\ No newline at end of file
