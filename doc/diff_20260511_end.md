diff --git a/crypto/sha256.go b/crypto/sha256.go
index 3cc2952..af19cbc 100644
--- a/crypto/sha256.go
+++ b/crypto/sha256.go
@@ -9,0 +10,8 @@ import (
+func EncryptPassword(pass string) string {
+	var password = pass
+	for cnt := len(pass)/2 + 3; cnt > 0; cnt-- {
+		password = EncryptSHA256(password)
+	}
+	return password
+}
+
diff --git a/doc/db/er.md b/doc/db/er.md
index c334420..45db1ae 100644
--- a/doc/db/er.md
+++ b/doc/db/er.md
@@ -29,4 +28,0 @@ erDiagram
-<<<<<<< HEAD
-        number support_id FK
-=======
->>>>>>> add-mock
@@ -176 +171,0 @@ erDiagram
-<<<<<<< HEAD
@@ -178,3 +172,0 @@ erDiagram
-=======
-    }
->>>>>>> add-mock
diff --git a/doc/meetup_esgprah.md b/doc/meetup_esgprah.md
index 12581cb..643620e 100644
--- a/doc/meetup_esgprah.md
+++ b/doc/meetup_esgprah.md
@@ -14 +13,0 @@ USERS {
-    number team_id FK
@@ -15,0 +15 @@ USERS {
+    number mentor_id FK
@@ -17,5 +17,3 @@ USERS {
-%% 部長職を兼任しているケースをカバーするためHasManyの関係
-USERS ||--|{ DEPARTMENTS: "ユーザは必ず１つ以上の部署に所属"
-USERS ||--o{ TEAMS: "ユーザは０以上のチームに所属"
-USERS ||--|| AUTHORITIES: "ユーザは１つの権限設定を持つ"
-USERS ||--|| ROLES: "ユーザは１つのロールを持つ"
+USERS ||--o| USERS: "ユーザは０か１人のメンターを指定される（mentor_id）"
+USERS ||--o{ TEAM_MEMBERS: "管理者もメンバーの１人として扱う想定のため必ず１人以上となる"
+USERS ||--o{ ANSWERS: "ユーザは０以上の回答を持つ（質問ごとに回答が存在するため）"
@@ -26,0 +25 @@ DEPARTMENTS {
+    number boss_id FK
@@ -28,8 +27 @@ DEPARTMENTS {
-
-%% 1on1の関係を紐づけるためのテーブル
-%% Mentorとしては０以上の関係、Menteeとしては１対１の関係
-PEERINGS {
-    number id PK
-    number mentor_id FK "1on1を実施する側のユーザID"
-    number mentee_id FK "1on1を受ける側のユーザID"
-}
+DEPARTMENTS ||--o{ USERS: "部署は必ず１人の部長（管理者）が存在する"
@@ -42,2 +34,9 @@ TEAMS {
-    number boss_id FK "上司のユーザID"
-    number member_id FK "部下のユーザID"
+    number boss_id FK "管理者(部長など)のユーザID"
+}
+TEAMS ||--|| USERS: "チームには１人の管理者が所属する（boss_id）"
+TEAMS ||--|{ TEAM_MEMBERS: "チームには１人以上が所属する（member_id）"
+
+TEAM_MEMBERS {
+    number id PK
+    number team_id FK
+    number user_id FK
@@ -49,0 +49 @@ AUTHORITIES {
+AUTHORITIES ||--o{ USERS: ""
@@ -54,0 +55 @@ ROLES {
+ROLES ||--o{ USERS: ""
@@ -57 +57,0 @@ ROLES {
-%% 
@@ -62 +61,0 @@ TEMPLATES {
-    number questionCounts "質問の個数"
@@ -63,0 +63 @@ TEMPLATES {
+TEMPLATES ||--|{ QUESTIONS: "テンプレートには１つ以上の質問が設定される"
@@ -67 +67 @@ QUESTIONS {
-    number question_number "質問版後 1からの連番"
+    number question_number "質問番号 1からの連番"
@@ -72 +72 @@ QUESTIONS {
-QUESTIONS ||--|| TEMPLATES: "質問はテンプレートと１対１の関係"
+QUESTIONS ||--o{ ANSWERS: "質問には複数人の回答が登録される"
@@ -76,0 +77 @@ ANSWERS {
+    datetime submited_at
@@ -81,2 +81,0 @@ ANSWERS {
-ANSWERS ||--|| QUESTIONS: "回答は質問と１対１の関係"
-ANSWERS ||--|| USERS: "回答とユーザは１対１の関係"
diff --git a/handler/apiHandler.go b/handler/apiHandler.go
index 3022fde..18b65c2 100644
--- a/handler/apiHandler.go
+++ b/handler/apiHandler.go
@@ -90 +90 @@ func (hm *HandlerManager) registerUser(api string, sendEvent func(string, string
-			form.Password = crypto.EncryptSHA256(form.Password)
+			form.Password = crypto.EncryptPassword(form.Password)
@@ -100,0 +101 @@ func (hm *HandlerManager) registerUser(api string, sendEvent func(string, string
+		created.Password = ""
@@ -351 +352 @@ func (hm *HandlerManager) updateUserByID(api string, sendEvent func(string, stri
-			form.Password = crypto.EncryptSHA256(form.Password)
+			form.Password = crypto.EncryptPassword(form.Password)
@@ -370,0 +372,2 @@ func (hm *HandlerManager) getNotice() echo.HandlerFunc {
+
+// TODO; Notice更新時にSSEでデータ送信を実施、クライアントサイドで結果を受け取りデータを更新
diff --git a/handler/authHandler.go b/handler/authHandler.go
index f851e14..356a7e7 100644
--- a/handler/authHandler.go
+++ b/handler/authHandler.go
@@ -53 +53 @@ func (hm *HandlerManager) loginHandler() echo.HandlerFunc {
-		info.P = crypto.EncryptSHA256(info.P)
+		info.P = crypto.EncryptPassword(info.P)
diff --git a/handler/eventHub.go b/handler/eventHub.go
index e76e19b..2491aa3 100644
--- a/handler/eventHub.go
+++ b/handler/eventHub.go
@@ -119 +119 @@ func (h *Hub) sendError(api, data string) {
-	h.logger(context.Background(), slog.LevelError, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelError, event, "data", crypto.EncryptSHA256(data))
@@ -125 +125 @@ func (h *Hub) sendGetEvent(api, data string) {
-	h.logger(context.Background(), slog.LevelInfo, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelInfo, event, "data", crypto.EncryptSHA256(data))
@@ -131 +131 @@ func (h *Hub) sendCreateEvent(api, data string) {
-	h.logger(context.Background(), slog.LevelInfo, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelInfo, event, "data", crypto.EncryptSHA256(data))
@@ -137 +137 @@ func (h *Hub) sendUpdateEvent(api, data string) {
-	h.logger(context.Background(), slog.LevelInfo, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelInfo, event, "data", crypto.EncryptSHA256(data))
@@ -143 +143 @@ func (h *Hub) sendDeleteEvent(api, data string) {
-	h.logger(context.Background(), slog.LevelInfo, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelInfo, event, "data", crypto.EncryptSHA256(data))
@@ -149 +149 @@ func (h *Hub) sendNotice(api, data string) {
-	h.logger(context.Background(), slog.LevelInfo, event, crypto.EncryptSHA256(data))
+	h.logger(context.Background(), slog.LevelInfo, event, "data", crypto.EncryptSHA256(data))
diff --git a/handler/notice.go b/handler/notice.go
index c245fe1..1378f6b 100644
--- a/handler/notice.go
+++ b/handler/notice.go
@@ -56,0 +57 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
+	const noticeLineHour = 72 * time.Hour
@@ -74,10 +75,2 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-				if question.Due.Add(-72 * time.Hour).Before(time.Now()) {
-					// question_id での検索で RecordNotFound が出る想定のため、ログ出力を抑えた関数を呼びだしています
-					if _, err := infrastructure.GetNoticeByQuestionSilent(context.Background(), hm.db, question); err != nil {
-						if errors.Is(err, gorm.ErrRecordNotFound) && question.Support.SupportStatusID != 1 {
-							infrastructure.RegisterNoticeByQuestionID(ctx, hm.db, question.ID)
-							checkedQuestions[question.ID] = struct{}{}
-							continue
-						}
-						fmt.Println(err.Error())
-					}
+				if isOverDue(question, noticeLineHour) {
+					overDueProc(ctx, hm.db, question)
@@ -87,6 +80,11 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-		case q := <-hm.ne.ReceiveUpdateQuestion():
-			if q.Support.SupportStatusID == 3 {
-				if nid, err := infrastructure.DeleteNoticeByQuestion(ctx, hm.db, q); err != nil {
-					fmt.Println(err.Error())
-				} else if nid >= 0 {
-					hm.hub.sendDeleteEvent("notice", strconv.FormatInt(nid, 10))
+		case question := <-hm.ne.ReceiveUpdateQuestion():
+			if question.Support.SupportStatusID == 3 {
+				removeNoticeProcByQuestionID(ctx, hm.db, hm.hub, question.ID)
+				delete(checkedQuestions, question.ID)
+			} else {
+				if isOverDue(question, noticeLineHour) {
+					overDueProc(ctx, hm.db, question)
+					checkedQuestions[question.ID] = struct{}{}
+				} else {
+					removeNoticeProcByQuestionID(ctx, hm.db, hm.hub, question.ID)
+					delete(checkedQuestions, question.ID)
@@ -94 +91,0 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-				delete(checkedQuestions, q.ID)
@@ -97,5 +94 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
-			if nid, err := infrastructure.DeleteNoticeByQuestionID(ctx, hm.db, id); err != nil {
-				fmt.Println(err.Error())
-			} else if nid >= 0 {
-				hm.hub.sendDeleteEvent("notice", strconv.FormatInt(nid, 10))
-			}
+			removeNoticeProcByQuestionID(ctx, hm.db, hm.hub, id)
@@ -107,0 +101,23 @@ func (hm *HandlerManager) checkQuesiton(ctx context.Context) error {
+
+func isOverDue(question infrastructure.Question, noticeByHour time.Duration) bool {
+	return time.Now().After(question.Due.Add(noticeByHour))
+}
+
+func overDueProc(ctx context.Context, db *gorm.DB, question infrastructure.Question) {
+	// question_id での検索で RecordNotFound が出る想定のため、ログ出力を抑えた関数を呼びだしています
+	if _, err := infrastructure.GetNoticeByQuestionSilent(context.Background(), db, question); err != nil {
+		if errors.Is(err, gorm.ErrRecordNotFound) && question.Support.SupportStatusID != 1 {
+			infrastructure.RegisterNoticeByQuestionID(ctx, db, question.ID)
+			return
+		}
+		fmt.Println(err.Error())
+	}
+}
+
+func removeNoticeProcByQuestionID(ctx context.Context, db *gorm.DB, hub *Hub, id int64) {
+	if nid, err := infrastructure.DeleteNoticeByQuestionID(ctx, db, id); err != nil {
+		fmt.Println(err.Error())
+	} else if nid >= 0 {
+		hub.sendDeleteEvent("notice", strconv.FormatInt(nid, 10))
+	}
+}
diff --git a/static/js/hash.js b/static/js/hash.js
new file mode 100644
index 0000000..a63b1f8
--- /dev/null
+++ b/static/js/hash.js
@@ -0,0 +1,7 @@
+export async function hashString(message) {
+    const msgBuffer = new TextEncoder().encode(message);
+    const hashBuffer = await crypto.subtle.digest('SHA-256', msgBuffer);
+    const hashArray = Array.from(new Uint8Array(hashBuffer));
+    const hashHex = hashArray.map(b => ('00' + b.toString(16)).slice(-2)).join('');
+    return hashHex;
+}
\ No newline at end of file
diff --git a/static/js/hrAppData.js b/static/js/hrAppData.js
index 0ca4927..8ddedc8 100644
--- a/static/js/hrAppData.js
+++ b/static/js/hrAppData.js
@@ -3 +3 @@ import { SSE } from './sse.js';
-import { Support, SupportStatus } from './model.js';
+import { hashString } from './hash.js';
@@ -61,8 +60,0 @@ function normalizeAnswerForViewModel(question) {
-async function hashString(message) {
-    const msgBuffer = new TextEncoder().encode(message);
-    const hashBuffer = await crypto.subtle.digest('SHA-256', msgBuffer);
-    const hashArray = Array.from(new Uint8Array(hashBuffer));
-    const hashHex = hashArray.map(b => ('00' + b.toString(16)).slice(-2)).join('');
-    return hashHex;
-}
-
@@ -95,0 +88,2 @@ document.addEventListener('alpine:init', () => {
+        alearts: [{type: "info", content: "テストアラート１"}, {type: "error", content: "テストアラート２"}],
+
@@ -469 +463,10 @@ document.addEventListener('alpine:init', () => {
-            return String(name ?? 'User');
+            return encodeURIComponent(String(name ?? 'User'));
+        },
+
+        /**
+         * 
+         * @param {String} text 
+         * @returns 
+         */
+        chatAvatarURI(text) {
+            return `https://api.dicebear.com/7.x/notionists/svg?seed=${this.chatAvatarSeed(text)}&backgroundColor=eff6ff`
@@ -636 +639,2 @@ document.addEventListener('alpine:init', () => {
-            return `残り時間: ${this.calcRemainingTimeAndString(n.question)}`;
+            else if (n?.question?.due > 0)  return `残り時間: ${this.calcRemainingTimeAndString(n.question)}`;
+            else if (n?.question?.due / 24 <= 1) return -1
@@ -809,0 +814 @@ document.addEventListener('alpine:init', () => {
+            this.updateQuestion();
@@ -832,0 +838 @@ document.addEventListener('alpine:init', () => {
+            this.updateQuestion();
@@ -878,0 +885,3 @@ document.addEventListener('alpine:init', () => {
+        // TODO; Toggleで未対応になった場合、SupportIDとSupportを削除し送信。サーバ側でもSupportをNullのものへと書き換えて対応者なしとする。
+        // TODO; Question更新時、SSEで送信されてデータを置き換えたMemoの内部は初期（ブランク）となっているため、更新後にもMemoの内部は書き換え内容にしたい。でないとAvatarのデータがブランクのものとなってしまう。
+        // TODO; Answer送信後、ステータスを完了へと更新する？一撃で帰ってこなかった場合の処理を考えないと終わらなさそう。
@@ -977,7 +985,0 @@ document.addEventListener('alpine:init', () => {
-        async getNotices() {
-            const res = await fetch('/api/v1/notice', { headers: this.apiHeaders(false) });
-            const json = await res.json();
-            const list = Array.isArray(json) ? json : [json];
-            this.notices = list.map((n) => Notice.fromJSON(n));
-        },
-
@@ -1081,2 +1083,22 @@ document.addEventListener('alpine:init', () => {
-            const [hours, minutes] = this.calcRemainingTime(new Date(question.due), this.timeNow)
-            return `${hours}時間${minutes}分`;
+            const baseDate = new Date(question.due);
+            const d = this.timeNow;
+            const diff = baseDate - d;
+        
+            // 期限内の場合は元の処理
+            if (diff >= 0) {
+                const [hours, minutes] = this.calcRemainingTime(baseDate, d);
+                return `${hours}時間${minutes}分`;
+            }
+        
+            // --- 期限超過（マイナス）の場合の処理 ---
+            const overdueMs = Math.abs(diff); // 超過分を正の数に変換
+            const overdueHours = Math.floor(overdueMs / (1000 * 60 * 60));
+        
+            if (overdueHours < 24) {
+                // 1日（24時間）以内
+                return `${overdueHours}時間超過中`;
+            } else {
+                // 2日目（24時間）以降
+                const overdueDays = Math.floor(overdueHours / 24);
+                return `${overdueDays}日超過中`;
+            }
diff --git a/static/js/login.js b/static/js/login.js
new file mode 100644
index 0000000..2a251af
--- /dev/null
+++ b/static/js/login.js
@@ -0,0 +1,32 @@
+import { hashString } from "./hash.js";
+
+document.addEventListener('alpine:init', () => {
+    Alpine.data('hrAppLogin', () => ({
+        init() {
+            if (typeof lucide !== 'undefined') {
+                lucide.createIcons();
+            }
+        },
+
+        login: {},
+        init() {
+            if (typeof lucide !== 'undefined') {
+                lucide.createIcons();
+            }
+        },
+        async doLogin() {
+            const res = await fetch('/login', {
+                method: "POST",
+                headers: { "Content-Type": "application/json" },
+                body: JSON.stringify({ email: this.login.email, pass: await hashString(this.login.pass) })
+            })
+            if (!res.ok) {
+                console.log("Login error.")
+                return
+            }
+            const body = await res.json();
+            const redirect = body.redirect;
+            location.href = redirect;
+        }
+    }))
+})
\ No newline at end of file
diff --git a/static/js/sse.js b/static/js/sse.js
index b207aff..da61214 100644
--- a/static/js/sse.js
+++ b/static/js/sse.js
@@ -124 +123,0 @@ const bindSSE = (eventSource, eventNames, fromJSON) => {
-                console.log(eventName, model)
diff --git a/templates/components/aleart.html b/templates/components/aleart.html
new file mode 100644
index 0000000..fd471a1
--- /dev/null
+++ b/templates/components/aleart.html
@@ -0,0 +1,13 @@
+{{ define "aleart" }}
+<div role="" class="alert alert-vertical sm:alert-horizontal">
+    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-info h-6 w-6 shrink-0">
+        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
+            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
+    </svg>
+    <span>we use cookies for no reason.</span>
+    <div>
+        <button class="btn btn-sm">Deny</button>
+        <button class="btn btn-sm btn-primary">Accept</button>
+    </div>
+</div>
+{{ end }}
\ No newline at end of file
diff --git a/templates/components/login_head.html b/templates/components/login_head.html
index b2fad6e..8ea0226 100644
--- a/templates/components/login_head.html
+++ b/templates/components/login_head.html
@@ -14,0 +15,6 @@
+<!-- 4. Hash -->
+<script type="module" src="../../static/js/hash.js"></script>
+
+<!-- 5. login -->
+<script type="module" src="../../static/js/login.js"></script>
+
@@ -21,35 +27,2 @@
-<script>
-    document.addEventListener('alpine:init', () => {
-        Alpine.data('hrAppLogin', () => ({
-            init() {
-                if (typeof lucide !== 'undefined') {
-                    lucide.createIcons();
-                }
-            },
-
-            login: {},
-            init() {
-                if (typeof lucide !== 'undefined') {
-                    lucide.createIcons();
-                }
-            },
-            async doLogin() {
-                console.log(this.login)
-                const res = await fetch('/login', {
-                    method: "POST",
-                    headers: { "Content-Type": "application/json" },
-                    body: JSON.stringify({ email: this.login.email, pass: this.login.pass })
-                })
-                if (!res.ok) {
-                    console.log("Login error.")
-                    return
-                }
-                const body = await res.json();
-                const redirect = body.redirect;
-                location.href = redirect;
-            }
-        }))
-    })
-</script>
-
-<!-- 4. Alpine.js -->
+
+<!-- 6. Alpine.js -->
diff --git a/templates/components/mock5_view_detail.html b/templates/components/mock5_view_detail.html
index 4914ec4..bae88ba 100644
--- a/templates/components/mock5_view_detail.html
+++ b/templates/components/mock5_view_detail.html
@@ -16 +16 @@
-                                :src="`https://api.dicebear.com/7.x/notionists/svg?seed=${encodeURIComponent(chatAvatarSeed(activeQuestion.sender))}&backgroundColor=eff6ff`"
+                                :src="`${chatAvatarURI(activeQuestion.sender)}`"
@@ -32 +32 @@
-                                    :src="`https://api.dicebear.com/7.x/notionists/svg?seed=${encodeURIComponent(chatAvatarSeed(item.userName))}&backgroundColor=eff6ff`"
+                                    :src="`${chatAvatarURI(item.userName)}`"
@@ -64,2 +64,6 @@
-                        <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 text-sm shadow-inner shrink-0"
-                            x-text="senderInitialChar()"></div>
+                        <div class="w-10 rounded-full">
+                            <img
+                                :src="`${chatAvatarURI(activeQuestion.sender)}`"
+                                class="w-10 h-10 rounded-full border border-gray-200 bg-white object-cover shadow-sm shrink-0"
+                                alt="" />
+                        </div>
@@ -78,2 +82,6 @@
-                        <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center font-bold text-slate-700 text-sm shadow-inner shrink-0"
-                            x-text="supportUserInitialChar()"></div>
+                        <div class="w-10 rounded-full">
+                            <img
+                                :src="`${chatAvatarURI(activeQuestion.support.user.name)}`"
+                                class="w-10 h-10 rounded-full border border-gray-200 bg-white object-cover shadow-sm shrink-0"
+                                alt="" />
+                        </div>
@@ -100 +108 @@
-                            <option value="3">完了</option>
+                            <option value="3" :disabled="activeQuestion.answer == null" :title="activeQuestion.answer == null ? '「回答を送信」してから押下できるようになります。':''">完了</option>
@@ -144 +152,2 @@
-                                class="input input-bordered input-sm w-full text-xs font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner px-2" />
+                                class="input input-bordered input-sm w-full text-xs font-bold focus:outline-none focus:border-slate-800 focus:ring-1 focus:ring-slate-800 shadow-inner px-2"
+                                @change="updateQuestion()" />
